# STATUS / 交接文档（2026-07-05）

> 新 session 接手先读这份 + `CLAUDE.md`(设计宪法)+ `prd.md`(v2 定位见头部标注)。仓库:github.com/zhizhunbao/pnp-job-tracker
> **🚀 站点已公网上线:https://offer2pr.com**(Render + Supabase;**live Stripe,M3 收费闸已开**)。
> ⚠️ **「真实收款」这四个字 2026-08-03 撤掉**:live 通道验过没错(07-04 开闸演练,已退款),但库里两条
> Stripe session 一条是测试号、一条是 Frank 自己那笔 —— **陌生用户付费至今 0 笔**。别再把「已收款」当既成事实。批次进度=`docs/implementation/_开发批次顺序.md`:**B0-B8 全部落地(2026-07-04 一天从 B4 打到 B8),24 工作项代码侧全完**。
>
> **🧰 当前工作区(2026-08-05,未提交/未上线):联邦对话接线 + Job Bank 竞态结构性收口 + EE 数据补全**
> - **A 对话**:`lookupPermit` 已接主编排(PGWP/CEC/FSW/FST);新增 `lookupCrs`,SQL 第一条件固定 `grid=$1`,返回层再防混表,`points=NULL` 原样保留。纯联邦直问不再强迫用户先给 NOC;普通职位问法不触发。新增 fake-pool 真组件测试 6 绿;既有对话纯函数 47 绿;`tsc --noEmit` 绿。**本轮未连生产库,未跑真实 SQL。**
> - **B 竞态**:build 全轮由跨进程内核文件锁包住,直到真实 `09_build_mart.py` 消费完成;Job Bank 两个 `postings.json` 写入口(`05_parse`/`05b_parse_details`)走同一锁。异常退出由内核释放,6 个跨进程/真实入口边界测试绿。不是三处 mart 兜底,也不是“复制了但 09 仍读 live”的假快照。
> - **C 数据**:只读 `data/crawl/fed-ee/` 97 页缓存。语言 T4–T26 → raw 23 表/105 档、mart 443 行;ECA 页 FSW 教育映射 151 行并入 FSW67。实际本地 mart=`ee_points_grid` 380(CRS 186 + FSW67 194,points NULL 22)、`ee_language_grid` 443;唯一键重复 0、evidence 缺失 0。**未跑 09 main/seed/DB_PUSH/DDL;新语言 mart 尚无 CMS/生产消费链,不能对外声称 IELTS 原分已可自动换 CLB。**
> - 全程未启 dev server、未 commit/push;`data/raw/ee/draws.json` 等定时 ETL 既有脏改保留,不属于本批。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-08-06,「NL 掉桶修复 + C6 效果图」)**
> - **✅ C5 遗留首修落地**(明细=C5 文档 §4.5,比原方案多挖一层根):① NL 经验门槛 op='none' 却因
>   「缺经验月数」被 pickGate 拖成 needs-info —— 现 need=0 ⇒ gap 恒 0 不记缺槽,**第一轮 NL 直接
>   open tier0 浮顶**;② needs-info 的 tier 改成**潜力上界**(缺槽门槛按 0 经验/0 居住记档)且排序
>   对 needs-info 也按 tier 升序(C6 选项卡推荐位同一套序);③ 裁决已出而 status 缺 → followups
>   第一条点名问工签(vAsk.status 三语)。顺带:sayFact 货币 `$100,006/年`($+千分位,差额注同步)、
>   CLB 语序翻正(「CLB 6」)。**验收**:C01 两轮 dev 实测(直连生产库+真 LLM)第一轮答复已含
>   「纽芬兰…拿到 offer 即可递」+ followups[0]=工签问句;全量 418 绿 1 红(既有 BC 池脆测试)、tsc 绿。
>   ⚠️ 实测顺带发现(未修,LLM 侧非引擎):同题重问命中朋友服务的 prompt 前缀缓存返回旧稿;
>   换句式后有一轮把「手里的 PGWP 还有效」误当 claims 且没触发裁决 —— 组稿/抽槽方差,facts 层已验对。
> - **C6 效果图两轮过审结果(Mode B,未动任何 UI 代码)**:裁决入口卡**两稿都没过**
>   (一稿「N 条通道」计数看不懂;二稿点名式仍不行)→ **Frank 拍板:卡片内容之后单独设计,
>   C6 本批不做这张卡**(卡位保留)。过程立了两条硬规矩(进设计文档 §五):**UI 文案零逗号**
>   (枚举顿号)、**省名不裸码**(「PEI 用户肯定不知道是啥」→ 全名主文案+码灰注)。
>   Claude 式选项卡效果图已发未回话——**等 Frank 点头才写 UI**。图在 docs/assets/mockups/c6-*.png,
>   设计=docs/design/对话选项卡与图片上传-20260806.md §五。
> - C7 付费闸未动(等 Frank 拍板切分);图片上传未动(视觉模型前置,Frank 亲自谈)。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-06 凌晨,「C5 判定层全链 + 两起生产事故」)**
> - **✅ C5 三批全落**(明细见 docs/implementation/C5-判定层pathVerdict-20260805.md):C5a 估分器×2
>   (CRS 183/199——抓出 C01 已婚年龄档 47→官方 45 笔误;MPNP 695/595/715)、C5b `pathVerdict` 13 通道
>   注册表(29 测,**机器自跑出「应避开曼省」**:经验放安省→天花板 615<632→excluded)、C5c 对话接线
>   (触发=NOC+路径问法+档案槽≥3;19 测;chat 全家 137/137)。**待办:生产真 LLM 实问一轮**(mock 已金标)。
> - **🔴 事故复盘(我引发,已修已验)**:手动跑 09 用了 jobbank 21:03 刚写、详情未解析的半熟快照并 seed
>   → 生产最新一批全「未分类」、表内中文全消失(Frank 实拍报障)。**手动 09 前必查最新批 noc 非空率**
>   (已进 memory);修复后生产复验:首页 50 行分类全中文,8/5 残量 6/2309。
> - **🐛 投递栏跑偏(Frank 报障,已修,生产已验)**:sticky 只在白卡盒内吸底,卡下还有页脚 → 窄屏整页改
>   fixed 贴底(iOS 安全区+占位),弹框/桌面不动;playwright 双位点 + 生产探针 barBottom=viewportH。
> - **Frank 口头反馈(记入 C6)**:详情页 OccReportCard「现在没什么用」——判断=病在出口(报告体系已摘),
>   C6 卡位换装成裁决入口(Mode B 效果图先行);他若要先摘锁行,单独处理。
> - `designated_employers` 补 url/fetched(C5b 发现 NL 3 家事实挂不上 evidence;DDL 已代跑生产)——
>   ⏭️ **push 部署后须清 seed_state('designated_employers') 重灌一次**,否则生产该两列恒 NULL。
> - **深夜追加(Frank 实测五连击,全部当场落地生产 `295334c..85cd616`)**:① 组稿旧剧本「缺的不是省份」
>   删除 → PLAYBOOK_VERDICT(tier0 必点名/省份差异即答案/第一份工作与选省同一决策)——复测第二轮
>   首句即「最快路径是纽芬兰国际毕业生类别,无需经验门槛」+ 曼省双倒扣毒点,C01 手工分析的对话版成立;
>   ② OccReportCard 摘除(组件保留,卡位留给 C6 裁决入口);③ 返回钮「…」前缀删;
>   ④ 顾问弹框右上四钮统一 iconBtnS + 重置图标化;⑤ 标题「说说你的情况」→「AI 顾问」(与占位符冗余)。
> - **🔴 C5 遗留首修(写在 C5 文档 §4.5)**:用户没提工签时 NL 掉进 needs-info 桶被挤掉(第一轮答复整个
>   缺席)→ needs-info 按 tier 潜力排序 + followups 点名问工签。顺带:facts 数字格式(「薪资至少 100006」
>   裸奔无 $ 无千分位、「6 CLB」语序)。
> - **新设计已落档** `docs/design/对话选项卡与图片上传-20260806.md`:Claude 式选项卡(需要决定才弹/
>   点选=以用户身份发话=跳过抽槽/推荐位带理由,推荐规则与 NL 掉桶修复同一套排序)纳入 C6;
>   **图片上传有硬前置**——实测朋友 qwen3.6 看不见图(1px 探针 prompt_tokens=16,答复装没看见),
>   加视觉模型要 Frank 亲自谈,模型看不见图就不许上传入口存在。
> - ⏭️ 下一批 **C6:裁决卡+详情页入口+选项卡**(Mode B 效果图先行,先修 NL 掉桶),**C7 付费闸**(§五待拍板);
>   欠账:AIP 门槛缺 URL(需 fed-aip crawl)、pnp_draws 子通道键、RCIP/MB-SWM 语言行、BC 池脆测试待修。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-05 深夜,「C4 数据补口:案例 C01 的数字全部入库」)**
> 上游:[路径裁决-C01复现计划](docs/design/路径裁决-C01复现计划-20260805.md)(C4→C7 把 C01 手工裁决变成对话产出+付费闸)。
> 工作模式照 agent-team:5 子 agent 按文件区并行(NB/MB 抽选、MB EOI 积分表、SINP 适用范围+PDF、NL 雇主、OINP 运营),主 agent 收口集成。
> - **✅ 金标 20/20 全绿**(`etl/audit_c01_gold.py`,C5 判定层拿它当单测底座):MPNP 695 逐档复现、
>   MB #276/632 与 #275/825 分流成行、NB 2026 建筑类 13 轮(58/209/114 在列)、SK 排除清单 `appliesTo=OID/EE`
>   且 72310 不在 Job Offer 排除清单(14 条,官方 PDF 解析)、NL 雇主 639 家 3 家申报 72310、OINP 配额 14,119。
> - **🔴 两处推翻 C01 原文,已照数据改案例文档**:① NL 指定雇主 645→**639** 家、申报 72310 的 1→**3** 家
>   (多出 St. John's 两家,对案例是利好);② **OINP 审理时长专页已官方下线**(注册 302 + 全 slug 负扫)
>   → not-collected(举证过),不是「官方不公布」。
> - **模型两列**(docs/sql/c4-pnp-gaps.sql,**已代跑生产** + seed_state 已清):`pnp_occupations.applies_to`
>   (清单管哪些子通道——SK 那 152 条只管 OID/EE,少这列会把「SK 走不通」判给不该判的人)、
>   `designated_employers.nocs`(NL 取官方省站全量名录 639 家取代旧聚合源 94 行,防重复)。
> - 抽选容量:mart 每省 8→12,NB 单独 48(按类别一轮拆多行,12 只装得下两轮半,三月建筑轮曾被挤掉)。
> - 已知瑕疵:MB #276 一行 LAA「605」是官方页 `<strong>60</strong><strong>5</strong>` 排版伤,照原文收;
>   OINP 提名数 2020/2022-2024 被 Radware 拦,如实缺;SINP PDF 多行条件款截首行(note 指向原 PDF)。
> - **⏭️ 下一件 = C5 判定层 `pathVerdict`**:Slots 扩五槽(年龄/婚姻/CLB/学历/加拿大学历),档案进裁决出,
>   估分器全查表(CRS/OINP/MPNP),金标即 C01;然后 C6 裁决卡+职位详情页入口(Mode B)、C7 付费闸(§五待 Frank 拍板)。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-05,「多 agent 并行:联邦 EE 入库 + 薪资竞态」)—— 5701ac2..be39dbc 生产已验**
> **工作模式**:Frank 拍板走 **agent team(多 agent 并行)** —— 一批活按**文件区不重叠**拆给 4 个子 agent 同时干,主 agent 派活+收口。
> 派活前必须先核基线(**差点重派上一轮已落地的 `lookupDraws` FED 与 `lookupPermit`**);交回的「已验」一律追问验的是真组件还是复刻件。
>
> - **✅ G9 联邦 Express Entry 入库(最大单一数据缺口,已在生产库)**:canada.ca 的 EE 区此前**零覆盖**
>   (联邦只有 fed-pgwp / fed-rcip),一个真实案例里 15 个数字全卡这。新 crawl slug `fed-ee`(97 页,
>   种子取自 `raw/ee/federal-categories.json` 里已举证的 URL,**没猜**)→ `crs-grid.json` 186 行 +
>   FSW67 43 行、`fed-eligibility.json` 23 条 quote-anchored 资格(CEC 8 / FSW 8 / FST 7)。
>   **生产库实测:`ee_points_grid` 229 行(CRS 186 + FSW67 43,points NULL 22)、`pnp_requirements` 227 → 250 行。**
>   `docs/sql/g9-ee-points-grid.sql` **已代跑生产**(17 列 / 4 索引 / `payload_locked_documents_rels` 补列)。
>   两个坑记死:① 官方字段原名 `table`/`column`**都是 PG 保留字** → `table_no`/`column_label`;
>   ② 23 条里 **13 条 `value` 是编码字符串**而该列是 integer → 直灌 22P02 掀翻整个 seed(此前 227 行全是数值,
>   这条路从没被走过)。照 `op='rule'` 先例:value 留 NULL、编码折进 `basis` 的 `valueCode=`。
>   **FSW 教育 25 分档标 `not-collected`**(挂在 ECA 解读页,URL 已在那 97 页里)——**不是「官方不公布」**。
> - **✅ 对话侧两件**:`lookupPlan`(C3 的 `buildPlan` 此前**零调用方**,现接进主编排 `collectFacts`)、
>   **答复逐句流式**(按句门控:数字回查/内部码/`=`形状/英文单位/中文数字/外文混用/两态揉一句**七道逐句判、一道没放宽**;
>   逐行抄与同开头留在整段只留痕)。断句复用 `SENT_SPLIT`,加「数字后的 `.` 不当句末」。52+29 绿。
>   **SK 的 `processingScope` 凑不齐**(按子通道公布时长,对话里没这个槽位)→ 判成算不出,**没硬凑**。
> - **🐛 薪资竞态(Frank 实拍报的:最新一批岗位薪资列一片横线)**——**这条的排查过程本身是教训**:
>   **抓取(jobbank)与建表(build)是两个并行容器**,jobbank 整文件重写 `postings.json`;build 从 04d 到 09
>   中间隔着 05e(12 分)+ 验尸(7.5 分)。实测时序:`00:22:35 跑 04d → 00:25:37 写入 24 条新帖 →
>   00:42:31 建表` → 那批绕过清洗直接进库。**症状永远是「最新一天没薪资、老数据全好」,下一轮自愈。**
>   修法双保险:① 慢活提到最前,窗口 20 分钟 → 十几秒;② **09 建行时兜底**(有原文没归一产物就用 04d 的
>   同一个函数现算现补,并打日志计数)—— **mart 是最终表,不该依赖谁先跑**。
>   ⚠️ **04c/05c/05e 三类清洗同样暴露在这个窗口下**,只是没薪资那么显眼,顺序调整只是收窄不是堵死。
> - **🐛 同批第二个**:护栏拦下不可信薪资后**回落显示原文**,于是 `$500.00 hourly`、`$295,000.00 daily`
>   (雇主把年薪填进日薪格)原样上台面 —— 用户会当成我们的数。**改成三类(金额离谱/区间比离谱/年化超顶,
>   118 条)一个字都不显示**;`50% commission per sale`、`$.60 commission per mile`、面议这类 **142 条照给**
>   (那是真信息)。前端同批统一两端:**手机不再兜底回原文**(原来 `salaryText || salary`,所以桌面横线、
>   手机冒原文,同一格两个样)、桌面绿色横线改灰。
> - **🔴 排查教训(两次被自己的推断带偏)**:① 先把竞态当结论说了出来,被 Frank 一句「不是什么原因啊」顶回来 ——
>   **后来是翻容器日志的时间戳才坐实的**;② 本地跑 04d 撞到 `UnicodeEncodeError` 差点定成病因,
>   **实际是我 shell 管道 cp1252 的事,容器里 UTF-8 跑得好好的**。**证据在 `docker logs`,别在推理里。**
> - **⏭️ 手上最划算的下一件:`lookupPermit` 全仓零调用方**(`chatOrchestrate.ts:21` 的 import 里根本没有它)。
>   PGWP 九条 + 今天新入库的 CEC/FSW/FST 23 条,**数据在库、工具写好、对话里依然问不出来**,差编排一行。
>   这跟当初「规则躺在库里、七个工具没一个读得到」是同一个病往上挪了一层。
> - **⏭️ 未证明**:那 118 条被压制的离谱值没出现在首屏 → **不等于已清掉**,要等下一轮 04d 落库后抽查。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-04,「答题卡下架 + 对话工具层开工」)—— a33c79c..f47fd1d 生产已验**
> - **🛑 Frank 连问两遍「把精力耗在答题卡上是不是收益太低」= 答题卡摘入口**(拍板「摘入口、保留路由」)。
>   起因是他拿 21232 实测拿 PR 报告后一句「一塌糊涂」。**教训进 memory:开工前必答「这是不是在给要退役的
>   东西刷漆」**——「对话即产品」08-03 就拍板了,文档摆在 `docs/design/对话即产品-20260803.md`,我当天开工没对这一眼。
> - **摘入口六处**(页面/路由/引擎/测试一律保留,直达仍 200):`/start` 四张活卡 + 页底 CTA(连带撤三张
>   「即将上线」占位卡)、**职位详情页「看报告」按钮**(最大流量页)、账户页蓝条、职位板「我的匹配」改指
>   `/account` 建档。sitemap 没收录、邮件零链接、Stripe 回跳走 `/account` → **无外部断链**。
>   `/start` 摘完不是空壳:还剩 Hero + 六个真数据节,页面性质回到「日更职位板」。
> - **报告可信度四刀**(留着的理由:`report.ts` 是对话产品的判定层,跟着复用):清单命中与在招量拆两句
>   (0 在招不再吹「当地在招 0 岗」)、这一节第一次有排序与择优行(打平就明说不替你排)、抽选线带分制名
>   (AB WEOI 52–65 vs MB EOI 632–825 裸并排像数据错乱;`pnp_draws.scale` 原先没进 SELECT,不补那列
>   **改了等于没做**)、**删「窗口内还有 40 轮」**(把轮次多当机会多的假承诺)、分节兜底 `?? 'prov'` → `'other'`
>   (联邦 EE 与加拿大经验原先全掉进「哪个省更有优势」标题下)。**诊断结论出乎意料:「在招 1 岗」不是 bug,
>   数字是真的** —— 病在拿「本站收录到 1 个岗」当「这省更有优势」的论据。
> - **🐛 修好一道从来没生效过的免费闸**:`resume-match` 往 `profile.matchUses` 写日限计数,而该键**从未在
>   collection 声明** → drizzle 按 schema 声明取值、多余键静默丢弃且不报错 → **非 Pro 一直可无限调用**。
>   三条独立证据(列不存在 / 全库 11 个 json 列扫命中 0 / 适配器源码)。同 [[new-etl-dim-table-checklist]]:
>   **Payload 侧不声明 = 数据不存在,而且不报错。**
> - **简历存档全链**(G3 §3b,Frank「按你的建议来」+「DDL 你跑」):`docs/sql/resume-archive.sql` 三列
>   additive **已代跑生产**(`profile_resume_text` / `profile_resume_saved_at` / `profile_match_uses`);
>   默认不存、勾选才存、弹框预填、账户页能看能删、隐私页三语补齐(顺带改准两处旧文案:当日计数并非全部
>   「不落库」;§3「不与第三方共享」与 §4 打架 → 补「服务提供方除外」)。LLM 通道保持 friend(Frank「不用换回」)。
>   **⏭️ 端到端未验**:勾选→存→预填→清除需登录态,**要 Frank 用 @test.local 号跑一次才算数**。
> - **✅ C1 工具层开工**(`docs/design/对话即产品-20260803.md` §三):`lib/chatTools.ts` 七个 lookup 薄封装,
>   13 测试绿。两条硬约束落进**类型系统**而不是 prompt:每个数字挂 `evidence{url,fetched,...}`,
>   `Availability` 四态 `ok / not-published(官方制度性不公布)/ not-collected(本站未收录,不得说成没有)/
>   not-applicable`。**历史坐实**:`/api/advisor` 当初被关掉(「看着是废话」)的根子就是它从没吃到官方事实表。
> - **🕳️ 挖出 G5 的真身份 → 当天做完**:`lookupOps` 原先全省给不出数字 —— AB/SK/BC 官方**确实公布**
>   池子人数、处理时长、配额 YTD,ETL 早抓成 `data/raw/pnp/*-stats.json`,却**从未接 mart**。
>   **G5 不是锦上添花,是「等多久/名额剩多少/被捞概率」这一整类问题的前置条件。**
> - **✅ G5 全链落地**(`941326f`+`61a873f`,生产库已灌 **93 行**:AB 52 / SK 30 / BC 11):
>   新表 `pnp_ops_stats`(**一张窄表**,不是三省各一张 —— 三省公布的指标本就不同,宽表必然一堆 NULL;
>   `lookupOps` 返回 `metrics[]` 正好对上,加第四个省 = 加数据行)。DDL 已代跑生产(含
>   `payload_locked_documents_rels` 补列)。**金标第一次两头都在库里且同一个官方页**:
>   AOS 池 **23,056 人**(as_of 07-30)与该通道 **833 邀**(07-15)。**工具只出这两个事实不做除法**——
>   概率是结论,归编排层。
>   **🔴 这张表的立身之本**:官方隐私抑制值(AB「Less than 10」/「Not applicable」、BC「<5」、SK「N/A」)
>   一律 `value=NULL` + `value_text` 存原文,**绝不折成 0** —— 折了就等于替官方编数字,报告会说出
>   「配额已用 0」这种假话。实测 `value=0` 的行 **0 条**,12 行抑制值全部活着。
>   `lookupOps` 已换 SELECT,三态按查到的行说话;顺带修 `OPS_POLICY.BC` 的 url 与 ETL 实抓页不一致。179 测试绿。
> - **📌 G5 记账(下一件)**:AB 通道名在不同 metric 间写法不一致(`Accelerated Tech Pathway` vs
>   `…(eligible list …)`)→ 编排层按 scope 跨指标拼「某通道配额 + 池内人数」会**静默漏配**三个通道;
>   金标那个 AOS 恰好同名所以测不出来。**运营类现在仍答不了的**:AB/BC 的等待时间(AB 只有文本积压游标、
>   BC 无处理时长)、BC 的名额与总分母、NS/NB/NL/PE 连官方页都给不出、全表只存最新一期快照(无趋势)、
>   所有行无 NOC 维度(「我这职业在池里排第几」答不了)。
> - **📌 债**:`fields.ts` 的 `status.unlocks` / `pgwpBand.unlocks` 仍指向被删的 `rpt.c.window`(悬空键),
>   且 `pgwpBand` 这道 Pro 题唯一的消费者就是那行 —— 答了它引擎一个字都不多说。答题卡已摘入口,不修,记账。
>   死键清单(`landing_cta_quiz`/`jd-report-open`/`home.g.*`/`quiz.bar.*` 等)保留未删,回装还用得上。
> - **🗣️ C2「对话即产品」当天上线**(`68bac70` 生产端到端验过:3.8 秒 / 25 facts / 276 字 / 内部码 0):
>   首页 `/start` Hero 之下主输入框 → 抽槽位 → 调 C1 七个 lookup → 带出处作答。
>   **红线不靠 prompt 求模型,靠出口五道校验**(重试一次→再犯降级):数字回查 facts、内部码不见客、
>   「官方不公布」与「本站未收录」不许揉、**中文数字盲区**(写「三份」时 guard 全瞎,已两头堵)、
>   **反向守门员**(英文答复不许有汉字 —— 88% 流量是英文,这一向此前无人盯,当场抓到两条自家泄露)。
>   **降级路径曾是所有红线的后门**(兜底 factSheet 一道检查都不跑,直吐英文内部标签),现已全跑。
>   金标 C01 木匠中英各实跑读过全文。
> - **⚡ 生产事故:朋友服务缓存串答**(`7f131dd`,实弹复现):上游按 **prompt 前 2000 + system 前 512 字符**
>   做缓存键 → 简历对照在 **JD ≥1976 字符时简历整段落在键外**(库里 28% 的岗描超此长度)→
>   **第二个用户拿到第一个用户的简历分析**,Pro 的 rewrite 还会把别人简历内容吐出来。修=传输层钉内容指纹
>   (一处治全部调用点;哈希非随机 nonce,同输入仍复用;**只用 a-z 不含数字**,否则 jdformat 的防幻觉校验会误伤)。
> - **✅ LLM 换 OpenAI 兼容端点**(`5f3153d`):把实测报告发给朋友,他当天加了 `/v1/chat/completions` 并逐条回验收。
>   **输入上限 6000 → 20000 字符 → Frank 真简历必败的根因就此消失**(JD/简历切法从 2800/3100 恢复到 8000/8000,
>   实弹 16,796 字符 200 通过)。`max_tokens` 真生效、缓存键哈希全文、`x-cache` 头可观测。
>   **/v1 没有联网搜索** → webSearch 强制走旧链(那是唯一通道不是回退)。
> - **✅ C3 时间线计算器**(`841bb31`,`lib/planTimeline.ts`,纯函数无 IO,尚无调用方):补上设计真空 ——
>   工具层不排序、LLM 不下结论,于是「谁快谁慢」没人负责生成。红线:不许编时长(evidence 缺失就地降级)、
>   **不做概率**(可摆「池里 23,056 人、上轮发 833 邀」两个事实,不许说成「你有 3.6% 机会」)、
>   含 unknown 段的路径不许与全确定路径直接比大小。
> - **📐 案例库 C02 补齐「完美结果」+ 首次复现率实测 12/36 ≈ 33%**
>   ([案例C02 文档](docs/design/案例C02-Frank阿省路径-完美结果与复现率-20260804.md)):Frank 拿一份真方案进来当 case。
>   核出**四条会让用户决策错的**:AOS 被误判整条不可行(丢掉阿省 53% 名额 3,425/6,403)、
>   PGWP「2+1 合并→3 年」无官方原文支持(我们自己的 pgwp_rules.json 早写着「引擎不得补这一跳」)、
>   AEES 的 CRS 300 是地板不是尺(真尺是 WEOI 55;ATP 池 2,136 人只剩 212 名额)、
>   联邦常规轮次近 12 个月 **0 轮**(唯一可比的 CEC 12 轮在 507–518,下限比方案写的 480 高 27 分)。
>   **最有价值的观察:省级这一半我们几乎全能验、联邦这一半基本验不了,而那份方案出错的两条恰好都在联邦侧
>   —— 缺数据的地方就是出错的地方。** 两处最划算的补口:`lookupDraws` 接受 `prov='FED'`(一行 SQL)、
>   新增 `lookupPermit`(PGWP 九条规则躺在库里,七个工具没一个读得到)。
> - **🧰 挂件可读性攻坚(08-04 深夜→08-05 凌晨,`a52bddd`..`2579202`)**:Frank 拿真设备连点出四轮问题
>   (「根本不可读」「没法放大缩小拖动」「格式也是乱的」「手机端呼不出来」「问题框跑偏」「点一下示例就闪没了」)。
>   **每一条都挖出了断言抓不到的真 bug**:
>   ① **手机呼不出来 = 懒加载 chunk 取不到时整站被掀掉**(`next/dynamic` 给的是 Suspense 不是 ErrorBoundary,
>   ChunkLoadError 冒到 Next 全局边界 → 页面换成错误屏,而按钮已在同一次 commit 收走)。
>   **手机标签页能活好几天,期间任何一次部署都换 chunk 哈希** → 旧页面点开必 404;桌面刚部署就刷新,永远撞不上。
>   **📌 这不只是挂件的账:全站任何 `next/dynamic` 在陈旧手机标签页上都能掀站。**
>   ② **375 上 composer 右边缘超出面板 10px、发送钮被裁角** —— 父级 `overflow:hidden`,
>   **「375 不横滚」的断言因此永远是绿的,而东西是坏的**。
>   ③ **示例块被 `max-width:640` 卡住**(同轴答复 860,差 220px)—— 单量一个元素都「对」,四个放一起才看得出不同轴。
>   ④ **杀手锏被截成错别字**(`not a private promi.`):固定部分 261 字符 + 原话 61 > 320 帽,
>   **帽和额度对不上账,砍的却是我们自己的话**。修法=先给解释句留够再给原话,并把它写成契约断言
>   (`LABEL_CAP - 固定部分 ≥ 24`,**以后写长了先在 CI 红,不许红在用户屏幕上**)。
> - **📖 Open WebUI 取样(docker 真跑,已删容器)**:最值钱的是一句因果 ——
>   **「格式乱」不是乱,是格式被我们自己禁掉了**(渲染不了 markdown → 提示词禁列表 → 只能一坨);
>   以及 **我们桌面正文 354px = 它的手机宽**(Frank 在电脑上看到的就是别人手机上的观感)。
>   该抄三样(markdown 最小子集 / 轨迹折叠一行带耗时 / 860px·15px·1.625·段距 8),
>   不该抄三样(参数面板 / 每条 8-9 个图标 / 会话列表 —— 我们不给选模型、不存会话、是嵌在职位页上的决策工具)。
>   已落地:渲染「短段落 + 项目符号」两样(零依赖、纯文本节点)+ 解禁 RULE 5(**只有「你可以自己核的这几件」走列表**,
>   答题句与结论句保持成句 —— 一篇全是项目符号和一篇没有一样难读)。
> - **💬 对话挂件 = 全站唯一对话入口**(`d546d6c`,Frank「之后把这个对话框放到挂件上,不要放到主入口里」):
>   首页内联框同批撤掉。依据:67.5% 的会话只看一页且入口=出口=职位详情页。壳自己写(原生 popover,零依赖),
>   设计参考 Intercom/Crisp 但**不自动弹开**。🔴 吸底「申请」钮的避让**按实时位置躲不是固定上抬** ——
>   生产实测 sticky 条会摆动,固定上抬 96px 照样盖住(`elementFromPoint` 返回挂件本身)。
>   漏斗加一条**并行**的对话链(chat-open/chat-answer),不塞进旧五步。
> - **🗣️ 答复「人话」化**(`3cb233c`,Frank「现在这回答不像人话啊」):**病根是喂给模型 `标签 = 值` 的表格行 ——
>   给它表格,它就还你表格。** 改成「半句话 + 值」,`=` 从 prompt 彻底消失;门槛省份按点名取前 3
>   (九省×4 行必然一省一句);没问抽选的人不给 draws/ops 任何 fact。RULE 0 改「答他的担心」+
>   RULE 0b「按他能拿去做什么分两拨,能核的排前」。顺带修六个见客事故,含**说反事实**那个:
>   `availability='ok'` 未命中时把内部占位符喂出去 → 英文答复把「一个 EE 类别都不在」说成「都能走」。255 测试绿。
> - **⚠️ 验证工具当天骗过我三次**(记死):静态复刻件量版式(真数据下一堆问题没抓到)/ 上游缓存把测试钉成
>   永远绿(真实通过率只有 2/3)/ **注入的静态 HTML 冒充真页**(我据此误判了一个根本不存在的语言 bug)。
>   三次形状一样:**工具说「验过了」,而它验的不是真东西** —— 比代码有 bug 更危险,因为它让人停止怀疑。
>   **以后凡交回「已验」,必问:验的是真组件还是复刻件。**
> - **🚫 效果图闸门 Frank 当天撤销**(「你不用给我看效果图,你觉得行就行,以产品经理、用户、公司老板的角度看」)
>   —— 版式自主拍板,硬指标(375 不横滚/不折行/不截断)仍算我的责任。已进 memory。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-03 晚,「Frank 实测五连改 + 简历上传全链」)**
> - Frank 真号实测简历对照,连发五条全落地(ff2c630+6931b7e+2f89f68+f5582d6,生产复验过):
>   ① **文件上传 pdf/docx/md/txt**(G3 二期提前):复用 E11-07 解析器抽成 `lib/resumeExtract`,新端点
>   `/api/resume-extract` 只抽文本不碰 LLM(登录+5MB,不占日限),md/txt 前端 FileReader 直读,
>   文本回填粘贴框;不落库红线照旧。② 眉题「AI 工具」删(ModalTitle eyebrow 可选)。③ 详情页锁行删
>   (与报告钮同链接纯重复 + 全国口径贴本省数字旁;**jd-lock-seen/click 退役,漏斗第 3 步只剩 rpt-lock-seen**)。
>   ④ 弹框小注砍成「免费每天 3 次」。⑤ **简历存档多岗复用已立项**(G3 §3b:默认不存点了才存、
>   账户页能看能删、隐私页同步)——「按你的建议来」拍板。
> - **PDF「dev 通生产必败」连剥三层**(教训进 memory [standalone-dynamic-loads]):bundle 化→
>   serverExternalPackages;Linux 缺 DOMMatrix→shim;**生产=standalone,pdfjs worker 动态加载
>   tracing 追不到**→outputFileTracingIncludes 点名。三层全被裸 catch 吞过 —— 修=错误留痕 +
>   **@test.local detail 探针**(拿不到 Render 日志时最快的眼睛)。E11-07 的 PDF 上传因此从上线就是坏的,同批治愈。
> - **LLM 换道朋友的 qwen3.6**(75b4244,Frank「不用 Haiku」;生产测试号真跑 200:10 行 8 命中、
>   缺项排前、扣次正常):completeText 加 opts.provider 按调用点定向,只有 resume-match 换道,
>   advisor/建档照旧走全局 LLM_PROVIDER;服务挂了报 rm.err 不静默切云(#102 账单教训)。
>   **LLM 全链已验**(测试号),Frank 真号可选验。手上欠着:死键清理批(rpt.lock.<类>/rpt.cta.*/jd.rep.lock*/rm.eyebrow)。
> - **Frank 真简历必败追凶三连**(7e8e674+73c4d6a,生产顶格复验 200):真凶=**朋友服务 prompt 上限
>   6000 字符**(400 拒收),我们 JD/简历各切 8000 一合计必超;短输入探针全绕开它 →「我测全通你点全挂」。
>   修=JD 2800+简历 3100 贴 6000 切(system 不占额)。顺带:noJd 服务端按 jobId 懒抓兜底、
>   前端错误码各说各话(noJd/auth 原先笼统报「稍后再试」)、llm/parse 接 @test.local detail 探针。
>   **教训:验 LLM 链路必须带一发顶格输入**(小探针测不出上游限额)。
>
> **🔥 生产僵死事故(2026-08-03 16:0x–16:28,约 20 分钟,a195ee3 已修)**:seed 大改 jobs 表后,
> 首页 `fetchTotalAndProof` 三连 count(全表扫、**每请求现算无缓存**)从 ~2s 涨到 ~10s,
> 应用池(pg 默认 max=10)被排队打满 → /、/stats、/jobs/[id] 与所有走池的 API 全超时;
> 轻页(/plan/*、/pricing)正常 —— **这就是分辨「池排队」与「实例挂」的判据**。DB 侧无锁、直查 1.9s、
> 连接大半 idle。修=60s TTL 缓存(countCache 惯例);恢复是队列自己泄完(16:28,先于部署)。
> **规矩(踩过才写)**:站级聚合数字(总数/证言数)一律 TTL 缓存,禁每请求现算;判僵死先打一轻一重两条路由。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-08-03 傍晚,「交接三件销账 + Frank 点名修提醒信」)**
> - **落库复验双落**:NL 雇主 3 行 + MB 1 行 + PR-fees FED 6 行($85–$1,590)全部在库。根因查实:
>   14:41 那轮 build 的 09 是 **G8 提交(14:37:46)前启动的旧代码竞态**(mart 14:38 落盘时还没折 fees),
>   不是 seed_state 哈希坑;jobbank 14:59 触发的下一轮 build(15:20 跑 09,16:02 seed 200)自然补齐。
> - **G3 埋点实读**(umami share 页 fiber 手法,7 天窗):`jd-match-open` **1**(疑为验收自点)、
>   `jd-match-run` **0** —— 上线后真人零触碰,**Frank 真号实测仍欠着**。顺带:pricing-open 9 / pay-click 1 / checkout 1。
> - **报告页锁区换装上生产**(`2536ff1`,Frank 效果图点头):旧「🔒 类别行 + Notice 大 CTA」→ LockedRows 打码
>   (码行数 = moreN + 非 more 类数,数字真纹理假)+ ProCard 悬浮正中;rpt-lock-seen / plan-<卡>-cta 埋点原样;
>   生产实拍复验(375/en,4 行码)。**债**:`rpt.lock.<类>`/`rpt.cta.*`/`rpt.pro` 成死键,组件换装收尾批清。
> - **提醒信三连修**(`4ebb7aa`,Frank 实收邮件点名):① 职位链接从 apply_url(直跳 Job Bank)改落
>   `/jobs/<id>`(A 档 SQL 与 fetchAlertHits 补 j.id);② A/B 提醒信一律英文(zh/ko 在 git 历史;weekly locale 不动);
>   ③ 有目标省的用户目标省岗排前(07-21「偏好不是资格」拍板,外省不排除只后置)。
>   16:02 那轮 alerts 已用新代码真发 9 封 saved-search 信。城市级推送要档案加城市字段,未动。
> - 手法一记:**umami cloud share token 调 API 全 401**,share 页数字被 count-up 动画卡 0;
>   真值=页内 React fiber `memoizedProps.value`(含 percent 键的行按 DOM 事件名序 zip)。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-03 下午,「案例库方法论 + G3/G2/G8 三连」)**
> - **产品方法论换轨(Frank 连拍,已进 memory)**:「问用户没用」「设计好了用户用了说这就是我要的」
>   「穷举案例,把**完美问题和完美结果(用户能付费的效果)**先设计好,之后考虑中间」。
>   主文档 [案例库-问题与结果先行](docs/design/案例库-问题与结果先行-20260803.md):16 案例 + 缺口池 G1-G12;
>   **案例库=验收标尺**,每批改动收口抽相关案例读全文对设计结果。Frank 拍序 G3→G2→G8,三件当天全落地。
> - **G3 简历对照 JD**(`e921475` 生产;案例库 C03=最大流量入口 google_jobs_apply 的已表达需求):
>   详情页/弹框投递钮旁靛蓝钮 → 粘贴简历 → Haiku 对照(completeText)→ 两栏表格(缺的红✗排前)→
>   打码区。铁律:简历不落库;免费闸服务端;成本双闸(登录墙+日 3 次挂 profile json+截断+免费不生成 rewrite)。
>   匿名面生产验收过(按钮/登录墙/API 401);**⏭️ LLM 全链首跑要 Frank 真号实测**(登录→贴简历→对照)。
> - **统一 Pro 卡 + 打码锁区规范定稿**(Frank 八轮效果图收敛,规范在 [G3 文档](docs/design/G3-简历对照JD-20260803.md)):
>   淡黄卡+琥珀短句(零符号、删词不折行)+蓝钮「解锁 Pro」;打码行数=真实剩余数(纹理假、服务端不下发),
>   n≥4 卡悬浮正中、n<4 码尾。组件已进 primitives(ProCard/LockedRows)。**⏭️ 下一批:报告页锁区全站换装。**
> - **G2 路线账**(`986e3f4` 生产验收过):「读书还是直接工作」两条官方算术(PGWP 窗口 vs 还缺的经验、
>   先读书晚约课程时长),锁区专属 route 锁行。**G8 联邦规费**(`212a519`):IRCC 官方费用表 quote-anchored
>   (主申 $1,590 含 $600 永居权费/生物识别 $85),FED/PR-fees 行第三次复用 pnp_requirements,
>   消费点=雇主句簇「中介报价超出官方费的是服务费」。
> - **⚠️ 待落库复验(下轮第一件)**:NL/MB 雇主行(B2-4)与 PR-fees 行 —— 14:41 那轮 seed 撞上部署 500,
>   14:59 一轮在途应双落;验法 `node scratchpad/count_emp_rows.mjs / count_fees.mjs`(scratchpad 是会话级,
>   重建就是查 `pnp_requirements` WHERE province IN ('NL','MB') AND subject='employer' / program='PR-fees')。
> - 踩坑一记:PowerShell `Get-Content|Set-Content` 改 .ts 把整文件编码搅坏(git checkout 回滚重做)——
>   **.ts/.tsx 禁 PS 管道改写**;i18n 新键撞 `match.*` 命名空间改 `rm.*`。159 测试绿、tsc 干净。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-03 深夜,「照未结清单挨个销:B0-4/B1-2/B1-3/B3-1」)**
> - **接手照旧读 [design/未结问题清单-20260803.md](docs/design/未结问题清单-20260803.md)**(§B 已销到 B1-3;下一个未动的是 **B1-4 PGWP 规则库**,再往后 B2 分栏)。
> - **B0-4 复验时抓到一条真断线并已修**(`3b26548` 生产复验):路由没把 `answers.goal`(卡③诉求)传进
>   `buildProvReport`(与 `body.goal` 卡种同名不同物)→ 生产两种诉求返回**逐字节相同**;测试也从没传过 goal。
>   修后:拿身份 → MB 居首,先找工作 → BC 居首 + easiest 对称行。**教训:「字段被真实消费」要一路查到路由层。**
> - **B1-2 学徒序**(`3403b59` 生产实拍复验):卡③复用 totalExpBand(2→3 题,SurveyJS 自动出题);
>   0 经验 → `rpt.g.zeroExp`(「设经验门槛的省最低也要 12 个月」——不说「最宽松的省」,MB/NB/NL 官方无通用门槛)
>   + 下一步换「先拿下第一份岗」;卡② have=0 走 `rpt.r.exp.zero` 排期措辞(免费付费同句,差值恒等于门槛)。
> - **B1-3 学徒岗标记**(`184b8af`):新清洗 `05e_flag_apprentice`(入 build 役,restart 过、日志验过真跑)。
>   判据只认官方:JD 结构化 Experience「Will train / No experience / Experience an asset」+ 标题 apprentice/apprenti
>   → `apprentice_friendly` **6,177/65,449**(木匠 77:BC 20/SK 13/MB 3 垫底,与 §C 复算一致)。DDL 已代跑生产;
>   消费点同批接上:卡③ 0 经验的第一份岗结论带「其中 N 个官方标了不要经验/带训」(`rpt.n.firstJobA`)。
> - **B3-1 新鲜度哨兵**(`ff54d10`):`etl/source_manifest.json` 契约(glob 默认,新产物自动进哨兵)+
>   `check_freshness.py` 钉 pnp 役末尾,超期 exit 1 → 不 ping → healthchecks 报警 ——
>   **ping 从「脚本跑完」升级为「数据是新的」**。首跑 48/48 绿;B3-3 半销(ircc 三产物补了 fetched 戳)。
> - **B5-4 核毕**(纯读代码):生产 LLM=anthropic Haiku 4.5(07-04 实测),~$0.003/次,不开 auto-reload=余额即硬上限。
> - **B1-4 PGWP 规则库当夜施工完毕**(Frank 拍三问:原文为准/只加两道/CIP 第二期;`5dcd633`+`7715431` 生产终验):
>   quote-anchored 构建器(9 条规则逐轮验官方引用,消失即 exit 1 报警)→ `pnp_requirements` FED 行(零新表);
>   卡②探索批 2 两道;结论全免费——上线时被免费闸「其余 N 条」桶吃掉过(引擎算了、响应里没有),
>   ALWAYS_FREE 保底+回归测试锁死。**教训:免费题承诺的解锁行要一路验到 gateReport 之后**(同 goal 断线一类:
>   引擎对≠用户看得到)。生产实拉:college 12 个月→permit 12;master 12 个月→permit 36(硕士特例)。
> - 150 测试绿、tsc 干净;今日 5 个构建窗口(3b26548/3403b59/184b8af/5dcd633/7715431),文档数据全带 skip。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-03,「一个真实用户撞死链」→ 死链四刀)**
> - 起因:Umami 会话 `0ab369b0`(Fort Qu'Appelle/SK/iOS)6 分钟里把 `/jobs/304947` 开了 10 次、
>   **注册了账号、收藏、点了两次「申请」** —— 而那个岗在 Job Bank 早已 `HTTP 410 Job posting expired`。
>   入口 UTM = `google_jobs_apply`:**他是从 Google 招聘富结果进来的**(该渠道 30 天 470 次浏览,站上最大一支)。
> - 根因:验尸(`verify_expired`)与下架(seed)**两道闸门都卡在 30 天**,第 0-30 天完全没人验。
>   随机 100 个「在招」实测死亡率:≤7天 8% · 8-14天 24% · **15-30天 48%** · 31-60天 32%;加权**约 1/3 是死链**。
>   死帖全部返 **410**(不是 404),老判据那半条一直是摆设,靠 marker 兜住。
> - 四刀(本轮改):① 410 进判据;② 验尸队列改按**死亡风险**排(last_seen 陈旧度:>14天 52% 死 / ≤3天 9% 死),
>   实测队头 25 个中 22 个是死的;③ `09` 出 `closed_jobs.json`,seed **见判死名单即置 closed 不等 30 天**
>   (响应新增 `closedDead` 计数);④ 详情页/弹框挂「已下架」横幅 + 投递钮降级成灰色「查看官方原帖」。
> - 顺带的 SEO 意义:过期岗此前带着**没有 validThrough 的 JobPosting** 结构化数据继续喂 Google,
>   长期有丢富结果资格的风险 —— 现在 closedAt=判死时刻,直接进 validThrough。

> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-08-03,「真实案例逼出七省数据 + 不撒谎判定」)——⚠️ 未提交未上线**
> - **接手第一件事:读 [design/未结问题清单-20260803.md](docs/design/未结问题清单-20260803.md)**,从 §B 编号挨个做。
>   那份文档同时含**数据盘点(有什么/缺什么)**与**每源该多新 + 定时实况**。
> - 起因是两个真实案例(木匠朋友 / Frank 本人),不是走查。**当天抓到生产在对付费用户撒谎**:
>   报告输出「查过萨省公开清单,NOC 72310 不在上面」——而我们从没查过萨省主线(库里只有三条行业专项)。
> - **已完成(本地全绿,135 测试 + tsc)**:七省数据补齐(SK 排除清单 152 / BC 新政排除清单 12 /
>   PE 8 / NL 抽选 12 轮 + 优先职位 35 / NS 政策事实 3);coverage 四态→五态;卡③ 重做
>   (诉求驱动排序、难度接进决策、砍题 5→2、海洋四省选项);`pnp` 役 13→17 步、`ircc` 役 2→3 步;
>   StatCan 临时人口占比(2026-04 = 6.18%,目标 5%)与月度学签流量接入。
> - **⚠️ 全部未提交**:工作区混着**另一个 session 的大量改动**(SSR 筛选重构 `filters.shared.ts` +
>   `JobsTable.tsx`/`jobs/page.tsx`,以及 `CLAUDE.md`/sitemap/`RankingView`/`advisor/route.ts`/
>   `Jobs.ts`/`payload-types.ts`/`etl/10_build_rankings.py` 等)。`i18n.ts` 两边的改动**同文件混在一起**,
>   无法只提交一半而不破坏报告文案。**push 前必须由 Frank 拍板那批要不要一起走**(Render 自动部署,
>   带上未验收的 UI 重构有生产风险)。
> - **漏斗本轮实读**:jd-open 107 → lock-seen 90(84%)→ pricing-open **15** → pay-click **0**。
>   `pay-click` 已核接线正常(共享 `PricingCard`)→ 这个 0 是真没人点。但那 15 人看到的是**旧的会撒谎的报告**,
>   所以 M3 分叉要等上线后重量。

> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-08-03,「M2 第一次真读漏斗」)——409c9bd 生产已验**
> - **第一次真读 `funnel_events`**(这张表 08-01 建好之后一直**只写没读**;`/funnel` 面板在,但没人打开过)。
>   读数当时(08-02 起,开发流量已挡在表外):
>   **① jd-open 8 · ② report-open 16 · ③ lock-seen 13 · ④ pricing-open 0 · ⑤ pay-click 0**;
>   ③ 按入口 jd 8 / job 2 / pr 2 / career 1;② 按卡 pr 12 / job 2 / career 1 / jd 1。
> - **🕳️ 第 4 步的 0 是量不到,不是没人点** —— 站内**唯一**直链 `/pricing` 的入口就是报告锁区那个 CTA,
>   而 `pricing-open` 只有 `PricingModal` / `UpgradeModal` 在 mount 时发,`PricingView` 里 `track(` **出现 0 次**。
>   于是「报告 → 定价」这条**主转化边整条不进表**。跟 08-02 抓到的「`jd-open` 从来没有调用点」
>   是同一类洞,只是往下挪了一格 —— **这张表每读一次就抓出一个没接线的探头,读之前别信它的数**。
>   已补:`/pricing` 挂载即发,并带来路 `?from=rpt-<卡>`(低基数白名单,脏值退回入口名);面板加「打开定价按来路」。
> - **📉 ②那一格的百分比是假的**:① 8 → ② 16 = **200%**。职位详情页不是报告的唯一来路,
>   首页 CTA 直接进 `/plan/pr` 的占绝大多数(16 里 12 条是 pr 卡)。② 不再给「比上一步」,表下写明为什么;
>   ③④⑤ 是真父子关系照旧给。
> - **✅ 埋点端到端验过**:curl 跑不了 JS → playwright 真开生产 `/pricing?from=rpt-pr`,
>   库里冒出 `pricing-open | rpt-pr`。**探测留下的 7 条已从生产表删除**(逐条对得上第 2–8 轮探测;
>   删前删后都打印过)—— 项目自己的铁律:掺了开发数据的漏斗比没有更坏。
> - **⛔ 现在还不能做 M3 分叉**:干净数据只有 08-02 一天、总量 40 条上下,而且量具刚修好。
>   下一轮先看修好之后的 `pricing-open` 有没有真人 —— **③ 13 次曝光 → ④ 真人 0 次**才是要证的那件事。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-03,「四张卡读全文」)——cb4a9b3 生产已复验**
> - Frank「**能不能赚钱就靠你了,不完善的卡片可以先关掉**」。把四张卡的报告**全文逐句读了一遍**
>   (行为树只查自相矛盾,查不了「话说得对不对」)。抓到三条,全在生产复验过。
> - **🔓 卡⑥「职业规划」整卡不再设锁**(不是关卡)。它的锁区只有两样:`rpt.k.peerGap`
>   「执业护士的中位年薪比你这行高 48%」—— $129K 与 $87.4K 免费层刚并排摆过,**收钱卖一次除法**;
>   另一样是再多 3 个同门职业的在招与中位,而这张卡的「下一步」正把用户往 `/stats` 送,
>   那页免费列着同样两列。**卖我们同时在白送的东西**,一次就把付费信任赔光 ——
>   而信任是拿 PR 那张卡(真有货)收得到钱的前提。
>   **没关整张卡**:它免费那半(在招量、中位薪资、同门职业)是真 aha,关了连这个一起丢;
>   homepage 版式也不动(活卡 4→3 会让 2×2 网格变瘸)。真该卖的是「你能不能转过去」,
>   而转换路径数据本站没有(gaps 里自己就这么说)——有了再开闸。`'move'` 这个锁类**只在卡⑥出现过**
>   (卡① 的 `alternatives` 恒为空),等于就此下线。
> - **🇨🇦 卡①「找工作」的「有 72 家雇主」是全国口径**(`reportFacts` 那条 count SQL **没有省份过滤**),
>   却紧跟在「BC 当前在招 17 岗」后面,一个字没说是全国 —— 读起来像 17 个岗背后有 72 家雇主。
>   三语补「全国」。同卡「相关职业…全国在招 63 岗」本来就这么写,现在口径一致。Pro 侧名单也是全国,对得上。
> - **🅾️ 卡③「选省份」漏掉了 2026-08-01 那次多态化**:全命中还在说「其中 N 个是省提名清单岗」
>   (实见 SK「在招 12 岗,其中 12 个是省提名清单岗」——正是 Frank 当天点名的那句废话)。
>   拿 PR 与找工作当天都改了,**这张卡漏网**。补 `rpt.p.bestAll` / `rpt.p.secondAll`(三语齐)。
>   **0 具名也走全量句式**:那时「其中 0 个」会跟上半句「在其公开清单上」直接打架。
> - 顺手:`'{occZh} 的中位年薪'` 占位符后多一个空格(「执业护士 的」)。`{prov} 的` 那些**不动** ——
>   BC/ON 是拉丁字母,中西文混排那个空格是对的。
> - 130 测试绿(+1)、tsc 干净。**读全文的工具**:引擎只回 key+params,拿 `i18n.ts` 的 zh 段回填成中文
>   再读(脚本在 scratchpad,一次性)。生产地址走 `BASE` 环境变量,同一套读线上。
> - **⏭️ 还欠 Frank 两件**:② 同族职业推荐;③ AI 接入设计文档。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-03,「免费层每省至少一条结论」)——325ddc6 已推**
> - **✅ 销掉欠 Frank 的第 ① 件**(先前挂着「等 Frank 拍」,本轮他点了这件)。
>   病根:结论是**逐省生成**的,免费层却按扁平 `slice(0, 2)` 切 —— 第一个省(口径判定 + 抽选区间)
>   自己就把额度吃干净,**选三个省时第二、三个省一句话都没有**,读起来像根本没查过那两个省。
> - **改法**:每个目标省先保底它的**口径判定行**(清单命中四态 / 排除 / 未收录 —— 库里查得到的官方事实,
>   本来就属免费层),再按原序补齐到原来的两条 → **单省不缩水**(实跑 BC / ON 照旧两条)。
>   分差、时间窗这些「拿你的答案算出来的」照旧进锁区;`FREE_CONCLUSIONS` 从额度变成**下限**。
>   实现上按**下标**去重不按 key:两个省可能同为 `rpt.c.listedMiss`,按 key 会把第二个省的判定行吃掉。
> - **🌳 行为树扩到多省**:目标省从单省改成 10 种组合(含排除态 AB / 未收录 NL / 走自己体系的 QC),
>   新增第 ⑦ 类矛盾「免费层一句没提某个目标省」。**做过反向验证**(把保底关掉看它是否真会红)——
>   除了「三个省只说一个」,还红出一条先前没人注意的**串台**:`AB+BC` 时「下一步」指向 BC,
>   而免费层从没提过 BC。同一个改动一并修掉。
> - 生产数据实跑 BC+SK+ON:BC 清单部分命中 / SK 清单命中 / ON 不公布清单过粗筛,**三省各一条**,
>   nextSteps 三个省全覆盖。129 测试绿、tsc 干净。
> - **⏭️ 还欠 Frank 两件**:② 选职业加「同族职业」(NOC 前 4 位,21231/21232 那种成对);
>   ③ **AI 接入设计文档**(「用户答案 + 库里事实一起喂」;我的判断=**AI 进两头不进中间**)。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-03 凌晨,「拿 PR 行为树 + 插件走查」)**
> - **🌳 行为树跑出来了**(Frank「先跑拿 PR 这棵树」):**不是手画的设想树,是拿真引擎 × 生产数据穷举的行为树** ——
>   8 职业 × 9 省(覆盖六种清单态)× 3 画像 = **216 条路径,长出 74 种形态**,矛盾扫描 **0 类**。
>   检查器已沉淀成常驻测试(`report.int.spec` 末尾,用 fixture 不连库):被排除还摆分数线 / 既命中又不命中 /
>   下一步指向免费层没提的省 / 说未收录又出门槛行 / 挂了锁行但那类正文是空的 / 结论冒出没选的省。
>   **注意**:它只查「有没有说自相矛盾的话」,查不了「话说得好不好」——语义仍要人肉读全文。
> - **🔍 走查方式定了**:Chrome 插件当主力(能看图、**能看 console**),playwright 脚本留作三语 × 四卡的回归网。
>   两个坑记死:① 插件的 `ref` 点击是**坐标快照**,页面重排就点错(实撞:点「汽修技师」选中了「家庭护理员」)→ 点完必校验状态;
>   ② SurveyJS 的导航按钮是 `<input type=button>` 不是 `<button>`,选择器漏了会误报「按钮没了」。
> - **🐛 插件看 console 抓到的**:`Cannot update a component (PlanPrView) while rendering a different component (OccPicker)` ——
>   `onChange` 写在 `setNocs` 的 updater 里,而 updater 跑在渲染阶段。挪到事件处理器里(2026-08-01 加 onChange 时带进来的)。
> - **📌 待办**:免费层只给前 2 条结论 → 选了三个省时第三个省一句话都没有。要不要改成「每个目标省至少一条」= **等 Frank 拍**。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-08-02 深夜,「短名扩三语」)**
> - **🈯 韩/英短名补齐**(Frank「还有中文韩语简称」):查实全库只有 `title_zh_short` 一列,韩文 `title_ko` 是全称
>   (「등록 간호사 및 등록 정신과 간호사」比中文全称还长)。`04g` 改成**三语表驱动**(加一门语言=加一行 `SPECS`)。
>   产出:ko 482/494、en 411/494(其余留空 → 前端回退官方名,**宁可留空不瞎编**)。
> - **⚠️ 撞车检测第一轮就抓到三组,跟中文当初撞的是同一批**(Cooks/Chefs、验光师/配镜师、两个高级经理)——
>   说明这几对职业的官方名本来就靠压缩区分不开,不是模型随机犯错。二轮又抓 5 组(航空/社工/管道/汽修/制造主管),
>   全部按官方英文名的语义补进 `SHORT_FIX_KO`;**收口时 ko 撞车清零、en 无撞车**。
> - **英文上限从 24 放宽到 32**:24 太紧,「Fish processing labourers」(25 字)这种好短名被误拒;
>   真正该拒的「模型原样吐回没压缩」由另一条校验负责,不靠长度。
> - 链路一次打通:`04g` → `09`(两个新字段)→ `docs/sql/noc-short-titles-i18n.sql`(**additive,已代跑生产**)
>   → seed 白名单 → `/api/quiz` 三个端点 → 前端 `pickName()` **一处判定**(住 `lib/occName.ts` ——
>   选职业控件与报告页互相引用,判定放任一边都会成环)。
> - **⏭️ 收口未完**:09 全量 mart 跑完 → 灌库(**先 `DELETE FROM seed_state WHERE name='noc_descriptions'`**,
>   否则表级哈希一致会静默跳过)→ 三语实拍。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-08-02 夜,「把报告读一遍」)—— 本地已验,未 push(当天构建窗口已用满 3 个)**
> - **📖 方法变了**:以前改完只验「测试绿 + /api/report 的 JSON + 静态截图」——今天 Frank 一口气抓出三条,
>   全是**只有把句子读出来才看得见**的。以后每批引擎/题库改动收口前:①`tests/e2e/report-smoke` 三语 × 四卡走一遍
>   (只抓读得出来的错:裸码/未翻译键/undefined/`{占位符}`/横滚),②**人肉读一遍报告全文**(语义错脚本抓不到)。
> - **🗣️ 语言题改问实测 CLB**(Frank「我选的是英语流利,为什么显示 CLB 9」):「初级/中级/流利→CLB 5/7/9」那套映射是本站编的,
>   报告却写成「你报的 CLB 9」=替他填了个他没说过的数;且自评偏乐观会把不达标说成达标。改成 CLB 4-5 / 6-7 / 8-9 / 10+,**取每档下界**。
> - **🔢 加拿大经验不得超过总经验**(实撞:总经验答「没有」+加拿大答「2 年以上」→ 引擎取大值,句子写「你填的 30 个月」,看着像胡说):
>   `choicesVisibleIf` 一行约束,两套档位序号天然对齐,不写换算表。
> - **⏳ 不拿 5 位码顶名字**(Frank「点击跳转为什么先显示数字后变文字」):职业名异步拉,没到就留占位条 —— 这条是我昨天自己引入的。
> - **📄 读全文抓到的两条**:①「BC 有官方分值表——答几道打分题就能算出差多少」**口不对心** ——
>   学历/年龄/语言/总经验刚问过,换省对照已经在用它们算下界分,再劝他去答=没记住他说过的话 → 已算过就不出这条(连同下一步那条);
>   ② 收入行原本并排摆「门槛 $26.1K–$31.3K」与「你这行中位年薪 $99K」—— 门槛算的是**全家**收入,职业中位既不是他的工资也不是家庭口径,
>   两个不可比的数并排放会被读成「我够了」→ 只留门槛 + 说清「算的是全家收入,本站没问」。
> - **📌 欠着的两件**(Frank 当天提,未动工):① **报告支持多职业** —— `OccPicker` 能多选但 `toEngineAnswers` 只取 `nocs[0]`,
>   选两个职业报告永远只算第一个;② **引擎接 AI** —— 他要「用户答案 + 库里事实一起喂」。我的判断是**AI 进两头不进中间**:
>   官方页→结构化事实(现在是手抄,加省就得再抄)、自由问答与改写人话交给 AI;算分与达标判定留在规则引擎(要能指回官方页)。两件都要先出设计。
> - **👥 报告支持多职业**(Frank「选多个职业,报告也应该支持多个职业」):**一个职业一份报告,不混算** ——
>   清单命中/门槛/抽选线全是按职业算的,把两个职业的数合起来没有任何官方口径支持。
>   `/api/report` 收 `nocs[]`(上限 3,每多一个 = 多两次库往返)、返回 `{report, reports[]}`(`report`=第一份,老前端与 advisor 不受影响);
>   报告页顶部切一下换一份,**不重新请求**(一次全拿回来);职业名逐个拉,切过去不会退回英文官方名。
> - **✂️ 窄位显示名改用库里的短名**(Frank「数据库里不是有简化的名字吗」):`noc_descriptions.title_zh_short`
>   (ETL 04f 早就产出了,一直没人用)接进 `/api/quiz` 三个端点 →「注册护士和注册精神科护士」在 375 的 H1 上折三行,
>   现在是「注册护士」一行。**前端不再自己截字符串**(那是 CLAUDE.md 明令下沉的清洗)。
> - **📌 立项:英文/韩文短名**(Frank「还有中文韩语简称,名字太长可以用简称」)。现在只有中文有短名,
>   en 走官方名(「Registered nurses and registered psychiatric nurses」)、ko 走 `title_ko` —— 两边都长。
>   照中文短名那套走:ETL 04f 用本地 Ollama 生成 → **撞车检测器必跑**(中文那次 Cooks/Chefs 双双变「厨师」,
>   靠 `SHORT_FIX` 人工裁决表兜的)→ DDL 两列 additive → seed 白名单 + 清 `seed_state` → 前端 `label()` 三语都优先短名。
> - 126 int 测试 + 12 e2e 走查全绿、tsc 干净。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-08-02,「漏斗补分母 + 报告不推算不上的路」)**
> - **📉 M2 的五个数只有一个数**(量数才发现):库里只有 `lock-seen` 4 次,第 1 步 `jd-open` **一次都没有** ——
>   职位详情页 `/jobs/[id]` 压根没埋浏览事件(只有列表页弹框的 `modal-jd`),分母是空的,**M3 的两种分叉照样分不开**。
>   补埋详情页(`kind=page`,弹框 `kind=modal` 分开);报告第 2 步改成**报告态真渲染才算**(从详情页进来是深链 `?view=report`,
>   先前只在「点出报告按钮」时记 → 最主要那条来路整条漏掉);详情页的 `jd-report-open` 退出漏斗(同一次跳转会双计)。
> - **🚫 判不了的行不再占位**(Frank「这一条不参与判定 这条对照不了 那还显示干什么」):
>   ① 门槛未收录的省 → 这一节**一行都不出**(先前出一句「本站还没收录」);② ON 学历免考条款那行撤掉(免考看「在哪读的」,题库问的是层级)。
> - **✅ 经验门槛真判得了**:题库刚补的总经验(含海外)就是官方口径 → 够=达标、不够=**fail 带差值**(免费层出不带差值的同义句)。
>   先前不够也只能出「本站只问了加拿大经验,判不了」。
> - **🇨🇦 EE 停抽的类别不再当锚**(Frank「STEM 都暂停多长时间了 还算 CRS 分数呢」):
>   库里坐实 STEM 上一次类别抽选 **2024-04-11**(28 个月前)、transport 2024-03、education 2025-09。
>   超过 365 天没抽 → 只陈述「上一次是什么时候、隔了多久」,**不给分数线对照、不劝他去算 CRS**;
>   这条**必留在免费层**(拿劝退的话去卖钱没道理)。
> - **🅾️ 不再把 ON 摆在前排**(Frank「安省现在是最难的吧 你还推荐安省」):换省对照的备选省排序加一档 ——
>   有分值表**且自报通道真有抽选记录** > 只有分值表 > 具名命中 > 在招量。ON 有 51 行分值表但改制后新通道一轮没抽过,先前正因为「有表」排第一。
> - **🧾 债一笔**:`lanes`(三卡)引擎还在算、`gateReport` 还在拼,但 v5 版式里**没有任何地方渲染它** —— 死代码,碰到再清。
> - **🧪 开发流量不再进漏斗表**(同批实撞):本地 dev 直连生产库 → 我在 localhost 上验一次版式,
>   生产漏斗就多几条假数(2026-08-02 当天 `report-open/pr` 3 条全是我截图跑出来的)。`/api/track` 按来源 host 挡掉本机。
>   **今天的漏斗数据不干净,读的时候记着:** 08-02 的 `report-open` 3 条(pr)与 `jd-open/page` 1 条是验证流量。
> - 125 测试绿(+7)、tsc 干净;生产实跑两处:NOC 20011 的 EE 行出「28 个月没有再抽」、经验行出「24 个月 —— 达标」;
>   真访问一次生产详情页 → `funnel_events` 冒出 `jd-open/page`,**第 1 步的分母通了**。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-08-01 夜,「题库扩充:学历/年龄/总经验」)—— 本地已验,等 Frank 点头再 push**
> - **✅ 三个字段进题库**([设计](docs/design/题库扩充-移民画像字段-20260802.md)):`eduBand` / `ageBand` / `totalExpBand`。
>   先前引擎把学历写死高中、年龄写死 0、海外经验当没有 —— 官方分值表最重的三项全在白扔分。
>   拿 PR 基本题 4→7 道、选省份 1→5 道(题目自动出现:SurveyJS 读字段库,前端零改动)。
> - **📈 扩充前后实测**(生产分值表,硕士/28 岁/CLB 7/海外 5 年):BC 24→**62**、SK 20→**63**(门槛 60)、ON 8→**16**。
>   **SK 那行从「离门槛差 40 分」翻成「已达标」** —— 先前那节不是保守,是把人劝退。
> - **🚫 两处按铁律否掉设计**:① 找工作卡不加「有没有 offer」(`buildJobReport` 不消费它,问了改不了任何一行);
>   ② 法语/配偶仍在路线图(SK 有 language2、BC 没有;CRS 计算器还不存在)。
> - **🧹 顺手三样**:首页占位卡三列在 375 把「选城市」截成「选 c…」→ 手机 2 列、桌面 3 列;
>   四张活卡的小注 zh/en/ko 全部压到不截断(实测 scrollWidth 逐条核);首页 CTA「答三道题」改「答几道题」(现在是 7 道,数字写死=假承诺)。
> - **📄 答题态定在翻页**(Frank「还是改成翻页的吧」):职业=第 1 页,其余一屏一题,进度条回来(「已填 3/7 项」)。
>   一屏全放试过一轮,7 道题在手机上要滚三四屏才看得到「出报告」;题干里的 ①②③ 同批撤掉(序号由进度条给)。
>   顺手修一个实拍撞到的真 bug:答过一轮再回第一页,chip 上甩的是「31301」——`OccPicker` 现在按码现拉职业名。
> - 117 测试绿(+5)、tsc 干净;375/1280 实拍无横滚。**⏭️ 下一步照旧:PDF 打印样式做加法。**
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-01 晚,「报告版式 v5 + 换省对照节」)——a39d7a9..c11c088 全生产**
> - **✅ 换省对照节**([L2-08](docs/implementation/L2-决策引擎/08_换省对照节.md)):「换一个省会更有利吗?」上三卡(拿 PR / 找工作 / 选省份)。
>   方案 A —— **不加必答题**,只拿已答的语言与加拿大经验去匹官方分值表,算的是**下界**(「至少 N 分,已答 x/y 项」);
>   `scoreProvince` 加 `only` 白名单,未答的因素不许被兜到最低档白捡分。
> - **💰 整节收费**(Frank 看实拍打回:「你这说完不是等于没说么」——免费层只剩「该省有没有分值表」)。
>   免费层整节不下发,锁区留一行问句当广告;Pro 侧配了可操作的三样:离门槛差多少、先补哪一项最值钱(未答项里官方分值最高的)、
>   拿到该省 offer 官方给几分。`rpt.lock.score` 挂了一个月,今天第一次背后有真货。
> - **📋 省份下拉 + 抽选明细**:引擎把每个候选省都算好(前 3 个直接摆、其余进下拉),行尾灰字点开看逐轮抽选(日期/通道/分数线/邀请数)。
>   **候选池规模与在池时长各省官方都不公布** → 不写不编。
> - **🧹 删了两处废话**:卡头「数据日期」(值是 `new Date()`,跟数据新旧无关;真日期跟着各自出处走)、每节的免责句(全站页脚已有)。
> - **🔧 验 Pro 版式的新手法**:本地建不了 Pro 态 → 一次性 spec 拿生产 facts 跑引擎导出 Pro 版 JSON,
>   playwright 拦 `/api/report` 灌进去实拍(spec 看完即删)。以后验 Pro 版式都用它。
> - 107 测试绿、tsc 干净;生产复验:`/api/report` 免费态 `switches=[]`、`locked=['score','sponsors']`,页面无横滚。
> - **⏭️ 下一步**:PDF 打印样式从做减法改成做加法(出处与日期、门槛逐条对照、雇主明细与 LMIA)。

> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-08-01,「规则引擎两刀 + ON 打分表 + 雇主线索」)——77854d9..a1797a4 全生产**
> - **🧭 先读 [主线与支线](docs/主线与支线-20260801.md)**(Frank 2026-08-01 拍板):判据只有一条「能不能把**有人真的掏钱**往前推一格」。
>   主线四格 M1 锁区铺到落地页 → M2 漏斗五个数 → M3 按数据分叉 → M4 第一笔付费;**下一步=M1**。当天 10 笔提交里主线只动了一格,这就是分线的起因。
> - **✅ 规则引擎两刀**(L2-04/05):BC 官方门槛 20 条(语言 CLB4 按 TEER / 最低家庭收入 7×2 / 24 个月经验 / 雇主侧 1 年·5 人·3 人)+
>   ON/OINP 11 条(**雇主侧**在营 ≥3 年、营业额 GTA $1M·点名普查区 $500K·其余 $250K、雇员 GTA ≥5 外 ≥3;申请人侧语言按 TEER 且技工低一档、免考条款只陈述、工资档 basis=occMedian)。
>   新表 `pnp_requirements`(31 行);引擎 `lib/rules.ts` 纯函数,**unknown 是一等公民**;报告新增「门槛对照」节,免费=门槛+达标判定、付费=差多少。
> - **✅ ON 成为省估分第三个省**(E12-09 §3.2 销账):OINP EOI 11 项 51 行入 `pnp_score_factors`。**官方不印总分也没申请门槛** → maxTotal/passMark 双空,卡片「有总分才显示 /总分」;
>   抽选线改抓 invitations 页(12 轮带线),但那些轮次属改制前已关停通道 → 打分表 system 自报通道名,卡片只认同通道抽选,对不上就说「这条通道还没有抽选记录」。
> - **✅ 雇主线索**(L2-06,锁区第一次有货):Pro 拿到具体名单(命中口径岗数/地点/ESDC LMIA/AIP/最近发布,直链公司页),每家再挂**雇主侧门槛**(GTA 内 $1M·5 人 / 点名普查区 $500K·3 人 / GTA 外只给人数)。
>   **口径两坑记死**:① ON 政策上不公布清单 → 只收「命中具名清单」会让 ON 一家都出不来,改成清单省用 named、`NO_LIST_PROVINCES` 用粗筛可提名 eligible,**两个数分开存**;② 计数与名单必须同一个 WHERE。
>   「有 N 家」那句从锁区**放回免费层**(免费层唯一的 aha),锁的是名字。
> - **✅ UI 六条队列一次清完** + `/plan/*` 骨架照职位详情页统一(白卡包 H1+右上返回、列宽 860)+ 选职业修两处(热门 24 个摊开、点进来不再「切换一下」= 兜底清单被当首屏渲了)+ 英文界面 A–Z 索引。
> - **📌 待办(按主线优先)**:M1 锁区铺到职位详情页 → M2 漏斗埋点串线;**Pro 态本地建不了 → 需 Frank 拿真 Pro 号验雇主名单是否真放开**;
>   支线欠账见 [主线与支线](docs/主线与支线-20260801.md) §2(政策留痕闸门、两个 build 脚本进批、unique-key 警告、#126 错标)。
> - **📏 push 节奏**(同日定):一个 push = 一次能验完的批(≈1.5 笔/250 行),**每天 ≤3 个构建窗口**,纯文档/数据带 `[skip render]`。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-07-28,「E8-14 统计主图与就业市场分析」)——94e26a1**
> - **✅ 主图上线**([E8-14](docs/implementation/E8-UI体验统一/14_统计主图与就业市场分析.md),/stats 首图,ECharts 原生):
>   横轴 职业 489 / 省份 10 / 城市 400 三切换 × 簇内 不分组/省份/职业大类 × **右轴叠中位年薪**;
>   缩放走 `dataZoom`(slider+inside),簇状柱=多 series 共 xAxis —— **一处没手搓**(Frank「不要自己实现」)。
>   **退化组合直接置灰**:一个职业只对应一个大类与一个 TEER,横轴=职业时按它们分簇每组只有一根柱;
>   城市×大类数据层没有 —— 宁可禁用,不画退化图。
> - **主结论(图自己会说)**:柱与线明显反向 —— 厨师 1,963 岗 $41.6K、食品柜台 1,021 岗 $37.0K,
>   而木匠 656 岗 $74.9K、餐饮经理 896 岗 $74.9K。**岗越多的地方钱越少**,这是「有 4 万个岗」与
>   「找不到工作」能同时成立的解释。TEER 4-5 占在招 34%。
> - **✅ 数据底座四块**:`stats_occupation`(3,401 行/489 职业)、`stats_city`(2,387)、
>   `title_zh_short`(450/489)、`stats_daily`(每日快照,**只 UPSERT 不清空**,07-28 起积)。DDL 四张已代跑生产。
> - **📌 口径实核推翻旧说法**:42,793 个在招里 **Job Bank 直发仅 44%**,其余 56% 是它聚合的
>   indeed.com 10,461 / Jobillico 3,361 / Québec emploi 2,768 / CareerBeacon 2,695 / SaskJobs 1,505 ——
>   **它≈Indeed+各省招聘板+政府直发的并集**,不是「LMIA 广告板」(我先前的判断错了,已推翻)。
>   仍缺 LinkedIn 与自建 ATS → 标题写「在招职位分布」**不写「加拿大就业市场」**。
> - **📌 中文职业名不是官方的**(Frank 追问查实):NOC 2021 官方只有英/法两版,中文是本站 04f 翻的。
>   所以三个名字各司其职:`title` 官方英文(引用依据,永不改)/ `title_zh` 完整译名(讲语义)/
>   `title_zh_short` 短名(窄位)。短名不是篡改官方名。
> - **⚠️ 短名撞车是模型看不见的错**:逐条压缩时它不知道别的职业压出了同名 → Cooks 与 Chefs 双双变「厨师」。
>   修法=`SHORT_FIX` 人工裁决表(15 组,Cooks 厨师/Chefs 主厨、Optometrists 验光师/Opticians 配镜师…)
>   **+ 撞车检测器跑完必报**,新 NOC 进来会自己亮,不静默上线。
> - **🐛 今天两次踩同一个坑,记死**:改了 ETL 产出形状后灌库,顺序必须是
>   **先部署代码 → 再 `DELETE FROM seed_state WHERE name='<表>'` → 最后灌**。
>   ①上午 `pass_mark` 三列全 NULL(首次 seed 用了没带新列的旧路由);
>   ②下午 `title_zh_short` 全 NULL(生产定时 ETL 抢先用旧产物重灌)。两次都因表级哈希一致而**静默跳过**(counts=-2),
>   错误数据留在库里不报错。
> - **📈 流量与注册(07-28 实查)**:近 24h 访客 72(↑15%)、浏览 170(↑58%);近 7 天真实注册 30 个
>   (07-27 单日 12),累计真实用户 33。**「陌生注册≈0」的旧前提已过时** —— pay-click 攒够两周就能拍付费面。
> - **⏭️ 下一步**:3.7 趋势图(Frank「等几天再做」—— 现在只有 07-28 一天,画出来是个点;
>   `stats_daily` 每轮自动积,几天后再画)。

> **📦 更早交接(2026-07-20 及以前)已沉档** → [docs/STATUS存档-2026H1.md](docs/STATUS存档-2026H1.md)(只留最近两轮惯例)。
