# STATUS / 交接文档（2026-07-05）

> 新 session 接手先读这份 + `CLAUDE.md`(设计宪法)+ `prd.md`(v2 定位见头部标注)。仓库:github.com/zhizhunbao/pnp-job-tracker
> **🚀 站点已公网上线:https://offer2pr.com**(Render + Postgres;**live Stripe,M3 收费闸已开**)。
> ⚠️ **「真实收款」这四个字 2026-08-03 撤掉**:live 通道验过没错(07-04 开闸演练,已退款),但库里两条
> Stripe session 一条是测试号、一条是 Frank 自己那笔 —— **陌生用户付费至今 0 笔**。别再把「已收款」当既成事实。批次进度=`docs/implementation/_开发批次顺序.md`:**B0-B8 全部落地(2026-07-04 一天从 B4 打到 B8),24 工作项代码侧全完**。
>
> **📍 当前状态速览(2026-08-10)**
> - **站在哪一格**:代码侧 B0-B8 全落地、站已上线、live Stripe 收费闸开,唯一没迈过的坎=**陌生用户付费 0 笔**(见上)。主线判据只有一条:能不能把「有人真的掏钱」往前推一格(`docs/主线与支线-20260801.md`)。
> - **最近在推**:判定合一/对话决策页(`/plan/pr` 答题入口 + 三关判定 + 案例库)与对话顾问闭环;名录匹配口径批 D 已收口。数据补全保鲜是常驻主线,缺数据当场抓,不对用户说「本站未收录」。
> - **工作区未提交**:`quiz/`、`plan/pr` 等若干前端改动 + 两份 08-10 设计文档(用户是谁与付费错配 / 破局整体方案),尚未落码上线。
> - **最紧未结项**:批 D 欠账②(真 Pro 号生产验判定卡直渲,待 Frank 亲手)、名录括号写法命中 281→305(待拍)、雇主中文名 brand 遍回写(待 `--yes`)。展开见下方两轮交接 + 记忆 `next-session-status`。
>
> **📍 2026-08-16 全天:估分独立 section + 撤浏览器缓存(`540e1501` → `4b5e12b8`,均已上线验讫)**
> - **起点是一句盘数**:Frank 问 PR 评估这功能怎么样。第一方漏斗 6 天 291 次打开,但**真人只有 5 个**
>   把答案留下来 —— 其余是我们自己的走查(Umami 近 24h 14 个访客里 4 个 Nepean = 他本人)。
> - **查下来估分「算了、上了页,却对排序与结论零影响」**:`belowLine` 两头都拿下界判、又把 partial
>   一刀切掉;AB 表带 12 条加分项 ⇒ 恒 partial ⇒ **沉底永不触发**,「够得着」压根没写过。
>   → `lib/scoreLine`(新)三态:**下界≥线=够得着 / 上界<线=够不着 / 中间留白**,两头各取各的那一侧才是硬结论。
> - **独立 section**(`ScoreLineCard`):近 6 轮抽选线(未答也给看)→ 你的估分 → 一行结论;
>   估分题的格子与结论同处一卡,计数与基础卷各算各的。省页签走通用 `ui/Tabs`,表走公共 `DataTable`。
> - **BC/ON/SK/NL 从「问了白问」到能算分**:它们的必答档位要岗位侧输入(时薪/地区/年收入)或
>   拆分经验,判定层整省不接 —— 可用户在分值卡上**明明答了**,只是从没上行。加分项勾选、直选档位、
>   时薪/地区、拆分经验与第二语言全部上行(信任边界逐项校验),**没答的仍然不接,不猜**。
>   实测 BC:$52/hr + 其余地区 → 估分 97、「最近 6 轮里高过 5 轮的线」。
> - **撤掉浏览器缓存(Frank「用缓存就是这么麻烦」)**:`users.answers` 成唯一真相,运行态住内存。
>   失败退避重试 2/6/18s + 离开页面 `sendBeacon` 强推(端点补 POST);老用户本地旧档一次性搬家后删除。
> - **过程中揪出三个真 bug**(都不是他要求改的):① **空档覆盖** —— 撤缓存那版内存初始为空,挂载时的
>   初始化写会把空档 PUT 上去,差一步盖掉整份档案(他看到 0/11 时库里 845 字节完好);加 `hydrated` 闸。
>   ② **换账号串档**(内存是模块级的,登出必须清)。③ **默认值当答案**(时薪/地区还写着「待填写」,
>   服务端却按 $0/大温算出 45 分)。
> - **口径纠正一条**:NS 那句中文写成「不设分值表」= 替官方断言不存在。官方原文只说「酌情」,
>   我们能断言的只有**没公布**。改「未公布分值表」——[[铁律]] 的同一条线。
> - **下一轮待办**:① ON/SK/NL 的真机实测(机制同 BC、单测已覆盖,只差真机走一遍);
>   ② NT 的 EOI Points Grid(本次核对发现的漏,官网明写有);③ AB/BC/NL 三份 PDF 原件落缓存
>   (crawl 役跳过 .pdf,加分项核对到它们就断了);④ NB/YT/NU 官网覆盖近乎为零(各只抓到 1 页)。
>
> **📍 2026-08-12 晚场收口(判定口径根治 B1+B3-min+B4,`61d7757` / `1ab0d9f` / `21ab7b3`,已上线验讫)**
> - **Frank 的问题是「用户能付费吗」——问的不是 Stripe 通不通,是初步方案准不准。**实测生产
>   `/api/profile-pathways` 五画像 → **15 条结论全是 open**,一条 needs-info 都没有,含
>   「从没来过加拿大的海外护士 → NLPNP 国际毕业生排进前三」。
> - **根因不是缺数据**:`pnp_requirements` 是开放世界的表(只装抽到的行),`pathVerdict` 却做封闭世界
>   推理。`missingSlots` 全文只在 noc/clb/expCanadaMonths/province 四处被填,且都要**先有一条匹配的
>   requirement 行**才触发 → **没有行 ⇒ 没有闸 ⇒ 没有 unknown ⇒ open**。所以补表不收敛,
>   且补的过程中系统仍在静悄悄输出错误结论。设计见 [通道判定口径根治](docs/design/通道判定口径根治-20260812.md)。
> - **落地**:① `lib/gateManifest.ts` 门槛清单(13 通道 × offer/境内身份/加拿大学历,官方原句锚定,
>   取证器 `etl/scan_gate_quotes.py` 从 crawl 缓存捞句再人工核定);② 三值折叠 met/failed/unknown,
>   **条文缺(我们的窟窿)与答案缺(他一步能补)分开**,只有前者是 `not-collected`;
>   ③ 标签报**最难拆的那道闸**(BLOCK_COST:offer 0 < 境内 1 < 自雇 2 < 语言 3 < 加拿大学历 4,
>   裁决与排序共用);④ pr 基础卷 6→8 题(offerBand 既有题进卷 + canadaEduBand 新增,境内身份由 status 推)。
> - **生产实拍**:海外护士无 offer 无学历 → NL 国际毕业生「要加拿大学历」、AIP「要先有 offer」、
>   EE「优先核对」;同一人补上 offer 与学历 → 三条全部解除阻塞。
> - **顺手逮到两件**:① **撞了本文件铁律①**(代码里不许手写官方原句)—— 清单的 quote 已降级成注释、
>   结论只挂出处链接;**正解是把清单下沉数据层**(ETL→mart),待拍。② **AIP 名录静默塌方**:
>   NB 官方 PDF 换版 → 解析只得 29 行,把上一轮 1263 行直接覆盖,一路灌进 mart(3867→2633)与生产库,
>   **全程零报错**。PE 早有这道闸只是没推广 → `MIN_ROWS` 覆盖四省,低于下限保旧不清空并大声喊。
>   raw/mart 已修回;**生产库仍是 2633,靠 build 容器下一轮(1h 兜底)自愈**;NB 新版式解析待修(另账)。
> - **✅ 名录已自愈**:build 容器那一轮把修好的 mart 灌回生产,`designated_employers` 回到
>   **3867(NB 1263 / NS 1574 / PE 391 / NL 639)**,实查生产库确认。
>
> **📋 下一个 session 的工作单 ①(最优先):按《PR 评估页三步重设计》落码 —— Frank 08-12 拍板另开 session 做**
>
> **先读** [design/PR评估页三步重设计-20260812.md](docs/design/PR评估页三步重设计-20260812.md)(含全量审计 14 条 +
> 三步的输入/输出 + 第三步效果图两张)。设计已成文,**细节自己拍,不必逐条回来问**;
> 只在要动定价/锁区、或要推翻文档里的拍板时才停下来问。
>
> **Frank 的交付方式(原话)**:「改完一部分我检查然后再继续」——**每批做完就停下来给他看**,
> 别一口气推到底。建议每批都:tsc + `npm run test:int` 全绿 → 起 dev 让他点 → 他点头再提交。
>
> **落地顺序(设计 §4)**:
> - **✅ B1 举证标准(已落码,未提交 —— 等 Frank 点头)** —— 粗筛撤对错符号并锚到真门槛;清单行补适用范围。
>   改动三处:`lib/tripleVerdict.ts`(粗筛行 state 恒 `info`/`unknown`,**永不 pass/gap**;新增 `teerScopes()`
>   按通道聚合 `pnp_requirements.appliesTeer` 找出「不收这一档」的通道并挂上它的官方原句与出处;
>   `notListed` 行带清单名/职业数/所绑通道,该省一张清单都没收录时说「本站未收录」不说「未命中」)、
>   `TripleVerdictModal.tsx`(粗筛行给中性圆点不给 ✓/✗;**职业匹配胶囊的绿勾只认具名清单命中**)、
>   `jobs/i18n.ts`(三语新文案 + `reqStreamDisplay` 通道短名表,英文短名只做前缀缩写不改本名)。
>   ⚠️ `teerScopes` 读的是**本站收录范围的下界**:只拿来否掉粗筛的对错符号,**永不**拿来下「你不行」的结论
>   (开放世界的表做封闭世界推理 = 08-12 刚根治的那个病)。
>   验:tsc 绿、34 文件 **509 测**全过(新增 B1 回归 4 条,老断言 `state:'pass'` 已按新口径改);
>   生产数据实拍 job **24309575**(Anne of Green Gables Chocolates,PE TEER 5)→
>   「本站粗筛:TEER 5 可试」+ 灰字「PEI 技术工人通道只收 TEER 0–3」+ 官方原句 + princeedwardisland.ca 出处;
>   桌面 1440 与手机 375 × 中英各一遍,均不折行。
>   **Frank 当场两拍(已改)**:①「不在清单上不等于走不了」是废话 → 删,清单名+职业数已把适用范围说清;
>   ② 指「重复」→ **A3 提前销账**:判定第三关「个人条件」与页面那张问卷回显卡「你的条件」同屏打架,
>   第三关改称**「你这边」**(胶囊与卡头同改;建档 CTA 改「填写你的条件」指向那张卡)。
> - **✅ B2 第三步重排(已落码,未提交)** —— Frank 看完 B1 后指着整张判定卡说「这一部分先删了,重新设计」,
>   选定「整张卡删掉直接重做」。新形状 = 设计 §2 第三步:**抬头(岗位+雇主+城市省灰字)→ 一句可复述结论 →
>   三关 → 下一步两个动作**;三项胶囊条撤掉;**带岗态不再摆「你的条件」问卷卡**(收成抬头下面一行
>   「按你答的 n/N 项判定 + 改答案」——输入面只留一个,A3 结构那半销账);付费块挪到下一步之后,
>   标题「还差多少与多久能到」。
>   **结论句由确定性层拼**(`tripleVerdict.conclude()`,红线:不让 LLM 合成):职业被官方排除 → 有能走的 →
>   被闸卡住(报最好拆的那道,共用 `pathVerdict.blockCost`)→ 判不了(点名只取新导出的
>   `PathwayVerdict.missingSlots`,**他一步能补的那几样**;点不出名字就落「本站未收录」)→ 本站未收录。
>   ⚠️ **免费/付费口径一个字没动**:结论句与「你这边」那条闸来自 pathVerdict,与同一页免费的
>   「你的初步方案」同源;逐项差值仍锁。**位置变了口径没变**,要不要改口径归 Frank 另拍。
>   验:tsc 绿、34 文件 **514 测**(B2 新增 4 条:报最难拆的闸/排除先于一切/无通道说未收录/目标省不参与结论);
>   生产数据实拍零答案态 → 「判不了:还缺目标职业、加拿大经验、job offer」(不再把工签剩余月数、
>   目标省这类与「能不能走」无关的槽塞进结论句)。
> - **✅ 工作单 ② 补数据(已落码,未提交)—— 13 条通道 `availability` 全 `ok`,一条「本站未收录」都不剩**
>   Frank 原话:「缺资料就去补资料啊,我的 crawl 工具不是全的吗,老弄一些半成品未收录」。**crawl 确实不全**:
>   `nb-imm` 1 页 / `pe-imm` 7 页(门户+新闻)/ `bc-immigrate` 40 页全是门户 —— 而 10 个未收录的闸 100% 落在这三个省。
>   - **取证器扩了 PDF 源**(`etl/scan_gate_quotes.py` 新增 `PDF_SOURCES`):有的省条文根本不在 crawl 里
>     (PE 的 HTML 挡在 Radware 后、BC 那页明写「完整条件见 Program Guide」),**而我们自己的 ETL 一直在读那两份 PDF**。
>     URL 逐条抄自 `build_pe_req.py:GUIDE_URL` / `build_bc_req.py:PDF_URL`,不猜。
>   - **PE-sw**:三类闸从指南 PDF 逐条读出(offer 必须、境内身份必须、加拿大学历**不要求**——官方要两年制以上学历但不限加拿大)。
>   - **BC-sw / BC-build**:直提官方 Program Guide PDF(63 页),§3.1–3.13 通用 + §4.1 技术工人逐节读完 →
>     offer 必须、境内身份**不是闸**(§3.3 是条件句,同 ON 那条)、学历闸**整份指南都没有**。
>   - **NB-sw**:老地址 302 到换版后的新站,资格页搬到 `www.gnb.ca/.../skilled-worker-stream.html`(链接从重定向落点现取)。
>     三条 pathway 里本站判的是 **NB Experience** → offer/雇主支持必须、境内(住满 6 个月)必须、加拿大学历不要求。
>     🔴 **同一处换版还坐实一件事:`etl/pnp/build_nb_req.py` 的 `guide_urls()` 现在返回空 —— NB 的门槛行已冻结**(另账)。
>   - **顺手逮到三个真错**(都是「半成品」的根,不补数据看不见):
>     ① `/api/triple-verdict` 把 `profile.teer` 硬写 null → `pickGate` 按 TEER 挑行时一条都挑不到 →
>        PE 这种只有一条 `applies_teer=0,1,2,3` 的省**恒报「本站未收录」**。改成由 NOC 第二位推(全站同一口径)。
>     ② `pathVerdict` 把「他没答职业」也算成 `availability='not-collected'` —— 拿用户没答冒充我们没数据。
>        新增 `GateEval.teerUnknown`:行在库里、只是不知道他哪一档 → 仍是 `ok`,走 needs-info + `missingSlots:['noc']`。
>     ③ **PE-sw 被 OID 清单整条判死**:官方原句(就在我们库里那行经验门槛的 label)写明受限的是
>        「the Occupations in Demand stream requires only 12 months, but is limited to its named NOC list」——
>        受限的是**那个子通道**;Skilled Worker 走 TEER 0-3 + 24 个月与清单无关。改成只在该 TEER 没有门槛行时才关。
>   - **实测(生产数据)**:PE TEER 3 的岗答满 → **「符合爱德华王子岛省 在需职业通道的已知门槛」**(tier 0);
>     零答案 → 「判不了:还缺目标职业、语言成绩、job offer、加拿大境内身份」(**全是他一步能补的**,不再甩「未收录」)。
>   - 验:tsc 绿、34 文件 **515 测**;`pathVerdict.int` / `chatVerdict.int` 里那两条
>     `.toEqual(['BC-build','BC-sw','NB-sw'])` 按工作单②的验收标准改成 `.toEqual([])`;金标 C01 的
>     「PE-sw excluded」按官方原句改判并补了一条 TEER 5 的反证(那时清单仍然关死)。
> - **✅ NB 采集修复(已落码,未提交)**:`build_nb_req.py` 的 `PAGE_URL` 换成换版后的技术工人通道页
>   (老地址 302 落在 PNP 总览页,而三份指南 PDF 挂在总览页下面的子页上,所以 `guide_urls()` 一份都找不到);
>   相对链接改用 `urljoin(响应最终 URL, href)`,不再把 `www2.gnb.ca` 焊死 —— 下次再换址不用改代码。
>   实跑通过:三份指南版本 **2026-06**,三条门槛值(CLB 4 / 同雇主 6 个月 / NB 居住 6 个月)与旧表**逐字相同**
>   —— 冻结期没出错数,只是差点开始过期。raw 已更新(URL + fetched),**没手工跑 09+seed**(会与容器抢,值没变,等下一轮)。
> - **✅ 判定卡 SSR 首屏 + 卡片化(已落码,未提交)** —— Frank 两拍:「上来为什么显示这个(骨架)」「section 分多个卡片,
>   每个卡片都是自包含的」。
>   - **SSR**:`respond()` 整块抽成 `lib/tripleWire.ts:buildTripleWire()`,`/api/triple-verdict` 只剩路由外壳;
>     `/plan/pr` 服务端先算一版传 `initial`,面板拿它当初值;**本地一条答案都没有就不再发那次 POST**
>     (同样入参问两次只会白闪)。`ready` 那道闸对带岗态放行(它本是防「首帧渲判定又被答题卡顶掉」,
>     而带岗态如今不摆答题卡)。**实证**:`curl /plan/pr?job=` 的 HTML 里直接有结论句与「省份政策」,
>     首屏不再是 ~1.5s 的灰条。
>   - **卡片化**:结论卡(抬头+结论句+依据行+8 格条件+下一步两钮)/ 职业 / 雇主 / 省份政策 / 还差多少与多久能到,
>     **一段一张白卡**,各自带标题与出处。
>   - 条件回显**8 项全列**(Frank:「不然用户怎么对比?如果要修改答案呢?」):答过的实线、没答的虚线,每格可点进答题。
> - **✅ 卡序按 Frank 当场口述定死(已落码,未提交)**:**① 这份工作 ② 你的条件 ③ 你的初步方案 ④ 职业
>   ⑤ 雇主 ⑥ 省份政策 →(下一步两钮)→ ⑦ 还差多少与多久能到**。「你的初步方案」抽成 `planCard` 变量,
>   带岗态经 `planSlot` 交给判定面板当第三张卡,无岗态原地摆 —— 一份 JSX 两处用,不复制。
>   顺带两处措辞按他实拍点名改:①「你还缺语言成绩」→ **「语言没到门槛 CLB {need}」**(他答过 CLB,
>   语言是差档不是没答;need 取 pathVerdict 已算好的官方门槛,**差多少不进免费句**);
>   ②「新斯科舍省 技术工人通道判不了:还缺目标职业…」→ **「还判不了:补上你的职业、加拿大工作经验、
>   有没有 offer」**(通道全名+内部术语没人读得下去;枚举改用 `tv.slot.*` 的人话问法)。
> - **⏭ 紧接着(Frank 已点名,未做)**:**走不了之后给出路** —— 他连问三次「是不是推荐其他职位/提升语言/换省份」
>   「雇主那个是不是有其他雇主的推荐」。手上已有的料:`pathLevers`(clb-boost / teer-downgrade,现成的
>   「提升语言/换职业档」推演)、`designated_employers` 按省(其他 AIP 指定雇主)、职位板按 NOC+省查(其他岗)。
>   **口径要先拍**:照记忆 `pricing-facts-free-conclusions-paid`,事实型推荐(其他岗、其他雇主)该免费、
>   推演型(考到 CLB N 能解锁几条、换省对照)该付费 —— 但本轮红线是「不动定价与锁区」,所以等 Frank 拍。
> - **✅ 竞争度进初评(已落码,未提交)** —— Frank:「竞争肯定是要排序的」「很多人不知道竞争激烈程度,
>   我们有这个数据并且是最新的就有这个能力」。三块:
>   ① **可行通道初评**每行带该省名额竞争,并在**同一障碍档内**按松紧排序(门槛难度仍是第一主键);
>   ② **各省名额竞争表**(9 省,SK 9:1 → BC 84.2:1)进资料区、摆在抽选表之前,走 SSR 能被爬到;
>   ③ **该职业分省竞争表**(新端点 `/api/occ-competition`,按 NOC 懒取):在招 / 近 30 天新增 /
>      平均在招天数 / 该省名额竞争,**四列不合成分数**。
>   数据面并进 `getScoreTables` 那份 10 分钟 TTL,不加每请求查询。
>   时间维度按 Frank 要求补齐:存量写到月(2024-12)、名额年份逐省不同(2025/2026)、本站更新日、
>   外加**新发学签流量**列(2026-05 YTD)——实查确认**年末存量官方就停在 2024**(xlsx 表头 `2000 - 2024`),
>   2025/2026 只有月度**流量**表,与存量口径不可混用,故只作趋势列不进比值。
> - **🔴 竞争度算法的欠账(Frank「先这样显示,之后数据全了再优化」)**:这个数**不是**「几人抢一个」——
>   ① 分子是全部临时居民存量(含 IMP 配偶工签/公司调动等大量不申请省提名的人),且**不含海外申请人**;
>   ② 分母(名额)来自本站人工核对表,源是 moving2canada/律所博客,**不是 IRCC 一手**;
>   ③ 年份错位:分子统一 2024-12、分母 2025 或 2026,而名额一年能变三成(ON +31% / BC −15%);
>   ④ 真竞争比 = 申请人数 ÷ 名额,官方不发(即 08-11 结案的 EOI 池问题)。
>   **本轮只修了一处事实错误**:页脚原写「两者来源 IRCC 开放数据」→ 改成「临时居民来自 IRCC 开放数据,
>   名额为本站人工核对表」(写错出处比措辞过头更严重)。**措辞优化(「名额竞争 9:1」→「每名额 9 名临时居民」)
>   等数据补齐后再做。**
> - **⏭ 再往后**:B4 前两步收成向导;B5 资料区下沉;城市 × 职业竞争(`stats_city` 只有城市总量,
>   要从 jobs 表按 noc+city 现算);走不了之后给出路(免费/付费口径待拍)。
> - **B2 第三步重排**:抬头 → 一句可复述结论 → 三关(第三关改称「你这边」)→ 下一步;带岗不再重复摆问卷卡。
> - **B3 省份消歧 + 一键「加进目标省」**(带岗页主语=岗位所在省)。
> - **B4 前两步收成向导**:未答只出 CTA 不摊 8 个空格;估分降为第二步的二段;每步一行摘要。
> - **B5 资料区下沉**:抽选表/案例撤出主干,抽选上移到案例之前。
>
> **红线(别破)**:结论句由确定性层拼、**不让 LLM 合成**;URL 不变(保收录);
> **本轮不动定价与锁区**(Frank:先做完整再谈收费);版式类改动照 Mode B 先出效果图再落码。
>
> **⚠️ 接手时 localhost:3000 上可能还开着一个 dev**(Frank 在测)——**只准一个实例**,
> 先 `netstat` 看一眼,别再起第二个。用 `localhost` 不要用 `127.0.0.1`(Next 16 当跨源拦掉 /_next,
> 页面能出但完全不水合、且零报错)。
>
> **📋 工作单 ②(排在 ① 之后):抓数据,把「本站未收录」从 3 条降到 0**
>
> **为什么是它**:判定口径根治后系统诚实了,但「判不了」变多 —— 其中 **3 条纯粹是我们的数据窟窿**,
> 不是用户的问题。这是唯一「工作量确定、验收清楚、直接减少判不了」的一批。
>
> | 要补的 | 现状(实查 crawl,非推测) | 落点 |
> |---|---|---|
> | **BC 技术工人 / BC Build** | `welcomebc` 那页原句「For complete information about eligibility and requirements, please see the Skills Immigration Program Guide.」——指南是 PDF,未入 crawl;`data/crawl/bc-immigrate` 40 页**全是门户页** | 抓该 PDF → `gateManifest['BC-sw'/'BC-build']` 的 statusInCanada / credentialCanada 从 `unknown` 改成带 quote 的 required/notRequired |
> | **NB 技术工人** | `data/crawl/nb-imm` 只有 **1 页**门户(`www2.gnb.ca/.../immigration.html`),没有资格页 | 补抓 NB 技术工人资格页 → `gateManifest['NB-sw']` 三类闸全部落实 |
> | **NB 的 AIP 名录 PDF** | 官方换版 → bullet 切不出来,现靠 `MIN_ROWS` 护栏冻在 1263 家旧快照(不会被冲掉,但会越来越旧) | 修 `etl/06_scrape_aip_employers.py` 的 `parse_pdf_bullets`(新版式) |
>
> **规矩(照旧,不许破)**:① **URL → 数据 → SQL**,先 grep `data/crawl/<slug>/manifest.json`,禁止凭印象猜 URL;
> ② 抓到的门槛**必带官方原句 + url + fetched**;抓不到一律落 `not-collected`,**不许**拿「页上没写」
> 当「官方不要求」;③ 清单里 `notRequired` 的 `basis:'absent'` 只在**读过该通道资格页**时才允许用。
>
> **验收(硬)**:`pathVerdict` 跑 C01 档案 → 13 条通道 `availability` 全 `ok`;
> `tests/int/pathVerdict.int.spec.ts` 里那条 `.toEqual(['BC-build', 'BC-sw', 'NB-sw'])` 改回 `.toEqual([])` 并全绿;
> AIP 名录实跑 `06` 不再出 `[WARN] NB`。
>
> **接手先读**:[通道判定口径根治](docs/design/通道判定口径根治-20260812.md) §3.1 + `cms/src/lib/gateManifest.ts` 顶部注释
> (三态与举证责任写在那儿);取证器 `etl/scan_gate_quotes.py`(从 crawl 缓存捞候选句,已验过)。
>
> **⏭ 再往后**(不在这一批):清单下沉数据层(现在 quote 写在代码里,撞铁律①,临时降级成注释)→
> 扩闸类(资金 / 雇主指定 / RCIP 社区推荐)→ 付费面复核 + 首屏免费事实(这两条要 Frank 拍口径)。
>
> **📍 2026-08-12 收口(PR 评估页评估 + 两批修复,`24936a2` / `f382fc6`,均已上线验讫)**
> - **起因**:Frank 让评估 `/plan/pr`。工作区当时压着一批未提交的「答题卡改弹窗」改动,评估先在这批上
>   撞出**三个阻断**:① 08-11 刚上线的**各省估分整段变成死路** —— 入口门槛是 `scoreLeft>0`,而题数只能由
>   `PnpScoreCard` 挂载后经 `onQuestionnaireProgress` 回报,弹窗化后回报的那份只在弹窗里存在,
>   「要进估分得先有题数,要有题数得先进估分」;且弹窗一关组件卸载,16 道官方表的勾选(组件本地 state)全清零。
>   ② 冷门 NOC 名字补全**无限打 `/api/quiz`**(缓存写在组件体内每渲染重建=永远空 + effect 依赖它自己 set 的
>   state)。③ 弹窗「清空重填」压在 Modal 自带的全屏/关闭钮上。
> - **修法(一招结两件)**:**弹窗只装基础卷,估分整段就地留在方案下面那个 section**。分值卡全程单实例、
>   常驻不卸载 —— 题数报得出来、答案不会因关弹窗蒸发,入口/题目/结果三样同处一张卡,正是 08-11
>   「单独一个 section 放到初步方案下面」的形状。顺带:题目搬到页内后 `.quizBar` 在 ≤640 的 `position:fixed`
>   会一直浮在案例/抽选表上 → 作用域改回 sticky;英文 375「Your details」被计数胶囊挤成两行 → 标题 nowrap;
>   OccPicker 撤分页后的死代码清干净(接口 `fetchBroadNocs` 硬顶 60 条,本来就不需要分页)。
> - **`f382fc6` 性能批**:`/plan/pr` 原先 force-dynamic 每请求两条查询,并把 **192 行官方分值表 ≈ 88KB**
>   整份塞进客户端 props —— 只有答完题、且目标省落在 ON/BC/MB/SK/NL 的人才看得到。新增
>   `lib/scoreTables.ts`(两张表的进程内单件缓存,TTL 10 分钟,手法同 `verdictCache`)+ `/api/score-factors?provs=`
>   按省懒取;**抽选表仍走 SSR**(这页唯一的免费硬事实,要被爬到)。生产实测 **167.6KB → 55.7KB / 0.13s**。
> - **eslint 一直是崩的**(`eslint-config-next` 16 起自己就是 flat config,原来经 `FlatCompat` 走 eslintrc
>   兼容层 → 校验失败 → 报错格式化撞循环引用,**随便 lint 哪个文件都一样**)。后果不是少几条警告,而是
>   **这道闸事实上是空的** —— 本轮两处死代码全靠人肉发现。改成直接摊平 flat 配置;修活后 87 error/476 warning
>   全是历史存量,于是定基线:React Compiler 时代的优化建议类降 warn(不是 bug,73 条一次性红着只会让人
>   再也不跑 lint),`no-html-link-for-pages` 关掉(本站内链**故意**用真 `<a>`,靠 Google 招聘富结果活着)。
>   现在 `npm run lint` = **0 error / 549 warning**(欠账本,新代码别再添)。
> - **验收**:桌面 1440 + 手机 375,本地与生产各走一遍 —— 估分入口「还有 9 题」→ 答两题退出「还有 7 题」
>   (答案不丢)→ 再进接着答;`?job=` 带岗按该岗省份取表、三项判定正常;新访客页面里已无分值表字段、
>   零懒取请求;单次加载 `/api/score-factors` 只发 1 次。tsc 绿,vitest 34 文件 502 测全过。
> - **⏭ 下轮候选(已与 Frank 过过,未动)**:首屏对新访客没有一条免费硬事实 —— 第一屏是六个空的「待填写」
>   格子,唯一的免费事实(各省最近抽选)排在整页最末。**最小改法是先试排序**(把抽选表提到常见案例之前),
>   属既有区块换位置,不算新动线;真要重做首屏再走 Mode B 出效果图。
>
> **📍 2026-08-11 收口(PR 评估答题 + 判定核 + 处境页,三次提交 `9178c2e` / `cf1e496` / `3679eee`,均已上线验讫)**
> - **答题流**:四种题统一到 QuizUI 一副皮,动作条落点钉死(桌面 803,797 / 手机 241,761);加分项一屏一组 ≤4 条;
>   二选一改互斥;题量 33→16 屏;计数分「条件 n/6」与「估分 n/N」两段;估分改成方案页上的自愿入口(题数写在按钮上)。
> - **判定核**:新增 `blockedBy`(语言/自雇这类**攒时间补不了、考试能过**的门槛),排序改四档
>   (能走 → 被卡住 → 缺资料 → 排除)。修的是「CLB 4 的厨师把联邦 EE 顶到方案第一位」——
>   根因是语言差档只生成理由、不进 `gaps`,于是 tier=0 + open 稳排第一。
> - **BC 执业资格 +5** 按 NOC 只问该问的人:官方对照表(10 个 NOC + 技工展开共 57 个)落进既有 `rule` 字段,不新建表。
> - **处境页样板** `/cases/carpenter-ontario-graduate-manitoba-agent`:先答他问的省 → 为什么 → 由易到难 → 走不通 → 第一步。
>   结论全部来自判定核,案例库「只有画像与问题」的红线不破。竞争度按各省公布口径分别摆,不硬凑统一比值。
> - **处境页入口 + 「常见处境」两钮全撤**(Frank 当晚二拍):上一轮处境页上了线却没人走得到 ——
>   决策页 C01 那条补真 `<a>`「看完整答案」进 `/cases/<slug>`;slug 的唯一来源改成 `caseLibrary.CaseEntry.page`,
>   服务端 `CASE_PAGES` 由它 × 事实层 `PROFILES` 交出来(**写了 slug 但事实层没跟上 → 自动落榜,不出死链**)。
>   同批撤掉两个钮:①**「按这个条件代入」连 `preset` 数据一起删** —— 它把案例主人公的画像写进用户自己的
>   答案,答过题的人一点就丢;②**「问 AI 顾问」**(Frank:「AI 顾问烂的一逼,根本没法和 GPT 比」)——
>   拿别人的原话去问,答的还是别人的事。页尾那个页级顾问钩子**没动**。
>   结果:15 条没事实层的只摆问题、一个可点的都没有 —— **答不了就不假装能答**,也把「补事实层」这件事顶到台面上。
> - **PR 线走查 + 当晚连拍五刀**(第 38 轮走查=`docs/功能盈利点检查.md` 清单 #291-#300):
>   ①「常见处境」改叫**常见案例**;②**折叠撤掉**(Frank「隐藏小字有必要吗,不如把标题写清楚」)——
>   原话小字并进标题,16 条标题三语重写,出口改行尾钮**「完整案例」**;③**估分入口挪成独立 section
>   摆在「你的初步方案」下面**(先给方案再谈加题);④页尾「看在招岗 / 问 AI 顾问」两钮撤;
>   ⑤方案卡「查看详细行动方案」撤(它也是唤起顾问)。**顾问不再从 /plan/pr 任何位置导流**。
>   英文 375 实测:标题原本 15/16 折行 → 按「一行放得下」删到只剩 C01 那条(它旁边有钮)。
> - **#291 判定核措辞层落地(`pv.*`)**:处境页英文态原本整页中文(章节标题英文、内容全中文)。
>   根不是漏接 —— **判定核 22 处理由从来只有中文**,`/plan/pr` 方案卡看着干净只是因为它压根不渲理由。
>   现在 `VerdictReason` 加 `key` + `params`(形态照 `tripleVerdict.ts`),`text` 原样留着给
>   chatOrchestrate 与既有断言;35 个键 × 三语进 i18n;`CaseView` 的 H1、原话、`generateMetadata`
>   一起接 L3/`ssrLang`。**官方原句 `quote` 不翻** —— 引用翻了就不是原句。
>   顺手修一条测试逮到的真漏:`score.refLabel` 是中文拼的展示串,被当参数塞进英文句子 →
>   改成取 draw 的官方通道名 + 日期。
>   **`verdictWording.int.spec` 四条守门**(6 份档案 × 13 通道):每条 reason 必须有键、
>   中文经措辞层渲染必须与 `text` **逐字相同**、三语都查得到键、英文态零中文。
>   ——「改一处忘另一处」是静默的(t() 回落中文、漏键回落 text),不钉测试必漂。
> - **处境页版式与排序当晚再拍三刀**(`83562fb`、`29aee47`):
>   ①**灰色小字整段全改 bullet**(Frank 连指三处:供需行、「关于概率」框、每节段首句),同批按
>   `copy-no-explanation` 删掉纯铺垫四句 + 排序科普整条(走查 #294 顺带销账);②「用户原话」标签撤掉;
>   ③**同档内按「本省该职业在招岗数」排序**并把这个数摆成 bullet(排序依据要看得见)。
>   **竞争程度不进排序**:各省公布口径不同,硬凑跨省可比的「几人抢一个」就是编数 —— 走查 #292 只解了一半,
>   另一半卡在记账②(SK/MB/ON 的 EOI 池公不公布未查证)。
> - **⚠️ 本地 dev 复现不了「出方案」态**:同一份种子答案生产出 `你的初步方案`、本地 dev 不出,
>   **回退到 HEAD 同码仍不出** —— 是 dev 与生产构建的差异,不是代码。方案态的验收一律上生产做。
> - **文案口径收紧**(Frank 定,全站照此):不要括号/破折号解释/前缀铺垫/重述已知量/重复标签。见记忆 `copy-no-explanation`。
>
> **🧾 记账(2026-08-11 攒下、未做)**
> 1. **纽芬兰国际毕业生「岗位须与所学专业相关」未入库** —— 官方页明写(Memorial/CNA 毕业生豁免),
>    我们 `pnp_requirements` 只有 PGWP 4 个月 / offer 12 个月 / 年龄 21-59 三行。**本站缺口**,不是官方没写。
> 2. ~~**各省 EOI 池人数**~~ **已查证(2026-08-11,只读 crawl 快照,零猜 URL)**:
>    - **MB 公布**,但**在年报 §9「Expression of Interest Pool」里,不在 MPNP 站的任何通道页上**
>      (`immigratemanitoba.com/resources/data/annual-report-<年>`)。原句=「N active EOI profiles at the end of YYYY」。
>      年末在池序列:2017 **15,957** / 2018 19,468 / 2019 25,814 / 2020 21,859 / 2021 23,395 / 2022 15,375 /
>      2023 20,392 / 2024 **26,678**。⚠️ **官方文档自相矛盾**:2024 年报把这个数标成「at the end of **2023**」,
>      而 2023 年报给同一年份的是 20,392 —— 几乎肯定是 2024 年报的标签笔误,但**不许静默替他改**,
>      入库要么原样带官方标签、要么不入。口径是**年度快照**,与 AB 的实时池不可混用。
>      月度数据页那节也叫「Expressions of Interest」,但给的是**当月抽走的 LAA 数**(流量),不是池子。
>    - **SK 未查到**(sk-sinp 31 页 + 专页 `sinp-processing-statistics` 逐页扫)。它公布的是名额/已提名/
>      封顶行业名额占用,并明说「不含在审申请」。**且有一条政策事实让这个问题对本案无关**:
>      International Skilled Worker 的 **Employment Offer 子类根本不走 EOI 池**(官方 EOI 页明示)。
>    - **ON 未查到**;且 2026-06-25 整个项目重设(砍掉 8 个通道→新 Ontario Workforce Priority),
>      新 EOI 系统 **2026-08-04 才开**,连历史池子都无从谈起。
>    - **SK/ON 一律记「本站未收录」,不写「官方不公布」** —— 只扫了官网语料,没扫开放数据门户与年报 PDF。
>    - **结论对产品的意义**:跨省统一竞争比仍然算不出(AB 实时池 / BC 分数段 / MB 年度快照 / SK·ON 无),
>      「谁公布什么摆什么」的口径不变。
>    - **✅ MB 那条已入库上线**(`ce8de21`+`20617bc`,生产实证):`build_mb_stats` 解析年报 §10 →
>      `eoi_pool_total`,period 取**官方标签里那一年**。显示层**按期次形态分两句**:纯年份=年报年末快照
>      (MB「2023 年末在池 26,678 人」),带日期=当天实时池(AB「2026-07-30 在池 36,948 人」)——
>      套同一句话就是把去年的数说成今天的(实拍撞到)。
>
> **🔴 2026-08-11 揪出的两个基建问题**
> 1. **处境页的数字被冻在部署那一刻**(`af1a326` 已修):`generateStaticParams` 与 `force-dynamic` 同在,
>    Next 在**构建期**预渲染了这一页。实证:库里 MPNP 提名已是 2,673,生产页仍渲 2,670,而响应头是
>    `no-store`(排除了 CDN 缓存)。这一页每个数都来自库,冻住 = 对用户撒谎。
>    **别再往这一页加 generateStaticParams**;其它靠库的动态页要一并查有没有同款。
> 2. **手工跑 ETL 会与无人值守容器抢**:`pnp-build-1` 每轮 08→09→seed,09 要跑几分钟。
>    实撞:我 21:20 写 `raw/pnp/mb-stats.json`,容器那轮 09 在此之前已开读、21:27 写出的 mart 里没有新行,
>    把刚 seed 进去的行冲掉了(**不是代码问题,容器挂的就是新码**)。
>    手工改完 raw 要么等容器下一轮,要么重跑 09+seed 后**立刻查库确认**。
> 3. **处境页仍长**(桌面 3036px,10 条路径全展开):要不要前 5 条展开、其余折叠,待定。
> 4. **其余 15 条案例未出页**:只有问题没有事实层,出成页是空壳,不该被索引。C01 样板值不值得铺,待 Frank 看后定。
>    ⬆️ 两钮撤掉后这 15 条**在页面上已经一个动作都没有**,优先级自动上来了。
> 7. **处境页 H1 三语只有中文**:`CaseView` 收的是 `entry.label.zh`,英文态进去仍是中文标题(88% 流量是英文)。本轮没动。
> 5. **方案卡「优先核对」**:被卡住的已写具体原因,但全达标的几条仍统一这四个字,没区分度。
> 6. **`dp-*` 埋点今天才进第一方表**,约一周后才够回答「有多少人进 /plan/pr、走到哪一步流失」。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-08-10,批D 欠账①「名录匹配」清账)**
> Frank 拍板**「AIP 雇主完全匹配的才算」**;审计=`docs/evaluation/名录匹配审计-20260809.md`(落地节已补口径标定表)。
> 改动只在匹配与渲染层:**零改库、零改 mart、零改 seed、零动 ETL**,懒查询口径不变。
> - **口径**:旧=双向大小写不敏感**子串包含**(`Esso`→`Wheeler Acc·esso·ries`、`ARMS Ltd`→`Wohlgemuth F·arms Ltd`)。
>   新=**完全匹配**,比的是名录名的**任一 o/a 名段**。⚠️ 字面完全相等不可用:名录 38%(1,460/3,867)是
>   「法定全称 o/a 营业名」,岗位挂营业名 → 四省命中 389→**81**(砍 79%),连本卡基准 fixture
>   (Grand View Manor)都会变「名录没认出」。**D 口径 = 281 命中 / 17 多配**(旧 389/52,最大 55 配)。
> - **审计建议 ①③ 被吸收销账**:没有「包含」就没有词内子串,也没有「谁胜出」。词边界方案实测**不够**
>   (Esso 仍 2 配 / Tim Hortons 仍 55 配 / HOTEL HALIFAX 仍被 Atlantica 抢配),故不采用。
> - **建议 ② 照做且仍然必须**:完全匹配**治不了连锁**——加盟法人的 o/a 段本来就精确等于品牌名,
>   `Tim Hortons`(NS)20 家 / `Subway` 19 / `Mary Brown's`(NL)13 全是合法完全匹配。多配 ≥2 →
>   新行 `tv.emp.designatedMulti`(free/info,只报家数不点名),且 `designation=null` 使
>   **AIP 线不进比路、不会被标「最快」**(付费结论不拿不可证的雇主去算;事实照摆=锁合成不锁事实)。
> - **落地件**:`lib/designationMatch.ts`(纯函数)+ `verdictCache.getDesignatedEmployers`(按省 TTL;
>   🔴 **不能复用 `VerdictData.designatedEmployers`**——那份是 NL 专用,是 pathVerdict「NL 名录里有几家
>   申报过该 NOC」的分母,扩成四省会把分母一起改掉)+ `tv.emp.designatedMulti` 三语文案。
>   顺手删了 route 里已成死代码的 ILIKE 转义 `esc`。
> - **验证**:`designationMatch.int.spec.ts` **18 例**(金标全部抄自审计点名的真实名录行,不自造样本)+
>   tripleVerdict 补 3 例(多配不点名 / state≠pass / 多配不进比路);全量 **30 文件 554 测 0 红**、`tsc --noEmit` 绿。
> - **⚠️ 留账(未修,已写进审计报告)**:D 口径把两类**真命中**一起丢了,卡上渲「名录暂未匹配到这家」
>   (本站缺口语义,不写「未被指定」,不算说错话,但信息量降了)——
>   ①**括号写法** `品牌 (法人名)` 名录里 538 行(NB 为主):`Tim Hortons (ROLAND RIOUX LTEE)`、
>   `Esso (Higho Market Ltee-Ltd)`;把括号内外也当名段 → 命中 281→305、多配 17→20。**待 Frank 拍**。
>   ②`… dba Hotel Halifax & The …` 这类营业名带后缀的,D 与 D+ 都是 0 配。
> - **未做**:没起 dev server、没出效果图(改的是既有弹窗里一行的文案态,非新动线/新组件)。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上一轮交接(2026-08-09 夜场二,「三件批」:中文名收口/超时降级/AIP 动线)**
> - **✅ ② 合成停摆看门狗(`86e5f42` 已换版,/api/version 验讫)**:friendLlm 新增 `stallMs`——
>   **补的是结构性口子**:原来 timeoutMs 在**响应头到手就 clearTimeout**,SSE body 读多久都没人管,
>   上游把头发回来再卡住我们能读到天荒地老(112s 那次就卡在这段);旧链 `res.json()` 同款漏洞一并收进
>   try/finally。合成那一发带 `CHAT_SYNTH_STALL_MS`(默认 25s,**env 可调不用重新部署**),触发即走既有降级路,
>   按「多久没动静」不按「总共多久」:字在流的时候用户看得见进度,掐掉正在流的答复更差。
>   **生产三连实测**(首版):冷启 43.5s(原 112s 空白)→ 热身 11.4s / 4.7s 正常流式、degraded=None。
> - **✅ ②-b 出口=「系统繁忙,稍后再试」不降级(Frank 当晚二拍:「不用降级 就显示稍后再试,系统繁忙」)**:
>   新错误码 `busy`(503)贯通 orchestrate→route→ChatBox,三语文案齐,**带重试钮**(它与 limit/guard 相反,
>   过会儿再问多半就答上了)。唯一例外:上一稿本来就干净、只是重写那稿停摆 → 把干净那稿发出去。
>   **不是等待的失败(掉线/上游炸)照旧降级发清单** —— 没被顺手改掉,有用例守着。
>   顺修一个真缝:ChatBox 的 JSON 分支是**错误码白名单**,`busy` 会掉进「没连上服务」(纯联邦问句没有
>   「认出职业」那一格、流压根不开,走的正是这条路)——改成认得的码原样用。
>   测试 chatStall.int 5 条 + friendLlm 停摆 5 条,全量 29 文件 533 测 0 红,tsc 绿。
>   **生产三连实测(`28b0bd5` 换版后)**:①冷启 29.9s → `SSE error=busy`(不再发清单,如拍板);
>   ②29.3s **答上了**(6 个 delta —— 慢但一直在吐字,看门狗没误杀,正是「按停摆不按总时长」的用处);
>   ③5.2s 正常。**⏭ 只剩一个数归 Frank**:N 取 25s 还是压到 15s(冷启那 30s 里有 25s 是在等它)。
> - **✅ ③ AIP「下一步」动线上线(`b0b37a8`+`62cbdbf`,生产实拍验讫)**:AIP 表加行尾「下一步」列
>   `看在招岗 →`(手机=卡底通栏钮),落该雇主在招岗、每行再接职位板既有「身份判定」;只给 AIP 表
>   (LMIA/named 列已挤,先看这张成效)。表题下两行:允许类=**TEER 0–4**(锚 pnp_requirements
>   program='AIP':workTeerMatch 映射 0-4、language 分 TEER 0-3 CLB5 / TEER 4 CLB4 ——
>   **官方门槛没有 TEER 5,效果图里那句是错的,按库改**)+「被指定 ≠ 入职就能 PR」(答 Tim Hortons 之问)。
>   雷区榜两拍同批:环比 flatDelta 去红绿(只这一榜;`NB 无通道` 红胶囊保留=它红得对)+「≠推荐」小注。
>   **英文态 375 实拍折行 → 两条文案当场删到一行**(`62cbdbf`);中文态单行,不动。
> - **✅ ②-c 对话三处走查快修(`372eee9`+`d2ac0cf`,实拍验讫)**:①首屏「试试这样问」原是窄灰胶囊、
>   答复下是通栏白卡=同一个东西两种样子 → 首屏改用同款选项卡;②**「每次都是这三个」**:根不在
>   「问过没问过」,而在**首屏示例块从不卸载**(08-05 拍板它随线程滚)——答复下再摆一遍就是同屏重复。
>   现在答复轮补位只剩判定导流句(要有 noc 才给),一条都没有就整块不出;引导轮照给示例(反问的解药)。
>   ③**点了必死的 chip**:「按我的情况判一判走哪条路最快」没 noc 时 100% 撞 noOcc(chat_logs 20:34 实录),
>   且引导轮当时一个可点的都不挂=死胡同 → 没 noc 就不生成它,引导轮也挂同一张卡。
> - **✅ ①-b 中文名回写已执行**(Frank 点头「按你建议来」):清掉不含汉字的空转灰注 6 家
>   (A&W×3/Quiznos×2/SHAN)+ **实写 1050 家**(候选 1285,差值=库里已有值或 slug 不在);
>   apply 脚本加两道:zh 与英文名同字跳过、`SHELF_ALIASES_IN` 输入覆盖(worktree 跑主仓那份在写的文件)。
>   **brand 遍本身归旧 session**(Frank 08-09「跑批归旧 session」),本 session 不再监看。
> - **⏸ ① 雇主中文名 brand 遍**:主仓后台**仍在跑**(19:41 已 560 家 ai-brand,~640 家/小时,按在招岗数
>   降序=三分表首几页早已覆盖)。裸跑审样已出(`可回写 795 家 {ai:184, ai-brand:560, wikidata:51}`),
>   **等 Frank 点头才 `--yes` 回写**。审样看到三件事:①质量整体可用(麦当劳/赛百味/冰雪皇后/麦肯食品);
>   ②**同类机构译法不统一**(Centre de services scolaire → 一个「爱国者教育局」一个「萨马拉斯学校服务中心」);
>   ③**A&W 的 alias 就是「A&W」**=中英同字的空转灰注,回写前该滤掉(建议 apply 脚本加一条 zh===name 跳过)。
> - **⏸ ③ AIP「下一步」动线效果图已出**(Mode B,注入生产页真样式真数据,电脑+手机各前后两张,零改码):
>   行尾「下一步」列=`看在招岗 →`(手机为卡片底通栏钮)/表题下两行=允许类引导 + 「被指定 ≠ 入职就能 PR」;
>   同母题两拍:雷区榜环比列**去红绿**(改中性灰,`NB 无通道` 红胶囊保留——它红得对)+ 表题下「本榜列的是
>   走不通的省,不是职业推荐」。**引导措辞的数值口径(TEER 0–5/CLB)落地前必须锚 `pnp_requirements`
>   program='AIP' 那 36 行 quote,不许照效果图抄。** 等 Frank 点头再写码。
> - **🔧 worktree 起手三件事(每次都要,别再摸索)**:主仓 `node_modules`+`cms/node_modules`+`data/mart`
>   打目录联接(mklink /J),`cms/.env` 复制一份——否则 vitest 起不来(config 解析失败/4 个 spec ENOENT)。
>
> **📦 更早交接已沉档（只留最近两轮）** → [STATUS存档-2026-08上.md](docs/STATUS存档-2026-08上.md)（07-28 ~ 08-09 晚场） · [STATUS存档-2026H1.md](docs/STATUS存档-2026H1.md)（07-20 及以前）。
