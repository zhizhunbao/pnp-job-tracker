# 第 31 轮(2026-08-03 · 收费走查)

> 从工作台沉档(第 32 轮翻页时)。

> **迭代工作台文档**。工作法:每轮 = **体检脚本跑全量 → 统一检查 → 逐条整改意见 → 实施 → 下轮复查闭环**。
> **轮次记录**:第 1-30 轮沉档至 [整改轮次存档.md](整改轮次存档.md);工作台只保留当前轮。
> **当前轮次**:**第 31 轮 2026-08-03(Frank「来一波收费走查」+ 追加「从 目标→报告→收费 这个链路算用户腹诽概率」)**。
> - **第 30 轮 #237-#239 三条均已闭环**(向导盖弹框、/pathways 斜杠枚举、JD 裸长 URL),本轮体检未复发。
> - **标准体检**:372s / 19 命中 / console 0 / 4xx 0;数字口径页面 43,470 == API 43,470(一致 0.00%)。
>   19 条全是历轮沿用项(tagline 截断 ×5、同页事实重复、触控靶 ×2),**收费链路零命中** ——
>   因为 `checkup.py` 的页面集里**只有 `/pricing`,没有 `/plan/*` 报告页与 `/jobs/[id]` 详情页**,
>   而那两处才是收费主场。本轮另起专项脚本(`scratchpad/paywall.py`:4 卡 × 3 语 × 2 视口,抓锁行/CTA/正文全文)。
> - **口径**:本轮按 Frank 追加的要求,每条给**腹诽概率**(用户心里骂一句的概率)——
>   估计值,依据是生产实读的句子本身,不是实测;锚点是「30 天 13 次锁区曝光 → 0 次打开定价 → 0 笔陌生付费」。

## 本轮清单(第 31 轮)

> **⚠️ 当轮特批实施两条(#240/#241)**:它们是我自己 2026-08-03 撤掉卡⑥付费墙(`cb4a9b3`)**当天引入的线上回归**,
> 留到下一轮等于让生产继续说胡话。违背「检查期间不动产品」惯例已记(同第 24 轮 P0 特批的先例)。

| # | 级 | 条目 | 证据(生产实读) | 改法 | 腹诽概率 | 状态 |
|---|---|---|---|---|---|---|
| 240 | P1 | **卡⑥ 段标题驴唇不对马嘴**:「这个职业在哪个省更有优势?」下面跟着「执业护士的中位年薪比你这行高 48%」 | `rpt.k.peerGap` 在 `BUCKET` 里没有条目 → 靠 `?? 'prov'` 兜底落进省份节。先前它被付费闸摘掉,免费层从没渲过,**撤锁才暴露** | `BUCKET['rpt.k.peerGap']='peer'` | 55% | ✅ 当轮修 |
| 241 | P1 | **「其他可考虑的省」下面摆的是职业**:「医师助理(NOC 31303):在招 49 岗」 | 卡⑥ 的 `alternatives` 是职业(`rpt.k.alt`),标题却写死用拿 PR 卡的「备选省」;同样撤锁才暴露 | 标题按**这一节装的是什么**判定,不按卡名;新增 `rpt.q.altOcc` 三语 | 60% | ✅ 当轮修 |
| 242 | **P0** | **免费态就承诺「PDF 里有雇主明细与 LMIA 记录」,而免费 PDF 里没有** | PDF 带只判 `rpt.noc`,**没有任何 pro 判断**(PlanPrView:651);免费报告的 `employers` 是服务端清空的 | 免费态只承诺 PDF 真有的(出处与日期、门槛逐条);雇主明细改成 Pro 才写,顺便当第二个付费理由 | **90%**(下载完发现没有) | ✅ 批 1(`26bcacf`,生产复验) |
| 243 | P1 | **「门槛对照」5 条里 4 条是「跟你无关/我判不了」**:「算的是全家收入,本站没问」「你还没填经验」+ 两条「这项只有雇主拿得出材料」 | 生产实读 plan/pr;**违反 Frank 08-02 自己拍的「判不了的行不参与判定,那还显示干什么」** | 门槛节从 5 行砍到 2 行:只留他自己动得了的(语言、经验);雇主侧三项改由**雇主线索每一家**挂档位(employerBar),收入那条判不了 → 整行不出 | **75%** | ✅ 批 2(`d1198c9`) |
| 244 | P1 | **拿 PR 卡的锁区里没有雇主线索** —— 锁区唯一的真货只在卡①「找工作」 | `buildPrReport` 的 `employers` 恒为空(只有 `buildJobReport` 查 `sponsorList`);而漏斗实测 16 次出报告里 **12 次是 pr 卡**(首页 CTA 直接进) | 拿 PR / 选省份卡接 `occ.sponsorList`:免费层出「全国有 N 家」(并进 `ALWAYS_FREE`,否则会被每省保底截进锁区),名单本体进锁区 | 65% | ✅ 批 2(375/en 实拍锁区出「Which employers have sponsored this occupation」) |
| 245 | P1 | **锁区文案「其余结论全文」** —— 用户不知道花 $19 买到什么 | 生产实读:pr 卡锁区只有两行,一行是它 | 改成「其余 **N** 条结论」,N 由引擎带出(`gateReport.moreN`)。**只给条数不编类别** —— 被裁的是哪几条随画像而变,硬写类别在某些画像下会变成假话 | 65% | ✅ 批 1(生产实读 moreN=2) |
| 246 | P1 | **定价页 Pro 栏第一条与免费栏看着重复**:免费写「省提名官方分值表与自评打分(BC、SK)」,Pro 写「我的通道报告:哪个省够线、差多少分」;且**全页一个字没提雇主名单** | 生产实读 /pricing 三语 | Pro 三格重排:**担保雇主名单**提首位、通道报告 detail 改「离门槛差多少分、先补哪一项最值钱」、原「投递辅助」下架 | 60% | ✅ 批 1 |

| 247 | **P0** | **中文清单名漏进英文界面**:英文报告第一屏就是 `This occupation is on the BC PNP list "BC 医疗"`,还有 `"SK 医疗"` `"MB 在需职业"` `"医疗社服"`(EE 类别名) | 375/en 实拍;`data/mart/pnp_occupations.json` 的 `label` **只有中文**,没有英文列 | **不用改数据层** —— `STREAM_L10N`/`EE_L10N` 三语映射早就有(职位板一直在用),只有报告页没调它。`Line` 的 params 里 `label`→`streamDisplay`、`cat`→`eeDisplay`;出处名(printOnly,**会印进 PDF**)同修 | — | ✅ 当轮修 |
| 248 | P1 | **PDF 卡片在 375/en 挤成一团**:标题「Download the full report (PDF)」折 2 行、小注折 3 行,右侧按钮把文字压成窄条 | 375/en 实拍 | 手机改上下堆叠(标题+小注一行块、按钮独占一行),不与按钮抢横向 | — | ✅ 当轮修(375/en 实拍) |
| 249 | P2 | **`4 basic question(s) unanswered`** —— `question(s)` 是程序员英语不是人话 | 375/en 实拍 | 改成任何 n 都成立的句式:`Basic details still unanswered: {n} — the report stays coarse until then` | — | ✅ 当轮修 |
| 250 | P2 | **顶栏英文标语在 1440 被截**:`Daily updates, all of Canada, immigration-val…` | 体检 R5,第 30/31 轮连续命中 5 个页面 | 英文标语删到放得下:`Daily updates, all of Canada, immigration-value lens` → `All of Canada, updated daily`(1440/en 实测截断清零) | — | ✅ 当轮修 |

**判假的(不追编号)**:`/plan/prov` 404 是我走查时 URL 写错(路由是 `/plan/province`,`prov` 只是 decision key),产品无此链接。
**沿用未闭环**:#224 同页重复、#225 空值格子手型、/stats/ab 中文页英文通道名截断、#227 职位板 ~1.2s 聚合、AIP 职业粒度补列、QC 职业清单立项。
**脚本记账**:① `checkup.py` 的 `FULL/LITE` 页面集该把 `/plan/pr`、`/plan/job`、`/plan/province`、`/plan/career`、一个 `/jobs/[id]` 加进去 —— 否则收费主场永远查不到;② 报告页深链要带 `?noc=<码>&view=report` 才出得了报告;③ 本轮专项脚本留在 scratchpad,下轮若沿用应并进 skill 的 scripts/。
