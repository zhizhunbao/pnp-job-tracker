# 第 32 轮 · 2026-08-03(Frank「继续走查,专攻决定这条线」+「完善功能 完善引擎」)

> 从工作台 [功能盈利点检查.md](../功能盈利点检查.md) 沉档(第 33 轮起用)。清单 = #251-#258。

- **第 31 轮 #240-#250 十一条全部闭环**(收费走查 + 英文优先三条),本轮未复发。
- **走查方式**:按 Frank 当天新立的三条规矩(英文优先、手机屏第一、开发完自己开浏览器实测),
  当一个 **375/en 用户**从 `/start` 一路点到报告,每步拍图 + 量排版 + 收 console。
  **注意 `/` 是职位板,决定线入口是 `/start`**(本轮先跑错过一次)。
- **决定线本身是通的**:7 题一页一题、进度 `1/7 → 7/7`、导航齐全、console 0 错、两端无横滚、无汉字残留。
- **新增一把量具:引擎完善度**。手法=从「全答」基线出发**逐项抹掉一个答案**,比对**未经付费闸**的报告
  有没有变;一项抹掉后一字不变 = 这道题白问(违反 `decisions.ts` 自己写的「挂不上结论就不问」)。
  **必须用未裁剪的报告测** —— 我先拿 `/api/report`(免费层)测,四张卡里三张显示「全白问」,
  那是付费闸把 `switches` 整节拿掉造成的假象,不是引擎的问题。
  结果:拿 PR 7 道全用上、选省份 5 道全用上、找工作 2 道全用上、**职业规划问的唯一一道(目标省)完全不消费**。

## 清单(第 32 轮)

| # | 级 | 条目 | 证据 | 改法 | 状态 |
|---|---|---|---|---|---|
| 251 | P1 | **选完职业后「Next」被埋在浏览区下面**:整页 1670px、视口 812px,用户选完要往下滚**一屏半**(过搜索框、11 个分类页签、26 个字母索引、24 个热门 chip)才找得到下一步 —— 而这是决定线**第一步**、漏斗最宽的地方 | 375/en 实拍 `j-3-picked.png` | 动作条 `position:sticky; bottom:0` —— 选中即出现、随时能走,浏览区照旧想逛就逛 | ✅ 当轮修(实测按钮 top=777/视口 812,不滚就在屏内) |
| 252 | P1 | **卡⑥「职业规划」问了目标省,引擎一次都没消费** —— 这张卡只问这一道题,等于白问 | 引擎完善度实测:career 卡八项全「白问」 | 选了省就多出一行该省事实(在招量 + 该省中位薪资)。**另起一行不改基准行口径** —— 上面那行是同门对比的基准(peers 是全国口径),换成省级会让「比你这行高 48%」变成拿省级比全国 | ✅ 当轮修(实测 BC:在招 16 岗、中位 **$99K**,而全国 $87.4K —— 这条信息先前被整个扔掉) |
| 253 | P2 | **决定线第一步没有进度提示**:选职业页 `进度=(无)`,而后面 7 页都有 `N/7 completed`。最劝退的第一屏,偏偏不告诉用户「只要 7 步」 | 375/en 逐页实测 | 第一页也给一个「第 1 步,共 8 步」之类的锚;或把选职业并进 SurveyJS 的进度计数 | ✅ 当轮修(本地实测 0/8→8/8 连续,四张卡各自 1/2、1/3、1/6、1/8;**版式改动 → 等 Frank 看效果图点头再 push**) |
| 255 | **P1** | **选工作页「科技」类里蹲着景观园艺技师、家电维修技师**(Frank 实机) | 生产实见;体检脚本 `etl/audit_noc_classes.py` 查实 | 两件事:① 中类兜底 `^2 → IT` 把 22 开头的各行业技术员全塞进「IT」(52 个职业)=**真错**;② 大类简称「科技」比官方第 2 组(Natural and applied sciences)窄得多,园艺技师在官方口径里本来就属于它 = **名字起窄了**。改法见「分类换血」 | ✅ 当轮修(官方层级替掉手搓桶) |
| 256 | P2 | **中/小分类的英韩名靠 i18n 里人肉维护 `cat.*`** —— 分类一变,英文界面立刻冒中文(#247 同款事故) | 换分类时实见:新中类 60 个,全要手加键 | 名字跟着分类走同一条管线:`noc_categories` 加 `mid_en/mid_ko/fine_en/fine_ko` 四列(DDL additive 已代跑生产),前端从维度表取名,i18n 那条只留作老值兜底 | ✅ 当轮修 |
| 254 | P2 | **职位板 `/` 在 375 上有 8 种字号**(10px×1、11px×177、12px、13px×310、15px、16px、17px、20px);11px 的是岗位卡元信息(TEER / eligible / EE / Updated) | 375/en 实测 sizes | 收敛到 3-4 档;11px 在手机上偏小(Frank「不要大小不一致」)。**非本轮焦点(决定线),先记账** | ⬜ 顺延第 33 轮 |
| 257 | **P1** | **分类名是统计年鉴的话,不是招聘网站的话**:中类下拉里出现「行政服务金融与商业服务及通信专业中层管理职业(不含广播)」 | Frank 实拍下拉 | 官方名退回「出处/小注」,显示层换人话名;进一步拍板走**本站浏览分类树**(17 大类按行业分,官方码留痕) | ✅ 当轮修(`16313f8`,已灌生产) |
| 258 | P1 | **职位板默认 13 列太挤**,「很多人一进来看这个页面设计就跑路了」;删列后「vs 中位」这种恒短值反而分到 130px 空地 | Frank 实拍 | ① PNP/EE/AIP 转默认不显示(13→10 列);② 列宽改成**按内容分**;③ 列宽取整数像素 | ✅ 当轮修(`aed6222`) |

**#253 的落法**(改法栏两个选项都没走):框架的进度条**只数得到它自己的题** —— 文字能靠 `onProgressText` 改成
「1/8」,条还是按 0/7 空着(实拍到:选完职业进第一题,文字说 1/8、条一格没走)。于是整条进度自己出
(`Progress` 组件,两页共用、同一个位置),`showProgressBar:'off'` 关掉框架那条;
项数走既有的 `fieldsOf` + `missingFields`,不在页面里另数一遍。

**脚本记账**:① 引擎完善度那把量具值得沉淀进 skill 的 scripts/(本轮在 scratchpad/engine.py + 一次性 int spec);
② `page.evaluate(字符串)` 是当**表达式**求值,传 `() => {...}` 只会拿到函数对象 —— 要写成 `` `(${SCAN})()` `` 或直接传函数;
③ 选职业页的 Next 是自绘 `<button>`,不是 SurveyJS 的 `input[type=button]`,选择器要两种都认。
