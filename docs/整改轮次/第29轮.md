# 第 29 轮(2026-07-28 · Frank「来一轮 dd」+ 全天高频拍板)


> **迭代工作台文档**。工作法:每轮 = **体检脚本跑全量 → 统一检查 → 逐条整改意见 → 实施 → 下轮复查闭环**
> (第 26 轮起默认零截图,只对真命中补拍;全量截图轮改按需)。
> **轮次记录**:第 1-28 轮沉档至 [整改轮次存档.md](整改轮次存档.md);工作台只保留当前轮。
> **当前轮次**:**第 29 轮 2026-07-28(Frank「来一轮 dd」;跑到一半他补报「职位板返回有时候老慢了 而且还不生效」)**:
> - **上轮四件全部已在库**(#216 新闻卡中文 tooltip、#217 触控靶四处、#218 榜单页 React #418、#219 账户页 ✎)
>   —— 第 27/28 轮当轮就修完了(5c3c247 / 617d82b),本轮体检**零复发**,四件正式闭环。
> - **Frank 实报的「返回」当轮特批实施**(92ce98f,见 #220):两条抱怨都是真的,但根因是两件事,分开处置。
> - 体检 `checkup.py --round 29`(538s,35 条命中)→ **判真 6 件**:#221 职位板慢(LCP 8.5s + no-store 关掉 bfcache)、
>   #222 /stats 主图控件触控靶 29px、#223「TEER」裸黑话、#224「点条形可下钻」同页 5 遍、
>   #225 空值格子仍是手型、#226 英文「三问」入口钮 28×14。
> - **console error 0 / 4xx 0**;页面 40,793 vs API 40,809 = 体检期间 ETL 落轮的取数时差(已给脚本加容差判定)。
> 口径:分层真相以 `cms/src/lib/plan.ts` 为准;**本轮无截图**(命中项判真靠 DOM 实测与定点探针)。

## 本轮清单(第 29 轮,2026-07-28)

| # | 级 | 条目 | 证据(生产实测) | 改法 | 状态 |
|---|---|---|---|---|---|
| 220 | P1 | **详情页/公司页「返回」在新标签页里点了没反应**(Frank 实报) | 站内进详情页一律 `target="_blank" rel="noreferrer"`(JobsTable 2691/2777/4413)→ 新标签页 `history.length===1`、referrer 被抹,`history.back()` 是空操作;探针实测点后 URL 纹丝不动 | `BackLink.tsx` 新增 `goBackOr(fallback)` 一处真相,详情页/公司页同接:有历史照走浏览器返回,没历史落 `/?back=1` 复用既有筛选快照 | ✅ **当轮特批实施**(92ce98f),待生产复验 |
| 221 | P1 | **职位板慢**(Frank 实报的另一半:「返回有时候老慢了」) | 串行实测(排除互相排队):同为动态页的 `/pricing` TTFB **0.13s**,职位板 **1.6~3.4s**;`/api/dims` 每次白拉 1.4MB / 1.2s。根因=**同一个病只治了一半** —— 2026-07-19「排序 3-4 秒」给 `/api/jobs` 加的 10 分钟维度缓存,职位板 SSR 没走,每个访客每次打开都在重拉 5000 行省提名清单 + 2000 行 EE 类别 + 1000 行 NOC 分类 | 维度装配抽 `loadDims()` 套 10 分钟 TTL(查询一字未改,只缓存与用户无关的维度);`/api/dims` 加 `public, max-age=300, stale-while-revalidate=3600` | ✅ **当轮实施并生产复验**(972d188):TTFB **1.6~3.4s → 1.15~1.28s**(冷 2.4s/10 分钟一次);总数 40,809==API、心跳 03:21、首屏 50 行全无回归 |
| 222 | P2 | **/stats 主图控件触控靶 29px**(横轴 职业/省份/城市、簇内 不分组/省份/职业大类/TEER、右轴 帖面中位年薪/不显示) | R8:375 三语两端同现(zh 97×29、en 149×24、ko 109×29) | 同 #212/#217/#219 的 ≥40 口径:只扩热区不改视觉(`.tapPad` 手法) | ⬜ 待整改 |
| 223 | P3 | 主图「TEER」按钮**裸黑话**,无 title/说明 | R1:`stats-index` 三语两端 ×6,该 button 既无 `title` 也无 `aria-label` | 挂口径 title(与站内别处 TEER 面同款),码面按 3de5caf 拍板保留 | ⬜ 待整改 |
| 224 | P3 | 「点条形可下钻」**同页出现 5 遍**(主图 + 4 张常见图表各一句) | R3:zh/en/ko 三语同现 | 收成一处(或只挂主图);解释类文案默认删的规矩 | ⬜ 待整改 |
| 225 | P3 | 桌面表格**空值格子(—)仍是手型**且 `title="—"`,点开没内容 | R9:`td.jcell.jcellAct` 实测 `cursor: pointer`,三语同现 | 值为空时不给手型、不挂 title | ⬜ 待整改 |
| 226 | P3 | 英文界面「三问」入口钮 **28×14**(`Which provinces fit your occupation? Start`),同族还有 `Free profile` 链接 92×16 | R8:`jobs/375x812/en` | 同 #222 一并按 ≥40 热区处理 | ⬜ 待整改 |
| 227 | P2 | 职位板剩余 ~1.2s:**职位行 + 证言计数查询**(`fetchTotalAndProof` 三个聚合跑满 4 万行 jobs + companies 子查询,每次现算) | `/api/jobs?page=0` 单测 0.95s;#221 上线后 TTFB 稳在 1.15~1.28s,剩下的就是这一段 | 先看索引(带 `is_dup`/`status` 的过滤条件)再谈缓存 —— 总数/证言数字**不宜缓存**(页头数字与 API 对不上就是撒谎,R11 会报) | ⬜ 待整改 |
| 228 | P1 | **`no-store` 关掉了浏览器「后退秒回」(bfcache)** | Frank 真机实测「不是秒回,1-2 秒」—— 正好等于当前 TTFB,即后退在整页重拉;我的自动化探针答不了这题(CDP 挂着时 Chrome 自己关 bfcache,连静态页 `/resources` 都显示未走),**结论来自他的真机** | 中间件对**文档请求**改发 `private, no-cache, must-revalidate`:no-cache 语义仍是「用之前必须回源确认」,而我们不发 ETag/Last-Modified → 浏览器照样每次真取,看不到过期页;唯一变化=后退允许原样恢复。图片/静态资源不受影响 | ✅ **当轮闭环**(389eb03):生产响应头 `private, no-cache, must-revalidate`、五页 200 全绿;**Frank 真机复验「是」(后退已秒回)** —— 改前他实测 1-2 秒 |
| 229 | P3 | `/public` 下的图片发 `public, max-age=0`,三张首屏 banner(~180KB)每次进页面都要回源确认 | 生产实测 `/img/banners/jobs-1.jpg` 响应头 | 给 /img 配长缓存(文件名带版本或走 /_next 静态管线) | ⬜ 待整改 |
| 230 | P2 | **解释类文案清一轮**(Frank「检查下页面有没有解释的文字」):移民路径页 13 处「见官方页/以官方页为准」尾巴(一屏可见 8 处)、/stats 主图口径注 46 字、PNP 弹框「怎么走这个通道」三步 | 八个页面生产实拍逐句过;对照四类保护(法律免责/≠资格认定/tooltip/空态引导) | Frank 逐条拍板:尾巴全删(只删甩锅半句,信息半句留)、口径注砍成一行(「在招量≠缺人」保留)、how 三步连键清 | ✅ **当轮实施并生产复验**(69cc38e:三语尾巴残留 0、口径注一行、步骤信息与页脚免责均在位)|
| 231 | — | 记账:`pnplist.howTitle/how1-3/howOfficial` **全站无人调用**,是留在 i18n 的死文案 —— 我先前把它当成页面上还在的解释文案报给 Frank,已更正 | 全仓 grep 零引用 | 连键清掉 | ✅ 同上 |
| 232 | **P1** | **1 个岗也在报「中位年薪」**:全国 17 个职业、分省 **723 行**是「1 个在招岗 + 一个中位年薪」——拿单样本叫中位数,砸「真实数据」招牌 | mart 实核(`stats_occupation`);Frank 点「从低到高」时整屏暴露 | 口径落数据层:职业表补 **ESDC 官方中位年薪 + 样本量**,前端按样本量决定报不报 | ⬜ 待办(方案已定,等 Frank 放行跑 ETL) |
| 233 | P2 | 职业表**只有帖面口径**,没有 ESDC 官方口径 —— 规划类业务(留学/职业规划,Frank 2026-07-28 提)必须站在官方工资上,帖面只能当行情参考 | `stats_occupation` 字段实核:仅 `medianSalaryAnnual`;`stats` 表两个口径都有(ON 官方 $54,080 / 帖面 $55,000) | `11_build_stats.py` 的 agg() 补 `medianWageAnnual` + `salaryN`;DDL 两列;seed 白名单;前端双线 | ⬜ 同 #232 一并 |
| 234 | P2 | 4 个职业**中文名数据层就是空的**(NOC 64201/94121/14110/92020),中文界面轴上露英文 | `stats_occupation` 与 `noc_descriptions` 两张表都为空 → 04f 翻译漏条 | 本地 Ollama 补这 4 条(翻译类一律本地跑) | ⬜ 待办 |
| 235 | P3 | 岗位数轴出现小数刻度(「0.2 个岗位」) | Frank 实拍;排到只有 1 个岗的职业时 echarts 自动切小数 | yAxis minInterval 1 | ✅ 当轮修(da75a80) |
| 236 | P2 | 主图控件区四行药丸、无搜索无筛选;通道(PNP/EE/AIP/QC)不能筛 | Frank:「需要重新设计一下,并且加一些搜索和过滤条件」 | 效果图已出(常用一行+更多筛选折叠,与职位板同语言);**Frank 已选:先上 PNP+EE**,AIP 等 ETL 补列,QC 数据层没有清单需另立项 | ⬜ 待实施 |

**判假的(不追编号)**:新闻页「Saskatchewan/Federal IRCC」重复=筛选 chip vs 卡片地区标签(结构性,历轮已拍板保留);
「Region stats」h1 vs 顶栏同名=定位;表格列内截断(`BC Construction trades`)=#35 拍板维持全宽横滚的既有代价;
rank-daily 长英文职业名折行=数据值非文案。

**脚本本轮两修**:① JD 弹框段的「职位描述」按钮早已不存在(职位名 `<a>` 自己开弹框),旧选择器每轮 30s 超时=弹框段空跑;
② R11 数字比对加 **0.5% 容差判定**(体检跑 9 分钟,期间 ETL 落轮属正常时差,第 28 轮已被它误报过一次)。

