# 第 28 轮 · 调阈后的真实基线(2026-07-26 深夜 · 与第 27 轮同日续跑)

> 第 27 轮把 5 条规则调准后**同口径重跑**,验规则是否真的收敛。`checkup.py --round 28`,零截图,509s。

## 一、基线:222 → 34 条

34 条里 **24 条属已拍板保留**(榜单页签=定位、新闻列表同构标签、弹框与被它盖住的页面重复)、
**8 条是顶栏**(Frank 拍板豁免),**真剩 2 件**:

| # | 级 | 问题 | 证据 |
|---|---|---|---|
| 218 | P1 | `/rankings/sponsor-likely` 控制台报 React #418(SSR 与客户端文本不一致),两视口同现 | 第 28 轮 console:`Minified React error #418` ×2 |
| 219 | P2 | 账户页 ✎ 改到 37×37 后命中测试仍不达标(邻居抢点) | 第 28 轮 R8:`account/375x812` 37×37 |

## 二、当轮实施(617d82b)

**#218 根因坐实**:「更新于」用 `new Date().toLocaleDateString('en-CA')` 封顶今天 —— 这行在
**服务端(Render 跑 UTC)与浏览器(用户本地时区)各算一次**,跨日窗口里两边算出不同日期 → React #418。
数据本身是 ET 口径(帖面日期),改成两端都按 `America/Toronto` 算。
对照实验(同一时区 America/Los_Angeles、同一时刻):生产旧代码报错 1 条、页面「更新于 2026-07-26」;
本地已修报错 0、页面「更新于 2026-07-27」。

顺带:EE 列休眠标注「运输(上次抽选 2024-03)」在 6% 宽的列里占三行 → 收成「运输 2024-03」(实测单行),
完整说明本来就在悬停 title 里,不在格子里重复;三语同改。

**#219**(5c3c247 之后仍不达标)→ 账户页 ✎ 热区按 ≥40 重做,见 `account/page.tsx:125` 注释。

## 三、脚本再修三处

1. 心跳探针跟上「更新时间」改名(否则每页 `heartbeat=None`);
2. R8 顶栏整条豁免(Frank 已拍板豁免,不必每轮复报);
3. R3 排除「弹框 vs 被它遮住的页面」与「页签 vs 榜标题」两类结构性重复。

## 四、一个假警报记账

R11 报「页面 39,186 ≠ API 39,002」—— 体检跑了 8.5 分钟,期间 ETL 落了一轮,不是真不一致
(同一时刻复测:页面 39,002 == API 39,002)。**跨分钟的数字比对要么同刻取,要么带容差。**

## 五、记账:探针 URL 必须与体检口径一致

排查 #218 时我用 `/rankings/sponsor` 探测 —— **该 URL 不存在**,返回 Next 错误页,一度让我以为推断被证伪;
体检脚本用的是正确的 `sponsor-likely`。
