# 第 33 轮(2026-08-02 · dd plan 决定线四页专项)—— 沉档自工作台

# 功能盈利点检查(迭代式整改 · 第 33 轮 · 2026-08-02 · 清单 = #254、#259-#262)

> **迭代工作台文档**。工作法:体检/走查 → 统一检查 → 逐条整改意见 → 实施 → 下轮复查闭环。
> **轮次记录**:第 1-32 轮沉档至 [整改轮次存档.md](整改轮次存档.md);工作台只保留当前轮。
> **当前轮次**:**第 33 轮 2026-08-02(`dd plan` —— 决定线四页专项)**。
> - **范围**:`/plan/pr`、`/plan/job`、`/plan/career`、`/plan/province` 四页(本轮起 `dd` 支持模块参数)。
>   四页共用同一套问卷外壳,所以下面每条都是**一处坏、四页全坏**。
> - **第 32 轮 #251-#253/#255-#258 七条**已闭环,本轮范围内未复发;#254(职位板字号)属 jobs 模块,顺延。
> - **手法**:体检脚本 `--only plan` 三语两视口(117s/24 次访问)+ 人眼走转化链路(Frank 的 Chrome 插件)。
> - **本轮教训(记给下一轮的我)**:先按 chunk 指纹判「部署卡住」是**错的** —— Render 事件表显示
>   `60fe178` 6:12 起、6:14 已 live,我 18:15 才开始轮询,量的是稳态。**判部署要查 Render 事件表,别拿指纹猜**。
>   真因在工作区:`i18n.ts` 等 14 个文件的改动**从没提交过**。

## 本轮清单(第 33 轮)

| # | 级 | 条目 | 证据 | 改法 | 状态 |
|---|---|---|---|---|---|
| 259 | **P1** | **职业选择器 27 个分类标签,21 个显示成 `broad.餐饮` 这种原始 i18n 键**,中/英/韩三语全中。这是决定线**第一步**、漏斗最宽处,四页全坏 | 生产实测(Frank 的 Chrome + 体检 R2×8,MAXPER 截断掩住了真实条数);值本身干净(`餐饮` = U+9910 U+996E) | **代码已经写好了,只是没提交** —— 工作区 `cms/src/app/(frontend)/jobs/i18n.ts`(+19/-7)补齐了 27 个短名键。**不由本轮提交**:它和 `JobsTable.tsx`/`colWidths.ts`/`colWidths.shared.ts`/`Jobs.ts`/`payload-types.ts` 等 14 个文件是同一批进行中的工作,拆开提交会切碎那条线 | ✅ 当轮修(Frank「全修了」→ 单独提交 `i18n.ts` 这一个文件,不动同批的 colWidths 那摊;上线后复查标签) |
| 260 | P2 | **问卷里所有可点按钮低于 44px 触控靶**:375 实测 29px(桌面量 53/53 全在 23-27px)。第一步就要在密密麻麻的小胶囊里点中一个 | 体检 R8(375/zh,`div.plQuizPad>div>button` 152×29、127×29);Chrome 实测 53 个全部 <44 | `chipStyle` 是全站共用(padding `4px 12px`、fontSize 12.5)—— 别全站一刀切。手机断点下只给 `.plQuizPad` 内的按钮抬到 44px(padding 上下加,字号不动) | ✅ 当轮修(`.occChips`/`.occAz` 手机断点 `min-height:44px`;定高 34 的已选/同族横滑条不动,抬了会撑破) |
| 261 | P2 | **英文桌面端职业选择区两块重叠**(area 34200) | 体检 R7 ×4(1440/en,四页全中) | 先在 1440 复量确认不是 sticky 动作条与流内元素的正常交叠(800 宽实测:动作条 399-455 压住已选职业块 338-440),再决定改法 | ✅ **判为误报**:`.quizBar` 桌面 `sticky bottom:0`、手机 `fixed` + `.plQuizPad{padding-bottom:78px}` 已避让 —— 粘住后 rect 盖住兄弟是设计上的浮层。改的是**规则**:R7 把 sticky 与 absolute/fixed 一并排除,免得每轮复发 |
| 262 | P2 | **已选职业胶囊与搜索框之间一大片空白**(1440/en 实拍:胶囊 250 结束,搜索框 330 才开始,中间 80px 全空) | Chrome 1440/en 截图 | 常驻高度 102 是为了不跳版(#251 那批),**不能撤**;空白只出现在同族还没拉回来的那段时间 → 补同族占位条,白板变「在加载」。拉回来真是空的照旧留白,不硬编内容 | ✅ 当轮修 |
| 254 | P2 | **职位板 `/` 在 375 上有 8 种字号**(11px 的是岗位卡元信息) | 375/en 实测 sizes | 收敛到 3-4 档 | ⬜ 第 32 轮顺延,属 jobs 模块 |

**当轮特批实施(Frank「全修了」推翻「检查不动产品」)**:#259/#260/#262 三条代码侧改完,#261 判误报改规则。
提交只带这三个文件(`i18n.ts`、`OccPicker.tsx`、`checkup.py`),**同批的 colWidths 那摊一个字不碰** ——
`git commit -- <路径>` 绕开索引,免得再把并行 session 刚 add 的在建代码卷进来(本轮踩过一次,已纠正)。
**验证口径**:`tsc --noEmit` 干净;**没跑 `npm run build`**(另一 session 的 dev server 占着 `.next`);
**没拍本地效果图**(起第二个 dev 会打爆 pooler —— 生产 500 那次事故的原因)→ Frank「赶紧上线」拍板
直接推生产复验。**生产实测(`3c05dab`,部署 ~90s,脚本 scratchpad/verify_plan.py)**:
en/1440、en/375、zh/375、ko/1440 四组**分类 27 个、漏键 0**(英文出 `Food service / Healthcare /
Trades / Construction / Retail`;`Healthcare` 也印证换了新包,旧包是 `Health`);
职业胶囊 375 两语**最矮 44px**(桌面 1440 仍 29px = 手机断点没误伤桌面,符合预期)。
→ #259/#260 生产闭环。

**脚本记账**:`checkup.py` 本轮加了 `--only <模块>`(MODULES 表:jobs/pricing/stats/rankings/news/pathways/plan/account),
全量 ~7min → 单模块 12-38s;`/plan` 四页同时进 FULL 与 LITE(付费转化链上,英韩漏翻要查得出来)。
`--only plan` 全语种 117s/24 次访问。


