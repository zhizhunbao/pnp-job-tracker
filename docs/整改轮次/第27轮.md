# 第 27 轮 · 无截图体检第二轮(2026-07-26 晚 · Frank「跑 dd 全站体检,但是不用截图吧」)

> 方法:`checkup.py --round 27`,零截图。用时 **472s**,命中 **222** 条(第 26 轮首轮 646 → 调阈 153)。
> 本轮先实施上轮遗留 #212-#214,再跑体检;体检结果再反过来把 5 条规则调准。

## 一、上轮遗留三件的处置

| # | 级 | 结论 | 依据 |
|---|---|---|---|
| 212 | P2 | **真问题,部分修**:页脚法务四链、新闻地区筛选改真实内边距(页脚 9px、chip min-height 36),/stats「口径说明」同款;顶栏按 Frank 前次拍板豁免;手机岗位卡内容链接标 `data-tap-card` 不算独立靶 | 命中测试(elementFromPoint 中心 ±14px)实测:`.tapPad` 伪元素热区**会被相邻元素抢点**;修后 /news 不达标 9 → 4 |
| 213 | P2 | **核销为假阳性** | /rankings/daily 与 /rankings/sponsor 用**逐行 rect** 实测零真重叠;上轮 499px² 来自「单行 span + 折行 a」的并集包围盒 |
| 214 | P3 | **核销**:TEER 码是 Frank 当日拍板保留的(3de5caf),口径挂 title,手机 chip 可点开分类弹框 | `cellActionable('teer') = true`;规则已加豁免 |

## 二、本轮体检结果(222 条 → 判真 2 件)

| 规则 | 条数 | 判定 |
|---|---|---|
| R8 触控靶 | 111 | 多数为豁免类(卡内内容链接、大条钮);**真的剩 4 处**(见 #217) |
| R1 TEER 无注 | 35 | 假(Frank 拍板保留码面,已加豁免) |
| R5 截断 | 25 | 假(页脚「关于」实测 `overflow: visible`,盒子比字窄但画得出来) |
| R3 同页重复 | 23 | 假(榜单页签=定位、新闻列表同构标签、弹框与被盖住的页面重复) |
| R2 汉字残留(属性) | 12 | **真** → #216 |
| R7 元素重叠 | 8 | 假(本轮新加的 `colgroup` 与 thead/tbody 必然相交) |
| R4 空态连片 | 8 | 假(匹配视图实测 50 行零空行,扫到了渲染中途) |
| console / 4xx | 0 | 干净 |
| R11 数字对库 | — | 页面 39,178 == API 39,178,三语两端一致;心跳「核对 2026-07-26 17:16」 |

## 三、新清单(下一轮实施)

| # | 级 | 问题 | 证据 |
|---|---|---|---|
| 216 | P1 | 新闻卡 `title` 属性是**中文摘要**,英文/韩文界面悬停出中文(12 处) | R2:`title=渥太华省提名计划(OINP)新增…` 出现在 news/en 与 news/ko |
| 217 | P2 | 触控靶真偏小四处:/stats 三个下拉 28px 高、账户页 ✎ 13×17、匹配视图「…」39×24、jobs「更多」按钮 29px | R8 命中测试不达标 |

## 四、规则调阈(5 条,本轮改进,写进脚本)

1. **R1 TEER**:元素带 `title`/`aria-label` 或可点 → 视为有注(否则每轮 35 条常驻噪音)。
2. **R4 空态**:整行全空视为渲染中途的骨架行,只报「有内容的行里连着 4 格空」。
3. **R5 截断**:仅 `overflow: hidden/clip` 才算截断(visible 时内容照样画得出来)。
4. **R7 重叠**:排除 `colgroup/col`(表格布局元素无可视盒);两两比改**逐行 rect**。
5. **R8 触控靶**:改**命中测试**(中心 ±14px 仍点得中本元素);跳过整卡可点的卡内内容链接;
   大条钮(高 ≥32 且宽 ≥120)视为达标。

## 五、本轮记账

- **假阳性率 ≈ 99%**(222 条判真 2 件)——但根因全在规则,已逐条调准;下一轮同口径再跑可作真实基线。
- 教训:**热区扩展方式决定量法**。`.tapPad` 用伪元素扩热区,量元素自身 rect 必然误报;
  我一度据此断言「上轮修复没生效」,实测后自纠 —— 量法不对就别下结论。
- 顺手处置(Frank 走查插入):注册框的匹配度预览样张删除 —— 那两行是**编造样例**
  (software developer 高 / data analyst 中),放在以「真实数据」为卖点的站里最不该有。


## 六、第 28 轮(同日续跑,调阈后的真实基线)

规则调准后同口径重跑:**222 → 34 条**(509s)。34 条里 24 条属已拍板保留(榜单页签=定位、
新闻列表同构标签、弹框与被它盖住的页面重复)、8 条是顶栏(Frank 拍板豁免),**真剩 2 件**:

| # | 级 | 问题 | 证据 |
|---|---|---|---|
| 218 | P1 | `/rankings/sponsor` 控制台报 React #418(SSR 与客户端文本不一致),两视口同现 | 第 28 轮 console:`Minified React error #418` ×2 |
| 219 | P2 | 账户页 ✎ 改到 37×37 后命中测试仍不达标(邻居抢点) | 第 28 轮 R8:`account/375x812` 37×37 |

**脚本再修三处**(本轮结束时):心跳探针跟上「更新时间」改名(不然每页 heartbeat=None);
R8 顶栏整条豁免;R3 排除「弹框 vs 被遮住的页面」与「页签 vs 榜标题」。

**一个假警报记账**:R11 报「页面 39,186 ≠ API 39,002」——体检跑了 8.5 分钟,期间 ETL 落了一轮,
不是真不一致(同一时刻复测:页面 39,002 == API 39,002)。跨分钟的数字比对要么同刻取,要么容差。
