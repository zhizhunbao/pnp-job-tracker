# 第 26 轮 · 无截图体检首轮(2026-07-26 · 设计文档 [无截图体检-20260726.md](../design/无截图体检-20260726.md) 落地)

> 本轮方法变更(Frank「如果不截图是不是省 token,能查出来问题吗」拍板):
> **体检脚本跑全量 → 出可疑清单 → 只对命中项截图确认**。轮次文档、编号规则、闭环标记照旧。
> 脚本:`.claude/skills/iterate-audit/scripts/checkup.py`(系统 Python312 + Playwright,全程零截图)。
> 覆盖:offer2pr.com 生产站,375×812 与 1440×900 两视口 × zh/en/ko 三语 × 匿名态 + 测试号 `fable5d@test.local`;
> 页面 10 个(/jobs、/pricing、/stats ×3、/rankings ×3、/news、/pathways)+ 移民价值弹框 + JD 弹框 + 匹配视图 + 账户页。
> 用时 429s,原始命中 646 条 → 调阈后 153 条 → 判真 10 件。**确认用截图 6 张**(上一轮全量截图 48 张)。

## 本轮开销对照(这是本轮要证明的事)

| | 第 24 轮(截图轮) | 第 26 轮(体检轮) |
|---|---|---|
| 截图 | 48 张(两端全量) | 6 张(只拍命中项) |
| 发现方式 | 逐张肉眼看 | DOM 量化 + 终端只打命中 |
| 覆盖 | 中文两端 | 两端 × 三语 × 匿名/登录 |
| 漏检风险 | 眼睛会跳过 | 可量化项不靠运气(观感类仍要眼睛) |

## 新发现(#202 起,承第 25 轮 #136)

| # | 级 | 条目 | 体检证据 | 截图复核 | 改法方向 | 状态 |
|---|---|---|---|---|---|---|
| 202 | P1 | **手机端「核对 时间」看不见** | DOM 有 `核对 2026-07-26 01:41`,但 375 下 `rect.width=0`(zh/en/ko 全中);1440 正常显示 | c1 手机 /jobs 顶部只有「38988 个职位」 | 07-23 刚为「数据时间怎么没更新」把页面时间改成 ETL 心跳,主流量端(小红书=纯手机)却看不到,等于白做。手机头部或列表上方保留一处 | ⬜ |
| 203 | P1 | **/stats 首页核心指标成片「—」** | 12 处 `—` 且 `cursor:pointer`(看着能点,点了没内容);两端三语稳定 | c5 桌面 /stats:ON/QC/NB/NL/PE「省提名清单岗」=—,QC「PNP 登陆(年)」=—,AB 页「AIP 指定雇主岗」=— | 真实数据站首页大面积「—」直接掉可信度。ON 2026-06 改制后排除集为空=全职业可,应说人话而不是留空;确实无数的省写明口径(「该省无清单制,按 TEER 判定」),同时撤掉假手型 | ⬜ |
| 204 | P1 | **手机顶栏触控靶过小** | 登录 25×19、注册 49×27、中 31×25、EN 35×25、한 30×25、☰ 34×34;英韩同款更窄(Sign in 42×14、로그인 35×19);10 页全中 | c1 | 手机优先铁律下这是主入口与主转化钮。按 ≥40×40(iOS HIG 44)补 padding/热区,视觉可不变 | ⬜ |
| 205 | P2 | news 卡「重要 5/5」裸档 + 「最新公告」伪链接 | `重要 5/5` 无 title 无注;`最新公告` 是 `<a>` 无 href、`cursor:auto` | c2 手机 /news | 裸词铁律:5/5 换成人话或加灰字小注;当前页签别用 `<a>`(看着像链接点不动) | ⬜ |
| 206 | P2 | **/news 图 404:`/img/regions/nb.jpg`** | 全站唯一 4xx,两端三语 6 次稳定复现;`naturalWidth=0` | c2(NB 卡封面裂图) | 补 nb.jpg,或缺图回退 federal.jpg | ⬜ |
| 207 | P2 | 匹配视图「与我的匹配」列裸字「高」 | 桌面 8 格文本恰为「高」,`title=null`,父元素也只有「高」 | 文本复核(登录态) | 裸字高/中/低无口径。人话名 + 灰字小注(如「高 · 三项维度全中」) | ⬜ |
| 208 | P2 | 手机 /stats/ab「主要城市」行挤成一团 | 5 组标签 `getBoundingClientRect` 相交(Calgary⟂Fort McMurray 等,交叠面积 2065-6539px²) | c3 手机 /stats/ab:城市名被拆行(Fort / McMurray),名与数字粘连 | 城市改一行一条或胶囊 `white-space:nowrap`;禁名字断行 | ⬜ |
| 209 | P2 | 「/」杂糅多信息残留 | 搜索占位符「搜索 职位/公司/地点/NOC…」(10 页全中)、pathways「护工 / 家庭护理」「还在国内/海外」(12 处)、榜单「医疗社服/医生」 | — | 违反 no-dot-separator 硬规矩(存量清理本就在队列);占位符改「搜索职位、公司、地点」 | ⬜ |
| 210 | P3 | 同页事实重复 | /news 同一条(NS Nominee Program…、Ontario Workforce Priority)头版轮播 + 列表各一次;rank-daily 页标题与卡片标题重复 | c2 | 轮播与列表去重,或列表跳过已上头版的条目 | ⬜ |
| 211 | P3 | /jobs 首屏慢(记账,不立即改) | LCP 手机 2.7-5.4s、桌面 2.5s、ko 3.6s;JS 266KB;其余页 LCP <0.7s | — | 20000 行 SSR 的代价,与服务端分页收口一起看,本轮只记账 | ⬜ |

## 复核为真但不立项

- **R11 数字对库一致**:页面「38988 个职位」== `/api/jobs` total 38988(zh/en/ko、两端、匿名/登录全一致)。
  韩文页体检一度报 2571 —— 是抓取正则先命中「PNP 목록 2571건 포함」,脚本已改锚在「개 공고/个职位/jobs」。
- **R2 三语汉字残留 0 条**(en/ko × 两端 × 5 页 + 弹框):第 14 轮起的语言回归保持绿。
- **hydration mismatch 0 条**;console 唯一错误就是 #206 那张图的 404。

## 假阳性与阈值调整(首轮实测,指导下轮)

| 规则 | 调阈前 | 调阈后 | 根因 |
|---|---|---|---|
| R5 折行 | 66 | 1 | **中英混排一行也会拆出 2-3 个 client rect**(「购买 30 天 CA$19」实测就一行)→ 改按不同 `top` 计行;数据表格单元格(长英文职位名)天然要折 → td/th 免检 |
| R9 手型指向空态 | 116 | 12 | 图标钮/轮播点天生无文字 → 只认值为「—」的格 |
| R7 元素重叠 | 76 | 6 | SVG 内部元素 `tagName` 是小写(circle/path),大写正则没拦住 → 图标内部全被当重叠 |
| R3 同页重复 | 16 | 6 | 同一 sel 路径的重复 = 列表里的同构标签(新闻卡省名)→ 只报「同一句话出现在两处不同结构」 |
| R1 斜杠杂糅 | 37 | 14 | 免责声明整句里的「移民/法律建议」是「或」不是枚举 → 只管 ≤30 字且无句读的短标签 |
| R4 空态连片 | 8 | 0 | 手机端表格 attached 但不可见,14 个空格全是隐藏 DOM → 加 visible 过滤 |

**首轮假阳性率 ≈ 76%(646→153),调阈后清单可直接逐条判。**
肉眼补抓、脚本首轮没有的:搜索框占位符的「/」杂糅(placeholder 不是文本节点)→ 已加属性扫描(placeholder/title/aria-label)。

## 方法论结论

能量化的都量到了:**#202/#203/#204/#206/#208 五件 P1-P2 是纯 DOM 判出来的,截图只是复核**;
观感类(留白、配色、好不好看)脚本一条没查,照设计文档划界留给 Frank 本人。
下轮起 dd 默认走「体检 → 命中截图」,全量截图轮改为按需(改版后、或 Frank 要看整体版面时)。
