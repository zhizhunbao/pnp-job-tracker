# STATUS 交接存档(2026-07-20 及以前)

> 2026-07-19 自 STATUS.md 沉档(Frank 问 token 成本,STATUS 头部 25k tokens 是每 session 固定大头)。历史交接原文如下,不再更新。

> 2026-08-01 二次沉档(Frank「文档太乱,太消耗 token」):STATUS 又攒到 9 段交接,只留最近两轮,以下为 2026-07-27 及更早。

> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-07-27,「E12-09 ③ 打分表扩到 SK + 跨省对照 + E5-07 定价页」)**
> - **✅ 第二个省定为 SK,四个省逐个实核后才选的**([E12-09](docs/implementation/E12-移民路径引擎/09_省提名分数参考与换省建议.md)):
>   · **ON** 改制后 EOI 未开,既没分值表也没分数线(立项时就写死不许拿旧通道的线充数);
>   · **AB** alberta.ca 全站 AAIP 页**不公布 EOI 分值表**(只公布每次抽选分数)→ 自算无从算起;
>   · **MB** 官网有 100 分制**申请评估表**,但抽选公布的是 **1000 分制 EOI Ranking score**(最近一次 825),
>     两套分制不是一回事、官网也不公布 1000 分制构成 —— 拿 100 分去对 825 是**错的对照**,弃;
>   · **SK** 官方 Points Grid 全表在 HTML 里(110 分制),且官方明说 **EOI 排名用的就是这张表**,
>     另有硬门槛 **≥60 分才能申请** → 自算分能对上一条真实官方线,选它。SK 官方不公布逐次抽选线(库里 SK 抽选本来就空),
>     对照锚就用 60 分门槛,**不编分数线**。
> - **✅ `etl/pnp/build_sk_points.py`**:自校照 BC 惯例是硬闸 —— 分节最高可得必须等于官方自己印的
>   `MAXIMUM POINTS FOR FACTOR I=80 / II=30 / TOTAL=110`,不过就保留旧表不覆盖。首跑即过
>   (23+10+5+20+10+12=80 ✓ / 组 II 按子类二选一取 30 ✓)。
> - **✅ 数据层泛化**:`IN_SCORE_TABLES` 一个 list,**加省只加一个文件**,组装逻辑不动;
>   `pnp_score_factors` 加三列 `factor_group / group_max / pass_mark`(DDL 先行,docs/sql/e12-09b-sk-points.sql,
>   纯 `ADD COLUMN IF NOT EXISTS`,**我已在生产代跑**),mart 32 行 → **69 行**(BC 32 + SK 37),seed 已灌。
> - **✅ 前端 BcSirsCard → `PnpScoreCard` + `pnpSelfScore.ts`(跨省对照)**:用户**填一套条件**,各省按各自官方表折算。
>   映射尽量**从官方标签自身解析数字**(年数/CLB/年龄区间),官方改版加一档自动跟着走;学历没数字可解析,
>   用关键词阶梯定级(显示层映射,命中的官方原文挂 title 供核对)。**分值一分都不许前端编。**
>   实测(木匠 $37/hr Surrey,学士 / 3 年经验 / CLB7 / 30 岁):**BC 69/200(本岗通道线 97,差 28)、
>   SK 56/110(门槛 60,差 4;勾上「拿到 SK 雇主 offer +30」合计 86 过线)**,并挂该省同职业在招数
>   (BC 217 岗 / SK 31 岗,复用 /api/quiz?noc=,不新增端点)。
> - **⚠️ 抽选线改按通道匹配(本轮抓到的真错)**:BC 现行是**按通道分别设线**(近 6 次 50-136 分),
>   原来「取最近一次抽选」会拿 **偏远医疗 50 分** 的线去比一个木匠 —— 改成按通道名实词子集匹配
>   (岗位侧「BC PNP Build: construction trades targeted ITA」↔ 抽选侧「Build: Construction Trades」),
>   **匹配不上就不给差分结论**,只摆近期分数线区间。SK 岗看 BC 那一栏就是这个形态。
> - **🐛 顺手修两个真 bug**:①`yearsOf` 正则顺序 —— 「At least 3 but less than 4 years」里有两个数字,
>   通用正则先命中 4,导致 3 年经验被判进 2-3 年那档少算 4 分(必须先认「At least N」);
>   ②弹框换岗不重置 —— `PnpScoreCard` 没加 `key={job.id}`,React 复用实例、useState 初值不再跑,
>   点第二个岗还显示上一个岗的时薪/地区/offer 默认值。
> - **✅ E5-07 定价页按「结论」重写**:卡序改 **免费 → Pro 90(主位)→ Pro 30(试水)**,主推靠版式不写「推荐」;
>   Pro 三条改成一句结论 + 小字写清具体给什么(我的通道报告 / 岗位盯梢 / 投递辅助);
>   **免费列补齐事实**:官方职业清单与抽选记录、中位薪资(07-25 已放开,原来还错列在 Pro 里=在卖已经免费的东西)、
>   省提名官方分值表与自评打分、每周摘要邮件。文案全程不提「次数」。三语齐。
> - **✅ 漏斗补两个埋点**:`pay-click`(**在登录判断之前发** —— 未登录点了也是付费意向,放后面会把「点了没注册」
>   整段漏掉;与已有 `checkout` 不重复)、`weekly-optin`(账户页周报开关,订阅/退订都发,退订量本身就是信号)。
> - **📌 三问结果页付费墙有意暂缓**(不是漏了):跨省对照现在是**免费**的(PNP 弹框里就能看),
>   在结果页立墙 = 把已放出去的收回来,与 07-25 拍板「付费面调整先看用量」冲突。
>   先让 pay-click 与打分卡用量跑几周,Frank 拿数据拍板收不收、收哪一段,再动 `lib/plan.ts`。
>   **在此之前定价页不写「通道报告要付费才有」。**
> - **✅ #219 账户页 ✎ 触控靶**:原来靠 padding + 负 margin 拼出来只有 ~37×35 → 改显式 40×40 inline-flex
>   (行高由 52px 头像撑着,不顶高这一行)。
> - **✅ 自评打分改独立一卡**(Frank 当场拍「算分的要单独一个卡片,不要嵌套到其他卡片里面」):原来塞在
>   判定卡内部 = 卡中卡;改成与判定卡平级的 MODAL_CARD(照本弹框「每块一卡」既有排法),卡壳归外层、
>   组件自己不再画边框。手机高度顺带 1730 → 1336。
> - **✅ 同轮四拍(Frank 连报)**:①自评打分改**独立一卡**(原来嵌在判定卡里=卡中卡);
>   ②**对齐** —— 加分项的 +N 原来跟在文字尾巴上飘,拆成 [勾选框|条目|+N] 三列;省结果的「12 / 40」
>   也是两个事实,拆成 得分/上限 两列,两张省卡等宽因此数字跨卡也对齐(同 FactGrid 规矩);
>   ③**按钮箭头全删**(「不允许使用箭头」)—— i18n 96 行标签 + BackLink/返回钮/雇主对比/榜单/统计/
>   投递钮/周报邮件里的 → ← ↗ 全清;**只删串首串尾的装饰箭头**,中间的「拿 offer → 工签 → 省提名 → PR」
>   是「然后」的意思,删了读不通,保留;表头排序指示 ▼▲↕ 是状态标记未动;
>   ④**重名职业不甩 NOC 码**(「厨师为什么后面有个码」)—— 那码是中文重名兜底(63200 Cooks / 62200 Chefs
>   中文都叫厨师),但 5 位码普通人一样分不清 → 重名改挂**英文官方名**,英文也重名才退回码。
> - **✅ 打分迁出 PNP 弹框,落到「移民路径」页(Frank「应该单独弄个功能吧,不应该放到 pnp 弹框里面」;拍板落位=/pathways 一节)**:
>   打分算的是**你这个人**够不够分,跟看哪一个岗没关系 → `PnpScoreCard` 的 `job` 依赖拆成轻量 `ScoreCtx`
>   (职业/目标省/时薪/城市,全可选),`ScoreFactor` 类型下沉到 `pnpSelfScore.ts`;
>   **/jobs 整条打分表维度链路拆掉**(每次 SSR 少灌 69 行);弹框里连跳转链也不留(#198/#199)。
>   /pathways SSR 取分值表 + 抽选 + **你的职业命中的具名通道**(抽选线仍按通道对照,对不上只摆区间)。
> - **🐛 抓到一个藏起来的真错:seed 新列静默不写**。SK 那栏本该显「官方申请门槛 60 分」却显「官方未公布分数线」
>   → 查库发现 `pass_mark / factor_group / group_max` **三列全 NULL**(首次 seed 用的是没带新列的旧路由),
>   而 **`seed_state` 表级哈希让之后每次 seed 直接跳过(counts=-2)**,错就一直藏着。
>   **修法记档:改了 seed 列白名单后,必须 `DELETE FROM seed_state WHERE name='<表>'` 再灌**,
>   否则「内容没变」的哈希会让新列永远补不上。清完重灌:SK 37 行全部写入(pass_mark=60、group_max=80)。
> - **✅ 匹配列退役(Frank「这一列没有必要吧」+ 拍板「默认列里删掉,匹配只作筛选与排序」)**:
>   该列本来就不在默认列集、也不在列选择器里,只在**匹配视图**被强制插第一列 —— 而那个视图本身就是
>   「你的匹配」,再逐行复读一遍「高」是零信息量(同 07-26 手机卡裸字胶囊下架的理由)。
>   匹配仍是筛选与排序维度(view=match 的 WHERE、fElig、sort=match),只是不占一列;
>   定价页 Pro 第一条随之改口:不再吹「全量与我的匹配」,改「按档案筛出命中你的岗」。
> - **📐 手机自验(375 真截图)**:自评面板两列(126×2+10 ≤ 弹框内宽 301 − 卡片 padding 28 = 273;
>   **先试的 132 差 1px 就掉回一列 —— 算的时候别忘了减卡片自己的 padding**),
>   两视口 `scrollWidth − clientWidth = 0`;「Area 2」官方原文是 5 个市镇清单,下拉里必截断 → 中文出短名。
> - **✅ 已全推生产并复验(44f7d57)**:哨兵 2 分钟确认线上 == origin/main;375 实拍生产
>   /pathways 打分节在(「官方申请门槛 60 分」「BC SIRS」「SK SINP Points Grid」齐、解释句已删、
>   )、/pricing 三条结论式且 90 天在 30 天之前、免费列含官方分值表;
>   两页按钮箭头零残留(仅路径名与步骤链保留「A → B」的「然后」义,那不是按钮)。
> - **⏭️ 下一步**:ON 待官方 EOI 开放再补;三问结果页锁块等 pay-click 用量由 Frank 拍板。

> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-07-27 凌晨,「收费方案 + E12-09 BC 打分」)——86dabd0 全生产**
> - **收费方案定调**([策略](docs/design/收费方案-20260727.md) + [E5-07 落地设计](docs/implementation/E5-商业化功能/07_收费方案落地(结论收费与留存钩).md),带三张 375 真样式效果图):
>   诊断=①付费卖的是次数不是结论 ②免费层几乎能干所有事 ③**没有任何回访理由**(求职周期以月计、产品按 30 天卖)。
>   原则定死:**事实免费(职位/标记/清单/抽选/中位薪资=获客本钱),结论收费**,界线永远画在「事实→结论」。
> - **✅ 留存钩第一步上线**:周报管线早就建好(E9-02b),但条件是「有收藏」——**刚答完三问的新用户一封收不到**。
>   生产 dry-run 基线 **0 封** → 补档案分支后 **6 封**。内容=你的职业本周新增 N 岗、其中可提名 M、**中位年薪**
>   (第一版用「最高年薪」实拍出 $520K,是个 $250/hr 合同岗的离群值,当场改中位)+ 三条真岗。
> - **✅ 邮件按语言发**(Frank「很多是英文人吧」):查出 `users.locale` 字段**从来没人写过** → 注册时存界面语言;
>   有 locale 发单语(zh/en/ko),没有才发双语;顺带修「英文邮件里出现中文职业名」(同 #216 那类漏)。
> - **✅ E12-09 第一块:BC 注册打分(SIRS 200 分制)全链路上线**
>   · 数据源实核:welcomebc 的 SIRS 网页**已 404**,细则只在官方 Program Guide PDF 里 → `etl/pnp/build_bc_sirs.py`
>     直取 PDF、pymupdf find_tables 逐表解析、抓指南生效日(2026-06-10)作过期锚;
>   · **逐节自校是硬闸**(每节最高可得必须等于官方 Max,不过就保留旧表):实跑拦下两次真错——稀疏表格一行没抓到;
>     教育 46≠40 因为「在 BC 读 8 分 / 在加拿大其它省 6 分」是**二选一**(原文「…, or」),已存成 xorPrev;
>   · 落库:mart `pnp_score_factors` 32 行 + collection + seed;**DDL 先行**(docs/sql/e12-09-*.sql);
>   · **手写建表的坑记档**:Payload 的 `payload_locked_documents_rels` **每个 collection 一列**,少了它 seed 直接 42703
>     回滚(表现为 /seed 500 无 body)——DDL 文件已补这段,以后新增维度表必须跟着跑;
>   · 前端:BC 岗 PNP 弹框出「BC 注册打分(自算)」——时薪与工作地区**按本岗自动算**(地区可改),人力资本三项自选,
>     加分项官方逐条、二选一组只算一项;**对照真实抽选线**(「比最近一次低 30 分,最近 2026-07-23…最低 50 分」);
>     口径行写明「按官方分值表自算,不是资格认定」;官方英文标签在中韩界面出人话(显示层映射)。
> - **📌 定价页暂未改(有意为之)**:E12-09 只做完 BC 打分,**「换哪个省更快」还没有**(要别的省的分值表)。
>   已上线的打分卡目前**免费**;等跨省对照做出来再按 E5-07 重写定价页——不卖还不存在的东西。
> - **⚠️ 生产库我代跑了两段 DDL**(建 pnp_score_factors + 补 rels 列,均为 `IF NOT EXISTS` 纯增量,未动任何已有数据),
>   随后 upload_mart + seed 全量成功(pnp_score_factors 32 行,jobs 36,615)。

> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-07-26 下午,「Frank 连环走查十四连」)——8ccf646 / de256e0 / 49b2067 / 195fc4b / fc17e4f**
> - **E6-10 联邦抽选看板落地**:EE 弹框新增「联邦抽选近况」卡(近 20 轮真轮次,类型着色 + 可展开),
>   口径注由写死的「现阶段以 CEC 与法语为主」改**活数据**(实测「近 20 轮:省提名 6、法语 5、CEC 5、类别 4」);
>   法语只作说明与分数线参考、**不挂岗位**(红线守住)。零新表(仍走 pnp_draws 的 province=FED 行);
>   顺手修 `build_ee_draws` 类型归一:「Federal Skilled Trades」原被 "federal skilled" 前缀吞进 FSW。
> - **ON 改制块改写**(Frank「老的历史记录删了吧,改成最新的打分规则」):改制日之前的抽选行**不再渲染**,
>   改列现行规则四行。ontario.ca 实核:O.Reg 422/17 于 2026-06-25 生效、原 **8** 条流全废(旧文案写的 9 是错的)、
>   只剩 Workforce Priority(全 TEER 均有路径 + 自雇医生)、新 EOI「今夏开放」**尚未开** → 现阶段无抽选可列;
>   难度卡「近 180 天抽选 10 次」加注「均为改制前旧通道」,新 EOI 一开抽注自动消失。
> - **排版三条成通则**(已入记忆 [[ui-align-and-columns]]):①多值卡拆列做网格对齐(新原语 `FactGrid`);
>   ②优先左对齐,别扭才右对齐;③**表格默认列集改百分比固定布局,横滚清零**(1440/1100/900 三挡实测 overflow=0);
>   操作列按按钮实宽钉 96px(以后加钮改这一个数),其余列 `calc((100% − 96px) × 权重%)`。
>   踩过的坑记档:先试的「短值列钉定宽」反而撑出横滚。
> - **弹框与文案**:ESDC 工资表加年薪列(数据层 ×2080 口径,前端不换算);所有字段弹框岗位名下补 NOC 中文译名;
>   「凭什么…以官网为准」→「依据:…」;「能走通用雇主类通道」→「凭雇主 offer 可提名」;
>   PNP 列「可提名」→「{省码} 可提名」(省提名是逐省的);抽选卡头存量「·」清除。
> - **入口三问弹框补真数据**(Frank「太单薄」):第 2 题热门职业挂在招数(厨师 2,050…)、第 3 题省按钮挂
>   该职业各省岗位数(安大略 554 岗…,进第 3 题即预取、结果页因此秒开)、顶部回显已答项、底部说清答完拿什么;
>   桌面弹框由 520 窄条改 900 居中面板。删结果页一句通用科普。
> - **🆕 新硬规矩:解释类文案一律删**(Frank「除非你和我确认之后的,才能保留」)——已确认保留四类:
>   法律免责、「≠资格认定」、tooltip 口径、空态与操作引导;**其余 ~110 条候选下一轮清**([[copy-no-explanation]])。
>   首轮删 10 条(含 4 条死键),五页冒烟 console error 0、无裸 key 泄漏。
> - **📋 三个新立项**(今晚未做):[E12-09 省提名分数参考与换省建议](docs/implementation/E12-移民路径引擎/09_省提名分数参考与换省建议.md)
>   (Frank「这是最大的价值」)、[E9-06 网页客服与解释库](docs/implementation/E9-闭环/06_网页客服与解释库.md)、
>   [全链路选择主线](docs/design/全链路选择-20260726.md)(最大缺口=生活成本无数据源)。
> - **入口三拍定稿=居中弹框**:520 窄条 → 900 面板 → 整屏 → **退回 960 居中弹框**(Frank「全屏看着内容太单薄」)。
>   全屏那版加的**品牌头 + 信任栏**留下:🍁 Offer2PR + 定位一行,右栏摆库内真数(在招/命中省提名清单/
>   有外劳雇佣记录的雇主 + 核对时间)+ 官方来源署名 —— 起因是 Frank「简陋得像钓鱼网站」。数字由 JobsTable 传,
>   与横幅同一口径。**记一坑**:纵向居中的 justify-content 写在内联 style 会压过媒体查询,必须写进 CSS 类。
> - **✅ 已推生产并复验(9 commit,e0a2112)**:哨兵=本轮新增的 `/api/quiz?counts=`,线上返回真数=换版成功;
>   生产实拍复验四处:入口居中弹框(375/1440)、表格 `scrollWidth-clientWidth = 0`(无横滚)、
>   EE 弹框「联邦抽选近况」四类型真轮次、表格「ON 可提名」显省码。

> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 最新交接(2026-07-26 深夜,「入口三问上线 + 通道口径五连修」)——25c7f79 全生产**
> - **✅ 入口三问(付费漏斗第 1-2 步)**:落地即弹三题(在哪一步/做什么工作/目标省)→ **免费结果页**(库内聚合毫秒级,不碰 AI);
>   两个出口=「看这些岗」套筛选、「免费注册保存」注册后落库成档案;弹窗只弹一次,缺口用 **banner 下常驻细带**补
>   (没答过=去答/答过=看结果+改答案,预填上次选择)。/api/quiz 两端点(?q= 职业搜索、?noc= 结果),SQL 全在 lib/jobsSql。
>   设计=[付费漏斗重设计](docs/design/付费漏斗重设计-20260726.md)+[冷启动获客](docs/design/冷启动获客与首笔付费-20260726.md)。
> - **✅ 通道口径五连修(Frank 连报,全部实核数据)**:①**【P0】列表说「可提名」弹框说「走不了」**——两套判定
>   (列表用服务端 pnp_eligible,弹框写死 teer<=3),实测 ON 3,254/AB 1,328/SK 106 个岗打架 → **弹框改以服务端为单一真相**;
>   ②「走通用」补「凭什么」一句;③QC 说清走 CSQ/PEQ 自有体系;④**安省旧通道改制注**(2026-06 O.Reg 47/26 废旧 9 流,
>   改制前抽选灰化 + 脏行过滤);⑤**EE 休眠降级**(实核 STEM 上次 2024-04 即 27 个月前、运输 2024-03、教育 2025-09;
>   >12 个月未抽=灰标 + 标上次抽选),EE 弹框补「现阶段联邦以 CEC 与法语为主,法语按语言能力不按职业」。
> - **✅ 其他**:TEER 2 档名漏「主管岗」按官方补全(canada.ca 实核);TEER 卡面按 Frank 拍板改回码 + 口径挂 title;
>   手机卡两枚红胶囊同时命中时合一;走查脚本一律关 umami(自污染教训:Playwright 点弹框会真发事件)。
> - **📋 两个数据层新立项排队**(Frank 走查提出,今晚未做):[E6-10 联邦抽选看板(CEC/法语)](docs/implementation/E6-数据补强/10_联邦抽选看板与法语CEC.md)
>   、[E6-11 偏远试点 RCIP/FCIP 接入](docs/implementation/E6-数据补强/11_偏远试点RNIP与RCIP接入.md)。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上轮交接(2026-07-26,「走查提速:无截图体检脚本 + 第 26 轮首跑」)——脚本落地,清单 #202-#211 已全实施**
> - **✅ `checkup.py` 上线**(`.claude/skills/iterate-audit/scripts/`,系统 Python312 + Playwright,零截图):两视口 × 三语 × 匿名/测试号,10 页 + 弹框 + 匹配视图 + 账户页,~7 分钟;规则 R1-R14(裸词、三语残留、重复、占位符、折行截断、出屏、重叠、触控靶、手型一致、死链、数字对库、空态、console、4xx/LCP),输出 JSON + **只打命中项**的摘要。dd 第 2 步默认改为「体检 → 只对命中项截图」,全量截图轮改按需(SKILL.md 已改)。
> - **首轮实测**:646 条 → 调阈后 153 条 → **判真 10 件(#202-#211)**;**截图 48 张 → 6 张**。P1 三件纯 DOM 判出:①手机端「核对」时间 DOM 有但不可见(07-23 刚立的心跳在主流量端白做);②/stats 首页 12 处「—」还带假手型(ON/QC/NB/NL/PE 省提名清单岗全空);③手机顶栏触控靶 25×19(登录/注册/语言钮全小于 40)。另有 `/img/regions/nb.jpg` 404(全站唯一 4xx)、匹配列裸字「高」、手机主要城市标签重叠、「/」杂糅残留 14 处。
> - **首轮假阳性 76%,已调阈并记档**(设计文档第 7 节 + [第26轮.md](docs/整改轮次/第26轮.md)):最大的坑=**中英混排一行会拆出多个 client rect**,按 rect 数判折行全是假阳性(改按不同 top 计行,66→1);SVG 子元素 tagName 小写导致图标内部全报「重叠」(76→6)。
> - **R11 口径偏差记账**:本机无 psycopg(系统 py 与 uv 环境都没有),「直连生产库」改用生产 `/api/jobs` 作库口径 —— 页面 38988 == API 38988,三语两端一致。
> - **✅ 当轮全实施+生产复验闭环(Frank「把 #202-#211 修了」;20fc3bc + 2bf9206)**:十件逐条上线——手机端心跳可见 / stats「无清单·不适用」说人话+AIP 卡只在大西洋四省 / 顶栏触控靶 ≥40(ghost 按钮内联 padding:0,样式表要带 `!important` 才压得住,记档)/ news 头版徽标统一+页签与榜单自链退役 / 缺图退 federal(NB·NL·PE 无地标图待补)/ 匹配档挂口径 title / 城市胶囊 nowrap / 「·」「/」杂糅三语清完 / 头条不再在时间线重复;#211 LCP 只记账。**体检复跑 143→91,死链·裸档·斜杠杂糅·手型空态全归零,4xx 与 console error 各 0。**
> - **记账两条**:①首轮把 #203 写成「假手型」不准——那 12 处手型来自省卡整卡是链接(真能点),真问题只有裸「—」不说人话,R9 下轮要排除「祖先是链接」;②榜单页头与当前榜同名保留(是「你在哪一榜」的定位,不是废话)。
> - **下一步**:新暴露 #212 站点级触控靶(页脚·页签·下拉·轮播点,首轮被输出上限挡住)/#213 rank-daily 序号与职业名重叠 /#214 手机卡 TEER 裸术语——留下一轮。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡⚡ 上轮交接(2026-07-26 凌晨,「E6-09 数据层:MB 在需清单 + NB 不受理清单」)——455ea1f 全生产闭环**
> - **✅ MB 在需清单接入**(旧「MB 无清单」假设已由 E6-09 核查推翻):`build_mb.py` 实抓 MPNP 现行总表 **`/mpnp/idol/`** —— 立项写的 `/work/in-demand-occupations/` 已被站方 301 到 2023 年一篇更新通告(只增补几条),**差点把 2023 年的增量当现行总表**;真页按 NOC 大类分 9 节=在需总表 156 条,另一节 Rural=仅首都区外算在需 2 条(分两桶不合并,否则温尼伯岗误显在需)。页面**没有**逐职业 stream 限定列 → 不造 stream 维度。生产实测:MB 命中清单 624 岗、可提名 +83(TEER5 material handler 凭清单可走)。
> - **✅ NB 不受理清单实抓 + 方案修正**(立项时只读了一条通告就定方案,实施时把整页读全才发现):**放弃「三行业→broad 大类」逐岗判定** —— 官方只给行业名不给 NOC,映射到本站 broad 必然硬猜(NOC 大类 4「教育」里教师律师警察社工同锅、护理员 44101 落「教育」不落「医疗」、73 含货车司机),且该限制只针对 **NB Experience 一条 pathway**,拿它否掉全省非三行业岗=过度断言。改抓官方**逐条给了 NOC 码**的硬限制:`nb-ineligible`(14 个,不论行业)+ `nb-ineligible-food`(13 个,住宿餐饮 NAICS 72;官方留口子「雇主不属该行业的同款岗仍可提交」→ 本站无雇主行业字段,按多数情形判不符合,条件写进 label/note)。行业限制事实照旧上站,走 NB 新闻卡。**生产实测 NB 可提名 1011→635**,餐馆厨师/零售/食品加工不再一律绿。
> - **✅ 08_score 新增第三种语义 `overlay`**:`type=ineligible + overlay=true` = **叠加式排除**——不动该省默认 TEER 规则,只是命中即不可。(套 AAIP 那种「本省无 TEER 门槛、除清单外全可」会把 NB 的 TEER4-5 岗全放开,反向错。)
> - **其余**:SK 三表加 `note`(需雇主 offer+EPA、2026 三行业配额封顶;**`type` 保持 indemand** —— 它是 08 的 inclusion/exclusion 语义开关,不是描述字段);两脚本进 `etl/sources/pnp/`(仓库 bind-mount 进容器,**无需重建镜像**);4 个 label × 三语;**前端修:一省有多张排除表时只铺命中本岗那张**(NB 现有两张,原逻辑会铺一张不相干清单 + 兜底还随便挑一条,同 #125③)。
> - **生产复验**:哨兵确认线上==455ea1f → upload_mart 18 表 + 增量 seed(pnp_occupations 428 行、closed 仅 6)→ 表格实见 MB HyLife「MB 在需职业」/「MB in-demand」/「MB 수요 직종」三语、NB Boston Pizza 厨师 PNP 列由「可提名」变「—」。
> - **✅ 追加两件(Frank 当场两拍「恢复可点」「AIP 那个也一起补」;aad5d23 + a482b32,生产复验绿)**:
>   ①**命中官方具名清单的「走不了」恢复可点** —— 批A 摘可点的语境是「点开只见走不了没意义」,而命中官方具名清单是**有名有据的结论**:桌面 PNP 格由裸「—」改显「不符合清单」、AIP 格显「职业不受理」(红、可点),手机卡同款红 chip(**复验时抓到桌面改了手机卡还在显「指定雇主」**——纯手机流量,同口径必须同步),通道弹框补 fail 行并点名清单、「清单▾」可展开看依据,PNP 弹框判定行由「在排除清单」改成点名哪张。**泛判定(TEER 不够、雇主不在名单)的「—」维持不可点,口径没变。**
>   ②**NB AIP 不受理清单接入** —— 同页官方另有一条通告列了 AIP **背书申请**不受理的职业(6 个不论行业 + 10 个住宿餐饮业);AIP 行改判「雇主在指定名单,但本岗职业本省不受理」。实测 NB 命中 **166 岗、其中 26 岗雇主是指定雇主**(Boston Pizza 餐馆厨师那类误导正是这批)。
>   ③**分路隔离**:`pnp_occupations` 加 `program` 列(PNP/AIP),AIP 表不进 08_score 资格判定、不进匹配 dims(`lib/jobsSql.pnpOnly` + page/api 两处),否则会在错的项目上判「命中/被排除」;生产 DDL 先行代跑(`docs/sql/e6-09-pnp-program.sql`,schema 先行再 push)。实测 NB 省提名可提名数 635 不变=零串味。
> - **✅ 同晚四拍:页面时间改「最近核对」(fbcec98)**——Frank「前端数据时间怎么没更新」。排查结论:**抓取没停,是午夜后真没数据可抓** —— 抓取器每轮拉「当天日期」的 JB 列表,00:30 那轮当天已是 07-26、官方还没发任何 07-26 的岗,实测 0 行(8 秒跑完;对比 23:26 那轮 7075 行/6702 更新)。所以旧口径 max(last_seen) 冻在 23:26 是真话,但凌晨来看站的人读起来像站死了。改口径:**新增 `etl_heartbeat` 单行表**(docs/sql,生产 DDL 先行),seed 每轮成功写 now();`jobsSql.checkedAt()` 读心跳、回退 max(last_seen)、30s 缓存;文案三语 更新→核对/Checked/확인。**顺手修一个真 bug**:SSR 首屏原来取「前 50 行的最大 last_seen」当全站新鲜度,比库里还旧且与 /api/jobs 口径不一致,两处现在同一个函数。线上实见「核对 2026-07-26 01:32」。**记档:凌晨 0-7 点 JB 不发新岗=0 行属正常,别当抓取挂了查。**
> - **✅ 同晚三拍:手机卡匹配裸字胶囊 +「移民通道」钮双双下架(77fc172)**——Frank「高 低 还有 移民通道是不是没必要显示,可以把对应的内容放到其他字段吧」:①手机卡「高/中/低」胶囊删(卡上没列头,孤零零一个「高」说不清是什么的高低,同 #132 禁裸 X/5 那条理);②桌面操作列 + 手机卡的「移民通道」钮删(操作列只剩收藏)——它开的三块处处重复:通道档=PNP/EE/AIP 三列已逐条直判、薪资质量=vs 中位列、雇佣质量=雇佣列;③**随之退役**:E12-08 三维档拆解弹框、批A #134 三行直判汇总卡(ChannelVerdicts=三列的汇总)、`/api/scoredetail`(档位数据照常入库,只是不再展示,仍供排序/筛选;免费池少一个消费端,数字不变)——**想拿回来 revert 77fc172 即可**;④「对我意味着什么」不丢,改挂「匹配」字段(点匹配格即开)。本地 `npm run build` 通过后才 push(**坑记档:删了 route 后 `.next/dev/types` 的陈旧 validator 会让 build 假失败,清掉再跑**)。
>   生产实见:手机卡「不符合清单 / 职业不受理」两枚红 chip,点开分别是「走不了:本岗在「NB 餐饮住宿不符合」内 + 命中清单 + 本省最新公告」与「雇主在指定名单,但本岗职业本省不受理 + 本岗职业「Cooks」在此清单内 NOC 63200」。
>
> **⚡⚡⚡⚡⚡⚡⚡⚡ 上轮交接(2026-07-25 session,「stats 收口与 BC/ON 清单接入日」)——图卡两件 + BC 五清单 + ON 空排除表,全生产闭环**
> - **✅ stats 图卡两件(Frank 两拍;代码由并行 session 以 0df4bcb 先行入账)**:①统计图表点条一律不跳职位板——toJobs 深链与 L1 无中类兜底 window.open 全摘,末级(L3、中位类 L2)不挂回调无手型,chart.jobsHint 三语键退役;②省卡零折行——标题/难度徽章/指标行全 nowrap+省略号,网格 min 230→270,NL 卡用短名 Newfoundland(悬停全名),EN 紧档文案 Highly competitive→Competitive(BC 卡放不下的元凶)。Playwright 三视口×中英 6 组合实测 0 折行 0 截断。
> - **✅ BC 2026 清单接入(14432a9)**:BC 新政(Care/Build/Innovate)在 WelcomeBC「About the BC PNP」单页恢复职业清单 → 新写 etl/pnp/build_bc.py 按节标题五桶(医疗 46=定向邀请∪卫生局通道、幼教 1、法语教师 2、兽医 2、建筑技工 9),pnp 源登记周更+抓失败保留旧表护栏;前端通道标签补中英韩。灌库后 **BC 省提名清单岗 —→999**(命中医疗/幼教/建筑技工/法语教师;兽医 0 在招属正常)。
> - **✅ ON 空排除表(同 commit,Frank 拍板「改」)**:OINP 6-26 改制(O.Reg 47/26 废旧 9 流+204/26 立 Workforce Priority 流)后官方**不设职业清单**(7-20 官方细则页复核)——TEER0-3/TEER4-5 两路径覆盖全职业,凭全职永久 offer+工资/雇主条件。落地=人工维护政策事实表 raw/pnp/on-workforce-priority.json(type=ineligible、occupations=[],照 pnp_allocations 人工表先例)+08 加载守卫放行空 exclusion(空 inclusion 仍视坏抓取跳过)。**ON 可提名 6,743→10,039(+3,296 全为 TEER4-5)**;AB 排除/SK 清单回归无副作用;CLAUDE.md 粗筛口径同步(排除式省 AB/ON)。**ON「省提名清单岗」保持 — 是制度性的(无清单可命中),不是缺数**;若 OINP 日后重出清单(传闻 CLB5 职业表),删该表按 git 史 build_on.py 模板重写。
> - **✅ 部署闭环**:push cee0495 → 部署哨兵(etl/check_deploy.py 轮询)2 分钟确认线上==origin/main → 生产实测 /stats/bc/health EN「Streams hit: BC Health」+ ON TEER5 岗 pnpEligible=true(dishwasher 等)。数据侧(seed 直连生产)先于代码部署已生效。
> - **📖 口径答疑入账(Frank 连问)**:省提名清单岗=岗位 NOC 命中省具名清单的岗数(**现覆盖 AB/NS/SK/BC 四省**,其余省无清单显 —);工签/学签=IRCC 2024 年末存量、PNP 登陆(年)=2025 完整年 PNP 类 PR 登陆人数、岗量与清单=日更;难度档=竞争比(学签+工签存量÷当年 PNP 配额)<20 机会较多/20-50 一般/>50 竞争激烈,配额同比砍≥30% 压竞争激烈,QC 不评(自有体系)。统计钻取到 NOC 小类即最细粒度,再往下是具体职位。
>
> **⚡⚡⚡⚡⚡⚡⚡ 上轮交接(2026-07-20 晚→07-21 凌晨 五辑 session,「内容站骨架 → JD 空白排障 → 手机优先与中文化」)——#120-#151 全生产:E8-07 六件(详情页/卡片流/搜索/抽屉/下拉/评论区)+#121 表卡双形态+#122 评分 1-5 档全链+#123 JD 懒抓三补+#124 统一免费池+#125 重复内容三修+#126 评分尾巴与口径收口(含 #123-#125 生产复验全绿)+#127 排序去旧分对齐 Job Bank**
> - **✅ #120 E8-07 内容站骨架批(Frank 看瓜站拍板「排版交互借鉴,配色不用」;七问全按推荐+三次追拍:分元素点击/侧滑抽屉/知名Tag退役)**:
>   **A `/jobs/[id]` 独立职位详情页**(单栏 860 骨架:面包屑→meta(公司名可点开公司弹框)→chips→事实块→JD五节⇄原文+顾问初判→PNP/EE 通道节→相关职位;closed 岗 Notice+noindex;组件复用=JobsTable **只加 export 不搬代码**零回归)+ **SEO 三件**:JobPosting JSON-LD/sitemap 分片 8×5000 仅 active(**Next16 坑:sitemap id 参数是 Promise<string>,不 await 直接 NaN 打库**)/og 动态分享图(每岗 1200×630 ImageResponse,站级兜底 og 同落=I 批 og 遗留收口);JD 弹框标题栏加「打开完整页 ↗」。
>   **B 手机卡片**(既有窄屏卡升级:职位名蓝链→详情页、公司名独立行→公司弹框、「·」杂糅清除)/**C 常驻搜索**(SiteHeader searchBar 槽:jobs 传受控输入即时筛选实测,二级页缺省 form 提交 /?q=)/**D 侧滑抽屉**(4/5 宽圆角块+资讯组二级展开)/**E hover 下拉**(NavDrop 统一 150ms 延时关+键盘通路;资料库改 hover+新增「资讯 ▾」=移民新闻+**政策时间线首获顶栏入口**;榜单/统计保持顶级 IA 未大动)。
>   **F 评论区亮回 v2**(COMMENTS_ON=true,Frank 拍板开+**日审归 Frank**):官方置顶楼(admin 帖+pinned 蓝底卡)+楼中楼一层封顶(hook 校验 parent=同文已过审顶楼)+「展开 N 条回复」折叠;登录+审核制不动(不学瓜站匿名直发);comments 加 parent_id/pinned 两列 **DDL 生产已代跑**(docs/sql/comments-thread.sql);匿名 POST 403 实测;**置顶楼真数据验证+每文官方解读=Frank 亲手项**。
> - **📄 E12-08 多维评分设计出稿(implementation/E12-移民路径引擎/08)**:三轨制已拍——职位分(自身三维:通道/薪资/雇佣)/公司分(担保记录/在库活跃/薪资水平/规模知名度)/地区档(=E12-07 复用);**跨层合成总分砍**(Frank「岗位总分没啥意义」)+**裸「知名」Tag 退役**(降级为规模知名度维度依据,公司名旁改公司分药丸);每分项 value/score/source 三元 jsonb 拆解弹框。**首版权重表已拟待 Frank 过目**(拍板③),批了才动 ETL。
> - **✅ #120 生产复验全绿(同晚)**:详情页 200+JSON-LD/robots 8 分片/评论区亮回线上确认;**#120b 补刀:jobs sitemap 分片加 force-dynamic**——构建期静态烘焙把空片烘死(Render 构建容器查库失败),修后线上分片 0 回 5000 URL。
> - **✅ #121 E8-08 表卡双形态当晚设计+实施双闭环(Frank 三拍:hover 要高亮/按逻辑拆别合并一个组件/三问按推荐)**:设计=全站页面形态总表(每页桌面/手机形态一次定死,新页照表)+通用卡片解剖+hover 规范(**可点必有态不可点必无**:a:hover 全局 brightness 加深 inline 样式压不住、按钮微暗、DataTable 不可点行 hover 摘除);实施=**DataTable 保持纯表格不动**,primitives 三积木(Card/CardKV/CardAction 纯样式零逻辑)+**每域一卡**(EmployerLmia/EmployerAip/RankCompany/RankJob/StatsCat 五卡+对比页/省对比两转置卡=dims/metrics 数组复用零双写),tcCards/tcTableWrap CSS 双渲染 ≤640;occupations 2 列窄表=例外保持表格;手机卡片流 v1 无排序控件。本地双端全绿;**转置卡 Pro 真值态待 Frank Pro 号**。
> - **📄 E12-08 评分设计终版全拍齐(同晚三连拍)**:①「权重怎么算都不合理」→ **全维度 1-5 档制**(不加权不合成,档位割点表在文档附表已批「按推荐」);②展示位=主表评分列改**「通道」列显 X/5**、公司名旁药丸显**「担保 X/5」**;③「都是先试用再付费」→ 档位全免费,**拆解弹框 5 次/日试用额度**(同 jobtext/顾问额度族)不设硬闸。设计零悬念,**下 session 首件开工**(ETL 档位三维+公司四维 jsonb → DDL/seed → 通道列/拆解弹框/药丸/知名 Tag 全站退役;一批一 session 惯例)。
> - **✅ #122 E12-08 多维评分当晚开工即闭环(「开工 E12-08」令)**:**etl/grades.py 档位单一来源**(职位三维 channel/salary/emp+公司四维 sponsor/active/salary/fame,全 1-5 不加权;09 组装处调用——薪资中位/雇佣/公司聚合只在 mart 层齐,08 的 0-100 分保留作同档内排序兜底不再展示);DDL 四列生产代跑+seed 白名单+COALESCE 过渡兜底+**增量 seed 已灌**(jobs 31,269/companies 21,649,分布健康:具名 5 档 545 岗、公司无记录 17,535 家不评);前端=评分列改**「通道 X/5」默认亮**(列偏好 v10)+**/api/scoredetail 试用额度端点**(免登 5/日 402、匿名 IP 限、明细不随行下发=服务端真闸)拆解弹框三维点阵+参照区+**担保药丸四处**(jobs 卡/详情页/名录表卡)+**裸「知名」Tag 全站清零**。尾巴:fScore 筛选标签仍旧 0-100 口径(内部一致)/公司弹框四维行 UI(companyDetail 已回传)——下批。
> - **✅ #123 JD 正文懒抓当晚闭环(Frank 报障央行 Senior Cloud Developer「没抓到」→ 实为聚合帖 JB 页无正文)**:根因=Jobs.gc.ca 等聚合转贴的 JB 详情页 description 是空 span(05b 结构性抓不到,正文在原站);修=**lib/jdLazyFetch**(lazy-first 铁律)——jobtext 读库为空时现场抓:JB 页自有正文(direct 帖)→ 空则抽 `#externalJobLink` 外链(JB 页自带,实测)→ 原站通用正文抽取(零依赖极简 readability:剥 script/nav/标签,≥300 字符才入库宁缺勿滥)→ **写回 jobs.description 永久缓存**;单飞+负缓存 10min+8s 超时+私网 host 拦截;**seed 的 description 改 COALESCE**(mart 空=05b 没抓到,不冲懒抓缓存)。实测:央行帖 11,494 字符落库,二次秒回。E12-08 生产复验同晚绿(scoredetail API 200 真数据)。
> - **✅ #123b-d 懒抓三连补(Frank 三报障当晚全修)**:b=懒抓正文首行标注原帖岗名(JB 把聚合帖标题标准化成职业名——McCain「Engineering Manager…」变「software developer」,显性化不掩盖;`<title>` 分段取最长非通用段);c=整理版 PAY 节帖面薪资兜底+「怎么投」恒附官方原帖真链+**jdformat 204 空串在 Next16 抛 TypeError 变 500 修**(整理拒收帖全在触发);d=**朋友 API prompt 硬上限 6000 字符**(实测 400「prompt too long」)——J2 原截 12000 对懒抓长帖必炸,改按预算截断,央行帖整理版实测生成落库。教训记档:friend API 上限 6000,任何新调用别超。
> - **✅ #124 统一免费额度池(Frank「统一一个不就完事了,整这么多用户不会嫌烦吗」)**:jobtext/advisor/scoredetail/resume 四端点收敛为**一个每日池**(lib/freeQuota.freeGate;FREE_DAILY_TRIES=20,匿名 IP 池 10 恒低于注册防倒挂 #5)——用户全站只看一个数,单价一律 1 次/调用(顾问已跑朋友模型,按成本分池的老理由不成立);旧四常量=同池别名(定价页数字自动一致);Pro 不限照旧(advisor 全局帽/Pro 日帽保留);resume 对 wizard 保持旧 429 契约。实测:匿名混调 jobtext×8+scoredetail×2 第 11 次跨端点 429=同池实锤。行为变化认账:顾问免费从 8→20(池共享)、resume Pro 不再单独限 5。
> - **✅ #125 重复内容三连修(Frank 三报障)**:①「怎么投」节整节文本直接渲成官方原帖链接(Click Here 废句自身=出口,附加按钮行删——与底部合规来源行重复);②**AI 整理与 AI 顾问不合并但砍重叠**(事实/结论分离红线:整理=零发挥可校验搬运,顾问=解读)——初判 title 模板去掉「这个职位做什么/需要哪些技能」两节(整理版 ROLE/REQS 已承担),改【移民视角怎么看这个岗】【怎么准备】三语,实测初判直切移民增量;③PNP 通道节命中具名通道 → 只展示命中清单(被排除→只展示排除清单;未命中才全铺)——对齐 EE 节「命中只展开命中类」先例,AB 电工帖实测 AAIP 不符合清单不再铺出。
> - **✅ #123-#125 生产复验全绿(2026-07-20 深夜二辑,队列①)**:jobtext 懒抓缓存 11,468 字符秒回+首行岗名标注/PAY 兜底「(帖面薪资字段;原帖正文未写)」线上实见/定价页统一 20 双处一致/「怎么投」整节链接无重复按钮行/初判新模板 zh·en 全新生成实测/PNP 命中态(AB 岗「本岗 NOC 21232 在「AB 科技」清单内」只展命中)。**唯一残留=央行帖 zh 初判在服务端内存里还躺着一条 #125 前旧模板缓存**(en 同岗全新生成已是新模板,证明代码在线上;残留条目 → #126 缓存键 v2 根治)。**复验方法论教训:直接 curl advisor 只传 id 不传 job 对象 → prompt 事实全空,模型说「缺乏公司名称」是对的——别误判成生产 bug(前端真实调用带整个 job row)。**
> - **✅ #126 评分尾巴与口径收口(同晚二辑,评分尾巴两件+懒抓两补+口径三修一 commit)**:①**拆解弹框参照区公司四维行**——scoredetail 的 companyDetail 一直回传只差 UI,现渲 担保(LMIA 技能类 X 份、合计 X 份、最近 XQ)/在库活跃(在库 X 岗、近 30 天新增 X)/薪资水平(vs 中位平均 ±%)/规模知名度(维基、覆盖 X 省、在库 X 岗)四行点阵,i18n 三语;央行帖本地实测 3/4/5/2 档全渲(注:「公司弹框」如 Frank 原意指公司名弹框则下轮挪,数据得走 scoredetail 同闸);②**fScore 谓词切通道档**(高=grade_channel 4-5/中=3/低=1-2,生产 API 实测分界干净;okScore 死代码删);③**懒抓头部残渣裁剪**——黑名单+短行启发式只裁「首个 ≥100 字符段落行之前」区域(字段值行「Term/Hybrid Model」靠前行冒号规则保留),央行原站页实测 21 行导航残渣(两轮菜单/语言切换/订阅控件)全剥、Requisition/Position/Location 事实行零误伤,裁没一半以上自动回退;④**同岗 jobtext 会话缓存**(事实块/JD 弹框/详情页三调用点共用 Map,重开零网络请求实测;402/空不缓存);⑤**advisor scoreFacts 旧 0-100 加权镜像退役**——拆解弹框 AI 解读实测吐「该职位总分为80」与上方 1-5 档 UI 打架,改喂档位语义+明令禁提 0-100(改后实测「综合等级为4/5」);⑥**advisor 缓存键 v2 前缀**(模板一改就 bump,陈旧条目永不再服务)。
> - **✅ #127 排序去旧分对齐 Job Bank(2026-07-20 深夜三辑,Frank「不要用之前的分数排序,和 Job Bank 保持一致」)**:旧 0-100 分从**所有排序兜底退役**——默认序保持发布时间最新在前(本就与 JB 一致),同日岗兜底从 score DESC 改 **id ASC=入库序**(增量抓取按 JB 页面顺序落库,同日内即 JB 自己的展示顺序;旧分兜底把高分岗浮到当天最上面,正是与 JB 不一致的来源);orderByClause tail/SSR 首屏/match 候选/相关职位五处同改;邮件提醒选「最好 20 条」改通道档主键;第三下取消排序回真默认发布时间(原代码回「评分降序」=陈年不一致顺手修);客户端死代码 sortVal 清除;CLAUDE.md 展示约定同步。生产库直查实测:同日岗 id 升序、score 61/71/55/75/29 完全乱序不再重排 ✓。**同晚账**:当日 jobbank 轮抓取正常(+176 新岗、4538 更新),17:07 seed 撞部署重启超时(盒子记「下轮补」)→ 已手动补灌;匿名池教训:**缓存命中也计额度(freeGate 在缓存检查前),复验脚本连打会烧光本 IP 匿名池**。
> - **✅ #128-#132 深夜三辑连环批(Frank 连环反馈当场闭环)**:**#128 移民数据源全提小时更**(pnp 周更→1h/ee 月更→1h/news 12h→1h/build 兜底 1h;ircc 例外日更=官方月度发布;容器重建日志确认,pnp 首轮 18 秒补齐滞后 3 天抽选);**#129 手机卡整卡可点进详情页**(卡内交互 stopPropagation 保留);**#130 详情页三修**(三「查看原文」收敛一处一职/懒抓首行 <title> 残留剥(与 <title> 全等才剥)/vs 中位锁位=打码占位数+「Pro 解锁」四字,长文案退役——**锁位打码=新先例,存量长文案锁位下轮统一扫**);**#131 点击分工复位**(职位名文字=开 JD 弹框(#120B 蓝链推翻),整卡才进详情页;担保药丸人话化+悬停解释);**#132 档名人话化**(Frank「X/5 用户能看懂吗」→ **全站禁裸 X/5 新铁律**:通道 命中省清单/紧缺技能岗/技能岗/低技能有通道/通道弱、担保 常年担保/近期有担保/有过担保/早年担保过、薪资/雇佣/活跃/知名同款,档名主显+数字灰小注;主表通道列/手机 chip/担保药丸四处/拆解弹框七行全换,三语)。**当晚运维账**:①盒子 17:07 seed 撞部署超时+我连发部署清了 Render /tmp 的 mart → 手动重传 18 表+补灌成功(jobs 31,563);②复验教训两条记档:curl advisor 不带 job 对象=事实全空别误判生产 bug;缓存命中也计额度,连打会烧光本 IP 匿名池;③「详情页跳回列表」新线索:预览浏览器扩展注入 html class(recslot)致 hydration mismatch,仅 dev 预览环境,生产未见。
> - **📄 E8-09 详情页体系设计出稿(Frank「一页一域/零重复/Pro 位重新设计」)**:三铁律(一页一域跨域只链接/一条信息只出现一次/Pro 位统一打码)+职位页 v2 排版(新增关键判断条三档名药丸/去重三件:年薪折算仅时薪帖显示、NOC chip 改锚点、城市只留 meta)+**公司详情页 v1 立项**(/companies/[slug] 六区:头部/担保记录/评分四维/简介/在招职位/对比入口;全免费;2.1 万家=SEO 可收录页翻倍;全站公司名点击收敛)+弹框=快看/详情页=完整的分工。**三开放问题待 Frank:①EE 未命中折叠(推翻 #125③ 全铺)②关键判断条要不要③公司页只列在招?** 获批即为下一实施批(与榜单改版「好雇主榜/好工作榜」可同批)。
> - **✅ #133-#206 深夜四辑「JD 空白排障日」(Frank 连环报障,三个真 bug 全修)**:**#133 档位纯文字化**(X/5 与点阵刻度全退役,advisor 同禁数字口径,缓存键 v3);**#134 限流谎报成「没数据」**(fetchJobText 只认 402,429 掉进 !res.ok 返空 → UI 渲「本站暂未收录」;**用户连点十几个后额度耗尽,之后每个岗都显示没数据**——正是 Frank 报的「连点几个都是空的」;改四态分明);**#135 EE 类别历次抽选时间线**(源 JSON 本就 428 轮全量,原只取最近一次;历史灌进**既有 pnp_draws** province=FED **零 DDL**,时间线页去重同步);**#136/#202 被拦原站空态说实话**(Indeed 实测 403 有意拦截,**不绕过反爬**;文案二改:只陈述「该站拒绝本站自动读取」,不替对方做版权断言);**#203 已回退**(用官方 NOC 通用职责填空白页=误导,踩「宁可留空也不瞎猜」红线,Frank 当场指出「雇主要求和官网 NOC 要求能一样吗」);**#204 长帖 AI 整理必失败**(根因不是长度——19/19 长帖模型侧全过;是 jobtext 懒抓与 jdformat **并发竞态**,后者读到 NULL 就 204,而懒抓帖恰是长帖;改走同一取文入口共用单飞);**#205 外链未解 HTML 实体**(`&amp;` 致带 query 外链全抓错页);**#206 懒抓漏读 JB description 字段**(Frank 甩链 49933160 实证:结构区没有但 `[property="description"]` 有雇主原文 2042 字符,外链是 Indeed 403 → 判「没有正文」,**而正文一直摆在政府页上**,ETL 解析器早在读、懒抓漏了;抽样救回约 12% 空岗)。
> - **⚠️ 记档:一次测量错误的教训**。我以「默认排序前 50 条有 30 条空(60%)」为由提议批量补抓 + 魁北克 SPA 爬虫;Frank 一句「懒加载加上不就完事了」点破——**那个 60% 量的是 DB 的 description 字段状态,不是用户所见**:用户点开时懒抓会现场补。实测首屏前 20 岗**用户点开 20/20 有正文**。批量补抓脚本与魁北克爬虫**均不必做**。Frank 原本的坏体验=#134+#206 两个 bug,已修。**两条方法论入档:①拿指标前先确认它量的是不是用户真实感受到的东西;②遇到抓不到,先把手上已有页面翻干净,再去怪对方拦截。**
> - **📄 魁北克探路结论(不立即做)**:开放数据只有蒙特利尔/拉瓦尔市政岗,无省级平台;站点是硬 SPA,**深链打不开**(连其自身分享按钮生成的 `?ide_poste=&ide_source=` 也弹回列表),详情走模态 → 抓一个岗要「载列表→搜编号→点击→读模态」,2,292 岗首轮 6-9h 且脆弱。真实缺口比 DB 数字小得多(见上条),**降级为待设计项**。另记:JB 页带雇主原始法语岗名(「Architecte Ingénieur·e naval·e」被 JB 标准化成 "architect, naval"),入库需新列,并入该设计。
> - **✅ #207-#151 深夜五辑「手机优先与内容中文化日」**:**#207/#208 详情页职业分类三级**(大/中/小类,每级可点按该级筛板;Frank 复拍「这个可以一行」→ 折成层级路径单行;口径厘清:禁杂糅针对不同类信息塞一行,分类三段是同一件事的三个粒度,`›` 串是路径不算杂糅;补 `?fine=` 深链参);**#209 面包屑摘大分类段**(与分类行同一信息两个家);**#210 废话删掉+LMIA/担保档去重**(Frank 截图两指:年薪注 47→24 字;担保档就是按 LMIA 份数算的,卡上并排=说两遍);**#211 省名中韩化**(13 省区,provName 只作显示——筛选值仍是英文全名,动了断深链;踩坑:makeT 缺键回退 zh,en 必须显式给);**#147/#149 NOC 职业名 487 条全翻**(本地 qwen3.6;**术语纠正入脚本**:Software developers→「开发商」错成厂商、trades→「贸易」错成商贸、一条残留英文,提示词+确定性替换表+存量纠正三管);**#148 手机卡片左右两列**(左身份右数字,右列连成竖线便于扫视比价;flex 而非 grid=长公司名不挤歪,46 字符实测仍对齐);**#150 详情页头部卡片化**;**#151 城市中韩名 49 条**。生产库终验:NOC 487/487、城市 48 个(多伦多/温哥华/高贵林/万锦),线上详情页实见「市 Toronto, Ontario(安大略省) 多伦多」。
> - **🆕 新铁律:手机优先**([[mobile-first-priority]] 已入记忆)——Frank 拍板「优先考虑手机卡片」(冷启动流量=小红书=纯手机);**自验顺序反过来:先 375px 再桌面**。换序当天即抓到面包屑/分类重复(#209)。
> - **⚠️ 第二次「宁可留空也不瞎猜」教训(#151)**:城市名首版让模型判断「有无通行译名」,94 个里 93 个都给了中文(不可能)——Rivière-du-Loup→「洛普河」错成河名、Port Coquitlam→「波特科奎特兰」而社区通行「高贵林」、North Battleford 纯音译。模型基本不说 NONE=硬编,与 #203 同类错 → 停跑改**人工核定表**(49 条,只收社区确实通行的移民聚居城市),加城市=表里加一行不跑模型。**方法论:让模型做「有/无」判断不可靠,有限集就人工定表。** 另修韩译串日文假名的校验(Montréal→「모ント리올」)。
> - **✅ #152-#159(07-21 白天续辑)**:锁位统一打码(表格 Pro 列由锁图标改「打码占位数+锁」、薪资弹框长文案退役)/**#153 OINP 通告过期修**(ETL 里 ON 整块是 6-26 手写常量 + 前端 i18n 也写死整句 → 新增 parse_on 实抓 ontario.ca,通告自动跟到 7-20 官方原文,顺带捞回 10 轮历史邀请)/年薪换算注不再常驻改悬停/**#155 三修**(职位描述首节小标题去重、PII 占位符从中文改中性英文 `[see original posting]`(存量 SQL 替换)、**EE 未命中折叠**=E8-09 开放问题①拍板)/**#156 站点地图索引**(GSC 实测 Discovered pages 仅 124 → 根因是 GSC 只认手填的那一个 sitemap,新增 /sitemap-index.xml 一次覆盖 9 个)/#157 面包屑与职业分类合并成一条路径 + 药丸一枚一事/**#158 三处重排**(三级同名去重「商务›商务›商务」、「对我意味着什么」手机版按卡片语言重排、公司 AI 简介按 JD 结构分节 [WHAT]/[BASE]/[SIZE])/**#159 榜首一整天不刷新修**(#127 用 id ASC 兜底 + date_posted 无时间 → 当天最早那批钉死榜首、当天新岗沉底;改 first_seen DESC,仍纯时间序不掺分数)。
> - **🆕 Frank 亲手已完成:GSC 已验证并提交 sitemap**(现状 8 收录/117 未收录;**待办:再提交 `sitemap-index.xml`**,#156 部署后生效,预期 Discovered pages 从 124 涨到 3 万+)。**「Google 搜 offer to pr 搜不到」= 新站正常**,收录要周级,不是 bug。
> - **📋 下轮待做(Frank 截图指出,均未动)**:①**公司详情页 /companies/[slug]**(Frank 明确要,方案见 E8-09;六区块+2.1 万页进 sitemap);②「怎么投」节时而渲英文原句时而渲「查看官方原帖」= 不一致,应固定动作出口+原文另置;③匹配额度升级卡(截图 Quebec 那张)应改**打码整块内容+锁**而非文字横幅(与 #152 同款);④「对我意味着什么」桌面版表格 + 省/地图/最近抽选整片**重新设计成胶囊**(手机版已在 #158 重排,桌面未动);⑤同弹框里「所在省」行与下方「省 Ontario」重复。
> - 下轮:①**07-22 观察窗复盘(队列头件)**——umami 漏斗(现状:32 访问/陌生注册≈0,瓶颈在流量;**GSC 验证提交=Frank 亲手最高优先**,31k 岗 JobPosting JSON-LD 已就绪只差提交);②E8-09 详情页体系(关键判断条/公司详情页)+榜单改版获批开工;③存量长文案锁位打码统一扫。**运维注记:每次 Render 部署会清掉 /tmp/mart,若撞上盒子 upload+seed 窗口该轮必失败(今晚撞 4 次,均手动 `python etl/upload_mart.py` + GET /seed 补);改小时更后自愈窗口=1h,密集发版日记得手动补。另:改了 seed 白名单要先部署再灌,否则新列静默不写(#151 踩过)。** 观察项:advisor 偶发把 Term 岗说成「永久全职」(qwen 越权,普遍性待盯)。
>

> **⚡⚡⚡⚡⚡⚡ 上轮交接(2026-07-20 全天 session,「组件收官与难度指数日」)——#107-#116 十批+两设计文档当日全生产**
> - **✅ #107 顾问公司初判接通 K 联网调查(2026-07-20,Frank 报障截图「资料不足」)**:根因=两条链路各干各的——K(companyinfo)会 web_search 且已有缓存,顾问初判只认 companies 富化列+web_fetch(friend 后端忽略 web_fetch,#105 切换后联网归零)。修=抽 lib/companyResearch 共享层(companyRow+investigateCompany,并发合流),顾问 field=company 先读 ai_brief 缓存、无富化数据才现场调查,注入 prompt 当次级事实(官网抓取仍第一权威);查不到照旧「资料不足」反编兜底。本地实测:L. Okafor 吃到缓存四段落地(Yorkton 诊所),Porcupine Mechanical 现场查 3.3s 落缓存。
> - **✅ #114 设计总表 E-I 批+组件 P3 闭环(2026-07-20)**:**F 提醒收口**=primitives 新增 Notice(四色四用禁新配色:warn 升级/额度、err 错误、info 口径、ok 成功;圆角10+图标+lead+右钮槽),换装 10 处散装框(UpgradeCard/顾问 429×2/薪资 Pro 提示/AuthForm 错误+找回已发/UpgradeModal 错误/账户支付成功/档案保存成功失败);**H 阴影收口**=账户卡摘 boxShadow 改平面描边 #e5e7eb r12(全站页面内卡片唯一违规处;下拉/冻结列阴影合法保留);**P3 chips 归并**=wizard/ProfileForm/news 本地 chip 定义退役统一 chipStyle 药丸(B映射「wizard 选项归 Chip」)+compare 省选钮换 Chip+employers 搜索钮换 Button;**E 弹框/G 表格已成体系零改动;I 剩 og 分享图 1200×630 待补(素材活,记档)**。本地冒烟:错误 Notice ✕ 红框/保存 ✓ 绿框/chips 药丸双态/账户卡无阴影/搜索提交跳 ?q= 命中,行为零回归。**教训记档:python 批改脚本 print 中文必带 PYTHONIOENCODING=utf-8,否则 cp1252 半途炸断(本轮 ProfileForm 改一半 import 悬空,当场补上)**。
> - **✅ #113 组件统一 P2 表单/账户批闭环(2026-07-20,按 mockups/按钮统一-设计-C实施 P2 范围)**:AuthForm 提交钮(渐变大钮→Button primary lg 通栏,form 内默认 submit 行为不变,禁用态=内置浅蓝替代 opacity)/wizard 上一步 ghost 灰+下一步 primary/账户昵称保存 Button sm+退出登录 ghost 灰/ProfileForm 保存 primary 通栏/定价免费注册钮 secondary;**购买双钮(30/90)按 A规格拍板保持现状不并入;chips 归并留 P3**。本地冒烟(新注册测试号 p2test-0720@test.local 全链实测):错误凭据→红框/注册即登录/wizard 来回/保存→已保存/退出→登录态清空/免费注册→开注册框,行为零回归。
> - **✅ #112 JD 弹框「AI 顾问」钮摘除(2026-07-20 Frank 反馈:职位顾问该像公司顾问一样自动分析,不要按钮;切换过去要能切回来)**:根因=#96 回补的标题栏钮点了会**关 JD 框跳顾问弹框**,描述/整理版一去不回,而初判本来就内嵌自动生成(#102)、按钮纯多余还造成单向跳走。修=钮+onAdvisor 管线整体摘除;整理版⇄原文双向切换实测(看原文 ▾ ⇄ 看整理版 ▴ 来回自由);深挖(字段解读/对比表)走字段格入口照旧。
> - **✅ #110 DataTable 余页换装闭环(2026-07-20)**:全站原生 `<table>` 清零——occupations 通道表(DataTable 新增 header 槽装通道标题行)/rankings 两变体(公司榜+职位榜,列全可排序;LMIA「数·季度」与「PNP · EE」杂糅拆一行一条=W 规矩存量清理)/stats 省页大类表(minWidth 槽治窄屏挤压)+Pro 对比表(转置=指标行×省列,列随选省动态生成);compare 免费态模糊示例表=视觉道具不动;RankingTable 弹窗消费方已退役、页面版唯一入口核实过。本地五页断言全绿(排序点击 ▼ 实测/杂糅 0/minWidth 560 传导)。timeline/账户无原生表,队列里写的那两页不存在此债。
> - **✅ #111 enrich 批量爬虫正式退役(2026-07-20,连着 #109 的懒化收尾)**:docker compose enrich 服务摘除(开关式注释,亮回=取消注释+up -d)+ 本机在跑容器 pnp-enrich-1 已停删;enrich_companies.py/_enrich_company_facts.py 头部退役注记(老数据 company_enrich.json 当静态缓存,09 照旧合并;行业段保留手动)。公司富化从此单一入口=K 懒探索(AI 调查+Wikidata 并行,谁被点开谁被查)。
> - **✅ #109 Wikidata 批量退役→懒查回填(2026-07-20,Frank 训示「不要提前跑,懒家仔跑不过来」+「灌进去吧」)**:批量重查停跑(终账 1668 候选/知名 37/失败 944——网络抽风每轮半小时跑不完,与「公司详情全懒」拍板矛盾,存量队列项执行前该先过懒化透镜=教训);已查到的存量灌生产(industry 20,964 家/知名 43/中文别名 28);**Wikidata 查询移植进 lib/companyResearch(TS 版严格匹配同门槛)**:K 懒探索 investigate 时并行查一次,命中 COALESCE 回填 alias/wiki 三列,一家公司一生一次不重试——名录别名/知名徽标随真实访问慢慢变富。本地实测 Famous Dave's 首开回填 wiki_url ✓。enrich 批量脚本(_enrich_company_facts.py Wikidata 段+enrich_companies 爬虫)正式待退役。
> - **✅ #108 文字链 sweep 闭环(2026-07-20)**:全站文字链二档定调=正文/动作链统一 #2563eb 蓝、来源引注小字统一 #6b7280 灰,一律 textDecoration none;归一杂色 6 处(pathways 靛 #6366f1×3→灰/顶部横幅 #1e40af→蓝/首页 news.more #9ca3af→蓝/定价 Pro 去账户 #92400e→蓝)+补显式 none 11 处(footer×4/定价×2/stats 口径/legal mailto/JobsTable×3,含 1386 唯一显式下划线拆除)。箭头分工核对:↗ 全为外链、→ 全为站内,零错配;**教训记档:news.official/tl.src/dir.occ.src 三个 i18n 键自带 ↗,JSX 别再拼(当场撞出「↗ ↗」回滚)**。:visited 紫=已排查不存在(styles.css 全局 a{currentColor} 压掉 UA 紫;dangerouslySetInnerHTML 仅 script、邮件模板全带 inline color)。本地 8 页 JS 断言全绿(/、pathways、timeline、news 详情、employers、occupations、rankings、stats:零下划线/零杂色/外链全 ↗)。
> - **✅ #115 E12-07 P1 全环闭合(2026-07-20,Frank 拍板「stats 卡先行/人话档名/配额我抽查」当日实施)**:①ETL 新域 `etl/scrape_ircc_stats.py`——IRCC 开放数据 XLSX 三包(学签/TFWP/IMP 年末存量 2024×10 省+PNP 登陆数 2025)直抓解析;`raw/ircc/pnp_allocations.json` 配额人工核对维护表(9 省带出处+口径 note,**Frank 抽查制**,NS 口径混杂已标);②`clean/04e_difficulty.py` 四因子(竞争比/配额趋势/抽选活跃/分数线水位=自身 24 月分位)+三档合成(竞争比主导,配额腰斩压档;**实施调整:竞争比单因子即给档**,原「<2 不给」会废 5 省——文档已回填);③11_build_stats 挂 difficulty jsonb 到 broad=all 行,DDL 已代跑生产+seed 白名单+Stats collection json 字段+loadStats(42703 容错分支不带列=自动降级);④stats 省页难度卡(H 基础卡:人话档 Tag 机会较多/一般/竞争激烈+因子一行一条+↗出处+粗口径注)三语。**首跑结果:BC 84:1 紧/ON 77:1 紧/AB 34 中/NS 22 中/其余 9-13 宽**——与小红书爆帖排名方向一致但带官方出处(冷启动素材+1)。本地实测 ON 卡 2 因子/BC 卡 4 因子全渲。**#116 补刀:IRCC 已挂独立 docker 月更役**(Frank「单独定时做不要临时抓」——etl/sources/ircc 源包自动发现+compose ircc 役,本机容器首轮全绿;配额维护表仍人工核对制不在役内);SK 2025 配额未核到。
> - **📄 E12-07 省难度指数设计文档出稿(2026-07-20,Frank 提问「学签/工签/PNP 数量能不能算难度」→「先出设计文档再考虑怎么做」)**:难度=竞争比(学签+工签÷PNP 配额,IRCC 开放数据**存在性已实测**)+配额趋势+抽选活跃+分数线水位(只跟自身历史比,分制不可比红线),三档分档+逐因子出处,禁概率预测;落点 stats 省页卡→timeline→E12 信号区。**三个开放问题待 Frank**:落点顺序/配额人工核对分工/档位命名。文档=implementation/E12-移民路径引擎/07;未获批不动代码。
> - **✅ #117 D3 多雇主对比上线(2026-07-20,Frank「三问按推荐,开工」当日闭环)**:/employers/compare(sitemap 不收录)——服务端聚合 lib/employerCompare(companies LMIA/行业/知名/K 简介+jobs 聚合封顶 400+E12-07 难度档+lib/match 高/中计数),Pro gate 在 SSR 层免费真值不出服务端;免费态=⑤先例(三价值点+模糊示例+升级钮实测开定价);入口=名录行「+ 对比」药丸(免费可点 FOMO)+浮条(localStorage 选择篮 ≤4)+对比页互跳;price.f10 三语进 Pro 卡。**教训:client 组件禁 import 带 payload 的服务端模块(chunk 生成炸)——常量/类型拆 employerCompareShared**。本地实测:选 2 家→浮条→深链→匿名 demo 全绿;SQL 三连对生产库验过;**Pro 真值态待 Frank Pro 号实测;公司弹框第三入口=小尾巴下轮补**。
> - **📄 D3 多雇主对比设计文档出稿(2026-07-20,implementation/E5-商业化功能/06)**:Pro 结论层,2-4 家并排,零新抓取(LMIA 技能股/AIP/在库岗聚合/难度档/K 简介/与我的匹配),摆事实高亮差异不下结论(E6-02 语义红线);免费=⑤先例模糊示例+服务端 gate。**三开放问题待 Frank**:入口免费可见否/匹配行粒度/价格页 f 列表文案。
> - **🆕 立项待做:管理员运维台(#119,2026-07-20 Frank:「给我按钮手点触发更新+实时看进度,要管理员模块」)**:架构草案=共享 Supabase 当信令通道——新表 etl_control(role/requested_at/status/progress 尾行);ETL 盒 auto_update 睡眠期 30s 轮询触发位(点按钮=提前开轮)+每步写心跳进度;cms 侧 /account 管理分区或独立 /ops 页(admin 角色 gate):每役一行=状态灯/上轮完成时间/「立即更新」钮/展开实时进度(3s 轮询)。**Frank 复拍(同日):管理台不赚钱先放——落地为 etl/run_now.py 手动脚本**(复用 sources/<役>/META 单一来源,默认全链或指定役,实时打印,与 docker 轮并行安全;ircc 役实测过)。网页版运维台=远期候选。
> - **下轮队列**:**07-22 观察窗复盘**(umami 漏斗访客→注册→建档→触闸,数据重排队列)→ D3 获批后开工;Frank 亲手项:配额表抽查(raw/ircc/pnp_allocations.json)/真实简历实测/发帖渠道。零真实注册=现阶段第一瓶颈在流量不在功能(用户表全测试/自有号)。
>
> **⚡⚡⚡⚡⚡ 上轮交接(2026-07-19 日间→晚→深夜,「收口与漏斗日」)——#87-#106 与十一连批全生产闭环**
> - **反馈快刀四件**:#87 操作列瘦身(公司信息钮=公司格同弹框纯重复删;职位描述并进职位格点击,桌面+手机双端,MIN_W 238→92)/#92 清除筛选同时摘 URL 深链参(?q/prov/broad/mid——否则刷新回潮)/#94 头条轮播图定高 300(根因 flex:1 弹性吃文字行数差;文字区 minHeight:176 吸收,5 张实测图恒 300 卡恒 478)/E11-03 遗留 oauth=fail 三语可见提示(读参→err 红框→replaceState 摘参)。
> - **#88 统计三级下钻**:省→大类→NOC 中类→职位板(Frank「统计=选行业选地区的概率指导」);stats 表加 mid 列(Frank 授权代跑 DDL)+ETL 119→530 行+loadStats 缺列 42703 容错+L1 无中类优雅降级;**#88b 末级/降级一律新标签页开职位板**(Frank「点击钻取不要跳转」)。**时序教训记档**:ETL 盒挂载工作区=脚本改动即时生效,旧 seed 会把中类行当大类行插(全国求和翻倍)——发射闸默认关、DDL+新 seed 齐了再翻,是必须的部署保护;测试期一版 530 行 stats.json 被盒轮搬进生产短时污染,自愈。
> - **付费分层立案+D1-D3 当日拍板落地**([免费付费分层设计-20260719](docs/免费付费分层设计-20260719.md):三原则=事实层免费结论层收费/初体验不设闸/广度免费深度收费;现状盘点=大盘已合拍):**D1 保存筛选降免费**(登录即存,免费 2/Pro 5,触上限才弹升级框;**APIError 教训**:collection hook 裸 throw Error 被 Payload 打码成 Something went wrong,前端分支要靠 `throw new APIError(msg,400)` 透传)/D2 排除不担保筛=免费零改动/D3 多雇主对比=Pro 排 E11-07 后。
> - **E11-07 简历上传解析全环闭合(付费漏斗顶)**:wizard 第 1 步上传块(可跳过/失败回退手填)→ /api/resume(pdf-parse v2/mammoth 内存解析,原件不落盘)→ llm.ts 新增 completeText 双后端(线上 Anthropic Haiku/本地 Ollama)→ NOC 候选=**在库职位 title pg_trgm 匹配**(真实岗位用语贴简历,比官方类名准)+IELTS→CLB 官方对照;免费 5 次/日。本地+生产双终验:合成双职业简历 21232/31301 精准命中+CLB 9;边界(坏 PDF/未登录/短文本)全优雅回退。**真实简历实测待 Frank**。
> - **#93 关键路径功能走查(Frank 选 A:先走查再转化)**:生产 24 项全过**零缺陷**——主链(注册→建档+简历→匹配→收藏→保存筛选→JD→顾问→升级→Stripe 结账页)+手机三项+EN/한 抽核+9 页冒烟;两处假阴性复核洗清(保存筛选=脚本 dialog 竞态/手机星标=点中已收藏岗的正确取消)。
> - **#95 不完美功能关闭批(Frank「不完美的也先关闭」,全开关式亮回条件在代码注释)**:①邮件承诺文案三语降级+周报开关暂藏(**Resend 测试模式发不出外部邮箱,承诺兑现不了**;亮回=域名邮箱+首封真发)②「学校名录(规划中)」灰项摘(B4 二期再挂)③news 评论区暂藏(COMMENTS_ON=false,后端原样)④한 钮暂藏(机翻无人校对;词典留,已选 ko 用户可切走)。保留:简历上传/EE 节奏卡。
> - **冷启动弹药**:小红书封面 4 版出图(docs/assets/xhs/:A 数字海报/B 痛点/C 移民信号/D 引用体故事钩,**推荐 D 当封面 A 当第 2 张**,发布后编辑换图不掉流量);记档 #89 雇主榜已有待互链/#90 媒体新闻源候选(版权红线:只能标题外链)/#91 顾问本地模型(触发条件=月账单>$20,分层路由:匿名走本地/含档案留 Anthropic——隐私红线)。
> - **✅ J·K·M 批当日闭环(2026-07-19 晚,Frank「都可以,先做 J K M」;本地 8 断言全绿,DDL 已跑生产)**:**J2/J3 JD 五节整理版懒生成**(点到哪个整理哪个存 jobs.jd_formatted 永久复用,Frank 拍板不批量;模型走朋友公网 API 同 news 懒翻译 env;校验=五节齐+输出多位数字必须在原文,过不了静默回原文;整理版默认+看原文切换;顺带补缺失工时/雇佣期)/**K 公司懒探索**(公司弹框首开 AI 联网调查 web_search 带出处,存 companies.ai_* 四列;companies 缺行懒建 source=ai-lazy)/**M 对我意味着什么→左右对比表**(维度×本岗×你×判定,依据链同源 reasons 1:1 映射;代码不裸奔=NOC 带职业名/省全名/TEER 带说明;窄屏堆叠)/**J1 多列**(事实块工时/雇佣期拆行+未标注灰字+证书一行一条;表格加工时/雇佣期两列默认藏,SORT_COLUMNS 已同步防 #73 三演)。**当日新立三条全站硬规矩(记忆已档)**:禁「·」「/」杂糅多信息一行一条(W 节)/代码不裸奔人话名主文案/一格一事灰注只放代码单位说明。**设计四总表**:价值时刻⓪-⑤(⓪=升级钮统一 UpgradeCta)/按钮统一 A-C 三批换装/雇主 D+K+M+组件 E-I/JD J1-J3,全在 mockups/,除 J K M 外待实施。同日 fix(#96) JD 框回补 AI 顾问钮+useFloatPanel 可拖动(生产复验过)/fix(#97) JD 相邻重复行去重(ZipRecruiter 349 帖)。
> - **✅ 价值时刻⓪-⑤+按钮 P1 同 commit 闭环(2026-07-19 晚,J·K·M 生产 7 断言全绿后接续;本地 8 断言全绿)**:⓪ UpgradeCta 统一升级钮(primitives Button 扩 pro/ai/lg/style 透传;已登录开升级框,未登录开注册框)/①匹配锁 hover+弹框带 FOMO 全量计数(缺失回退限额文案;免费匹配视图服务端只回 cap 内行,锁位罕见=已知)/②薪资弹框免费态补「Pro 显示与官方中位差距」琥珀行+实心钮/③锁列 hover 按列说人话(up.lockTip 拆三键)/④顾问 402 文案改「明天自动恢复+Pro 不限次」(jobtext 同式)/⑤compare 空态=三行价值点+模糊示例表+「升级 Pro 看真实对比」;**P1 换装**:顶栏登录 ghost+注册 primary(SiteHeader+AccountArea 双处)/更多筛选+清除+字段钮 secondary h38/载入更多 secondary/AI 顾问钮收编 Button ai/账户下拉升级钮收编 pro。文案自查过 W 规矩(「高/低」斜线杂糅当场改掉)。
> - **✅ 雇主 D 当日开工落地(2026-07-19 深夜)**:行业=在库岗 NOC 大类多数派(mart companySlug 归组,20,949 家零新抓取;首版踩坑:mart jobs 无 company 名只有 companySlug,经 companies.json 联结)/中韩别名=Wikidata 跨语言官方标签(严格名称匹配宁缺勿滥,守「不机翻」拍板)/知名徽标=有英文 Wikipedia 条目(点开可核验;别名与知名同门槛)。管线=etl/clean/_enrich_company_facts.py(幂等,wiki 查询缓存)→ processed/company_facts.json → apply 脚本直写 companies 四列(industry/alias_zh/alias_ko/wiki_url,seed 白名单外增量对账不动;DDL 已代跑生产)。前端=/employers LMIA 表加行业列(broad 三语 chip)+别名灰字随界面语言+知名 Tag;公司弹框/榜单接线=v2。Wikidata 1,666 家候选后台跑(~30min),跑完灌库即生效(SSR 直查,零发版)。
> - **✅ 深夜连环批 #101-#106 全生产(2026-07-19 收口)**:#101 公司简介尾部「[…]」剥(WordPress 源站截断标记,09 去根+存量 66 行 SQL 清)/#102 JD 弹框内嵌 AI 顾问自动生成(Frank「不要再点一下」;额度闸照走,同岗会话缓存)/#103 公共 DataTable(排序+拖宽+hover,/employers 双表首批换装;jobs 主表保持独立只对齐 token)/#104 顾问追问暂关(CHAT_ON=false,#95 开关式;亮回=初判切本地后)/#105 llm.ts 第三后端 friend(**Render 已置 LLM_PROVIDER=friend,生产实测过——顾问初判也走朋友 qwen3.6,Anthropic 账单归零**)/#106 Wikidata 失败与未命中分开缓存(首跑知名 23/1666 偏低根因)重查后台跑。**当日总账 #96-#106 十一批全部当日生产。**
>
> **⚡⚡⚡ 上轮交接(2026-07-16→17 通宵 session,「闭环日」)——E9 闭环两件全链上线+生产验收,整改 #1-#49 全闭环,冷启动弹药就绪**
> - **E9-01 最小求职看板 ✅**(80b5146):SavedJobs collection(免费开放/上限 200/本人 access,SavedSearches 同构)+ 生产 DDL 先行;/jobs 操作列「☆ 收藏」(匿名点→注册框=转化钩子);账户 sidebar「我的求职」=状态流转(想投/已投/面试中/offer)+移除。**生产 Playwright 全链验收过**。实现文档 docs/implementation/E9-闭环/01。
> - **E9-02 发现层 ✅**:① **每日分类榜矩阵**(6f863c0):`10_build_rankings` 出 daily-top(全国)+daily-top-{10 大类}(近 48h×评分≥60×TOP20,大类<5 岗当日缺席);榜单页导航 chips 只列有数据的;每榜=SEO 落地页;**生产实拍 11 榜全起**。② **推荐横幅 1+3**(1db00d0):本地偏好画像(localStorage 不上传,弹窗+1/收藏+3)→「根据你最近浏览:SK·科技·$80–100K → 看这些岗(N)/建档案精确匹配 →」,当日可关,信号五五开或命中 0 时按宁缺不显;**生产实测渲染+CTA 正常**。
> - **当天其余全部落定**:第 18/19 轮 dd(整改 #1-#49 全闭环,含 #47 NOC 失配护栏 05d 上线+榜首 intern 生产消失);十三件 UI 拍板(JD 去表格+38k 帖格式保真回填完工/统一 header·footer/用户头像下拉/「我的账户」选项卡/账户 sidebar/筛选只留薪资/loadMore 只报剩余/登录 minLength 修复等);**数据节奏:jobbank 1h + enrich 拆独立角色(6h)+ #47 护栏进 build 轮**,页头每小时整点后 ~15 分钟内推进;文档拆档=一轮一文件(docs/整改轮次/);产品 v3 六环愿景+闭环四素材记档(记忆 product-v3-jobseek-funnel)。
> - **下一步(用户口头「下个 session 见」)**:① **发帖开闸**(docs/冷启动帖子.md 四渠道成稿,已含收藏/每日榜卖点;发布动作=Frank);② 晨查 healthchecks 隔夜全绿(注意:昨夜容器多次重启,check 周期可能有抖动误报,看当前态即可)+ 07-23 后删 Supabase;③ 流量进来后看 umami,评论痛点喂**价值账**(按六环盘;#45 CLB 数据缺口/简历上传立项/周报邮件/分享卡在矩阵排位);④ ~~第 20 轮 dd~~ **已跑(2026-07-17 凌晨 00:30-01:30)**:凌晨 00:01 build 轮全绿实证(seed ok:true+alerts+ping);**#47 生产复验终闭环**(本周新增榜零 intern)、#49 保持、JD REPARSE 段落保真复验过、E9 新面四件入图(截图脚本+7 镜头)、48 屏零回归;**新发现 #50-#53 只记不改**(#50 AI 断言用户身份 P2 / #51 横幅小数字 / #52 手机无收藏入口·效果图先行 / #53 移除钮换行),留第 21 轮;工作台已翻轮、第 19 轮落档。**真正的隔夜晨查(healthchecks 面板)天亮后仍要看一眼**。**当晚续(用户拍板「你先完善功能闭环,明天我来发帖」)**:#50-#53 四连修当晚实施+生产复验全过(30cc20d):#50 advisor 身份红线(新样本实测条件句「若你持 PGWP…」)、#51 横幅无数字态、#52 手机卡片星标(匿名点=注册框/登录点=★,**效果图 docs/assets/profit-shots/mobile/card-star.jpg 留 Frank 过目**)、#53 我的求职 × 归位——**手机端闭环打通,发帖前置条件已齐;#1-#53 全闭环**。**再续(用户「先来文档」→「改」)**:E3-07 忘记密码自助重置 + E9-02b 免费周报邮件**两件当晚文档→实施→生产复验**(05cac25):忘记密码=Payload 内置端点+mailer 适配器+找回/重置两态(频控 6 连打实测 5×200+429、防枚举、坏 token 错误态全过);周报=alerts 通道 C(dry-run 实测 weeklyEmails:1)+HMAC 一键退订+账户开关(双向持久实测)。**留 Frank 两发实弹:①本人邮箱走一遍忘记密码收信→重置;②首封周报真发目检+真 token 退订点一次**(@test.local 收不到真信)。
>
> **⚡⚡ 最新交接(2026-07-16)——E7-04 整体迁移 Render ✅ 当天割接完成,Supabase 已退役(观察期回退口保留到 ~07-23)**
> - **库=Render Postgres `pnp-postgres`**(basic-256mb+1GB \$6.30/月,Virginia 与 Web 同区内网):dump 16MB → 恢复 → **28 表行数逐表全等**;用户表增量核对零漂移;**/jobs SSR 3.6-6.8s→1.7s、seed→25s**(内网红利)。**mart 中转=ETL gzip 直推 `POST /api/mart/<name>`**(token 门禁,落 /tmp;>6MB 表分片 `__part0..N`+`__meta` 最后传=提交语义)。**日更当天恢复**(07-11 停摆→07-16 数据,Render 侧 seed 无 OOM 实证)。
> - **途中撞掉两个生产 OOM**(端点全量 parse / seed 整文件解析 27k 行,512MB 实例)→ 分片+逐片入库根治,记档 E7-04 §5;**新防线**:jobs mart 空时跳过 30 天下架(防空清单误杀)+ mart 双目录全无=抛错回滚(演练②实证维度表零损)。
> - **ETL 镜像已重建换 postgresql-client-18**(PG 18 下 pg_dump 17 拒 dump;五容器 19:2x 重建,build 容器 Supabase env 已清)+ **备份链实跑验证**:2026-07-16.sql.gz 16MB 从 Render 库 dump 落盘 ✓。
> - **只剩两件**:① 观察 07-17 夜更全绿(演练③:healthchecks 五 check / backups 当日 dump / 职位板日期推进);② ~07-23 后删 Supabase 项目(回退口=cms/.env 注释行)。
> - **第 18 轮 dd 已跑(2026-07-16 晚)**:夜更链路当轮实证全绿(五 check ping/pg18 当日 dump 16MB/build 一轮 seed 200 ok:true 维度表全在/职位板 07-16);**#44 生产复验 ✅ 闭环**(四处实拍,建议占位纯中文+「这家公司」);41 屏零回归;**新发现 #46 顾问正文偶发繁体被缓存(快刀)/#47 榜首 intern@肉店挂医生 NOC 90 分(源数据错标已核查实锤,修法待拍板)/#48 sponsor 榜跨省雇主省列空白(快刀)**——三件留第 19 轮,详见工作台。真正的隔夜无人值守观察(07-17 晨看 healthchecks)仍待明天。#45 语言/CLB 门槛数据缺口 → 待立项,与价值账一起排优先级。
> - 实现文档:[E7-04](docs/implementation/E7-运维与增长/04_整体迁移Render.md)(§5 有完整时间线/演练记录)。
> - **第 19 轮 dd 也已收口(07-16 深夜)**:#46/#48 当轮闭环;当日用户高频拍板九件全部实施+生产入图(JD 去表格+格式保真/登录 minLength/统一 header·footer/用户头像下拉/筛选只留薪资/账户排版/1h 更新/#44);新发现 #49(建议占位公司名裸嵌复发,前端兜底留第 20 轮);产品愿景升级记档(v3 求职全流程六环,记忆 product-v3-jobseek-funnel);工作台拆档=一轮一文件(docs/整改轮次/)。**冷启动四渠道帖子已起草**:docs/冷启动帖子.md(小红书/一亩三分地/Reddit/微信群+发布节奏),发布动作归 Frank。
> - **下一步(已和用户对齐)**:① Frank 发帖开闸(帖子草稿过目改口气)→ 72h 看 umami/注册;② 07-17 晨确认隔夜 healthchecks 全绿;③ 价值账正餐(按六环盘痛点→定价复核;简历上传立项优先);④ 第 20 轮 dd=发版后回归(#49/#47/JD 回填复验)。
>
> **上轮交接存档(2026-07-11 深夜,已全部落定)**:Supabase egress 爆 402 → 拍板整体迁 Render(上面已完成);seed 空灌防线 `22c8d6a`。
> - **迭代整改现状**:第 17 轮收口,**清单 #1-#43 首次全清零**🎉(工作台=docs/功能盈利点检查.md;第 17 轮:#21 LMIA 列全链/#41 中介整帖过滤/#42 只报命中数/#43 剥\*\*,另有当日追加七件 UI 拍板全部生产复验)。清零后正餐=价值账(痛点全量盘点→定价复核)。
> - **挂起口头确认**:Stripe 品牌图标/支持邮箱、Alipay 审批、站名随 offer2pr。
>
> **⚡ 新 session 快速上手(2026-07-05 交接)**:
> - **顾问弹框重设计已拍板待实施(2026-07-05,下个 session 接手)**:[顾问弹框字段规范](docs/顾问弹框字段规范.md)——哲学四条(三层零混淆/值口径来源三件套/29 字段归 9 家族统一骨架/field·job 级分区)+ 五段骨架 + 逐族内容排版表。拍板:D1 建档 CTA 只在 match/身份信号族;D2 免责只留 AI 段头(删 footAI);D3 动作链恒定页脚条;D4 下个 session 按 molit 开实现文档再动手(纯前端 1-2 SP,§5 差距清单=工作项,§3 表=验收基准)。
> - **快刀:顾问弹框薪资域四连修 ✅(2026-07-05,用户截图指出「中位年薪弹框乱」)**:① **免费用户点 Pro 锁列(中位/vs)不再弹顾问**——服务端本就剥离了中位数据,弹了只会误导+旁路付费墙(match 免费额度内有值仍可弹);② **advisor 红线补丁**:薪资类字段中位缺失时 prompt 明令「不得凭记忆报中位数」(实测抓到过编出 $72K-$78K;修后实测 AI 明说 not available);③ 弹框大标题只取本列字符串值,元素类 cell 不再回退成薪资文本(「中位年薪」页眉配「$36/hr」大标题的乱象);④ 事实块口径注分两种诚实:免费=「中位对比为 Pro 功能」/ Pro 真缺数据=「该 NOC×省暂无 ESDC 中位数据」(fact.medianPro/noMedian 三语)。免费态浏览器全程实测过。
> - **E6-02 LMIA 外劳雇佣记录 ✅ 全链上线(2026-07-05 深夜,单日立项→生产)**:ESDC 季度开放数据(8 季 141,504 行→75,426 雇主)→ `build_lmia.py`(挂 ee 月更;**镜像 +openpyxl 需下次 compose build**)→ 09 按 norm_name 精确匹配(公司 18.2%/岗位 19.8%,抽检零误报)→ companies 四字段(**jobs 零改动**,列表 SQL 本就 join companies)→ 前端「外劳记录」列+弹框事实块+citation → **sponsor-likely 榜单第一排序键改 LMIA 技能股职位数**(只认 High Wage/GTS/PR-only;榜首从鱼厂变 AHS/庞巴迪/BRP/Sony,D-c 措辞随实证自然解决)→ match 规则 6(+5 轻加权,无记录 na 不扣)。生产实锤:1,657/9,126 公司有值。语义红线:**「雇过外国人的历史事实」非「能担保」**(2026 TFWP 收紧:低薪股 30 大都市冻结,EE offer 加分 2025-03 已取消——展示必须带股别/季度)。详见 E6-02 实现文档。
> - **E6-03 公司分析接 web_fetch ✅ 生产验证过(2026-07-05,立项→验证半天)**:advisor 公司初判(field=company 且有官网)现场抓官网首页 grounding——**顾问最后一块无真实数据兜底的空白补上了**。冒烟拍板:`web_fetch_20250910` 在 haiku-4-5 **免 beta 头**;llm.ts `fetchUrl` 可选参(域名白名单/max_uses=1/输入 4K 封顶,ollama 忽略);prompt 带注入防御(页面内容=数据,页内指令忽略)。生产实测 Magnet Forensics:输出含官网当前产品矩阵+slogan,记忆编不出的新鲜度。company 输出上限 480→640(抓到真材料后会截断)。覆盖有官网的 ~24% 公司(ATS 第一方主力);缓存按公司名不变,成本增量 ~$0.004/次。
> - **产品对账文档**:[功能价值盘点与数据缺口](docs/功能价值盘点与数据缺口.md)——岗位价值六轴模型 + 数据缺口按痛点 ROI 排序。已落地 P0(LMIA=E6-02)与原 P3 换代方案(官网=E6-03);**剩余优先级:省 PNP 抽选线(P1)→ PE AIP/RNIP(P1)→ 挂帖时长信号(零抓取)→ 就业前景星级/处理时长(P2)**。域名决策仍卡邮件提醒(北极星不可测)。
> - **E6-04 省 PNP 抽选线 ✅ 全链上线(2026-07-05,立项→生产终验一天)**:PNP 弹框顶部新增「本省最近抽选」块(日期/流/最低分/邀请数,行级来源链)。实现文档 [E6-04](docs/implementation/E6-数据补强/04_省PNP抽选线.md)。**源盘点实测推翻交接假设**:① 「CRS 对齐可算差分」档全灭——**ON 2026-06-26 OINP 改制**(旧 8 流全删/EOI 关闭/新 Ontario Workforce Priority 流细则待公布)、**AB 2026 换 WEOI 自有分制**(连 EE 流最低分 47-71 也是 WEOI 非 CRS)、SK 2025 改制后无 EOI 抽选;→ 全部走「事实展示」档,match 不加规则,「差 X 分」维持联邦 EE 独有,**分数带分制标注「非 CRS」**(三语);② BC httpx 200(交接猜的 403 不成立,带 Chrome UA 即通)。链路:`etl/pnp/build_draws.py`(BC 表 rowspan 展开+同抽选多因素行去重/AB Table10/MB prose/ON notice,挂 pnp 源周更)→ raw/pnp/draws.json(**08 目录驱动天然跳过**,无 occupations 键,实证过)→ mart pnp_draws 25 行(BC8/AB8/MB8/ON通告1)→ 新 PnpDraws collection(生产 DDL 用户授权执行,B7 rels 清单齐)→ 生产 seed ok + 弹框四类终验(BC/AB 块、ON 通告、SK 无块)。**插曲**:本地 dev 验证时 pooler 打满(EMAXCONNSESSION 15,本地+Render 同抢)→ 直连正式库验证完记得关 dev。**连带发现**:OINP 改制后现有 oinp-in-demand(56 NOC)/oinp-tech(9 NOC)具名 chip 语义存疑(旧流已关,抓取还能解析到旧清单页)→ 已立项 E6-05(见下条)。
> - **快刀:/jobs 三处 UI 修 ✅(2026-07-05,worktree session,用户指出;build 过+DOM 实测)**:① **中/小分类国际化**——数据值仍中文(etl/noc.py 单一来源、有限集),显示层照大类先例新增 `cat.*` 键(EN/KO 各 ~89)+ `catName` 回退链(cat.* → broad.* → 原值,noc.py 兜底会把大类名当中/小类),筛选下拉/表格两列/弹框事实行全接,TEER 下拉「未分类」一并翻;**遗留**:PNP 列「AB 科技/SK 医疗」具名通道 chip label 仍中文(数据层 label,小集合,另课题)。② **弹框滑动选中文本误关根修**——新 `jobs/overlay.ts useOverlayClose`(mousedown 与 click 都落在 overlay 本身才关;框内按下框外松开时浏览器把 click 派发到共同祖先=overlay,旧 `onClick={onClose}` 必误关),AuthModal/AdvisorModal/ActModal/新 UpgradeModal 四处接入。③ **注册与购买分离(用户定)**:AuthModal 加 `mode="register"`(横幅/定价页注册 CTA 直达注册 tab,不再停在登录);新独立 `UpgradeModal`(30/90 天购买+价格,NEXT_PUBLIC_PRICE_DISPLAY 同源,checkout 复用 /api/billing/checkout);升级卡 CTA、Pro 锁列 🔒、保存筛选 gate 全改开弹框不再跳 /account(未登录先弹注册框);`up.cta` 改「升级 Pro →」、新键 `up.compare`、保存筛选不再 alert+跳页。实测(本地 dev=生产库,验完已关):EN 全下拉零中文残留、拖选保持打开+点外正常关、未登录锁列→注册框、登录免费号锁列→升级框(Buy 30/90 · CA$19/39)。**新测试号 fable5c@test.local 建在生产 users(惯例 @test.local)。已合 main push 部署(2026-07-05 晚,与 E6-05 同批)。**
> - **E6-05 OINP 具名 chip 下架 ✅ 全链上线(2026-07-05,当日立项→拍板方案 A→生产终验)**:背景=ON 2026-06-26 改制,实测新「Workforce Priority」流**只有 reg 级 TEER 分档条件(0-3:CLB6 / 4-5:CLB4 / 自雇医生),无职业清单**(按全 TEER 全职业设计,大概率不会再有 56/9 式清单);且两清单 6-28 周更仍解析出 56/9 = 旧页未撤会一直续命,必须主动下架。落地:退役 build_on.py + 删两 json → **08/09/match/前端全链目录驱动自动回退,零规则/前端代码改动**(ON 945 个 TEER4-5 岗回灰、抽查 44101 岗 55→43 分=撤 +12、pnp_occupations 182 行 ON 0 行、弹框 ON 通告行照在)→ **10/11 连带重跑**(sponsor-likely 第二排序键聚合具名通道,不重跑会带旧 OINP 聚合上载——教训:凡动 jobs 信号列,10/11 连带重跑)→ mart 上传 → 生产 seed ok:true → offer2pr.com SSR 终验。**残值已清(用户授权「跑」)**:「本批 mart 未含+发布<30 天」的 3 个 open ON 岗 seed 不更新也不下架(**增量模型结构性边角,记档**)——DML 清 stream 3 行+翻 eligible 3 行,残值归零,SSR 复核 0 chip;SQL 与执行记录在实现文档 §7。跟进钩子:e-Filing 夏末重开后复查(新清单→丢 json 即恢复;TEER4-5 全职业可走→新 type 语义另立项)。实现文档 [E6-05](docs/implementation/E6-数据补强/05_OINP具名chip下架.md)。
> - **架构已切「直连正式库」**:本地 `cd cms && npm run dev` 连的就是 Supabase 生产(护栏:dev 不自动推 schema,改表=显式 `DB_PUSH=1` 单次或手写 SQL;seed 必带 `x-seed-token`;测试号 @test.local)。本地 postgres/cms:3001 已过时。改 collection 的 DDL 清单看 memory `prod-migration-workflow`(⚠️ 别漏 payload_locked_documents_rels 关联列)。
> - **当前状态**:M1/M2/M3 ✅;**M4 手续全部办结(2026-07-05 夜)——只剩 7 天无人值守观察期**。五账号全通:healthchecks(5 check 全绿,ping URL 进 docker/.env)/ UptimeRobot(/jobs 5min 监控)/ Resend(key 进 Render,`alerts/run` 实测 dryRun:false;域名前测试模式只能发账户本人)/ umami cloud(两 env 进 Render,面板实测收到 pageview)/ GSC(property 验证过+sitemap Success 119 页)。备份链闭环(backup 容器日更 pg_dump→backups/,首份 6.8MB)。**三演练全过**(断源=缩周期法/宕机=404 法/恢复=临时库行数全等),记档 E7-01 §5。
> - **M4 记档补充**:GSC 验证走的 **HTML 文件法**(`cms/public/google021724190957bb4b.html`,勿删)——meta 法因根路径 307→/jobs 被验证器拒;layout 的 `metadata.verification` 保留备用。`/backups/` 已补 .gitignore(生产 dump 含用户数据)。**STRIPE_WECHAT_PAY 已删**(2026-07-05 用户拍板):Stripe live 侧实查 **WeChat Pay 与 Alipay 均 Pending approval**、仅卡 Enabled → 开关留着=每笔结账先撞失败再兜底纯卡(且兜底连坐砍掉支付宝)。删后 checkout 请求卡+支付宝;**支付宝获批后自动出现(代码写死)、微信获批后再把 env 加回**。⚠️ 在 Stripe 审批通过前,生产结账实际只有信用卡可用。umami website id=`a648865a-acc2-4f34-822c-a8f98412b58d`。
> - **🎉🎉 域名 offer2pr.com 全链上线(2026-07-05 当天拍板→注册→接线完毕)**:用户 Porkbun 注册($11.08/年,账号 offer2pr,营销邮件已拒);接线全部收口——**① DNS**(Porkbun,6 新增:ALIAS 根域+CNAME www→pnp-cms.onrender.com / Resend 的 DKIM+SPF+MX(send 子域)+DMARC;2 删:uixie parking;保留 MX fwd/根SPF/acme);**② Render 绑域** 双域+自动证书,www 301→根域;**③ Resend 域名 Verified**(6:03PM,37 分钟)→ **发信通道正式打通**,env `RESEND_FROM=PNP Job Tracker <alerts@offer2pr.com>` 已进 Render;**④ `NEXT_PUBLIC_SITE_URL=https://offer2pr.com`** 已换(Render env,触发重部署;sitemap/回跳/邮件链接随之);**⑤ 301 中间件**(cms/src/middleware.ts:onrender→新域,**排除 api/seed/admin/_next**——Stripe webhook 与 auto_update POST 不跟 301,实测 /seed 401、/api 400 不重定向、/jobs 301→新域);**⑥ GSC** 新 property https://offer2pr.com/ HTML 文件法秒验证 + sitemap 已提交(首提交「Couldn't fetch」占位,隔天复查);docker/.env SEED_URL 已切新域。**终验:https://offer2pr.com/jobs = 200**。**北极星自此可测**(dry-run 0 收件人=暂无 Pro建档用户,管线就绪待实弹)。**追办**:Porkbun 2FA(用户)、Stripe 品牌/收据+品牌名是否随 offer2pr、隔天看 GSC sitemap 转 Success、旧 GSC property 保留观察 301 权重转移。**接线插曲记档**:Porkbun DNS UI 的删除/校验用原生 confirm()/alert() ——自动化下弹框把渲染进程整个卡死(冻了三次),解法=先 `window.alert/confirm` 覆盖(截获消息+自动确认)再操作;另 TXT 类型的值输入框是独立的 `#editDNSModal_answer_long` textarea(普通 answer 框会被隐藏,盲填报「must have an answer」);根域 ALIAS 首轮被 parking ALIAS 冲突吞掉未建成(成功消息截断没看全),删 parking 后补建才通——**校验 DNS 落地要用 DoH 实查,别信 UI 成功提示**。
> - **Render Free→Starter ✅(2026-07-05,用户绑卡+升级,$7/月)**:实测 /jobs 冷启动 48s→消失(不休眠)、热请求 14-17s→3.6-6.8s(升级前 Free 档 0.1 CPU)。**剩余性能大头已开任务卡:/jobs SSR 一次发 1.2 万行(压缩后 1.5MB)**——首屏分页/懒加载重构(动客户端筛选核心交互,先写实现文档,估 2-3 SP),做完才能进 1s 档。
> - **悬而未决的决策**:~~品牌名/域名~~ ~~Free→Starter~~(均已落地)、Stripe 品牌/收据设置(追办)、站名是否随 offer2pr 改(公测前)。
> - **下一步可选**:① **痛点全量盘点(用户 2026-07-05 点名的产品正餐,见记忆 painpoint-driven-methodology)**:~100 条细粒度痛点清单 → 功能×痛点映射矩阵 → 推导「用户愿付 $20」的价值账——现盘点文档只有 P1-P5 太粗,E5-01 正式定价复核也靠它,冷启动前做最合适;② 弹框重排实施(规范已拍板,docs/顾问弹框字段规范.md §6);③ B9/E6-01 数据补强(PE AIP/RNIP);④ 公测冷启动(域名/邮件/GSC 已全就绪);⑤ 复核两个后台 session 产出(E6-05 OINP 改制应对、职位表分页重构)。
> - **seed 已批量化(2026-07-05)**:一轮 <1 分钟、单事务;⚠️ 改 Jobs/维度表字段 → 必须同步 `seed/route.ts` 列白名单(写路径耦合 snake_case,老坑 5 同款)。「抓取时间」= 数据层 last_seen,重灌不动它。auto_update 的 seed 判定已修:**2xx 且 ok:true 才记 ✓/才触发 alerts**(以前 502/500 也记 ✓)——看到「✗ seed」才是真失败,E5-03 自动提醒的触发条件自此可靠。
>
> **本轮(2026-07-05 白天 —— UI 图标化 + 抓取时间语义 + seed 批量化提速,三件全部署)**:
> ① **全站 emoji → 内联 SVG ✅**:装 lucide-react(用户拍板,弃手写),新共享出口 `cms/src/app/(frontend)/Icons.tsx`(统一 size=1em 跟字号 / stroke=currentColor / 基线 -0.125em,调用点只 import 这一个文件);/jobs 列表+弹框、账户、定价、统计、榜单、法务、横幅、升级卡全换;i18n 字符串内的 emoji 前缀剥离、图标改渲染点挂(`price.yes` 键删除,定价表勾改 `<IconCheck/>` 节点;alert/prompt 纯文本串只去符号);邮件 📣 改纯文本;品牌 🍁 保留(顶栏/登录框/about/邮件)。验收:grep cms/src 无残留 emoji(🍁 除外)+ build 过 + 本地 DOM 目检 + 生产 SSR 审计五页全过。
> ② **「更新时间」→「抓取时间」✅(用户提出:重新入库不许推这个时间)**:清洗下沉——`clean/05_parse_jobbank` 合并快照时给帖打 `last_seen` = manifest.fetched_at(抓取机本地时间转 UTC Z;同一快照重复解析幂等)→ 09 透传 `lastSeen`(ATS = jobs.json mtime)→ **seed 不再打钟**(mart 给了才写;缺则更新不碰旧值、新建留空);i18n 三语改名(抓取时间/Scraped/수집,顶部「更新→抓取」)。老口径残值(=历次入库时间)按「宁可留空」一次性置 NULL(12,500 行,用户授权)。**实测连灌两次 last_seen 分布逐字节不变** = 验收过。
> ③ **seed 改批量 SQL ✅(~40 分钟 → 44-63 秒,提速 ~40 倍)**:查「为什么这么慢」时发现 **近 72h 没有一轮 seed 真成功**——Payload 逐行管线(12k 岗 × find+update ≈ 数万次往返)在 Render Free 0.1 vCPU 上一轮 ~40 分钟,必撞代理 ~100s 超时:客户端记失败、服务端继续跑出半写状态,auto_update 日志还把 502/500 记成「✓ seed 502」。重写 `seed/route.ts`:分批 `INSERT … ON CONFLICT`(300 行/语句;companies upsert RETURNING 建 slug→id 映射)+ **BEGIN/COMMIT 单事务**(任一步失败整体回滚,消灭半写);列白名单按**生产 information_schema 实查**(别猜 camel→snake:stats.new7d 无下划线、rankings.date_posted 是 varchar);维度表删除前先清 locked_documents_rels 关联列(B7 教训照做)。语义不变:token / reset / 增量 / lastSeen 透传 / 30 天过期下架。**代价记档:写路径也耦合 snake_case 列名(老坑 5 同款)——改 collection 字段要同步 seed 列白名单。**
> **云端复验 ✅**:Render 侧触发 seed **42 秒一把过**(ok:true,12432 岗;远低于 ~100s 代理线,ReadTimeout 根治);页头「抓取 2026-07-04 23:01(ET)」= 数据层抓取时刻,连灌三次不动。
> ④ **auto_update seed 成功判定修复 ✅**:老版把任何 HTTP 响应记「✓ seed 502/500」并照样触发 alerts/心跳。改为 **2xx 且响应体 ok:true 才算成功**(502 返回 HTML,json() 炸也归失败),否则记 ERROR 并中止本轮;alerts 触发同样按状态码记 ✓/✗;seed 超时 600s→180s(批量化后 <1 分钟)。**restart build 实测一轮全绿:`✓ seed 200 ok:true` → `✓ alerts 200 dryRun`** —— E5-03「seed 成功后自动发提醒」的触发条件首次真实可靠。
>
> **本轮(2026-07-05 凌晨 B8 提醒与运维收敛 —— 代码侧全部落地,M4 只剩托管账号+观察期)**:
> ① **E5-03 匹配版邮件提醒 ✅(dry-run E2E 全通)**:`api/alerts/run`(x-seed-token,双通道:A=Pro+建档匹配日报(match high 新岗前10+当日新抽选 vs CRS 段,users.lastAlertAt 游标)/B=saved search(filters json 原样→lib/jobsQuery 解释,lastNotifiedAt 游标);首轮回看 36h 防倒灌;**RESEND_API_KEY 未设=dry-run 不发不回写**);SavedSearches collection(create=Pro,上限 5 钩子)+筛选区「保存此筛选」+/account 管理;auto_update seed 成功后自动触发;lib/matchDims 抽共享。**剩 Resend key(域名前=测试模式只能发账户本人)**。
> ② **E7-01 监控备份(代码侧 ✅,R3 修订记档)**:无 VPS → 家里构建机=运维盒;auto_update 成功后 ping HEALTHCHECK_PING_<SOURCE>(compose 已透传);新 backup 源(日更 pg_dump Supabase→backups/ 留14天)+compose 服务+httpx 镜像加 postgresql-client(**要 docker compose build 重建**)。剩:healthchecks.io×5/UptimeRobot/Anthropic 告警复核/BACKUP_DATABASE_URI 进 docker/.env/三演练。
> ③ **E7-03 SEO(代码侧 ✅)**:sitemap 121 URL(含 stats 全矩阵)+robots(挡 admin/api/account)+/jobs 专属 meta。剩:GSC 验证+提交、三平台发帖(72h 反馈记档)。
> ④ **B8 生产 schema 先行**:saved_searches 全清单(主表+users.last_alert_at+locked_documents_rels.saved_searches_id 列/索引/FK——B7 教训清单照做);DDL 生成用「本地库当生成器」:临时 DATABASE_URI=本地+DB_PUSH=1 起一次 dev(⚠️ 记得 taskkill node 子进程,kill npm 壳杀不掉,撞过双 dev 拒启)。
> ⑤ **老坑③ 又演**:bash curl 发中文 JSON 变 `??`(saved search 的 fBroad 损坏,误判为 alerts bug 查了一轮)——**中文测试体一律 UTF-8 文件 + --data-binary @file**。
> **M4 清单(用户手动)**:Resend 注册→RESEND_API_KEY 进 Render;healthchecks.io 5 check→ping URL 进 docker/.env;UptimeRobot;GSC+sitemap;umami 账号(B7 遗留);docker compose build+起 backup;然后三演练+7 天无人值守观察=M4。
>
> **本轮(2026-07-04 深夜 B7 信任与引流 + 架构拍板:直连正式库)**:
> ① **E4-04 字段级 citation ✅**:`etl/build_field_sources.py`(7 数据集着陆页 httpx 验证抽 title/meta 原文+9 派生口径,挂 pnp 源周更;21 verified/0 unverified)→ mart `field_sources`(30 行)→ 弹框统一 `SourceLine`(记录级官方原帖优先/数据集级兜底/unverified 降级/派生显口径)+ advisor jobFacts 行尾 [src:…] 标注。断源演练 ✅。
> ② **E5-02 榜单 ✅**:`10_build_rankings.py`(weekly-top TOP50 按 datePosted 7 天——mart 无 firstSeen 偏离记档;sponsor-likely TOP30 第一方+具名通道聚合)→ /rankings/[slug] 三语 SEO 页;顶栏入口;/jobs 接 ?q=。
> ③ **E5-04 地区统计 ✅**:`11_build_stats.py`(省×大类 115 行,双口径中位:ESDC+帖面)→ /stats 省索引+省页+大类页(口径行复用 E4-04 来源;职位板入口 ?prov=&broad=)+ **Pro 跨省对比**(档案 NOC 预选大类);市级后置 topCities 顶上。**又踩老坑 6**:共享常量放 'use client' 模块服务端拿 undefined → 抽 stats/shared.ts。
> ④ **E7-02 埋点(代码侧 ✅,选型按 R3 修订)**:compose 自托管方案过时 → **umami cloud 免费档**;layout env-gate script + signup/checkout 两事件;隐私政策补 analytics 条目;layout 顺手修掉「Payload Blank Template」残留 metadata。**剩用户手动:cloud.umami.is 开账号 → Render 填 NEXT_PUBLIC_UMAMI_SRC/ID**。
> ⑤ **架构拍板(用户,2026-07-04):所有数据真相=Supabase(库+Storage),本地库/本地 mart 读路径过时,本地 dev 直连正式库**。护栏已全上:payload `push: DB_PUSH==='1'`(dev 不再自动推 schema!改 collection=显式 DB_PUSH=1 单次推或手写 SQL)/本地 .env 配 SEED_TOKEN(seed 必须带 token,reset=1 会重灌生产)/测试号一律 @test.local/本地 postgres+cms:3001 标过时不删/`data/` 仍是 ETL 构建工作区(mart 上传 Storage 是唯一交接)。**生产 3 新表(field_sources/rankings/stats)DDL 先行**(pg_dump 导本地→psql,方案内授权)。
> **B7 收尾 ✅**:生产 seed 灌满三新表(115/80/30)并页面终验通过。**踩坑记档:新增 collection 手写 DDL 时,除主表外还必须给 `payload_locked_documents_rels` 补 `<slug>_id` 列+索引+FK**(Payload 给每个 collection 在该表加关联列;漏了 → seed 的 delete 阶段整事务炸,错误还被 25P02 吞掉难定位)——已补进 prod-migration-workflow 记忆与本段。另:本地→远端库逐行 seed 太慢(跨网延迟),触发生产 seed 让它在 us-east-1 内网跑才是正解。剩 umami 账号(用户,2 env)。**下批 B8:E5-03 邮件提醒 + E7-01 监控备份 + E7-03 SEO(sitemap 收录 stats 页)= M4。**
>
> **本轮(2026-07-04 深夜续 —— M3 正式收费开闸 🎉 + 登录改版)**:
> ① **E3-06 切 live 完成**:live Product `prod_UpEXEb8j3hFyBG`($19=`price_1Tpa4dGre9TF1l9zHOsx7tR4`/$39=`price_1Tpa5sGre9TF1l9zFjycLhSf`)+ live webhook `we_1TpaA9Gre9TF1l9zVVoSM7Uz`(completed+async 两事件)——助手驱动用户浏览器建的,**sk_live/whsec 全程用户自持不经助手**;Render env 切 live(STRIPE_WECHAT_PAY 已删,checkout 有未获批自动退纯卡兜底)。
> ② **真实收款验证**:admin 号真卡购 30 天 CA$19 → live webhook 一次投递成功(984ms)→ proUntil=2026-08-03(+30d 精确)→ 账户页 ⭐Pro。**真实退款演练**:Dashboard 全额 Refund → admin 后台清 Pro Until → 免费版 ✅。**运维口径:退款不自动降级,人工两步(Refund+清 proUntil)**。= **M3 正式收费开闸**。
> ③ **登录改版(用户提出)**:AuthForm 视觉翻新(品牌头/分段切换/聚焦态/渐变按钮);**全站唯一登录入口=首页弹框**——未登录访问 /account → 弹回 /jobs?login=1 自动开框,登录成功整页刷新让 SSR 分层态生效;/account 只留已登录态。
> **追办**:Stripe 品牌/收据设置(名称/图标/支持邮箱);live 侧 Alipay 可用性待确认(现兜底退纯卡);品牌名/域名决策仍悬。**下批 B7:E4-04 citation + E5-02 榜单 + E5-04 地区统计 + E7-02 analytics**。
>
> **本轮(2026-07-04 深夜 B6 合规与收费开闸 —— 代码侧全部就绪,M3 只差 live 手续)**:
> ① **E4-02 四件套 ✅**:`/legal/{disclaimer,privacy,terms}` + `/about` 三语(共享 `legal/LegalShell.tsx`,法务长文各页自带字典不进 i18n.ts;footer 四链)。**拍板落档**:定价 $19/$39 CAD 转正(D5)/退款=购后 7 天未大量使用可全额退(写进条款 §4)/公开支持邮箱 wangsansi9527@gmail.com(NEXT_PUBLIC_SUPPORT_EMAIL 可换)/适用法 Ontario。下架演练 ✅(applyUrl 删岗前端 1→0;**seed 回灌局限记档**:真实异议需同步清 postings/mart,backlog)。
> ② **E4-03 republish/PII ✅**:前台 SQL/API 零 email/phone 字段(grep 留档);**JD 出口统一脱敏** `lib/jobDescription.scrubPii()`(email+电话→「[见官方原帖]」,jobtext/advisor 共用,实测 0 泄漏);JD 弹框+事实块两处显著「↗ 查看官方原帖」按钮(D6:摘录+导流官方)。
> ③ **E5-01 定价页 ✅**:/pricing 服务端读 plan.ts(对照表与分层单一来源)+按钮三态(未登录→注册弹框/已登录→Checkout/Pro→账户);价格走 NEXT_PUBLIC_PRICE_DISPLAY,改价零代码;/jobs 未登录价值主张横幅(可关,localStorage)。埋点留给 B7/E7-02。
> ④ **E3-06 代码就绪**:checkout 加**live 支付方式兜底**(alipay/wechat 未获批时创建失败自动退纯卡打日志,防一个方式炸全部收款);无 test 假设写死。**M3 清单(用户手动,见 E3-06 §7)**:live 激活确认 → live Product/Price($19/$39)→ live webhook(两事件)→ Render env 换 live 三件+**删 STRIPE_WECHAT_PAY** → 真实付一笔 → Dashboard 退一笔+admin 手动清 proUntil(退款运维口径 v1=人工)→ 品牌/收据设置。
>
> **本轮(2026-07-04 晚 B5 档案匹配 + 付费墙 —— 付费头牌就位,M2 完整达成)**:
> ① **E5-00 档案匹配(付费核)**:Users 加 `profile` group(nocCodes/clb/crs/targetProvinces/pgwpMonthsLeft,json 存数组,本人可改无字段锁);**规则引擎只住 `lib/match.ts`**(纯函数同构:五规则 NOC对口/省通道/EE距离/TEER可达/工资信用 → 加权分 → 高≥60/中≥30/低/不适用,每条 reason=i18n键+参数+sourceRef 指回维度记录);/account 档案表单(NOC 搜索下拉用 **noc-descriptions**——文档写的 noc_categories 没有码,偏离已记);列表「与我的匹配」列(服务端算,Pro 全量/免费前 10 岗激活钩子);弹框「对我意味着什么」(客户端同一 match() 重算依据链,✓⚠✗ 逐条+来源↗+免责短句);advisor 档案感知(Pro 注入自报档案+本岗匹配结论,**个性化初判缓存按人隔离** :p<uid>)。快照测试 10 项含**措辞键白名单**(红线:永不说"你能/不能移民")。
> ② **E3-05 付费墙**:常量收口 `lib/plan.ts`(试用 8/20 次、Pro 日限 200、免费匹配前 10、PRO_COLUMNS,全可 env 覆盖);**gate 全服务端**——advisor/jobtext 免费登录用户按 userId 计数超 **402**(前端渲升级卡,四处)/Pro 429 日限/未登录 IP 限流不变;Pro 列(match/vs中位三件套)数据在 page.tsx 映射层剥离(算完匹配再剥),改 cookie 绕不过。列偏好 bump:COLS_COOKIE→jobsCols3 / PREF_KEY→v9(新默认含匹配列)。
> ③ **本地 15 项端到端全过**:档案 CRUD/回读、role/proUntil 字段锁回归、SSR 免费前 10 chips + 11 起🔒、HTML 无中位数据泄漏、curl 直调 402、Pro 全解锁不限次。**踩到一个真 bug**:免费"前 N 岗"最初取 SQL 序,与前端默认序(同日按评分兜底)不一致 → SQL 排序补 `score DESC` 对齐(读 JobsTable:537 的兜底逻辑)。
> ④ **生产 users 表 6 个 profile 列已先行补齐**(psql,用户授权;render-env.txt 已删,连接串用 cms/.env 里注释的 Supabase 行)——B4 "schema 先行再 push" 教训落实。
> **下一步:B6 合规与收费开闸**(E4-02 四件套 / E4-03 republish 自查 / E5-01 定价页 / E3-06 切 live = M3)。E5-01 拍正式定价后建新 Stripe price 换 env(现 $19/$39 是占位)。
>
> **本轮(2026-07-04 下午 B4 支付贯通 —— 代码侧全部完成,收口只剩 Stripe Dashboard 手续)**:
> ① **E3-03 时长包 Checkout**:`lib/stripe.ts` 单例(key 未配置返回 null → 无支付配置站点照常跑)+ `api/billing/checkout`(getUser 401 → mode=payment,30/90 天 price,卡+Alipay,返回 {url} 前端跳转;WeChat 待 Dashboard 确认后设 `STRIPE_WECHAT_PAY=1` 即开,client:web 已带)+ `/account` 两档购买按钮 + `?ok=1` 回跳绿条(三语)。
> ② **E3-04 webhook**:`api/stripe/webhook`(raw body 验签 → completed 且 paid → `proUntil = max(now,现值)+metadata.days`;幂等=Users 新隐藏字段 `stripeSessions` 记已拨 session.id;未知事件 200 ack、异常 500 让 Stripe 重试)。**超文档一处:同时处理 `async_payment_succeeded`**(alipay/wechat 异步到账,completed 时可能 unpaid → 不处理会丢单;⚠️ Dashboard 建 endpoint 两个事件都要勾)。**本地自签 HMAC 伪造事件走真实验签路径 13/13 实测过**(401/400/假签名 400/首购+30d/重放不叠加/续买顺延 +120d/unpaid 不拨 async 补拨/未知事件 ack/幽灵用户 ack)。
> ③ **E4-01 免责声明 v1 收口 ✅**:页脚三语升级(聚合工具/非 RCIC/以官方为准)+ 链 `/legal/disclaimer` 占位页(新,client 读 LANG_KEY 循 /account 约定)+ AdvisorModal AI 判断区顶部小字提示;advisor SYSTEM 本就禁 disclaimers,UI 层与 AI 文风分离核对无需改。
> ④ `npm run build` 过(改了 Users 字段照老坑先本地 build);payload-types 已重新生成;`.env.example` 补 STRIPE_* 五项注释。
> ⑤ **部署插曲(已解决,留作教训)**:推代码后才意识到 **Render Auto-Deploy=On 意味着 push main = 直接上生产**,而 Supabase 生产库当时还没有 `users.stripe_sessions` 列(生产 Payload 不自动推 schema)→ 生产登录一度 500 约 10 分钟。处置:立即 revert 保生产(登录恢复 401 实测)→ 用户授权后 psql 给生产库补列(`ALTER TABLE users ADD COLUMN IF NOT EXISTS stripe_sessions jsonb;`,与本地 payload push 产物一致)→ reapply 两个 commit 重新部署。**教训:改 collection 字段的 commit,push 前必须先给 Supabase 补列(SQL editor 或授权 psql),再推代码**——B3 是先推 schema 后部署,这次顺序反了。
> ⑥ **B4 ✅ 真实回归收口(2026-07-04 晚,test key 全链路)**:Stripe 账户激活全程走完(SaaS 类目/descriptor『PNP JOB TRACKER』/CIBC 手动绑卡拒了网银凭据聚合/2FA/Tax+Climate 均关);Product/Price 用 API 建(`prod_UpCiFof1mmH07M`;30d `price_1TpYJWGfQhhawEig3gBeks8Q` $19 / 90d `price_1TpYJWGfQhhawEig8oGsYnVd` $39,**占位价,正式定价等 E5-01**);**4242 卡全流程 +30 天、Alipay 模拟支付 +90 天顺延,均为 stripe listen 转发的真实事件**;WeChat Pay test 模式可用(Checkout 页三方式齐,live 待 E3-06);Stripe CLI 已 winget 装机。**生产 webhook endpoint 已建**(`we_1TpYVOGfQhhawEigoe68vKtD`,两事件)。= **M2 支付链路贯通(本地)**。
> ⑦ **M2 线上确认 ✅(2026-07-04 晚)**:Render env 填入 STRIPE_* 后,生产真实跑通一笔——注册测试号 m2check@test.local → /account 购 30 天 → 4242 支付 → **生产 webhook(Stripe→Render)送达,proUntil=+30 天整**;生产 /account 购买入口/免责声明/legal 页均正常渲染。**插曲两个**:① 首次 500 = Render env 值粘贴有误(用户修正后即通)——症状:同参数直打 Stripe OK 而站点 500;② **`NEXT_PUBLIC_SITE_URL` 漏配** → 支付回跳落到 `https://0.0.0.0:10000`(req.nextUrl.origin 在容器里的回退值),支付/webhook 不受影响但用户体验断——已让用户补第 6 个 env(`NEXT_PUBLIC_SITE_URL=https://pnp-cms.onrender.com`),**教训:上生产的回跳/绝对 URL 永远显式配,别信 origin 回退**。已补并复验(新 session 的 success/cancel_url 均=https://pnp-cms.onrender.com/*)——**B4 全关,M2 完成**。下批 B5(E5-00 档案匹配 + E3-05 付费墙)。
>
> **本轮(2026-07-03/04 上线冲刺 —— 计划体系 + B0-B3 一天半打完,站点上线)**:
> ① **计划体系**:`docs/整体开发计划.md`(4 Sprint→8/28 收费,四里程碑 M1-M4)+ `docs/implementation/`(一工作项一文档,molit 规范)+ **产品重设计 v2 已采纳**(`docs/产品重设计提案.md`:三问定位 去哪/投什么/怎么拿身份;**付费核=档案匹配 E5-00**,AI 顾问降为个人化层;地区统计 E5-04;E6 让位;**付费 v1=30/90 天一次性时长包**,无订阅无 Portal,D5/D8 修订)。
> ② **B0 收尾**:分支已合 main 并 push;EE 类别抓取改 httpx(`etl/build_ee_categories.py`,产出与浏览器版逐类一致 9类94职业,ee 容器换轻镜像,draws 一并进月更 steps);04c/04d/05c 三处 postings 直写补原子(05/05b 原已原子)。
> ③ **B1/R3 上线(D2 修订:Render+Supabase,VPS 降备选)**:Render web `pnp-cms`(Docker,rootDir=cms,**Free 档待升 Starter**,Auto-Deploy=On)× Supabase 项目 `jnulqjhtqdwthtyccypj`(us-east-1;库走 **Session pooler :5432**;私有 bucket `mart`,**建项目关掉了 Data API**——Payload 表无 RLS 开着会裸奔)。**mart 交接走 Storage**:build steps 末尾 `upload_mart.py`(SUPABASE_* 未设自动跳过→本地/VPS 模式不变);seed 改双模式(env 设=从 Storage 拉,否则读本地);**家里 build 容器已接云端**(`docker/.env`:SEED_URL=云端/SEED_TOKEN/SUPABASE_*)=**$7 起步档**(ETL 留家,笔记本合盖不更新是已知代价;M4 前可上 Render worker 转全托管,`docker/render/Dockerfile.etl`+supervisor 已备好)。VPS 备选文件(docker-compose.prod.yml/Caddyfile)保留未删。
> ④ **安全**:seed 加 `x-seed-token` 鉴权(生产 401 实测;本地不设 token 放行);advisor/jobtext 进程内限流(`lib/rateLimit.ts`:IP 日限 40/200+全局 1000,缓存命中不计);**生产 admin 首用户已注册提权(402707192@qq.com=admin)**——曾裸奔一天,教训:上线即抢注。
> ⑤ **E2-03 云 LLM**:advisor 抽 `cms/src/lib/llm.ts` 双后端(`LLM_PROVIDER`:ollama=本地 dev/anthropic=线上 **claude-haiku-4-5**);生产三项实测过(数字精确引用/缓存 hit/追问无数据明说"我没有这些数据");成本≈$0.003/次,**Anthropic 账户不开 auto-reload=余额即硬上限**。
> ⑥ **B3 账号**:Users 加 role/proUntil/stripeCustomerId(**字段级 access 锁**,实测冒填注册与自 PATCH 提权均被忽略;role 进 JWT);`lib/entitlement.ts`(getUser/isPro=proUntil>now,时长包语义);`/account` 三合一页 + **顶栏弹框登录**(共享 `jobs/AuthForm.tsx`,弹框循 ActModal 既有约定,不跳页);顶栏顺序=语言切换在前账户最右;根路径 307→/jobs(模板欢迎页已删)。
> **坑/教训(新)**:① **dev(turbopack)不跑完整类型检查——改 collection 后必须本地 `npm run build` 过了再推**(role 加 required:true 让 tests/seedUser 编译爆,生产构建连挂三次才发现);② Render:免费档 web 会休眠(50s 冷启动杀 SEO),worker/持久盘无免费档;**控制台 SPA 长连接卡浏览器扩展读屏**(自动化走 API 或用户手点);③ PowerShell 5.1 发中文 JSON 必须显式 UTF-8 字节,否则测试假阴性;④ Supabase 新版 `sb_secret_` key:Storage 请求带**双头**(apikey+Authorization);⑤ **`cms/.env` 的 DATABASE_URI 切到 Supabase 行时=本地 dev 直连生产库,`seed?reset=1` 会重灌生产**——日常开发保持本地库行,推 schema 时临时切。
> **规则更新**:git 提交身份=**Wang Peng**、**不带 Co-Authored-By 尾注**(repo config 已设)。
> **下一步:B4 支付贯通** —— E3-03 时长包 Checkout(mode=payment,卡+Alipay,WeChat 待 Dashboard 确认;Stripe test key 就绪,激活的 Business website 等 M1 后回填)+ E3-04 webhook 单事件拨 proUntil + E4-01 免责声明 v1。M1 收尾:三天日更观察(**家里 build 自动推云端的首轮还没核过**)+ Free→Starter + 域名(等品牌名决策)。杂务:让用户删桌面 render-env*.txt 密钥文件。
>
> **本轮(2026-06-29 顾问弹框三层 + Part B 数据 + 表格固定列 + JD 格式)**:
> **① 弹框三层(事实/判断/对话)**:上半=可核验事实(绝不经 LLM)/ 中=只基于上半事实的 AI 判断 / 下=多轮 grounded 对话([route.ts](cms/src/app/api/advisor/route.ts) 加 `messages[]`,system 带整条岗位事实+铁律,问到没有的数据直说"未提供"不编)。各字段「事实块」`FieldFactsSection`(地点/薪资/分类/来源/经验/时间状态零成本;wiring:firstSeen 进 SQL、designated_employers 维度进前端=AIP 记录、职位 JD 摘录走 `/api/jobtext`、评分明细前端重建)。
> **② Part B 数据缺口(全开放数据 httpx,可进 docker 自动更新)**:**#0** ATS 公司简介进 mart;**#4** 工资 low/中/high+年份(`build_wages` 多抽 + `Jobs.ts` +5 字段);**#1** EE 抽选分数线(`build_ee_draws` 抓 IRCC `ee_rounds_123_en.json`,无 Akamai → ee_categories 加 drawCrs/Date/Size);**#2** NOC 官方名+职责(`build_noc_descriptions` 抓 StatCan NOC 2021 Elements CSV → 新 `noc-descriptions` 维度 397 行)。**#3 PNP 门槛 = 评估后决定留空**(门槛散在各省 checklist prose、工资多定性,解析易错;移民门槛宁可留空,弹框已带官方来源链接)。
> **③ 表格固定左列 + 横滚**:发布时间/大分类/公司/职位 **sticky 固定**(只冻最左连续段,先量列宽算累计 left),其余列给最小宽 → 列多时整表超容器**横向滚动**看隐藏列、列少 `width:100%` 拉满;默认 10 列;**bump 列偏好版本** COLS_COOKIE→`jobsCols2`/PREF_KEY→`v8`。
> **④ JD 正文保留格式**:[clean/05b_parse_details](etl/clean/05b_parse_details.py) `description()` 改抓**可见结构区** `.job-posting-detail-requirements`(h4+ul/li)做块感知提取(原是读 `[property=description]` 压平一坨);聚合帖 property 里的转义 HTML 再解析一次。`REPARSE=1` 重解析 6808 岗 → mart(4699 有正文)→ reseed。**原生 JB 岗格式漂亮;少数聚合纯文本帖源头无结构,天花板**。
> **本轮坑/教训**:① **seed 各维度是显式字段白名单**([seed/route.ts](cms/src/app/seed/route.ts) `dims[]`)—— 加维度字段必须同步加进 map,否则重灌不入库(EE drawCrs 踩过)。② 改 `Jobs.ts`/新 collection → **必须重启 host dev**(Payload 推 schema 加列/建表)再 reseed。③ **noc.esdc 证书链坏 + 不透明 objectid** → 弃用,改 StatCan 开放 CSV。④ canada.ca **category-based-selection 页现可 httpx 直取**(无 Akamai),EE 类别抓取可改 httpx 替掉有头浏览器(未做)。⑤ **EE「类别抽选」≠ 普通 EE**:类别列「—」只代表无定向快车道,不挡普通 EE/PNP/AIP(三条独立)。
> 代码在分支 `feat/lists-autoupdate-and-table-ux`(**未合并 main**,本轮 +20 余 commits)。
> **下一步(新 session)**:① **重建 docker cms :3001 到最新**(本轮改动后 :3001 又旧了:`cd docker && docker compose --profile unattended up -d --build cms`)② #5 公司官网抓取(仅 ~24% 有网址,脆)/ #6 RNIP ③ EE 类别抓取改 httpx(数据已准,只为进 docker 自更)④ 合并 main。详见 [docs/advisor-fields-plan.md](docs/advisor-fields-plan.md)。
>
> **上轮(2026-06-28)**:① **PNP/AIP/EE 三类清单全部 docker 自动更新** —— `pnp` 源(周更 httpx:AB/ON/SK/NS 实时抓 + `06_scrape_aip`)、`ee` 源(月更**无头 chromium**,canada.ca 实测无头+stealth 直接通,无需 xvfb;crawl 镜像保留有头能力给硬墙)。② **每省脚本全实时抓,md 只作参考**(`etl/pnp/build_<prov>.py`)。③ **08_score 把具名通道 stream 与资格 type 解耦** → exclusion 省(AB)也能挂 inclusion 通道;新增 **ON 科技(OINP Tech Draws 9)/ AB 科技(AAIP Accelerated Tech 44)**,全国具名命中 ~247。④ **BC tech 下架**(tech 抽选 2024-12 已关、welcomebc 无清单页;原 bc-tech 是手工从第三方补录的,违反"实时抓")→ BC 岗落绿「可提名」。⑤ **表格显示升级**:PNP 列 3 档强度(具名=琥珀 chip / 可提名 / 不符 + 魁省 N/A)+ 评分列 5 档色阶 + 更新时间时分秒 + 列宽拖拽/缩窄换行。⑥ **修**:09 空省份排序崩溃、04c 非城市占位词清洗。
> 代码在分支 `feat/lists-autoupdate-and-table-ux`(**10 commits,未合并 main**)。
> **下一步(已规划未做)**:见 [docs/advisor-fields-plan.md](docs/advisor-fields-plan.md) —— 每字段弹框「上原始数据块 + 下 AI」+ 补 6 个数据缺口(①EE 抽选分数线 ②NOC 职责 ③PNP 门槛 ④工资 low/high ⑤公司信息 ⑥RNIP)。
>
> **上轮(2026-06-26 移民信号 + 弹框)**:
> ① **PNP 列显示具名通道**:08_score `pnp_stream()` 算命中省清单的短标签(OINP 紧缺技能·科技 / AB 科技 / SK 医疗·科技·农业 / NS 紧缺空缺·毕业生;stream 与资格 type 解耦,exclusion 省也能挂),
> 列里不再只是泛「技能岗」。② **联邦 EE 类别——独立一列**:Express Entry 类别抽选 ≠ PNP(看 CRS、多不需 offer),
> 独立信号;`etl/crawl/_fetch_ee_categories.py` 用 browser_fetch 过 canada.ca 403、展开 DataTables 抓全 9 类 94 职业 → `raw/ee/`。
> ③ **省清单从已抓 policy md 解析,每省一个自包含脚本**(`build_<prov>.py`:`build_bc`/`build_sk`/`build_ns`)读 `raw/policy/<省>/md/*.md`
> (自动读 frontmatter 的 source/fetched)→ `raw/pnp/{bc-tech,sk-health,sk-tech,sk-agri,ns-critical,ns-grad}.json`,无需重抓。④ **清单维度表化**:mart 产 `pnp_occupations`(229)/`ee_categories`(94)
> → seed 入库 → 前端**读 DB props**(删 `/api/pnp-list`、`/api/ee-list` 文件读取;删空壳 `PnpStreams` collection,手动 drop 表+残留列)。
> ⑤ **点 PNP/EE 字段 → AI 顾问弹框**:上半「真实清单」(数据层维度表,**绝不经 LLM**,命中行高亮「← 本岗」),下半 LLM 建议。
> ⑥ **弹框升级**:默认 720×620、右上角全屏切换、标题栏拖动、右下角 resize(原生 pointer,尺寸记忆 localStorage)。
>
> **数据来源坑(见 memory)**:省政府站 + canada.ca 都对 httpx/WebFetch 返 403 → 用 `etl/crawl/browser_fetch.py`(headed Playwright);
> ⚠️ 必须**系统 python**跑(playwright 没装进 uv venv)。HTML 无损、md 便利有损 → 带分组的清单(OINP/AB)从 HTML 解析更可靠。
>
> **下一步(已确认)**:JD 正文灌库 —— 现在 jobtext/advisor **运行时现读 `data/*.md`**(部署需挂文件);拟 Jobs 加 `description`,
> mart 按 applyUrl 匹配 .md 写入,jobtext/advisor 改按 id 读 DB(列表 SQL 不 SELECT 它,避免页面撑爆)。公司简介无正文,需另抓。
>
> **AI 顾问真实性**:职位描述/评分/各字段/PNP·EE 清单——均有真实数据+精确数字作依据(prompt 强制不许编);
> **唯独公司分析靠模型自身知识**(没抓公司正文,冷门公司可能不准)。

## 这是什么
**PNP Job Tracker** —— 每日更新的**全加拿大全职业职位板**,带移民价值视角。能走「雇主 offer → 省提名(PNP)」的岗打 `pnpEligible` 标记(粗筛信号,非资格认定)。
Job Bank 覆盖全 10 省全职业(含 QC);ATS(Kanata 科技公司)仍 Ottawa。

## 架构:数据仓库式分层
```
etl/ raw(抓取) → clean/(清洗,按字段) → mart(集市层,列对齐DB) → cms/ seed(纯加载器) → Postgres → /jobs
```
- `etl/_paths.py` 是**唯一路径真相来源**。按来源分顶层:`raw/ats/` · `raw/jobbank/`;processed 同理 `processed/ats/`。
- **mart(`09_build_mart.py`)**:把各源拼成 `data/mart/*.json`,每文件=一张DB表。中介过滤/去重/评分关联/分类/来源标签 全在这层或更上游。
- **seed(`cms/src/app/seed/route.ts`)= 纯加载器**:只读 mart → 灌库(并发分批)。`?reset=1` 全清重建;不带 reset = 增量对账(没出现的岗→closed)。

## 现状:全国多省,端到端跑通,库里 ~2084 岗 / 1578 公司
- **DB 10 张数据表**:事实 `jobs`(~4834) `companies`(~3667);维度 `provinces`(10) `cities` `districts` `noc_categories`(101) `sources`(5) `experience_levels`(5) `designated_employers`(2917, AIP名单) `pnp_occupations`(216, 各省具名通道职业) `ee_categories`(94, 联邦EE类别职业)。
- **分类全在数据层**:NOC 大/中/小分类+TEER 在 `etl/noc.py`(单一来源)→ 存 job 字段;来源显示标签(JB→Job Bank)在 mart 洗 → `sourceLabel`。前端**只读字段、不再算 NOC**。
- **区(district)= 自维护 FSA→区表**:`reference/fsa-districts.json`(从 GeoNames 加拿大邮编开放数据建,1651个FSA,零API)。04c 按邮编查表洗区,全国可用(Ottawa社区折叠成 city=Ottawa)。769/2084 岗有区。
- **中位工资**:`reference/wages.json`(`build_wages.py` 从 ESDC 开放数据建,NOC×省 中位)。mart 按 NOC+省 join → job 带 `wageMedHourly/wageMedAnnual`(1473 岗匹配)。薪资顾问直接显示「中位 + 本岗 vs 中位 %」。
- **筛选全读维度表**:国→省→市→区(provinces/cities/districts)、大/中/小分类+TEER(noc_categories)、来源(sources)、经验(experience_levels);全字段搜索、表头三态排序、字段自选(含「主要」一键核心列)、**AI 顾问弹框**。
- 列顺序:发布时间第一、评分最后。默认排序发布时间降序(同值评分兜底)。地点列点击跳 Google 地图(各列用自己那一级)。
- **AI 顾问**:职位/公司用本地 Ollama(`OLLAMA_URL=http://192.168.1.150:11434`)流式;其余字段模板。**⚠️ 线上访问不到家里 Ollama,部署前要决定去向。**

## ⚠️ 性能 / 已踩坑
- **`/jobs` 列表走原始 SQL**(`page.tsx` 用 pg pool `SELECT+join`),绕开 Payload 的 per-doc 读取管线(2000+行要16s→0.9s)。**代价:耦合 Payload 的 snake_case 列名,改 Jobs schema 要同步那段 SQL。**
- **衍生抓取数据必须 gitignore**:`postings.json`/`mart/`/`all-scored.json`/`geonames|wages 源`/jobbank公司目录 全已忽略。**教训:之前 postings.json 被 git 跟踪,反复被 restore 回旧版丢数据。** 维护的表(fsa-districts.json/wages.json/AIP)才跟踪。
- 改 Jobs collection 字段 → **必须重启 dev server**(Payload 同步 schema)再重灌。
- **职位 externalId = `jb:<posting_id>`(JB)/ 投递 URL(ATS)**:JB 从帖子 URL 的 `/jobposting/<id>` 取,**不用完整 URL**(2707/2733 带 `?source=` 查询串,带不带就成两个 ID)。⚠️ **它是 08_score↔09_mart 的 join 键,两处必须一致**——只改一边会让评分/NOC/pnp 全丢(踩过:只改 09 → 2203 岗只剩 59 有评分)。
- **下架按发布日期过期,不靠 seenIds 对账**:增量抓取只含最近几天,用「本次没出现→closed」会误杀仍在招的旧岗(实测一次误杀 805)。seed 改为「本次未见 **且** datePosted 超 30 天」才下架。
- **重灌前必须跑完整链**:别只跑半条链(如漏 05b 详情)就 reset 重灌——会灌进缺官网/地址/区、NOC 没匹配的退化数据。完整链:05→05b→04c→04d→05c→08→09→seed。

## data/ 结构(2026-06-25 扁平化:统一 `raw/<源>/[<日期>/]`,删了 reference/output 桶 + 方式层 + ats 地理深嵌套)
```
data/
  raw/                          # extract:每个子目录=一个源/维护表;抓取「方式」记在 sources.py,不进路径
    jobbank/<日期>/             #   JB 列表快照 <省全称>-pNN.html + <日期>/details/<id>.html(gitignore)
    oinp/<日期>/  aaip/<日期>/  #   各省 PNP 政策页原始 HTML(gitignore;维护表见 pnp/)
    ats/                        #   ATS 公司名录 roster(扁平,单区,.json 跟踪;.csv/.md gitignore)
    pnp/  oinp-in-demand.json · aaip-ineligible.json   # PNP 维护表(跟踪,08 读)
    aip/  aip-designated-employers.json                # AIP 名单(跟踪)
    wages/ wages.json + wage*.csv源   fsa/ fsa-districts.json + CA.txt源   policy/<省>-immigration/  # 维护表跟踪+源gitignore
  processed/                    # transform:累积去重的「当前态」(不按日期)
    jobbank/  postings.json + details/<slug>.md
    ats/<slug>/                 #   profile/jobs.json 跟踪;jobs/*.md gitignore
    all-scored.json             #   08→09 评分中间产物(gitignore;文件式更利于上云,不落 DB)
  mart/    8张表 .json(gitignore,09 产出,seed 灌库)
```

## ETL 流水线(`etl/`)—— 抓取/解析已分离(raw 只存原始 HTML,解析在 clean/→processed)
| 脚本 | 作用 |
|---|---|
| 01-03 | ATS:Kanata 名录 → 公司文件夹 → 找 careers(写 raw/ats、processed/ats,扁平)|
| 04 scrape_ats_jobs | ATS 第一方岗(greenhouse/lever/workday…)→ processed/ats/<slug>/ |
| **05 scrape_jobbank** | **纯抓**:`--all-occupations --prov ALL --since-days N` → 每页原始 HTML 存 `raw/jobbank/<日期>/<省全称>-pNN.html` + manifest,不解析 |
| **clean/05_parse_jobbank** | 读最新日期快照 → parse_article 解析 → 增量去重合并 `processed/jobbank/postings.json`(temp+os.replace 原子写;去重键=url 派生 posting_id)|
| **05b scrape_jobbank_details** | **纯抓**:对未富集的帖抓详情 HTML → `raw/jobbank/<日期>/details/<id>.html`(增量靠 detail_fetched/文件存在)|
| **clean/05b_parse_details** | 解析详情 HTML → 富集 processed postings(address/website)+ 写 processed/jobbank/details/<slug>.md |
| **clean/**04b/04c/04d/05c | 抽薪资 / 地点(FSA查表洗区) / 薪资归一 / AIP标记。脚本顶部声明 `IN_*/OUT_*` 全路径 |
| 06 build_jobbank_companies | (遗留,无下游消费者)把 postings 物化成公司目录;不在容器管线里 |
| 08 score | NOC→TEER+评分+pnpEligible(按省:08 读 raw/pnp/*.json,inclusion/exclusion 两型)→ processed/all-scored.json |
| build_fsa_districts | GeoNames → fsa/fsa-districts.json(偶尔重建)|
| build_wages | ESDC开放数据 → wages/wages.json(年度更新)|
| etl/pnp/build_<prov> | 每省一个自包含脚本 → pnp/*.json。**AB/ON/SK/NS 全实时抓**(ON 双流含科技,AB 含科技 PDF;SK/NS 复用 HTML→md 转换器)。BC tech 抽选 2024-12 已关→**无具名通道、已下架**。**docker `pnp` 源(周更)**|
| 06_scrape_aip_employers | AIP 指定雇主 NL/NB/NS → aip/(PE 仍 TODO);随 `pnp` 源周更 |
| _fetch_ee_categories | 联邦 EE 9 类/94职业 → ee/。**已上 docker `ee` 源(月更,crawl 镜像无头 chromium,canada.ca/Akamai 无头直接通,无需 xvfb)**|
| 09 build_mart | 拼装 → data/mart/*.json(8张表 + 分类/来源/工资 join)|
| auto_update | 调度器:读 sources/<SOURCE>/META 跑 steps;**loguru 统一日志**,逐行截获子进程输出套「时间\|级别\|源\|消息」|

## 怎么跑(新机/新 session)
```bash
cd pnp-job-tracker/docker && docker compose up -d postgres   # 起库(全栈 compose,项目名 pnp,容器 pnp-*)
cd ../cms && npm run dev                                      # 开发 :3000(库走宿主 5432)
# 完整重跑 ETL(走 _paths):
#   05 --all-occupations --prov ALL --since-days 3  → 05b → clean/04c → clean/04d → clean/05c → 08 → 09_build_mart
#   (ATS 链 01-04+04b 另跑;build_fsa_districts/build_wages 偶尔重建)
# 重灌: curl "localhost:3000/seed?reset=1"   |  增量: curl "localhost:3000/seed"
#
# 自动日更 / 开机自启(docker/):
#   开发(cms 用 host npm run dev):  cd docker && docker compose up -d                        # 只起 etl 自动机
#   无人值守(cms 也容器化):         cd docker && docker compose --profile unattended up -d --build
#   日志 docker compose logs -f  |  停 docker compose --profile unattended down
#   ⚠️ 两模式都抢 :3000,别同时跑。开机自起再去 Docker Desktop 勾「登录时启动」。
```

## 待做(优先级)
- **Phase 3 — Docker 开机自抓服务 ✅ 已建并跑通验证(2026-06-24)**:单一 `docker/docker-compose.yml`(项目名 `pnp`,容器 `pnp-postgres/cms/jobbank/build`)+ `etl/auto_update.py`。`restart: unless-stopped` + Docker Desktop 登录自启 = 开机自更新。用法见上「怎么跑」(开发=`up -d postgres`+host npm;无人值守=`--profile unattended up -d`)。
  - **实测一轮端到端通**:jobbank 抓(05/05b,东部时间日志)→ build 清洗/评分/mart → curl cms → seed 200;今天(6-24)数据入库、全部有评分、区/官网富集、稳定 `jb:<id>`。
  - ⚠️ **运维注意①(富集时序)**:build 按自己的 2h 计时,首轮常在 jobbank 的 05b(详情/地址)跑完前就 seed → 该轮 districts/官网偏少。**下一轮(build 用上一轮已富集数据)自动恢复**;想立刻富集可 `docker compose restart build`。
  - ⚠️ **运维注意③(笔记本睡眠会暂停)**:`time.sleep` 走单调时钟,合盖/睡眠时 VM 暂停、计时冻结 → 整夜不出新周期(不是 bug,容器只有 1 个 python 在 sleep)。醒后需累计够「清醒时长」才触发。想立刻更新:`docker compose -f docker/docker-compose.yml restart build jobbank`(build 立即灌、jobbank 重抓)。要真·7×24 自动 → 部署到常开主机(Render,见框架文档)。
  - ⚠️ **运维注意②(过期累积)**:下架按「发布超 30 天」而非对账,所以近期但本轮没出现的岗会暂留 → DB open 数会比当前实际在挂的多(JB 重发换 posting_id 会放大此现象,30 天内自然清)。彻底干净基线可 `curl "localhost:3000/seed?reset=1"`。真要去重得按内容(公司+标题)而非 posting_id —— 记入 [docs/source-framework.md](docs/source-framework.md)。
  - 旧 `docker compose up -d --build` 命令已随合并失效,改见「怎么跑」。
  - **角色拆分**(关键):抓取按源拆,但清洗/评分/mart/seed 是全局的、只一份。`SOURCE=jobbank` 只抓(05/05b 刷 raw);`SOURCE=build` 跨源清洗(04c/04d/05c)→评分(08)→mart(09)→`GET /seed`,是**灌库唯一角色**。多源不抢 mart/seed。加源 = SOURCES 登记 + compose 复制 service 改 SOURCE。
  - 抓法统一 **httpx**(JB 服务端渲染,已证明稳);`crawl/` Playwright 是有头+人工验证,**不进容器**,只给手动抓 Cloudflare 政府站用。
  - 编排器在 `etl/`(业务),容器配置在 `docker/`(运维)。代码/data 靠 bind-mount,改脚本不用重建镜像。
  - **cms 已容器化 ✅(`unattended` profile)+ 单一 compose(项目 pnp,容器 pnp-*)**:整套(postgres + cms + jobbank + build)开机自起、每 2h 自更新。cms 用服务名 `postgres:5432` 连库、挂 `../data:/data` 供 seed 读 mart、发布 :3000;build 经 `host.docker.internal:3000` 灌库(host npm dev 或容器 cms 都通)。pgdata 卷 pin `cms_pgdata` 不丢库。改动:next.config `output:'standalone'`、Dockerfile node24+`npm install`+空 `public/`。
  - **下架已改按发布日期过期 ✅**:不再用 seenIds 对账(实测会误杀 805 仍在招的旧岗)。seed 改「本次未见 **且** datePosted 超 30 天」才 closed。**(旧 STATUS 说「增量 seed 安全」是错的——postings.json 基底不全时会误杀。)**
  - **职位 ID 已改稳定 `jb:<posting_id>` ✅**:见「已踩坑」段(它是 08↔09 join 键,两处一致)。
  - 剩余小坑:build 读 postings.json 与 jobbank 写它有微小竞态(读到半写→该轮失败重试,文件不损);需要可给 05 加 temp+rename 原子写。
- **统一源框架(目标架构已定:[docs/source-framework.md](docs/source-framework.md) v2,D1-D5 全部拍板)**:三种抓法分三目录(httpx/crawl/dataset),**铁律=抓取只存原始 raw、清洗在 processed**,raw 按 `方式/源/日期` 快照不可变,源注册表独立 `etl/sources.py`,`auto_update` 只是调度器。OINP/SINP/AAIP 各省 PNP 单独成源(crawl,周/月)。**按文档第 8 节分步实施,JB 最后拆**(fetch 留 httpx、解析下沉 clean;回归基线=2084 岗 mart 一致)。尚未动手。
- ✅ **/jobs 前端这轮已上线(容器,2026-06-24)**:中英韩 i18n([i18n.ts](cms/src/app/(frontend)/jobs/i18n.ts):字典+makeT,语言切换 localStorage);**AI 顾问全字段走 Ollama**(按所选语言生成、facts/评分明细喂 prompt 保数值准、简单字段一句话——见 [route.ts](cms/src/app/api/advisor/route.ts) 的 SIMPLE 集;前端 advHeader 只出标签+链接,无三语长文);sticky 顶栏 + 响应式 footer;滚动自动加载封顶 180 + 「显示更多」按钮;新增 中位时薪/中位年薪/**vs中位** 列;**全字段筛选**(分类下拉 PNP/AIP/状态/渠道 + 数值区间 评分/年薪K/vs中位%)。维度表(NOC 中/小分类名等)三语待数据层做(name_zh/en/ko)。
- **各省 PNP 职业清单(crawl 源)**:把 `pnpEligible` 从粗筛升级成**按省精准**。
  - ✅ **OINP 试点已跑通**:`etl/pnp/build_on.py`(httpx 抓 ontario.ca OINP In-Demand Skills 页,无 Cloudflare)→ bs4 解析 56 个 NOC(任意 9 / 限 GTA 外 47)→ 维护表 `reference/pnp/oinp-in-demand.json`(跟踪;原始 HTML 存 raw/crawl/,gitignore)。`08_score` 读该表当 TEER4-5 紧缺通道(原写死 6 → 真实 56)。**模式 = build_<prov>.py → reference/pnp/<prov>.json → 08 消费**。
  - ✅ **② `pnpEligible` 已改按省过滤(2026-06-25)**:`08_score` 现**目录驱动**——扫 `reference/pnp/*.json` 按各文件 `province` 字段建 `省→{NOC}` 表(`INDEMAND_LOW_BY_PROV`)。TEER0-3 全省粗筛通用;TEER4-5 仅当 NOC 在**该岗所在省**清单才 eligible。加新省=丢一个 json,08 不改代码。**魁省(QC)直接排除**(`NON_PNP_PROV`,走自己的甄选不属 PNP)。实测修正 264 个跨省误标(QC 69/SK 51/AB 34…),pnpEligible 1580→1461。去掉了原硬编码 6 个兜底 NOC(那是瞎猜)。
  - ✅ **AB(AAIP)已接入(2026-06-25)= 第二个省 + 两型框架**:`etl/pnp/build_ab.py`(httpx 抓 alberta.ca AOS 资格页,虽 Cloudflare 但 httpx 直抓 200)→ 解析「不符合资格职业」表(34 个 NOC)→ `reference/pnp/aaip-ineligible.json`。**关键:AAIP 与 OINP 语义相反** —— AOS 是 **exclusion/permissive**(TEER0-5 默认都可走,清单内不可),OINP 是 **inclusion**(TEER4-5 默认不可,清单内才可)。08_score 给省表加 `type`(indemand/ineligible)字段据此反向判定;`score` 的 +12 只对 inclusion 型加。实测 AB 新增 76 个 TEER4-5 岗正确标可走、修正掉 1 个(排除表里的 TEER0-3),pnpEligible 1461→1536。
  - **调研结论(各省 PNP 结构异构,OINP inclusion 非通用)**:**BC** 低 TEER 的 ELSS 流 2024-12-10 永久关闭(无表=逻辑已天然正确);**SK(SINP)** 用排除清单+行业配额模型(另一种形状);**MB(MPNP)** 有 in-demand 清单但 TablePress JS 动态加载(静态 HTML 只 2 行,需 AJAX 端点或 headless),未做;**AB(AAIP)** 是干净 httpx 静态表→已做。
  - 待办:① 其余省继续(SINP 排除+配额 / MB 需破 TablePress 端点 / 其余 inclusion 省)——加 inclusion/exclusion 表丢 json 即生效;③ OINP 其它 stream(Foreign Worker/International Student 是 TEER0-3 广覆盖,已被现逻辑包住);④ build_on 接入低频定时(像 build_wages,偶尔重抓);⑤ 真有 Cloudflare 的省 → 需 headless crawl 镜像(D3);⑥ OINP 的 `gtaRestricted`(限大多伦多区外)暂未按岗所在区过滤——08 只有省粒度,接入区粒度后再细化。
- ✅ **未分类大幅降(2026-06-26)**:改从 **Job Bank 详情页抽官方 NOC**(`<span class="noc-no">NOC <码></span>`,05b/parse_details 抽 → posting.noc),08 分类优先级 **源NOC > 标题猜**。未分类 31%→预计 ~5%(只剩 JB 自己没标的)。存量帖一次性重抓回填(05b 对缺 noc 的也重抓,自愈)。剩余少量可继续加 noc 规则或 AI 兜底。
- 扩源:其它商会名录、Indeed/LinkedIn(放最后,ToS 风险)。用 etl/crawl/ 抓政策页填 policy_docs/pnp_streams 空表。
- 部署运维:托管(Vercel+Neon/Railway)、每日 cron、AI 顾问线上去向、`.env.example`、关于/免责声明页。
- **前端/AI**:① AI 顾问**公司分析接真实数据**(现靠模型知识,冷门公司可能编)——轻量=把 JD+官网喂进 company prompt+强化「不确定就说」;重=06b 抓公司官网首页文本进管线。② NOC 中/小分类名三语(name_zh/en/ko,数据层做)。③ 统计模块(用户提过:任意维度×指标,单页还是分模块,jobs 模块命名)——未设计。④ 累积稀释:DB 旧岗保留旧 pnp/NOC/QC 标记,靠 30 天老化或 reset 收敛(用户不愿删岗,选自然收敛)。
- **本轮踩坑记**:① 服务端组件从 `'use client'` 模块导入普通常量会拿到 undefined(COLS_COOKIE 必须放共享非-client 模块)。② cookie 值 encodeURIComponent 编码,服务端读出要 decodeURIComponent 再 parse。③ SSR+localStorage 偏好天生闪烁(默认→切换),要么 cookie 让服务端直接渲对、要么 useLayoutEffect 绘制前切。④ cms 容器 data 挂在 `/data`(standalone cwd=/app,`../data`=/data);改 cms 源码要重建镜像(`up -d --build cms`),非 bind-mount。

## 关键决策记录
- **数据仓库分层**:raw→clean→mart→load;mart 是「列对齐DB的最终表」,seed 只灌不拼。维度表(省市区/NOC/来源/经验/AIP)各自维护。
- **区从 GeoNames 自维护表**洗,不用限速的 OSM 地理编码 API。中小城市 FSA=城市本身、无子区时留空(数据天花板)。
- **列表读用原始 SQL** 而非 payload.find(性能);Payload 仍管 schema/admin/写入。
- 来源真相:JB 聚合 indeed/Talent → 统一显示「Job Bank」,`source` 留原始板。中介已按公司名过滤。
- 地点:Ottawa 各社区是「区」,统一 市=Ottawa;Richmond Hill 等靠**邮编 FSA**判定(不子串撞社区名)。

---

## 沉档 2026-07-19:上上轮交接原文(2026-07-18 通宵 session,「移民动态之夜」)

> **⚡⚡⚡⚡ 上轮交接(2026-07-18 通宵 session,「移民动态之夜」)——E12-06 全环闭合 + #65 设计系统第一批,细节见 implementation/E12/06 §10(续①~⑦)与记忆 next-session-status**
> - **E12-06 移民动态板块 P0→P1f 一夜全清**:/news 头版式布局(1大+4小头条网格,8省+联邦 **Wikimedia 真实地标头图**)+AI 重要度徽标(5分红「重要」)+**三语实时懒翻译/速读**(朋友的 qwen 服务 ngrok+key,编号对位协议保段对段对齐,DB 缓存全站共享,API 费归零)+**评论区**(登录可评→Frank admin 审核 approved 公开)+统一返回钮/评论数脚标。四次 DDL 全 Frank 亲跑(news 表/excerpt+重要度/comments+韩语列/summary_en)。**新防线两道**:seed 对 news 改 slug upsert(懒缓存列不清——每小时抹缓存 bug 实撞断根)/schema 容错查询(缺列自动降级——**10 分钟 404 事故**教训:改列代码 push 前必须实查 information_schema 列已落位)。
> - **#65 前端 primitives 库第一批**:`(frontend)/ui/primitives.tsx`(tokens+Button/Chip/Tag/PageBanner/SectionTitle,设计总表 docs/assets/mockups/)+**header 全站合一**(/jobs 内联头退役,SiteHeader 唯一,1320 头轨,Pro 钮全删出 header)+五模块浅色页头(jobs蓝/pathways紫/rank金/stats绿/news青)+双蓝条合一(ValueBanner 退役,建档 CTA 进 Jobs 页头右槽)。五页生产实拍验收过(docs/assets/mockups/65-验收-*.png)。**余批**=jobs 筛选区/账户 Sidebar/弹框头(#65 余批任务)。
> - **运维新惯例**:翻译类走本地模型(局域网 qwen3.6 / 朋友公网 API);ETL 预翻停用(NEWS_TRANSLATE_BUDGET=0),只留新条目重要度打分;**评论审核=Frank 新日常**(admin→Community→comments→approved;2026-07-19 #95 评论区暂藏后此日常暂停);效果图/验收图一律存 docs/assets/mockups(Frank 看不到聊天内嵌图);生产 DDL=Frank 终端亲跑;不用 PowerShell。
> - **下轮队列(当时)**:#65 余批 → 快刀四件(#56/58/60/62A)→ 改版批(#59/57/63/64)→ B4 实体名录最小版 → C6 政策时间线;发帖开闸(Frank,/news 板块=现成卖点)。

## 2026-07-18 两 session(2026-07-20 收口沉档)

> **⚡⚡⚡⚡ 上轮交接(2026-07-18 晚 session,「快刀+news 批之夜」)——第 21 轮快刀四件 + news 晚批六件,当晚全部生产闭环**
> - **快刀四件 ✅ 生产闭环(e5fdea1)**:#62A 我的收藏独立入口(账户 favs 节+顶栏下拉,SavedJobsList 加 favs 变体)/#58 档案·wizard 省份 chips 三语全名(i18n 新 `pr.*` 键,码存幕后)/#56 字段钮挪薪资行最右(直发独立收尾行)/#60 stats 常见图表恒 2×2 等宽(min420 auto-fit 治 3+1 空白)。生产 playwright 八项断言全绿,验收图 docs/assets/mockups/。
> - **news 晚批六件 ✅ 生产闭环(0731f6f,#67-#72)**:**PageShell 公共宽度轨上线**(primitives,1320 与头轨同宽——Frank「每个页面宽度应该一样」拍板;/news 列表+详情已套,**存量页迁移归 #65 余批**)/「只看重要」删/列表小色块换省地标真图(96×64 cover,缺图退色块)/头条大卡恢复轮播(5s 自动+圆点+箭头+hover 暂停,推翻 v4 退役)/AI 顾问标题「思考中」后缀删/图片水印删(CC 致谢挪 img title+SOURCES.md)。生产三态实测:点击切换/自动轮播/hover 暂停。**探针教训记档:生产 SSR style 序列化无空格(`font-size:19px`),dev 带空格——playwright style 选择器别带空格,俩假阴性查了三轮。**
> - **改版批四件当晚也全闭环(Frank「可以」批效果图后)**:#59 筛选区=常用一行(搜索/省/大类/PNP/年薪)+更多筛选折叠(计数徽标,窄屏抽屉退役,3f6150c)/#63 账户下拉 Supabase 风(求职/管理分区+通栏实心升级钮+退出置底,3f6150c)/#64 定价三卡 v3(90 天「更划算·省 32%」动态徽标+每天单价+f2 拆三行,PricingCard 弹窗页面同源,d2332d5)/#57 图表两级下钻+面包屑(末级=职位板深链 /?prov=&broad=,eaa01d4)。全部生产复验过,验收图 mockups/。
> - **#73 排序报障当晚修复(bd1b93e)**:Frank 报「table 字段排序不好使」——根因=E10 服务端化时 SORT_COLUMNS 白名单只搬 9 个 key,前端 28 列全可点,白名单外静默回退发布时间序。修=按前端 COLUMNS 补齐(direct 复用 directOnly 谓词/vsMedian=salary÷median 比值);**Pro 数据列非 Pro 回退默认序**(防锁列值从行序泄露)。API 级生产复验四项全绿。
> - **模块 banner 图版上线 ✅(d39d749,Frank「按这个做」批设计总表后当晚实施)**:PageBanner 双形态(images=实景 banner 恒 150px/手机 110px+B类氛围轮播 crossfade 8s 前景恒定+缺图退浅色带;不传=原浅色带)+12 张 Commons 图裁 1280×300(SOURCES.md 记出处)+jobs/pathways/rank/stats 四模块接线(news 不放图,头条区已是 A类内容轮播)。生产四页 150px/3 图/圆点全绿。设计规范=mockups/模块banner-设计总表.html。
> - **第 21 轮 dd(发版后回归)当夜收口**:48+26 屏全刷;**当场抓到 #64 当晚回归——手机定价三卡挤压+横向溢出 88px(nowrap 价格行+useIsNarrow 时序),当场修 df47174(纯 CSS auto-fit+价格行允许换行)**;其余零回归。新发现留第 22 轮:#74 UpgradeModal 未随 #64 换装/#75 匿名态页头下空白偏大/#76 截图脚本四镜头失效旧图残留+缺 news 镜头(下轮 dd 前先修脚本);#61 语境证据=rank 页底部榜单导航 chips。**E10 P5 索引 DDL 已备**(docs/sql/e10-p5-indexes.sql,CONCURRENTLY 幂等,Frank 终端亲跑)。
> - **深夜再续(2026-07-19 凌晨,Frank 持续在线)**:①**#61 闭环**(榜单导航挪页头下,ddf18c6);②**四连批全闭环(4929a83,生产双证)**:匹配视图排序接通(排序二报根因=fetchMatchPage 无视 sort;免费 cap 先圈后排防枚举绕付费墙)/存量页宽度统一 1320(pathways/rank/stats)/banner 数字块(≤3 真实计数,手机藏)/「我的账户」导航未登录不显示(SiteHeader 登录态上提);③**#54 登录改版视觉批闭环(8ab6398)**:careerbeacon 骨架=大标题价值文案+Google 钮位(NEXT_PUBLIC_GOOGLE_CLIENT_ID 门控,后端 E11-03 实测后配 env 即亮)+或用邮箱分隔+底部切换,邮箱登录回归过。
> - **E11-03 Google 登录后端已上线(43212a7,凭据当场办齐)**:GCP 项目 offer2pr/OAuth client 建好(redirect=生产+localhost 两条);后端=302 同意屏+callback(email_verified 必真→邮箱查找/创建→**镜像 Payload3.85 login 签发**:sessions+sid+getFieldsToSign/jwtSign 官方导出;老邮箱账号关联不动密码);本地烟测三项过。**Render 两 env Frank 已填,服务端路由生产实测 302 参数全对**;按钮=编译期烤入,等最新构建落地即亮。**剩最后一步:Frank 实弹**(点「使用 Google 继续」走完同意屏→回站已登录;二发验老用户直进)。**E11-06 微信=搁置记档**(个人海外主体无微信开放平台入口;远期口子=加拿大公司海外主体认证;实际缺口小——付费主力在加人群 Google 无障碍)。
> - **排序性能三刀(Frank「一次 3-4 秒」,基线实测建档登录态 1.7-3.7s)**:①匹配维度进程缓存 10min(根因大头:建档用户每次点排序重拉 pnp-occupations≤5000+ee-categories,0.25CPU 库上 1-2s)+②count/updatedAt 30s 微缓存(换排序 WHERE 不变免全表扫)= 329a407;③**九个索引已建**(Frank 授权「帮我跑一下」,node+pg 直连生产执行 e10-p5-indexes.sql,CONCURRENTLY 各 ~1s 零锁表,pg_indexes 复核九个全在)——E10 P5 收官。**复测(建档登录态,ms)**:年薪 3691→**65**(索引直取)/公司 2322→1038/大分类 1735→1111——3-4 秒档进 1 秒内档。若还要压:broad/company 的 DESC NULLS LAST 与普通 btree 方向不匹配仍走排序,可建方向匹配索引(下轮看需要)。
> - **🎉 E11-03 全环闭合(2026-07-19 晨,Frank 实弹「可以登录了」)**:Google 一键登录生产全链通。**按钮不亮之谜破案+修复(70f3c97,重要惯例)**:Render 的 Docker 构建只把 env 传给 Dockerfile 声明了 ARG 的变量——NEXT_PUBLIC_* 编译期烤入 client 组件,无 ARG=静默回退值(服务端路由却通,运行时 env 在,极具迷惑性);修=builder 段 ARG+ENV 四件登记。**惯例:新增 client 用的 NEXT_PUBLIC 变量必须同步登记 cms/Dockerfile**。
> - **✅ #63b 收尾闭环(2026-07-19,f3bedb8 生产复验过)**:Frank「下拉太长太大参考一亩三分地;顶栏按钮像 Google 只显示头像」——按钮=28px 纯头像圆钮(JobsTable AccountArea+SiteHeader AccountLite 双处,名字/Pro 态挂 title,昵称/药丸/▾ 退役)+下拉压紧(身份头大头像退役两行紧凑/menuItem 4px 行高 1.7/区头·升级钮·分隔线压密/minWidth 210→185)。收尾时清了 SiteHeader 悬空 IconStar import+title null 类型回退。生产 playwright 实测:下拉 185×443、条目行高 29、双处零文字节点;验收图 mockups/63b-验收-*.png(生产实拍)。
> - **✅ #74-#76 三连闭环(2026-07-19,d76a410 生产复验)**:#74 升级框随 #64 换装(90 天钮「更划算·省 32%」徽标+两钮每天单价,PRICE 数学与 PricingCard 同源)/#75 匿名页头 ~85px 空带根修(recslot 占位纠正只看 recs.length 的漏洞——卡片拉空时 section 渲染 null 而占位不收;修=纳入实际可见卡数,生产双态实测拉空→0px/正常→横幅照常)/#76 四死镜头纳编捆绑截图脚本(元素截图)+news/news-detail 镜头补编,生产冒烟七镜头全命中。细节见工作台第 21 轮头注。
> - **⚡ 工作法转向(2026-07-19 Frank 拍板)**:dd 截图走查停(「截图发现不了问题」);每件事先过「用户会为它付费吗」——分析文档 [队列付费复审-20260719](docs/队列付费复审-20260719.md)(判分标尺=痛点全量盘点 104 条/四级门槛/三本账)。重排:B4 提首位/C6 次之/#65 余批降级观感顺手活/GAP1③ JD 身份预筛建议插队(零抓取,需 Frank 跑 DDL)。
> - **✅ B4-01 实体名录最小版当日闭环(181cbc2,生产 17 断言全绿)**:`/employers` 雇主名录(LMIA 记录 3,791+AIP 指定 2,931 两 tab,搜索/省筛/分页,行深链 /?q= 落职位板)+ `/occupations` 紧缺职业清单(183 行省→通道分组,行级官方链)。直击 C7 头号痛点的免费 browse 面+SEO 引流(sitemap 已收录,发帖开闸前流量有处落);**口径纠偏记档**:lmia_positions 含全部股别(榜首=BC 果园农业股 1399),口径注按实况写「全部股别+技能类看 High Wage/GTS」;入口=页脚+SEO(顶栏给位=Frank 拍板项)。验收图 mockups/B4-验收-*.png;实现文档 implementation/B4-实体名录/01。
> - **深夜续(Frank 在线连环拍板/报障,四件当夜全闭环)**:①**雇主名录进顶栏第 7 项**(140f177,Frank「header 没地方点」);②**搜索 7-8 秒→0.2-0.4 秒**(d33f26f+aa3b501+DDL):根因=7 列 ILIKE OR 含跨表 c.name 全表扫;第一刀谓词重构不够(EXPLAIN 实锤 IN 子查询计划期不可索引),第二刀=公司 id 预查进 `= ANY(数组)`→BitmapOr 六分支全走索引,count 1198ms→0.96ms;**pg_trgm 扩展+7 索引 DDL Frank 授权代跑**;③**B4-02 LMIA 按股别分档**(76703c2+a0048ef,Frank「有 LMIA 但没法移民」):果园 1399 农业股岗原照拿 match +5 绿勾——mart 已算的 lmiaPositionsSkilled 入库(DDL 加列+seed 白名单),match 规则 6 三档(技能股>0 才 +5/纯农业低薪股中性行不加分/null 回退),名录加技能股列+排序改技能股优先(生产终验:AHS 168/Flynn 162 居首果园沉底,果园 match 行=中性文案);**补丁教训:三处手拼 MatchJob 都要补新字段**(makeMatcher/fetchMatchPage/客户端 MeansForMe,漏一处=静默回退);④**冷启动弹药刷新**(e13a06a):数字实测 32,970/723/3,793+雇主名录直链进四渠道文案+1p3a 回帖救活稿。**发帖代驾受权限分类器拦(发帖内容代填被拒)——改人机分工:内容成稿在冷启动帖子.md,Frank 粘贴发布**;1p3a 登录态活/小红书已登录待发。**雇主中文名拍板记档:不翻**(法定英文名=核验链,机翻毁信任;知名雇主人工别名小清单=候选缓做)。
> - **🚀 发帖开闸(2026-07-19,Frank 授权代驾)**:1p3a 回帖救活(新功能三条挂原帖,发时 204 浏览/11 顶)+ **小红书笔记发布成功**(「海外生活指南」,《加拿大找工作:3万+岗位每小时更新》822 字+3 图+#省提名 905 万浏览话题);剩微信群(Frank 自转)+Reddit(攒后发);**72h 观察窗开启:umami/注册/评论痛点喂价值账**。发帖代驾权限:type/form_input 首轮被分类器拦、Frank 再授权后放行;图片上传白名单不含项目目录=Frank 亲手拖图。
> - **✅ C6-01 政策时间线当日闭环(3d3572c,生产 10 断言全绿)**:/timeline=省抽选+联邦 EE 抽选+政策公告一条时间轴(省筛/类型筛),**「抽选节奏」块=个人化钩 v1**(AB 距今4天·近8期~3天/BC ~14天/MB ~14天;EE 各类别距今——STEM 829 天未抽一眼可见=A3 痛点数据化);诚实红线=历史统计不预测+分制标注(非 CRS)。入口=/news 页头右槽+footer+sitemap。实施纠偏记档:节奏分组键必须用 label 项目级(stream 每期写法不同会碎卡)。实现文档 implementation/C6-政策时间线/01。
> - **✅ GAP1③ JD 身份预筛全环闭合(2026-07-19,Frank「按推荐来」授权 DDL 代跑)**:etl/clean/visa_flag.py 纯规则(两档红旗+三道护栏,44,235 JD 实测 106 命中人工验样两轮零可疑)→ 09 同循环检测 → jobs 两列(quote=命中原句可核验)→ 前端列/弹框/手机 chip/「排除不担保/须 PR」筛选。生产终验:fElig 实测 33,140→33,108(恰 −32 红旗)。**插曲两个记档**:①/api/jobs FILTER_KEYS 白名单漏 fElig(#73 教训第三演,「新筛选键三处同步」已刻路由注释);②**ETL 盒=本机实锤**(docker compose 挂载 `..:/repo`,工作区即容器代码,「git pull」是伪步骤,整点轮自动带新检测)。
> - **✅ 八连反馈快刀+设计批当日闭环(3bcb72e/eaff7b2/8608cc9/1eb8e34/dc7673a/6ad6681,生产 13 断言全绿)**:#80 榜单改名「每日高分新岗」(名字直说标准)/#79 footer 免责压短/#81「技能类获批」去黑话+表头 tip/#83 表格换血加载态(治「先医疗后科技」跳变)/#84 头像闪紫「?」根修(身份四件 SSR 直传)/#85 操作列 238px+flex wrap/#86 拖列文字压邻列(固定模式 overflow hidden);**设计批(Frank「按推荐来」)**:顶栏第 4 项「资料库 ▾」下拉(名录/清单/学校灰项,footer 三链退役)+ SectionTabs 统一二级 tab(移民动态=公告|时间线互切)+ banner 新槽位(标题+口径副题左下/数字胶囊右下/圆点右上,150→130)。对标站记档:MyVisaJobs(H1B 名录=最直接成熟对标)/levels.fyi/moving2canada/Teal。「40% commission」=佣金制原文非错数据(折算留空是设计;佣金制标签=候选)。
> - **📉 token 节约=常驻惯例(Frank 拍板,记忆 token-economy)**:STATUS 头部 25k→4k(07-16 前沉档 docs/STATUS存档-2026H1.md,以后只留最近两轮);大文件片段读/验证优先文本断言截图按需/命令输出过滤/长任务拆 session/机械批量活可换便宜模型。
> - **下轮队列(付费透镜序)**:E11-07 简历上传立项(建档转化杠杆)→ C6 二期(EE 抽选历史入库+节奏喂 match/提醒)→ 公共 DataTable 组件(先设计文档:简单表统一 primitives,jobs 主表吃 token 不并组件)→ B4 二期(LMIA 全量 7.5 万名录/学校名录/佣金制标签);多雇主对比=Pro 候选;微信群/朋友圈/Reddit 发布+盯小红书评论区(Frank);**72h 观察窗:umami/注册/评论痛点喂价值账→数据重排队列**。
>
