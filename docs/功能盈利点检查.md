# 功能盈利点检查(迭代式整改 · 第 39 轮 · 2026-08-15 · 清单 = #301-#326)

> **迭代工作台文档**。工作法:体检/走查 → 统一检查 → 逐条整改意见 → 实施 → 下轮复查闭环。
> **轮次记录**:第 1-38 轮沉档至 [整改轮次存档.md](整改轮次存档.md) 与 [整改轮次/](整改轮次/);
> 逐图整改意见块(电脑 图 1-22 / 手机 图 M1-M20)沉档至 [整改轮次/图注存档.md](整改轮次/图注存档.md)。
> **工作台只保留当前轮**。
> **当前轮次**:**第 39 轮 2026-08-15 —— 只收集,不给方案,不动代码**(Frank:「你先收集下问题吧」
> 「你不需要给方案,只需要把问题列清楚」)。
> - **触发**:同日夜场十连提交(通道策略分文件 + 五类闸 + FCIP 立成第 14 条 + PR 排序补地理成本)之后,
>   Frank 在决策页连拍三问 ——「为什么安省更有优势???」「关键是分数线怎么样,有多少人排队啊」。
> - **手法**:① 体检脚本全量(两视口 × 三语 × 匿名/测试号,554s,481 命中);② 生产实拍核真伪;
>   ③ 判定/排序/题库三层的代码与 mart 盘点。
> - **上轮遗留**(2026-08-15 深夜生产复查刷新):#298 仍 ⬜(现为 56×56 收起圆球,fixed y=740 仍压右下);
>   #299 仍待拍板但**前提已变**:375 实测中文 3111px / 英文 3628px(38 轮时 5167),英文只比中文长 17%;
>   #300 仍 ⬜(全站批)。#294 的「/plan/pr 还剩两条」尾巴**已销**:`dp.planHint` 成死键无引用、
>   `dp.verifyJobHint` 键已删、生产 DOM 两句文案均无 —— 被 08-12~15 重构批顺手带走;剩 i18n 三语死键三处可清。
>   38 轮已销各项(#291/#293/#295/#296/#297)生产复测全部保持:两端截断 0、处境页英文态可见中文 0。

## 本轮清单(第 39 轮)

### A · 判定与排序层

| # | 级 | 问题 | 证据 | 状态 |
|---|---|---|---|---|
| 301 | **P1** | **分数线与排队是 12/14 条通道的盲区。** 估分器只有 CRS(FED-EE)与 MB 两个,其余 12 条算不出用户的分;本通道抽选线只有 FED-EE(12 轮)与 AB(1 轮)对得上,BC/MB/NB/ON 设了 `drawStream` 却 0 轮匹配,六条压根没设。而 ON 51 行、SK 37、BC 32、AB 30、NL 28 的官方分值表**已在库里没接**。于是 `belowLine`(唯一反映「够不够得着」的排序规则)**实际只对联邦 EE 生效**:数据缺的省永远不会被判「排不上」,数据全的省反而吃亏 | mart 盘点:逐通道扫 `drawStream` × `pnp_draws` × `pnp_score_factors` | ⬜ |
| 302 | **P1** | **省外「更优」提示与排序不是同一把尺。** 地理成本(本省优先 = 现居省 ∪ 学历省)只在客户端 `PrDecisionView.tsx:608`;省外提示在服务端 `route.ts:192` 只比档位、不认地理成本。实拍:人在阿省、学历在阿省、正在阿省读书,页面下方仍劝「将安大略省加入目标省」 | Frank 实拍(目标省=AB 两行表 + 底部提示);`3ba38933`(服务端提示)与 `395a5744`(客户端本省优先)同晚一前一后 | ⬜ |
| 303 | **P1** | **「更优」名不副实。** 判据只比「差什么 + 差多久」,不含分数线、不含排队规模、不含搬省成本。安省名额竞争 67.1:1、阿省 34.7:1,队伍长一倍仍被称「更优」;而措辞配一键「加入目标省」,读起来像零成本 | 竞争度定义 `route.ts:44`(该省临时居民存量 ÷ 当年省提名名额) | ⬜ |
| 304 | **P1** | **没有 offer 的人仍被问 offer 派生加分题,勾了真加分。** AB 分值表 30 行里 `offer / offerSector / offerArea / regulated` 四族全以「有 offer」为前提;`rowApplies`(`PnpScoreCard.tsx:250`)只按 NOC 过滤、不看 `hasOffer`;`bonusPoints` 纯按勾选加分 → 最多虚增 21 分 | Frank 实拍条件卡:「手上的 offer 没有」与「加分项 受监管职业 offer 且已持本省执业资格 = 是」并列 | ⬜ |
| 305 | **P1** | **分值卡加分题不读基础卷已有答案**(#304 的同构面)。阿省六道加分题里:「学历完成地」重复(基础卷已答学历所在省=阿省)、「加拿大经验所在地」恒假(已答加拿大经验=没有)、「英法双语 CLB/NCLC 均 4 以上」恒假(已答不会法语)、三道 offer 类恒假 —— **真正新问的只有「本省有父母/子女/兄弟姐妹」一道**。基础卷本来就有 `visible`/`choiceVisible` 门控(`fields.ts`),分值卡没接 | 同一张条件卡:已答 15/21,阿省 6 项里 5 项「待填写」 | ⬜ |
| 306 | P2 | **tier 文案会被读成递交或下签时间。**「offer 后约 3–6 个月」实际是「攒够门槛所需」;此后的 EOI 排队 → 邀请 → 省提名审批 → 联邦 PR 审批四段,**一段都没建模、也没提示** | Frank:「你安省 3 个月递交了?但是排队好几万人在前面等着呢」 | ⬜ |
| 307 | P2 | **排序口径散在三处。** 引擎 `obstacle()`(障碍难度 + 在读学生工签降本)、服务端 `bandOf` + 竞争比、客户端(0 岗沉底 / 岗少沉底 / 本省优先 / 岗数降序)。没有单一来源,加一条规则就多一处分叉 —— #302 即由此产生 | 三处代码同页并存 | ⬜ |
| 308 | P3 | **`verdict='open'` 命名歧义。** `open` 表示「没有判不了的项」,差一道闸也是 `open`(靠 `blockedBy` 区分)。任何人单读 `verdict === 'open'` 都会把「差一份 offer」读成「能走」 | 海外零条件档案实测:10 条通道 `verdict='open'`,全挂 `blockedBy` | ⬜ |
| 316 | **P1** | **`eduYears`(学制年数)全站从没问过,路由写死 `null`**(`route.ts:106`)。后果两处:① 安省「近 3 年安省毕业 + 2 年制以上文凭 → 经验门槛 3 个月」那一档,`conditionHolds('recent-on-graduate')` 恒返回 null(判不了),**所有安省毕业生都退到通用 6 个月档**;② 曼省学历分 `mbEduYears` 取 `(p.eduYears ?? 0) >= 2 ? 2 : 1` → 恒按 1 年制算,**2 年制以上的曼省毕业生系统性少分** | `fields.ts` 全文无 eduYears 题;`route.ts:106` 写死 null;`pathVerdict.ts:259/396` 两处消费它 | ⬜ |
| 317 | **P1** | **NL 国际毕业生对「省外院校毕业生」少判一条硬条件。** 官方对非 NL 院校毕业生另有要求:先在 NL 工作满 1 年、且 offer 与专业**直接**相关。这条只写在策略文件的 `note` 注释里,**没入库、没成闸**。实测阿省学生(阿省学历)拿到 `NL-intl-grad tier=0` —— 即「拿到 PGWP 就能走」,而实际还要在 NL 干满一年。tier=0 让它在排序里非常靠前 | 生产接口实测(木匠/在阿省读书/阿省学历/0 经验):`NL-intl-grad/NL v=open block=statusInCanada tier=0`;条文出处见 `nl-intl-grad.ts` 的 note | ⬜ |
| 318 | P2 | **被判死(excluded)的通道整条不显示。** `route.ts:140` 把 excluded 全滤掉,同一份档案不限省时返回 24 行、**没有 FED-EE**。用户因此拿不到「联邦 EE 走不了、因为加拿大经验为 0」这个结论 —— 而这恰恰是最常被问的一条 | 同上实测:24 行里无 FED-EE;`route.ts:140` `open = scoped.filter(v => v.verdict !== 'excluded')` | ⬜ |
| 319 | P2 | **「约需积累 24 个月」对在读学生的起算点是错的。** 学签在读者攒不出「全职」经验,这 24 个月要等毕业、拿到工签之后才开始;页面读起来是「从今天起 24 个月」。与 #306 同族但更硬:#306 是缺后段,这条是**起点就不对** | 实测档案:`currentStatus=studying` + `AB-opportunity tier=3`;AB 门槛行原句「24 months of full-time work experience … within the last 30 months」 | ⬜ |
| 320 | **P1** | **AAIP 经验门槛的官方「或款」被压平,阿省本地在岗者等待时间高估一倍。** 官方同一句条文给了两条路:「24 个月境内外经验(近 30 个月内)——**或阿省境内 12 个月(近 18 个月内)**」,mart 只建了 `value=24` 一行,12 个月替代款只活在 label 文字里。持 PGWP 已在阿省干满 12 个月的人官方已达标,引擎仍判「还差 12 个月」;与 #319 同族但独立(#319 是起算点,这条是**门槛数值本身取错了枝**),恰好砸在本站主画像(阿省在读/PGWP)身上,拖低 AAIP 档位与排序 | 本地 API 探针实测(木匠/PGWP/阿省 12 个月经验/目标 AB):`AB-opportunity tier=2`(应为达标);门槛行 label 原句含「alternatively 12 months in Alberta within the last 18 months」而 `value=24`(`data/mart/pnp_requirements.json` AB 行) | ⬜ |
| 321 | P3 | **带岗态整个隐藏两张竞争卡。** `?job=` 进入时「该职业分省竞争」「各省名额竞争」两卡不渲染(`PrDecisionView.tsx:1189-1190` `!tvJob &&`),从职位点进来的用户全程看不到竞争数据(初评表内的名额竞争列还在)。是否有意,待拍板 | 代码实证 + Frank 带岗态实拍(无两卡) | ⬜ |

### B · 展示层(体检 481 命中,判真后)

| # | 级 | 问题 | 证据 | 状态 |
|---|---|---|---|---|
| 309 | **P1** | **中文界面榜单卡主标题是英文官方名,人话中文名被降成灰字。** `Food counter attendants, kitchen helpers and related support occupations` 14.5px 蓝链、折两行;「餐饮服务员」11.5px 灰字副标题 —— 与站规「人话名主文案 + 代码灰字小注」正好反过来,也是 R5 折行 81 条的主因 | 生产实拍 `stats-index/375/zh` computed style | ⬜ |
| 310 | P2 | **折行 81 条。** 主因同 #309;另有 `/jobs` 雇主名(Vedder Mountain Veterinary Clinic)、`plan-*` 英文态 h2「Nomination competition by province」折两行 | 体检 R5,375 两语 | ⬜ |
| 311 | P2 | **匿名态每页一个 401。** `/api/account/answers` 在 `plan-pr / plan-job / plan-career / plan-province / pathways` 五页匿名必打 401,两视口三语共 **26 次**,console 全是红 | 体检运行时段:`http 401 …/api/account/answers` ×26(hydration 错误 0) | ⬜ |
| 312 | P2 | **同页事实重复 46 条。** `stats-index / pulse` 的导航行与页内 h2 同名同页(Sponsor employers hiring now / Occupation boards / Province overview / Recent draws / Market Pulse);`news` 页省份 chip 与正文重复 | 体检 R3,三语两视口 | ⬜ |
| 313 | P3 | **`stats-index` 375/中文 LCP 7.15s**(JS 仅 30KB,不是包体);次高 3.84s(ko)、3.65s(`/jobs` zh) | 体检 LCP 段 | ⬜ |
| 314 | P3 | **英文态弹框按钮属性残留中文。** `immig-modal` 的 `title=全屏展开`、`aria-label=全屏展开` | 体检 R2(属性),`en/1440` | ⬜ |
| 315 | P3 | **雇主名用无 `href` 的 `<a>` 当按钮。** `/jobs` 54 处、`match-view` 8 处;有 `onclick` 能点(**不是死链**),但键盘不可聚焦、不能新标签打开、爬虫不认 | 生产实拍:`href=null`、`cursor=pointer`、`onclick=true` | ⬜ |

### C · 数据与动线缺口(2026-08-15 深夜补录)

| # | 级 | 问题 | 证据 | 状态 |
|---|---|---|---|---|
| 322 | P2 | **安省定向清单未入库。** `pnp_occupations` 只收 7 省(AB/BC/MB/NB/NS/PE/SK),ON 0 行,判定卡对每个安省岗只能说「本站未收录安大略省的定向清单」;而 OINP 的定向材料(In-Demand Skills stream 职业清单、tech draws 定向抽选)**URL 已在 crawl 役里躺着没建行**。ON-workforce 资格本身不吃清单(官方原句 any NOC occupation),缺的是定向信号展示 | mart 盘点 ON 0 行;`data/crawl` manifest 含 `oinp-employer-job-offer-demand-skills-stream`、`oinp-tech-draws` 等 22 页 | ⬜ |
| 323 | P2 | **初评表缺「操作列」:选完省之后的动作没接上。** 用户选定方向后的自然下一步是 找在招岗 → 选雇主 → 发邮件/投简历,现在初评表只有「在招 N 岗›」一条链接,后面整段动线(挑雇主、邮件投递、简历)要自己摸去职位板 —— 与「简化用户操作的才收费」的代劳定位正对口 | Frank 2026-08-15:「选完省之后,肯定是找工作,选雇主,发邮件,投简历。这个是需要加一个操作列,把这些功能加进去的。我之前说的」;既有投递能力(邮箱抽取/mailto/落库)在职位详情页已通(第 24 轮 MA 系列) | ⬜ |
| 324 | **P1** | **「推荐原因」列对无 offer 用户整列「差 offer」,零区分度。** 没 offer 的人每条通道都差 offer —— 把全行共有的缺项当逐行理由,一列废话(仅 RCIP 行多出「社区雇主」四个字)。列的本职是**行间差异**:这条相对别条的独有信号(经验门槛更短 / 本省学历豁免 / 竞争更松 / 起步即达标…);全行共有的缺项(没 offer)整表说一次就够。与 #306(tier 文案歧义)同列同病,一起重做 | Frank 2026-08-15 实拍五行「差 offer ×3 + 差社区雇主 offer ×2」+ 原话「写差 offer 全他妈是废话」 | ⬜ |
| 325 | **P1** | **带岗态「可行通道初评」不认岗位语境。** 初评只吃档案(职业=档案职业、省=目标省),看哪份岗都一个样:看 NS 的岗、第一名照旧推安省;目标省为空时连「本表按目标省评估,本职位在{jobProv}」的错位提示都不触发(`dp.provMismatch` 只在 targetProvinces 非空时出);职业不匹配时(看 NOC 10010 岗、档案是木匠)整表评的是木匠,读起来却像在评这份岗。与 #302(地理成本不进服务端)同族,但这条是**岗位语境整个缺席** | Frank 实拍(NS financial administrator 岗 + 木匠档案:初评第一名仍 ON;「职业不匹配」黄标已出但初评表无任何岗位语境);代码:planCard 仅吃 answers,`provMismatch` 触发条件见 `PrDecisionView.tsx` | ⬜ |
| 326 | P2 | **「职位名」瓦片中文态裸英文。** 带岗态「本职位」卡职位名直出帖面原文(financial administrator),违「人话名主文案 + 代码灰字小注」。帖面标题无逐帖译文;可用的中文资产是 NOC 职业名(`noc_descriptions.title_zh` 已在库):方案候选=NOC 中文名做主文案、帖面原名降灰字,或接线上实时翻译([[local-llm-for-translation]] 公网后按需翻)——待拍板 | Frank 2026-08-15 实拍「职位名 financial administrator」+「这个需要显示中文名」 | ⬜ |

### 判为假阳性(记账,免得下轮重查)

| 规则 | 命中 | 判定 |
|---|---|---|
| R1 NOC 码无职业名 | 144 | **假阳性**:卡片里有中文人话名,只是被排成灰字副标题 → 真问题记在 #309,不另立编号 |
| R1 斜杠点号杂糅 | 10 | **假阳性**:「蒂姆·霍顿斯」是品牌音译里的间隔号,不是「·」当分隔符 |
| R10 死链 | 40 | **假阳性**:雇主名 `<a>` 无 href 但有 onclick,功能可点 → 降级记为 #315 |
| R1 TEER 无注 | 144 | **待拍板**:与 08-06「消费页 tooltips 全撤、靠列名自解释」冲突,不进本轮清单 |

### 本轮未发现问题的项

数字对库一致(页面 44529 = API 44529,差 0.00%)、hydration 错误 0、裸 i18n key 泄漏 0、
可见文本英文态残留中文 0(属性除外,见 #314)、5xx 0。

**素材**:`checkup-39.json`(scratchpad,481 命中 / 82 页态)。
