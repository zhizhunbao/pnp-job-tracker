# 未结问题清单(2026-08-03 session)

> 这一轮不是走查,是 **Frank 的两个真实案例**逼出来的:
> ① 朋友:亚岗昆木匠毕业、在安省、**中介说曼省有合作公司**,问「MB / SK / 海洋省哪个靠谱」;
> ② Frank 本人:IT、安省、1 年 AI 研文 + 想再读 1 年 cloud,问「继续读还是直接找雇主」。
> **新 session 从 §B 挨个做,按编号来。** 已完成的看 §A(别重做);两条新铁律看 §D。
> 配套文档:[数据资产盘点与不撒谎整改方案](数据资产盘点与不撒谎整改方案-20260803.md)、[智能客服-用朋友的模型](智能客服-用朋友的模型-20260803.md)

---

## A. 本轮已完成(勿重做)

### A1 数据补全:草原三省 + 海洋四省全覆盖

| 省 | 补了什么 | 数量 | 脚本 |
|---|---|---|---|
| SK | **主线排除清单**(OID/EE,PDF;官方原话「不在此表 + TEER 0-3 即可申请」) | 152 | `etl/pnp/build_sk.py` |
| BC | **主线排除清单**(指南 §3.11,**2026-06-13 起生效的新政策**,此前完全不知道) | 12 | `etl/pnp/build_bc.py` |
| PE | Occupations in Demand(**走官方指南 PDF** —— PEI 网页在 Radware 后面) | 8 | `etl/pnp/build_pe.py`(新) |
| NL | ITA 抽选批次(**海洋四省第一份抽选**;官方只发邀请数不发分数线) | 12 轮 | `etl/pnp/build_draws.py` |
| NL | 优先处理职位(免劳动力市场测试;**职位名文本非 NOC,不硬映射**) | 35 | `etl/pnp/build_nl.py`(新) |
| NS | 主线政策事实(主线不列职业 / OID 通道官方明示当前为空 / 2026-04-27 优先级 TEER 0-4 医疗技工) | 3 条 | `etl/pnp/build_ns.py` |

`mart/pnp_occupations` 444 → **616** 行;`pnp_draws` +NL。

### A2 判定层:不再撒谎

- `provListCoverage` 四态 → **五态**(加 `partial`),判据从「数行数」改成**逐省人工核对的主线口径表** `MAIN_LIST_COVERAGE`
- `exclusion` 拆两种措辞:**手上有排除清单**(SK/AB/NB/BC)→「查过排除清单你不在上面」带官方出处;**制度性无清单**(ON/NS/NL)→ 才是「不公布清单,按 TEER 粗筛」
- 病灶原文:生产对付费用户输出「Checked SK's published list: NOC 72310 is not on it」——**我们从没查过萨省主线**

### A3 卡③「选省份」重做

- **排序目标交给用户**:新增 `goalBand`(拿身份 / 先找工作),一题定目标函数。此前助手写死过两版(按岗位量、按难度),两版都错
- **「能走」不等于「在在需清单上」**:exclusion 省(不在排除清单 + TEER 0-3)与命中清单同级,否则全国最松的萨省(9.0)被压出前二
- **「首选」只给够得着的省**:在招 ≥ 全国最大省的 1/10(相对不绝对;绝对值 20 会误杀护士 BC 16 岗、漏掉软开 AB 2 岗)
- 命中但没岗 → `rpt.p.thinHit`(说清「清单收了你,但岗位其实在 X 省」),不许叫「首选」
- **砍题 5 → 2**:实测 `buildProvReport` 对 edu/age/clb/totalExp 的消费次数是 **0**(而原注释写着「都真的进分值表」)。现在只问「诉求」+「有没有 offer」
- 答题页目标省加**海洋四省**(挂 value 5 不动 4,不改已存档案语义)
- `difficulty` 接进决策(此前 9 省齐备、引擎引用 0 次)

### A4 定时与依赖

- `pnp` 役 13 → **17 步**:新增 `build_nl` / `build_pe`;`build_bc_sirs` / `build_sk_points` **手动 → 入役**(钉在末尾,自校失败拖不到别人)
- `ircc` 役 2 → **3 步**:新增 `scrape_statcan_npr.py`
- `scrape_ircc_stats.py` 加**月度新发学签流量**(存量表官方停在 2024,这是唯一当期口径;已进 `provinces.info.studyFlow`)
- `openpyxl` 补进 `pyproject.toml`(此前只写在 `docker/etl/httpx/Dockerfile`,依赖真相分裂两处,.venv 跑不了 ircc 役)
- 难度卡文案三语补上**分母年份**(原来只标配额年 2026,读起来像在库人数也是当期)

### A5 质量

135 测试绿(+4)、tsc 干净。新增护栏:`partialMiss` 不冒充查全、`notExcluded` 不说「不公布清单」、命中但没岗不许叫首选。

---

## B. 待解决(新 session 从这里开始,按编号)

### B0 上线(⚠️ 卡在 Render,不是代码问题)

- [x] ~~B0-1 提交 + push~~ ✅ 四个提交已推 `fa68976..e0f8363`(Frank 拍板把并行 session 的
  语言服务端化 + SSR 筛选一起带走,单独一个提交 `51be2ba`,出问题可单独 revert)
- [x] ~~B0-2 灌生产库~~ ✅ **不用手动**:数据走 `build` 役 → mart → Supabase,不经 Render 构建。
  生产库已验:SK ineligible 152 / BC ineligible 12 / PE 8 / NL 抽选 8,全部 `fetched=2026-08-02`
- [x] ~~**B0-3 Render 没上线**~~ ✅ **虚惊,已自行翻篇**(2026-08-03 复验):线上 = `683dece`,
  而 origin/main 领先的两个提交 `f6f9593`(docs)与 `0d4e55c`(验尸 perf)**自带 `[skip render]`**
  —— 本来就不该触发构建。`check_deploy.py` 只比 SHA,不认 skip 标记,所以会一直报「没上线」。
  **下次读它的输出先看差的那几个提交是不是全带 skip**(这条本身值得改进哨兵,记在 B3 附近)。
  原文记录如下:
- [ ] ~~B0-3(原文)~~ `etl/check_deploy.py` 报:origin/main `e0f8363` vs 线上 `3c05dab`,
  **积压 6 个提交**(其中 `d9fb7bd` 在本轮之前就没上线 → 部署卡住不是本轮造成的)。
  Frank 需去 Render → pnp-cms → Events 看是 `Build blocked`(构建分钟耗尽 + spend limit,补买要花钱)
  还是 `Build failed`(贴报错来修)。**在此之前任何「改了怎么没生效」都不必当代码 bug 查**
  —— 2026-07-21 六提交白躺一天的事故就是这么来的
- [ ] **B0-4** 上线后复验:`python etl/check_deploy.py` 一致 → 再拉 `/plan/province` 确认
  萨省那句谎话消失、卡③ 出「首选/诉求」句式

### B1 决定「他现在够不够格」的数据(最贵的缺口)

- [x] ~~**B1-1 各省经验门槛**~~ ✅ **2026-08-03 落地,七省全补**(QC 走自有体系不属 PNP,「其余八省」实为七个)。
  `pnp_requirements` **31 → 205 行**,九省齐;七个新脚本 `etl/pnp/build_<省>_req.py` 已进 `pnp` 役
  (17 → **24 步**,`docker restart` 后日志确认七步全跑、七张表 `fetched=2026-08-02`)。
  - **语言:七省全有**。AB CLB 5/4 按 TEER + 33102 单档 CLB 7;SK CLB 4;NS CLB 5/4;NB CLB 4(三份
    pathway 指南互校);PE CLB 4;NL **TEER 4/5 才要 CLB 4、TEER 0-3 免考**(全国最松,档位是从官方
    两句话相减**算出来**的,官方收窄时自动跟);**MB 最值钱 —— 语言门槛是逐职业发的**,
    In-Demand 清单那列 `Minimum CLB` 收进 158 行(木匠 72310 → CLB 5、软开 21231 → CLB 5、33102 → CLB 7)
  - **经验:四省有硬数**(AB 24 个月境内外 / SK 12 个月近十年 / NS 12 个月近五年+1560 小时 /
    PE 24 个月仅 TEER 0-3)。**MB、NB、NL 三省官方没有通用经验门槛**,它们的「6 个月」是
    *与该雇主的在职时长*,口径与本站问的同职业总经验对不上 —— 按 rules.ts 铁律①判不了就不出行,
    **这三省的空是结论,不是漏抓**(理由写在各脚本文件头)
  - **最低收入:查完的结论是「全国只有 BC 发布收入表」**。AB/SK/MB/NS/NB/PE/NL 一律不发数额:
    NS 指向联邦 LICO Table 1、SK 指向 IRCC proof-of-funds,其余只写「足够财力」。
    那是联邦口径不是省门槛,且报告的 income 判定要拿它比「该职业该省中位年薪」——两个不可比的数
    (08-02 已因此撤过一次并排展示)→ **一行都不收**。这是本条的答案,不是缺口
  - 顺手修两个:① mart 的 `appliesTeer` 对字符串 `",".join()` 逐字符插逗号,ON 那几行一直是
    `0,,,1,,,2,,,3`(会给每个 TEER 列表混进一个假的 0;因行序凑巧没显形,属潜伏 bug);
    ② `rules.ts` 的 experience 不按 TEER 挑行(PE 的 TEER 4/5 有「经验**或**学历」替代路径,
    套上 24 个月会误判)。③ `langNone` 文案去掉「注册阶段」(BC 专属口径,NL 没有注册这一步)
  - 验收:143 测试绿(+8,新增七省挑行口径)、tsc 干净。木匠(72310/TEER 2/0 经验)实跑:
    **五个省出经验行(BC/AB/SK/NS/PE 全 fail)、九个省全出语言行** —— 原来只有 BC 一省有话说
- [ ] **B1-2 学徒序**:无经验用户的第一段路。加一道「你有这行经验吗」→ 无经验走「先找能算数的学徒岗」,有 1 年才走选省序
- [ ] **B1-3 `apprenticeFriendly` 清洗标记**:JD 全文识别「带学徒 / 不要经验」+ TEER 分档,下沉到 ETL(前端只读字段)。今天实测全国 476 个木工建筑学徒岗,**一半标成 75110(TEER 5)= 攒的经验走不了大多数通道**
- [ ] **B1-4 PGWP 规则库**(时长叠加 / field-of-study)。Frank 本人的问题:两个 1 年研文能不能换 3 年 PGWP —— 现在只问「还剩几个月」,没有规则能算「再读一年会变几个月」

### B2 「自己能办 vs 必须雇主配合」(该收费的功能)

- [ ] **B2-1** 报告按 `pnp_requirements.subject` 分栏:申请人侧(语言/收入/经验)vs 雇主侧(经营年限/营业额/员工数)。数据已有(BC 17+3、ON 5+6)
- [ ] **B2-2** 雇主够格反查:`companies` 31,061 家 + LMIA 匹配 5,286 家 → 筛「历史上做过外籍招聘」的公司
- [ ] **B2-3** 合规提醒印进报告:**雇主/中介向外籍工人收费换 offer 违法**——而那正是「包 offer 套餐」最贵的部分
- [ ] **B2-4** 其余八省雇主侧门槛(同 B1-1 一并抓)

### B3 保鲜与诚实层(今天诊断出的第 3、4 号病)

- [x] ~~**B3-0 哨兵改成 skip 感知**~~ ✅ 2026-08-03:`check_deploy.py` 原来只比 SHA,遇到纯文档/纯数据
  提交(项目惯例带 `[skip render]`,本来就不该构建)就误报「没上线」—— 喊多了没人信,正好毁掉它
  2026-07-21 那次事故换来的价值。现在:差集全带 skip → exit 0 并说明原因;有真该部署的提交积压 →
  照旧报警**并逐条列出**是哪几个(光给 SHA 判断不了严重程度)。顺手修一个 Windows 坑:
  `subprocess(text=True)` 默认 cp1252,中文提交消息在 reader thread 里 UnicodeDecodeError,
  外面只看到「拿不到」——已强制 `encoding="utf-8"`。
  ⚠️ **同期教训**:本轮推完后线上一度停在 `3c05dab` 不动,我当时归因「构建分钟耗尽」**猜错了** ——
  真因是 `stats/ui.tsx` 漏了 `useState` import 导致构建失败(`5ea228a` 修的就是它),
  而那个文件正是我提交时没带走、由并行 session 补交的。**报警要看 Events 里的真实原因,别按最近一次事故的模式猜。**
- [ ] **B3-1 数据新鲜度告警**:每源 `fetched` vs 期望 `cadence`,超期告警。**ON 抽选断档三个月没人发现**;healthchecks 的 ping 只证明脚本跑完,不证明数据是新的
- [ ] **B3-2 `source_manifest.json` 数据契约**:每源一行(`scope`=main/pathway/exclusion/none + `cadence` + `authority`)。同一份契约供 coverage 判定 / 报告 asOf / 新鲜度告警 —— 三件事本来是一件
- [ ] **B3-3 补 `fetched` 字段**:`designated_employers`(2931 家 AIP 雇主,要拿来下结论却不知是哪天的)、`pnp_allocations`、`temp_residents`、`pnp_admissions`、`study_flow`
- [x] ~~**B3-4 查役到底跑没跑**~~ **✅ 当天解决,根因是最隐蔽的一条**:容器全部 Up 7 天、healthy、ping 正常,
  但 `auto_update.py` 启动时 `import sources.<role>` **只执行一次**,META 的 steps **冻结在容器启动那一刻**。
  实况:`pnp` 役每轮只跑 **8 步**(META 声明 17)→ `build_mb`/`build_nb` 停更 7-8 天;`ircc` 役只跑 **2 步**
  → 新写的 StatCan 临时人口占比**一次没跑过**。`docker restart pnp-pnp-1 pnp-ircc-1` 后:pnp 17 步全跑、
  MB/NB/NL/PE 全刷到 2026-08-02、ircc 3 步含 NPR 20 个季度。**教训见 §D 第三条**
- [x] ~~**B3-5 ON 抽选断档核实**~~ ✅ **真停抽,官方原文坐实**(2026-08-03):
  ontario.ca「2026 OINP updates」页 6-26 公告 —— EOI 系统对新注册关闭、
  **「No further invitations will be issued under the former program streams」**、
  未获邀的旧 EOI 将被自动撤销。断档不是抓取烂了,是改制。
  尾巴:新 Workforce Priority stream 的邀请何时开始发,官方还没说 → news 役盯这页
  (https://www.ontario.ca/page/2026-ontario-immigrant-nominee-program-updates)
- [ ] **B3-6 BC / MB 主线口径官方复核**:两省官网是 JS 渲染,httpx 拿到空页;`.venv` 没装 playwright(`uv sync --extra publish && playwright install chromium`)。BC 已用指南 PDF 绕过,MB 仍靠数据推断

### B4 待接线(算好了没人用 = 今天的第 4 号病,别再犯)

- [ ] **B4-1** `study_flow`(2026 至 5 月新发学签)→ 省弹框 / 统计页显示「当期新增」
- [ ] **B4-2** `npr_share`(全国临时人口占比 2026-04 = **6.18%**,峰值 7.59%,目标 5%,按当前降速要到 2027 上半年)→ 难度卡背景事实。**属版式改动,先出效果图**
- [ ] **B4-3** SK / 海洋省抽选补齐:SINP 官方不公布(记为政策事实,别当抓取失败);NS / NB / PE 待查
- [x] **B4-5(新增,2026-08-03)官方运营统计** ✅ 两省已抓并入役(`pnp` 役 24 → 26 步):
  - `build_sk_stats.py` → `sk-stats.json`:SINP 季度处理时长(EO 申请 **2 周**、雇主 EPA **3 周**)、
    2026 配额三档 + **Nominations YTD 官方日更**(总 4,761 已用 2,628 = 55%)、优先行业 7 个
    (**skilled trades 在列,不设上限**)/受限行业 3 个(卡车/零售/餐饮住宿,带绝对名额)。
    note 里钉了政策事实:**Employment Offer 子类不走 EOI 池**(官方 EOI 页明示只有 OID 与 EE 递 EOI)
    —— 有 offer 即直接递,无「等抽选」步骤
  - `build_ab_stats.py` → `ab-stats.json`:AAIP 逐 stream 配额/已发/剩余 + **积压游标**(正在审哪天
    收到的申请)+ **EOI 池内人数**(全国唯一发分母的省:AOS 23,056 人;7-15 单轮捞 833 ≈ 3.6%/轮
    —— 「被抽中概率」第一次可算)+ 64 轮抽选史(库里原只有 8 轮;canonical 维度表仍归 build_draws)
  - `build_bc_stats.py` → `bc-stats.json`:BC PNP **注册池按 SIRS 分数段的人数分布**(11 档,
    asOf 07-07,共 8,683 人)—— 三省分母里颗粒度最细:配 pnp_draws 的最低邀请分,能读出
    「我这个分数上面压着多少人」。与 build_draws 同页(SSR,httpx 直取),那张表 build_draws
    注释里一直写着「池分布表不取」—— 分母在我们眼皮底下挂了很久
  - 此前「分母没有任何省公布」的断言**是错的**,已被 SK/AB/BC 三页推翻
  - **手法教训(铁律 2 重申)**:猜 URL 猜出一排 404 之后,改用仓库自带 `bfs_crawler` 探站,
    几分钟就把三省的页翻出来了 —— 抓数据先跑爬虫,别手搓 httpx 猜路径
  - MB:全站 324 页爬完,**没有**处理时长/池子页;有 `/draws` 逐轮页(build_draws 已覆盖)与
    年报 PDF(最新 2024,年度口径,非运营)→ MB 记为「官方不发运营统计」的政策事实
  - ON:运营数据 = `invitations-apply` 页 32 张抽选史表(全是改制前旧 stream 的历史,已冻结)
    + `2026-updates` 公告页;无处理时长/配额页
  - 三张新表**尚未接 mart/前端**(先数据层,消费点走 L2 接线登记簿,别又「算好了没人用」)
- [ ] **B4-4** AB / MB 分值表(现在只有 ON / SK / BC)

### B5 智能客服(方案已成文,见另一份文档)

- [ ] **B5-1** `lib/llm.ts` 加 `friend` provider(朋友的 ngrok qwen3.6;env `TRANSLATE_API_*` 本地已配)+ **探活探针**(ngrok 免费域名会变,挂了要在 `/funnel` 面板看得见)
- [ ] **B5-2** 抽槽位 API + 首页一句话输入框(与四张卡并存,**卡不删**)
- [ ] **B5-3** `claims[]` 外部主张对账三分法:**能核 / 核不了 / 他没说的**。第三格是杀手 —— 中介只会说他有渠道的那个省
- [ ] **B5-4** 生产 LLM 成本核查:`llm.ts` 默认 provider 是 ollama(生产不可达),生产实际走的是不是 anthropic(按量花钱)?要核

### B6 版式(需先出效果图,Mode B)

- [ ] **B6-1** 首页「你想做什么决定」删掉三张灰卡(选城市/选学校/选专业)——占 1/3 版面、零价值、还等于承诺了没做的东西
- [ ] **B6-2** 卡③ 副标题「去哪个省对你最有利」要改 ——「最有利」取决于诉求(拿身份 → 曼省;先找工作 → BC)
- [ ] **B6-3** 本轮所有版式改动的截图验收:被另一个 session 的 dev server 挡住(**只准一个 dev 实例**,上次打爆连接池弄挂过生产)

### B7 主线量数(M3 分叉的依据)

- [ ] **B7-1** B0 上线后隔几天重读漏斗。**本轮实读**:jd-open 107 → lock-seen 90(84%)→ pricing-open **15**(9 个来自报告锁区)→ pay-click **0**
- [ ] **B7-2** `pay-click` 已核**接线正常**(在共享 `PricingCard` 里,页面版与弹框版同一个 `buy()`)→ 这个 0 是真没人点,不是量具坏。但那 15 个人看到的是**旧的、会撒谎、缺三省数据**的报告 —— B0 上线后再量才有意义

---

## C. 木匠案例:现在报告说什么(本地实跑,线上还没有)

**诉求 = 容易拿身份**
> 首选 MB:你的职业在其公开清单「MB 在需职业」上,当地在招 25 岗
> SK:排除清单「SK 主线不合格清单」查过,NOC 72310 不在上面,TEER 2 也过 0-3 门槛;当地在招 30 岗
> 岗位最多的是 BC(246 个),但那边每个提名名额有 84.2 人在争,MB 只有 11.9 人(2024 年末)——岗多不等于容易拿提名

**诉求 = 先找到工作** → 首选翻成 BC(246 岗),并提示「最容易拿提名的是 SK,每个名额 9 人」

**还缺的那句(B1)**:他 0 经验 → 得先找「算数的第一份岗」再谈选省(经验是**排期**不是资格,fail 措辞要改,见 B1-2)。
~~学徒岗实测:安省 41 / BC 72 / AB 58 / 曼省 10,一半标成 75110(TEER 5)~~ ⚠️ **这组数 2026-08-03 复现不出来,别再引用**。当日 mart 重算(在招 39,938):
- 标题含 apprentice:263 个,**TEER 0-3 占 262**(「一半是 TEER 4/5」不成立,任何口径下都只有 ~8%);NOC 75110 全文含 apprentice 的全国仅 25 个
- JD 全文含 apprentice + 建筑族(72/75)且 TEER 0-3:**892 个** —— ON 224 / BC 203 / AB 171 / SK 58 / NS 58 / **MB 31**
- 木工专项(NOC 72310 + 标题含 apprentice):全国 **29** 个 —— AB 7 / SK 6 / BC 6 / ON 3 / MB 0
- **方向仍成立**:曼省学徒岗任何口径都垫底,中介把顺序讲反了;但 B1-3 的立项理由要改写(不是 TEER 错配,而是「谁肯要 0 经验」本身)
- 另:曼省 SWM 对**外省毕业生**要与雇主连续全职**满 1 年**(不是 6 个月;6 个月是本地条款),且 co-op/在读工作不计 —— 亚岗昆毕业的这位适用 1 年档

---

## D. 本轮立的两条铁律(已进 memory 与《主线与支线》§1.5)

1. **补全数据 + 保鲜 = 永久主线**,不参加支线分诊。缺数据当场抓;**对客户永远先给答案,不许说「本站未收录」**(那是我们的库存报告,不是他要的答案)
2. **抓完必须当场进 docker 定时役**,不许「抓一次完事」。交付前自查一句:这次抓的东西,明天还会自己更新吗?
   配套:抓数据一律先用仓库现成的 `etl/crawl/` 工具链(bfs_crawler 探 URL / download_md 转 md / browser_fetch 兜底),不临时手写 httpx;官网被反爬挡住先试**文件服务器**(PEI 那 8 个 NOC 就藏在官方指南 PDF 里)
   **2b(2026-08-03 升级为常设架构)**:URL 探索本身已进定时役 —— `crawl` 役(周更,`etl/sources/crawl/`
   + `etl/crawl/discover_sources.py`)维护**十地图**站点地图 `data/crawl/<slug>/manifest.json`
   (九省 + QC;QC 是当天 Frank 改拍入列的,quebec.ca 无墙 125 页,引擎仍不给 QC 下 PNP 结论;
   PE 靠浏览器兜底 —— crawl 役用 docker/etl/crawl/Dockerfile 重镜像,Radware 验证壳标记已补进
   browser_fetch),并 diff 出新增/消失页(changes.json + 日志)= **政策雷达**。
   **此后「想要什么数据」的第一步是 grep 这些 manifest**,命中了再写抓取脚本;别再猜 URL。
   顺手记:QC 地图里有官方「招聘中介警告」页(warning-recruitment-agencies)—— B2-3 的出处素材。

4. **有问题先找 URL,没有数据先补 URL**(2026-08-03 Frank 原话立律):任何「答不了 / 库里没有」,
   第一步是 grep `data/crawl/*/manifest.json` 找官方页;**manifest 里也没有 → 先补种子建地图**
   (`discover_sources.py` SEEDS 加一行),再谈写抓取脚本或对用户说缺。
   「本站未收录”从此有标准动作:补 URL → 抓 → 入役,三步之内变成「已收录」。
   当天战果:此律执行一天,地图从 0 → 14 份(九省 + QC + 三地区 + 联邦 RCIP),
   SK/AB/BC 三省官方运营统计(此前被断言「不存在」)全是这么翻出来的。

3. **挂进 META ≠ 会跑**(当天实撞):`auto_update.py` 长驻,启动时 import 一次 META,steps 从此冻结。
   改了 steps **必须 `docker restart pnp-<role>-1`**,否则新步骤永远不执行,而容器 healthy、ping 正常、
   日志里也看不出少跑了什么 —— 这是本轮最隐蔽的坑(见 B3-4)。**交付自查:`docker logs pnp-<role>-1 | grep "→ python"`
   确认那一步真的出现过**,不能只看代码挂上了。

## E. 本轮教训(给下一个 session 省时间)

- **数行数猜覆盖=会撒谎**:同一次会话里助手两次把部分数据当全量(SK 三条行业专项当全省清单、AB 排除清单当清单式省)。口径必须写在数据里,人和引擎都只能从行数猜时,就都会猜错
- **排序目标不许助手拍**:写死过两版都错。「每个人诉求不一样」——目标函数是用户的输入,不是我们的常量
- **注释会撒谎,测试不会**:`decisions.ts` 的注释说四道题「都真的进分值表」,实测消费 0 次。今天把「字段必须被 builder 真实消费」这件事往测试里挪了一步(`rpt.p.*` 进白名单),但**还没有自动校验 unlocks 与引擎实际产出的一致性** —— 值得单独立项
- **数据变好会让断言过期**:补了 BC 排除清单后,我自己 20 分钟前写的测试立刻失效(BC 从 partial 变 exclusion)。这是对的,断言跟着数据变强
---

## F. 数据盘点:有什么 / 缺什么(按「能回答哪个问题」排,不按表排)

### F1 已有(可直接下结论)

| 要回答的问题 | 靠哪份数据 | 覆盖 |
|---|---|---|
| 这职业哪个省在招、多少岗 | `jobs` 40,759 在招 | ✅ 全 10 省,日更 |
| 哪个省的公开清单收了这职业 | `pnp_occupations` 616 行 | ✅ 七省;MB 主清单 / SK·AB·NB·BC 排除清单 / BC·SK·NS·PE 专项 |
| 哪个省不收这职业 | 同上 `type=ineligible` | ✅ SK 152 / NB 43 / AB 34 / BC 12 |
| 这职业能不能走联邦 EE 类别 | `ee_categories` 94 | ✅ 含最近类别抽选 CRS |
| 各省抽选线与频率 | `pnp_draws` 97 | 🟡 只有 ON/BC/AB/MB/NL + FED;**SK 官方不发、NS/NB/PE 未查** |
| 哪个省更卷 | `difficulty` 9 省 | ✅ 竞争比/配额趋势/抽选活跃/分数线位;⚠️ 分母是 **2024 年末**存量(官方最新) |
| 当期学签新增趋势 | `study_flow`(本轮新增) | ✅ 月度到 **2026-05**;⚠️ 未接前端(B4-1) |
| 全国临时人口占比 | `npr_share`(本轮新增) | ✅ 20 个季度到 2026-04(6.18%);⚠️ 未接前端(B4-2) |
| 官方门槛(语言/收入/经验) | `pnp_requirements` **205** | ✅ **九省齐**(B1-1,08-03):语言 7 省全补(MB 逐职业 158 行)、经验 4 省有硬数、MB/NB/NL 官方无通用经验门槛;**最低收入全国只有 BC 发布**(结论,非缺口) |
| 省自评分值表 | `pnp_score_factors` 120 | 🟡 只有 ON/SK/BC;**AB/MB 缺** |
| AIP 指定雇主 | `designated_employers` 2,931 | 🟡 NS 1574 / NB 1263 / NL 94;**PE 无**;⚠️ **无 fetched** |
| 哪些公司做过外籍招聘 | `companies` 31,061 + LMIA 匹配 5,286 | ✅ 未用于「雇主够不够格」(B2-2) |
| 可申 PGWP 的学校 | `dli` 295 | ✅ |
| 政策动静 | `news` 60 | ✅ |

### F2 缺(编号对应 §B)

| # | 缺什么 | 为什么要 | 对应 |
|---|---|---|---|
| ~~1~~ | ~~其余八省的经验/语言/收入门槛~~ **✅ 08-03 补齐(七省)** | 木匠现在五个省出经验行、九个省出语言行;收入项**查完的结论是全国只有 BC 发布收入表**(不是缺口) | B1-1 |
| 2 | **PGWP 规则**(时长叠加 / field-of-study) | 「读书 vs 直接工作」这类最贵的决策(一年学费 + 一年收入)完全答不了 | B1-4 |
| 3 | **学徒岗标记**(JD 全文 + TEER 分档) | 476 个学徒岗里一半是 TEER 4-5,攒的经验走不了多数通道 | B1-3 |
| 4 | 其余八省**雇主侧**门槛 | 「哪几步必须公司配合」目前只算得出 BC/ON | B2-4 |
| 5 | NS/NB/PE 抽选 | 海洋省「等多久」给不出区间(NL 本轮已补) | B4-3 |
| 6 | AB/MB 分值表 | 「我够不够分」这两省算不了 | B4-4 |
| 7 | PE 的 AIP 指定雇主 | 海洋四省里唯一空缺 | B3-3 |
| 8 | 转换职业的路径数据 | 卡⑥「你能不能转过去」——`gaps` 里自己就写着没有 | 未立项 |
| 9 | 中介/顾问收费的可核数据 | Frank 问「找中介得花多少钱」——库里没有,**不许编** | 未立项 |

## G. 哪些数据要「最新」+ 定时状态(实测 2026-08-03)

**役与周期**(compose 的 `SCRAPE_INTERVAL` 覆盖 META 的 `interval`,以 compose 为准):

| 役 | 周期 | 步数 | 管什么 |
|---|---|---|---|
| `jobbank` | **1h** | 4 | 全国职位增量(最新性要求最高) |
| `pnp` | **1h** | **17** | 省清单/排除清单/抽选/门槛/分值表/AIP/DLI/字段来源 |
| `ee` | **1h** | 3 | 联邦 EE 类别 + 轮次 |
| `news` | **1h** | 1 | 官方政策动静 |
| `build` | 1h 兜底 | 10 | 08→09→seed 灌库(正常由 jobbank 新轮触发) |
| `ircc` | **1d** | **3** | IRCC 存量+流量、StatCan 临时人口占比、难度重算 |
| `backup` | 1d | 1 | pg_dump 生产库 |

**按「必须多新」分档**(这是 B3-1 告警要用的 `cadence`):

| 档 | 数据 | 理由 | 现状 |
|---|---|---|---|
| **小时级** | 职位、EE 抽选、政策新闻 | 岗位当天就会关;抽选两周一轮,漏一轮就过期 | ✅ 在役 |
| **日级** | 省抽选(`draws`)、省清单 | 抽选结果最多滞后一天;清单改制会静默发生(BC 2026-06-13 那 12 条就是) | ✅ 在役,但见下 ⚠️ |
| **周级** | 分值表、官方门槛、DLI | 指南按版本更新(BC 指南 effective 2026-06-10) | ✅ 在役(本轮把两个手动脚本钉进去) |
| **月级** | IRCC 存量/流量、StatCan 占比、AIP 雇主名录 | 官方本身按月/季/年发布 | ✅ ircc 在役;AIP 在 pnp 役 |
| **人工核** | `on-workforce-priority`(政策事实表)、`pnp_allocations`(配额) | 官方公告后手改,Frank 抽查制 | 🟡 `checkedAt=2026-07-20` |

**⚠️ 役挂着但数据没刷新(B3-4 要查的):**

| 文件 | fetched | 差几天 |
|---|---|---|
| `mb-indemand.json` / `mb-indemand-rural.json` | 2026-07-25 | **8 天** |
| `nb-*.json`(4 个) | 2026-07-26 | **7 天** |
| `on-workforce-priority.json` | 2026-07-25 | 人工表,可接受 |
| `bc-req` / `on-req` / `on-points` | 2026-07-31 | 2 天,可接受 |

`pnp` 役是**每小时**,MB/NB 却停在 8 天前 → **容器没起,或跑了失败静默保留旧表**。这正是 B3-1 要治的病:
`healthchecks` 的 ping 只证明脚本跑完,不证明数据是新的。

**⚠️ 完全没有 fetched 的**(不知道是哪天的 = 不能拿来下结论):`designated_employers`、`pnp_allocations`、`temp_residents`、`pnp_admissions`、`study_flow`、`companies`、`cities`、`districts`、`stats_daily`

## H. 本 session 谈过但没立项的(别忘)

| 话题 | 结论 | 触发条件 |
|---|---|---|
| **讯飞数字人**(2026-07-30 发布超拟人实时生成) | **不进产品内**:表现层不是瓶颈、跨境需国内主体与结算、与手机优先版式冲突、按分钟计费而现有 AI 侧零边际成本。**获客侧可试**:离线生成口播视频进 short-video-studio 分发 | 文字版结论先证明卖得动 |
| **短视频获客** | 内容源现成(日更抽选/清单变化/news/rankings);可用 Frank 本人照片做站长口播(华人社群吃真人 IP) | 同上 |
| **痛点全量盘点(~100 条)推导 $20 付费意愿** | Frank 的方法论,一直挂着未立项 | 冷启动前的产品正餐 |
| **同族职业推荐** | STATUS 里欠 Frank 的第 ② 件 | 未定 |
| **AI 接入设计文档** | STATUS 里欠的第 ③ 件 → **本轮已成文**(智能客服文档) | ✅ 销账 |
| 卡③ 用户诉求之外的分叉(比如「已经有 offer」走另一条序) | `offerBand` 已在题里,但只驱动一条结论 | B2 一并做 |
| 「事实免费、结论收费」在卡③ 的落点 | 现在锁区只有「完整顺序与每省差距」;补 B1/B2 后锁区才真有货 | B1/B2 之后 |
