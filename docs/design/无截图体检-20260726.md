# 走查提速：无截图体检脚本（2026-07-26 立项）

> Frank：「如果不截图是不是省 token，能查出来问题吗」→ 拍板「新 session 写体检脚本，跑第一轮」。
> 本文件是**给下一个 session 的施工说明**，写完即照此实施，不必重新推导。

## 1. 为什么

一张 375×812 截图 ≈ 1.5k token，一轮两端全量 30 张 = 40–50k；同一页文本 dump ≈ 1–3k。
**截图占走查开销八九成**，而过去几轮真正查出的问题（废话、重复、裸词、折行、该点点不了）
**绝大多数能用 DOM 量出来**，不需要眼睛。

## 2. 工作模式（改这一条，别改整套工作法）

原：全量截图 → 逐张看 → 出整改意见。
新：**体检脚本跑全量 → 输出可疑清单 → 只对命中项截图确认（一轮 3–5 张）** → 逐条出整改意见 → 实施 → 下轮复查。
轮次文档、编号规则、闭环标记**一律照旧**（`docs/功能盈利点检查.md` + `docs/整改轮次/`）。

## 3. 脚本落位与约定

- 位置：`.claude/skills/iterate-audit/scripts/checkup.py`（与 capture_*.py 同处，走查 skill 一起调用）。
- 运行：**系统 Python312**（playwright 装在那儿，`uv run` 会 ModuleNotFoundError；`PYTHONUTF8=1` 必加）。
- 目标：生产站 offer2pr.com；两个视口（375×812 / 1440×900）× 三语 × 匿名态与测试号（复用 skill 里 credentials.local.json 与 lang_sweep.py 的登录手法）。
- 输出：`scratchpad/checkup-<轮次>.json` + 终端摘要表（**只打命中项**，不打全量通过项——输出过滤是省 token 的第二刀）。
  每条命中带：`page / viewport / lang / rule / selector / text 片段 / 建议截图=true|false`。

## 4. 检查项清单（首版就做这些，逐条可编程）

**文案**
1. 裸词/黑话：正文出现 `X/5`、单字「高」「中」「低」、`TEER` 无注、NOC 码无职业名。
2. 三语缺键回退：非中文界面里出现中文字符（白名单：公司名、地名原文、NOC 英文名）。
3. 废话与重复：同一容器内文本近似重复（归一后 Jaccard > 0.8）；同页同一事实出现两次。
4. 占位符泄漏：`{`、`undefined`、`null`、`NaN`、`[object`、`—` 连续多格。

**布局**（这几条替代肉眼看图，是本方案成立的关键）
5. 折行/截断：`el.scrollWidth > el.clientWidth`；或 `nowrap` 元素被 ellipsis 截断。
6. 出屏与横向滚动：`rect.right > innerWidth`、`documentElement.scrollWidth > innerWidth + 1`。
7. 元素重叠：同层兄弟节点 `getBoundingClientRect` 相交面积 > 0。
8. 触控目标（仅手机）：可点元素 `rect.w < 40 || rect.h < 40`。

**交互**
9. 可点必有态、不可点必无：`onClick`/`jcellAct` 与 `cursor:pointer` 不一致。
10. 死链与空动作：`href="#"`、点后 DOM 无变化的按钮（抽样点，不全点）。

**数据口径**
11. 页面数字与库对不上：抽样几个（在招总数、命中清单岗数、心跳「核对」时间）直连生产库比对。
12. 空态说人话：`—` 格是否有对应解释路径（本轮刚改过 PNP/AIP，重点复查）。

**运行时**
13. console error / warning（hydration mismatch 单列）。
14. network 4xx/5xx；首屏 LCP 与 JS 体积（有现成数就记，别为它加依赖）。

## 5. 不查什么（诚实划界）

留白舒适度、配色深浅、字体渲染、"好不好看"——**这些必须眼睛看，而且 Frank 自己判断比模型准**。
脚本只保证：**凡是能量化的问题不靠运气发现**；观感类留给命中项的那 3–5 张截图与 Frank 本人。

## 6. 验收

- [x] `checkup.py` 跑通两视口三语，输出 JSON + 摘要，全程无截图。（429s／66 次页面体检／646 条原始命中）
- [x] 第一轮实测：命中清单成型，逐条判真伪（假阳性率记一笔，指导下轮调阈值）。
- [x] 只对真命中截图确认，图数 ≤ 5。（拍 6 张：5 张页面 + 1 张按钮特写——第 6 张是为判「购买钮折行」真伪补的，判出假阳性）
- [x] 轮次文档照旧记录；本文件补一节「首轮实测与阈值调整」。（轮次 = [第26轮.md](../整改轮次/第26轮.md)）

## 7. 首轮实测与阈值调整（2026-07-26）

**结果**：原始 646 条 → 调阈后 153 条 → 判真立项 10 件（#137-#146），其中 P1 三件（手机端看不到「核对」时间、
/stats 首页成片「—」、手机顶栏触控靶 25×19）**纯 DOM 判出，截图只是复核**。三语汉字残留 0、hydration 0、
页面总数与 `/api/jobs` 对得上；全站唯一 4xx = `/img/regions/nb.jpg`（NB 新闻卡裂图）。

**首轮假阳性率 ≈ 76%**，五个根因与修法（细表见轮次文档）：

1. **折行不能按 client rect 数判**——中英混排（「购买 30 天 CA$19」）一行也会拆出 2-3 个 rect；改按不同 `top` 计行，
   66 条 → 1 条。数据表格单元格（长英文职位名）天然要折，td/th 免检。
2. **SVG 内部元素 tagName 是小写**（circle/path），大写跳过表没拦住 → 图标内部全被当「元素重叠」，76 → 6。
3. **手型指向空态只该认「—」**；图标钮、轮播点天生无文字，116 → 12。
4. **同页重复要看结构**：同一 sel 路径的重复 = 列表里的同构标签（新闻卡省名），只报「同一句话出现在两处不同结构」。
5. **短标签才管「/」杂糅**：免责声明整句里的「移民/法律建议」是「或」不是枚举，限 ≤30 字且无句读。

**规则表补两条**（首轮肉眼补抓、脚本原本抓不到的）：
- 属性也是 UI 文案：`placeholder/title/aria-label` 不是文本节点，搜索框「搜索 职位/公司/地点/NOC…」原本漏检 → 已加属性扫描。
- 「·」「/」杂糅多信息（no-dot-separator 硬规矩）本不在 4 节清单里，已并入 R1。

**口径偏差一处（诚实记账）**：第 4 节第 11 条写「直连生产库比对」，本机系统 Python 无 psycopg、uv 环境也没有，
按 Ponytail「不为一条检查加依赖」改用生产 `/api/jobs`（同一个库、同一套 WHERE）作库口径，
比的是「渲染出来的数」对不对得上「后端算出来的数」。ETL→DB 一致性是数据层话题，不归 dd。

**下轮起的默认工作模式**：dd = 体检 → 命中截图；全量截图轮改按需（大改版后，或 Frank 要看整体版面时）。
