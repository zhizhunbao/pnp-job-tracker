# 模块边界统一(设计稿 + 落地记录)

> 2026-08-19 立、当天落地。取代同日废弃的 `lib目录重构-20260819.md`(已删)——
> 那版三处错:① 提了个已被 Frank 驳回的 `sql.ts` 拆分;② 桶的形态抄的是 08-18 事故**之前**的 `lib/jobs/`;
> ③ 把 `tsc` 当唯一硬门槛,而真正的失败模式恰恰是 tsc 全绿。
>
> 范围:`cms/src/lib` 的目录组织与命名,外加 `app/(frontend)` 里**两个错位的数据访问文件**。
> **一行判定/业务逻辑都不改**,纯文件移动 + import 路径更新。

## 〇、这是主线哪一格

按 [主线与支线](../主线与支线-20260801.md) §2 分诊:**债务类** —— 「碰到就顺手清,**不单独排期**」。
所以排成一格一提交、每格独立可回滚,不攒批不占 push 窗口。

## 一、落地状态(2026-08-19)

| # | 一格 | 状态 |
|---|---|---|
| 1 | `plan.ts` → `quota.ts`(11 处 import) | ✅ |
| 2 | `lib/score/` 两个门(6 文件) | ✅ |
| 3 | `lib/verdict/` 两个门(8 文件,reportFacts 退回顶层见 §五) | ✅ |
| 4 | `lib/employers/` 两个门(6 文件 + 收编 `board.ts`) | ✅ |
| 5 | `lib/plan/` 两个门(3 文件) | ✅ |
| 6 | `quizTop` → `lib/quiz/server.ts` | ✅ |
| 7 | `lib/stats/` 两个门(收编 `stats/lib.ts` + `stats/shared.ts`) | ✅ |
| 8 | `lib/quota/`(`plan.ts` → `quota/caps.ts`,旧名与 planRank 撞车)、`lib/llm/`、`lib/resume/` | ✅ |
| 9 | `reportFacts` → `lib/chat/`;`pilotQuota` → `lib/pathways/`(新开 `pathways/server` 门) | ✅ 见 §十 |
| — | `db/sql.ts` 拆分 | ❌ **不做**,见 §二 |
| — | 路由目录互引、组件大文件 | ⏭ 后续批次,见 §七 |

**结果**:`lib/` 顶层 50 → **17** 个平铺文件;子目录 7 → **15** 个。
(表里 1-7 是设计稿当天写下的那几格,8-9 是同日续做的 —— 数字以这一行为准。)

## 二、诊断与不动的部分

代码内部质量高 —— 每文件头部有设计依据与红线。问题全在**目录组织与运行环境分界**。

### 不用动的(重要,防手痒)

- **`db/sql.ts` 不拆。** 908 行 / 180 export,看着像该拆,但 [STATUS.md](../../STATUS.md) 记着 Frank 08-17 的原话:
  「`lib/sql.ts` **真·一个文件装全部** ~150 条查询(已就『2500 行 vs 不许攒 main.js』提过一次异议,Frank 重申,照做)」。
  **这条已经被驳回过一次,不提第三次。** 要动得单独去问。
- **verdict 三件套不是重复,是分层**:pathVerdict(判定核,契约定稿不许改形状)/ tripleVerdict(组装器)/ employerVerdict(雇主关)。
- **score 四件套同理**:pnpSelfScore / crsEstimate / mbEoiEstimate(文件头有三条代数级的「为何不能复用」论证)/ scoreLine。
- **`i18n/report.ts`(1701 行)**:文案文件大而平是常态。

## 三、这次真正定下来的一条规矩:运行环境分门

原先同一条缝有三种拼法(`lib/jobs` 的两个门 / `employerCompareShared` 的驼峰后缀 / `app/*/x.shared.ts` 的点后缀),
三处头注释讲的是同一个坑的三个侧面。**统一成 `lib/jobs` 那种:`index.ts`(浏览器也能跑)/ `server.ts`(要连库)。**
理由只有一条:只有它带 eslint 闸,另两种靠人记 —— 而这条缝的失败模式是编译期看不见的。

**分门的判据订正**(设计稿原来写错了):不是「有没有 import `./db/*`」——
`db/sql.ts` 自己**零 import**,是纯 SQL 字符串,进浏览器包只是多几 KB,不炸。
真正的判据是**依赖链上有没有 `payload` / `@/payload.config`**,以及**函数要不要外部注进来的 `pool`/`Db`**(那种在浏览器里根本没法跑)。

### 顺带定了一条可 grep 的边界

> **数据访问只允许出现在两处:`lib/**` 与 Next 的服务端入口文件(`page.tsx` / `route.ts` / `sitemap.ts` / `opengraph-image.tsx`)。**

落地前全仓有两个违例(`app/(frontend)/stats/lib.ts`、`app/(frontend)/employers/board.ts`),都已收编。现在这条是一条 grep 就能验的闸。

## 四、落地形态

| 目录 | `index.ts`(客户端也安全) | `server.ts`(要连库/要 pool) |
|---|---|---|
| `lib/score/` | pnpSelfScore、crsEstimate、mbEoiEstimate、scoreLine | scoreTables、occCompetition |
| `lib/verdict/` | pathVerdict、tripleVerdict、employerVerdict、designationMatch、caseLibrary | tripleWire、verdictCache、caseFacts |
| `lib/employers/` | employerCompareShared + 名录枚举/行形状 | designatedEmployers、sponsorEmployers、employerCompare、companyResearch、directory、**board**(收编自 app) |
| `lib/plan/` | planRank、planTimeline | timeline |
| `lib/quiz/` | fields、answers、decisions(原有) | **quizTop**(收编自 lib 顶层) |
| `lib/stats/` | **index.ts 自己就是门**(收编自 `app/*/stats/shared.ts`) | **server.ts**(收编自 `app/*/stats/lib.ts`) |

四处 `'use client'` 取的是**值**,正是必须两个门的原因:
`PnpScoreCard.tsx`(scoreProvince)、`Cases.tsx`(CASES)、`Employers.tsx`(EMP_PROGRAMS)、`Compare.tsx`(CMP_KEY)。

**引用规矩**(订正设计稿):`lib/` 子目录之间走 **`../<域>` 相对路径**,不是 `@/lib/<域>` ——
这是仓库里已有的多数写法(chat/ pathways/ quiz/ 里 13 次 `../i18n`、`../db/sql`、`../rules`…),新目录跟既有的走。
`app/` 侧一律 `@/lib/<域>` 绝对路径。
**测试是例外**,照 `lib/chat` 的规矩直接点文件(`@/lib/score/pnpSelfScore`),
只被测试用的名字不进桶(bonusPoints / estimateFsw67 / marginOf / ScoreVsLine / RankableRow / tripleVerdict 全家 / designationMatch 全家)。

## 五、⚠️ 实撞:桶会把「不是环」焊成环

`reportFacts.ts` **退回 lib/ 顶层,没进 `verdict/`**。原因是落地时实撞:

- `chat/tools.ts` 要 `assembleReportFacts`;
- `verdict/verdictCache.ts` 反过来要 chat 的 `loadVerdictData`(**值**,不是类型)。

两个文件各自没问题,一旦同属 `verdict/server` 这一个桶,「chat → verdict → chat」就成了运行时环 ——
`tsc` 全绿、`build` 全绿,**只有测试炸**:`chat/normalize.ts` 初始化时 `PNP_PROVINCES is not iterable`
(和 `lib/chat/types.ts:6` 记的是同一次教训的第二现场)。

**推论(比这次搬迁更值钱的一条)**:桶的边界要看**运行时值依赖的方向**,不只看域归属。
两个文件同域、同运行环境,但只要它们的值依赖指向相反的两边,就不能进同一个桶。

**留给下一批的判断**:reportFacts 唯一的消费者就是 `chat/tools.ts`,按「只有一个消费者的东西不该住共享叶子」,
它更像该搬进 `chat/`,而不是留顶层。但那是域归属的判断,不是这次的搬迁。
→ **已定案,搬进了 `lib/chat/`**,见 §十。

## 六、连带改动

1. **eslint 边界闸**(`cms/eslint.config.mjs`):`BARRELS` 补 5 个(score / verdict / employers / plan / stats;quiz 原有),
   `ALLOW` 每个 `server.ts` 门补三条(绝对 + 两种相对),放行必须排在整组**最后**(配置里记了实撞)。
2. **`db/sql.ts` 的段注释**:它按**消费者文件**分段,搬完 12 条变成陈旧指针,已同批改成新路径。
3. **两道新闸**(2026-08-19 加,均已用合成用例验过真的会响):
   - `local/client-no-server-values`(自写,~30 行在 eslint.config.mjs 里):`'use client'` 文件不许对
     `/server` 门做**值**导入,`import type` 放行。补的是 `ALLOW` 的盲区 —— 它只管路径,不管取的是类型还是值,
     而后者就是白屏。立时存量 **0 处**(6 处客户端引 `/server` 全是 `import type`),所以直接给 error。
     没有现成规则能做:`no-restricted-imports` 的 `allowTypeImports` 无法只对 `'use client'` 生效,
     flat config 的 `files` glob 也选不了「文件内容里有没有那行指令」。
   - `import/no-cycle`:实测**存量 0 环**,直接 error。验过它**追得穿桶**(`export … from` 的再导出照样算边)——
     所以 §五 那个 `chat → verdict → chat` 本来就该被它当场拦下,而不是等测试炸。
4. **散在代码里的指路注释**:`stats/lib.ts` / `stats/shared` / `lib/plan.ts PRO_COLUMNS` 等 8 处已订正。
   其中 `Table.tsx` 那两行是**双重失效** —— 它指的 `PRO_COLUMNS` 常量早就退役了,只剩注释在提。

## 七、明确不做(记下来,免得下次又当新发现)

- **路由目录被当共享库用**(真实病灶,不是「文件太大」):`app/(frontend)/jobs/` 有 7 个组件被别的路由引用,
  `quiz/` 有 4 个。而共享位已经有两个(`ui/` 13 次、路由组根 Header/Footer/BackLink/Icons/LangProvider ≈24 次)。
  **新证据**:`app/(frontend)/stats/` 根本**没有 `page.tsx`** —— 它从来不是路由,是个住在路由树里的库。
  这次搬走了它的两个 `.ts`,留下的 `charts.tsx`(被 start/Pulse 用)、`ui.tsx`(被 news/News 用)是组件,归这一批。
- **组件大文件**:Decision 1573 / Advisor 1121 / Pulse 1018 / Jobs 981。行数是那批的事。
- **`.shared.ts` 旧拼法收编**(jobs/filters、jobs/colWidths、jobs/columns、news/shared):跟着各自的域走。
- **`docs/implementation/` 里 8 处 `lib/plan.ts` 死指针**:归档文档,改它等于改历史,待 Frank 定。

## 八、验收(实测)

1. **`npm run build` 通过 —— 首要硬门槛**(08-18 事故原话:「tsc 全绿,build 才炸」);✅
2. `npx tsc --noEmit` 零错误;✅
3. `npm run lint` 零新增错误 ✅(唯一 1 个 error 在 `plan/pr/Decision.tsx:232`,`react-hooks/immutability`,与本次无关);
4. 判定层 14 个测试文件:**347 passed / 2 failed**。两个失败都是**数据漂移**,不是搬迁:
   CEC 分数线 516 → 523(`data/raw/pnp/draws.json` 本 session 开始前就是 `M` 状态,抓取役刷新过),
   `chatTools` 那条自称「生产库只读」,读的就是刷新后的库。
   → **已收口**(全量 699/699),但结论与当时写的不同:不是「跟着数据走」,而是**这些断言本就不该写死**,见 §十。
5. 上线后仍需:生产 `/api/version` 核对换版;受影响路由实拉
   `/employers`、`/employers/compare`、`/cases`、`/jobs/[id]`、`/start`、`/timeline`、`/occupations`。

## 九、假设的事后核对(错了三条,记下来)

| # | 假设 | 结果 |
|---|---|---|
| 1 | 没有动态 `import()` 引用 lib | 🟡 **半错**。`instrumentation.ts` 有 `await import('@/lib/quizTop')`。不是字符串拼接,grep 得到,但只 grep `from '...'` 会漏 —— 查残留必须同时 grep `import(` |
| 2 | 没有测试按文件路径引用 lib | 🔴 **错**。**13 个**测试文件直接 `@/lib/<文件>`。幸好 eslint 对 `tests/**` 本来就开了口子,改成深路径即可 |
| 3 | eslint 只需加 `BARRELS`/`ALLOW` 条目 | ✅ 对 |
| 4 | `stats` 两文件的消费者全在 `app/(frontend)/stats/` 内 | 🔴 **错,而且错的方向相反**。有 6 个跨路由消费者,外加两个 API 路由从 `@/app/(frontend)/stats/lib` 反向 import。原计划「发现跨路由消费者就不搬」——实际情况恰恰**更该搬**:跨路由共享正是它该进 lib 的理由 |
| 5 | 无人并行改 lib | ✅ 对(提交前仍要 `git show --stat` 查一遍,并行 session 会往索引里 add 东西) |

**最值钱的一条不在假设表里**,是 §五 那个环 —— 设计时完全没想到「合并两个各自健康的文件会造出环」。

## 十、收尾:三笔未结(同日续做)

### 1. 写死的 CEC 参照线 → fixture/不变量现推

5 条断言写死 516(另有一条写死「12 轮 507–518」)。联邦每两周开一轮 CEC,08-18 那轮一到就全红 ——
**炸的不是判定层,是「抓取役今天跑没跑」**。参照线这种按周漂的数写进断言,测的就成了数据新鲜度。

- `pathVerdict` / `chatVerdict`:两个 spec 本来就在读 `data/mart/pnp_draws.json`,改成从同一份 fixture
  现推 `CEC_LINE`(FED + `kind=draw` + 有分 + stream 匹配 CEC,按 `drawDate` 取最新),推不出来直接抛。
  断言由此变成对**接线**的:`refDraw` 认的是 CEC 这条流、且取最新一轮。
- `chatTools`(读生产库):轮数改 `≥ 12`(只增不减);区间改成守不变量 ——
  窗口内全部轮次的 min/max 必须罩得住 `rows` 截回来的那几轮;再加一条 `scoreLow > 480` ——
  案例 C02 §4-4「联邦常规 480~530」的证伪本来就靠这一句,写死 507 反而把立身之本埋了。
- 金标里真正不许动的数(CRS 199 / MPNP 695 / 715)照旧写死:那些是档案 + 规则算出来的,不漂。
- ⚠️ 已知同款未清:`chatVerdict` guardAnswer 那句里的 **MB 632** 是抽选线,下一轮曼省抽选就会炸。

**通用判据**:断言里的数字分两种 —— **算出来的**(档案+规则,写死;变了就是代码变了)和
**抓回来的**(抽选线、轮数、名额,现推或守不变量;写死等于把 ETL 的日程绑进测试)。

### 2. `reportFacts` → `lib/chat/`

不是留顶层,也不是进 `verdict/`。三条判据同指:唯一消费者是 `chat/tools.ts`(它原先喂的 `lib/report.ts`
引擎早已退役,顶层那个位置只剩历史)/「只有一个消费者的东西不该住共享叶子」/ 它自己只 import
两条 type-only 加一条纯 SQL 文本,**搬到消费者身边一条新运行时边都不加**(与进 verdict 造环恰好相反)。
**不上 chat 的桶**:模块内件,不是对外接口。

### 3. `pilotQuota` → `lib/pathways/` + 新开 `pathways/server` 门

- **不进 `lib/quota/`** —— 那是同名不同物:`quota/` 的桶自己写着「装的是『一次调用要不要放行』这一件事」
  (免费/Pro 上限、限流)。RCIP/FCIP 的**移民名额**跟它半点关系没有,同抽屉只会让两个 quota 互相冒充。
- **进 `pathways/`**:问一句「RCIP 的名额状态在哪查」,该和 `rcip.ts` / `fcip.ts` 一个抽屉。
- 它 import `payload`,而 `pathways/index.ts` 有 `'use client'` 消费者取值(`plan/pr/Decision.tsx` 的 `uiOf`)
  → 必须开第二扇门。eslint `ALLOW` 补三条、`BARRELS` 原就有 pathways。
- 门立完拿合成用例验过:`'use client'` 对 `@/lib/pathways/server` 取值,`local/client-no-server-values` 当场报错。

**验收**:tsc 0;`eslint src/lib` 0 error(`import/no-cycle` 没响);全量 int **699/699**(改前 5 红);`npm run build` ✓。
