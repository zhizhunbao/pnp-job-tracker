# C6-01 · 政策时间线最小版(/timeline)

> 模块 **C6 政策时间线**(架构总图挂位 2026-07-18,Frank「还要政策时间线流」)· 负责人 Frank · 2 SP · 2026-07-19 立项(Frank「继续做 C6」)
> 上位分析:[队列付费复审](../../队列付费复审-20260719.md) §1-C6 —— **付费判词:时间线视图免费=留存理由(日更回访);付费钩在个人化「你关注的流多久没抽」**。
> 指回痛点:**G12★**(政策碎片化+过时,元痛点)· A3/B3(「我的类别/流多久没抽」)· F 组衍生时间线需求。
> 与 C5 分工:C5(/news)=公告原文与翻译;C6=事件的时间轴视图。**数据 90% 已在库,零 schema 改动**。

## 0. 设计要点(付费透镜)

1. **三路事件源合并一条时间轴**(全在库):省抽选 pnp_draws(BC/AB/MB 各 ~8 期+ON 改制通告)/ 联邦 EE 类别抽选 ee_categories(各类别最新一期)/ 政策公告 news(带日期+省+重要度,链详情页)。
2. **抽选节奏块=个人化钩的 v1**(不能只做展示,复审拍板):按 省×流 算「最近抽选距今 N 天 · 近几期平均间隔 M 天」——这是「我的流多久没抽」(A3/B3)的数据底座,后续喂 match/提醒;EE 只有最新一期(历史未入库),只显「距今 N 天」,历史入库=二期。
3. **诚实红线循 E6-04**:省分数带分制标注(SIRS/WEOI/MPNP ≠ CRS);ON 通告=事实展示;「节奏」只报历史统计,不预测下一次(G14 教训:预测=纯外推伪权威)。
4. 事件总量 <100(draws ~25+EE ~10+news 60)→ 一次 SSR 全量,省筛/类型筛纯客户端,零分页。
5. 入口:/news 页头右槽「抽选与政策时间线 →」+ SiteFooter + sitemap;顶栏不加(7 项已满)。

## 1. 整体目标

`/timeline` 公开页(SSR,照 rankings/employers 模式):按日期倒序的事件流(省 Tag+类型图标+标题+要点+来源链),顶部「抽选节奏」摘要块(省×流:最近/距今/平均间隔),省筛+类型筛 chips。

## 2. 验收标准

- [x] ① 生产 /timeline 200:三类事件混排按日期倒序,省 Tag/重要度徽标/分制标注(SIRS,非 CRS)在;policy 链站内 60 条、draw 链官方来源
- [x] ② 抽选节奏块:AB AAIP 距今4天·近8期~3天 / BC 距今3天·~14天 / MB ~14天;EE 各类别距今(STEM 829 天可见);ON=通告事件
- [x] ③ 省筛+类型筛客户端即点即筛(BC 筛后抽选事件实测)
- [x] ④ 三语键 ×3;generateMetadata 双语;sitemap;/news 页头右槽+footer 双入口
- [x] ⑤ build 过;本地+生产 playwright 各 10 断言全绿,console 零警告(2026-07-19,3d3572c)

## 3. 实现步骤

- [x] **3.1** `cms/src/lib/timeline.ts`:fetchTimeline(pool) → { events, cadence }(三查合并+节奏计算,零计算下沉不了=纯读加聚合,SQL 侧只 SELECT)。
- [x] **3.2** `/timeline/page.tsx` + `TimelineView.tsx`(PageShell+PageBanner news 青色系+chips+事件流+节奏块)。
- [x] **3.3** i18n `tl.*` 三语;/news 页头右槽链接;SiteFooter;sitemap。
- [x] **3.4** build → 本地验证 → push → 生产终验 → 记档。

## 4. 涉及文件

`cms/src/lib/timeline.ts`(新) · `cms/src/app/(frontend)/timeline/page.tsx`+`TimelineView.tsx`(新) · `jobs/i18n.ts` · news 页头(入口) · `SiteFooter.tsx` · `sitemap.ts`

## 5. 现有代码(复用点)

SSR 模式=rankings/employers;UI=primitives(Tag region/federal/imp 现成);pnp_draws 分制标注文案先例=PnpDrawsBlock(i18n pnpdraws.*);news 详情路由 /news/[slug]。

## 6. 完成定义(DoD)

- [x] §2 全勾 + STATUS 记档 + 架构总图 C6 行更新 + commit/push。

## 7. 实施记录

- 2026-07-19 立项(付费透镜队列第二位,Frank「继续做 C6」);当日实施→生产终验。
- **实施纠偏**:节奏分组键首版用 stream(每期写法不同)碎成一期一卡——改 label 项目级聚合(顺带修 React 重复 key);EE 抽选历史未入库,节奏只报距今(历史入库=二期)。
