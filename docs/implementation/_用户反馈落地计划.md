# 用户反馈落地计划(2026-07-17 批)

> 2026-07-17 Frank 白天连发十余条反馈。**快刀当天全部闭环**(见文末已闭环清单);本文档只管**没实现的**:
> 盘点 → 排批次 → 各批工作量/依赖/拍板点。配合工作项文档使用:一批 ≈ 一个 session,一批收口再开下一批。

## 一、未实现清单(5 件,按用户原话溯源)

| # | 用户原话(摘) | 工作项 | 文档 | 状态 |
|---|---|---|---|---|
| 1 | 「哪些是 part-time / full-time / 可以打零工低门槛的」 | 职位类型筛选 | [E6-06](E6-数据补强/06_职位类型筛选.md) | 文档 ✅ 数据实证 ✅,待实施 |
| 2 | 「基于 job details 解析需要考哪些证书,给整体规划」 | 入职要求解析 | [E6-07 §A](E6-数据补强/07_职业路径规划.md) | 文档 ✅,待实施 |
| 3 | (同上「路径是什么」) | 证书考取路径 | [E6-07 §B](E6-数据补强/07_职业路径规划.md) | 文档 ✅,待编辑数据集 |
| 4 | 「先选哪个学校哪个专业,一毕业就满足要求」+「每省 PNP 政策是否有更新、安省不稳定」 | 择校映射 + 政策动态 | [E6-07 §C](E6-数据补强/07_职业路径规划.md) + 本文 §F4 | C 段文档 ✅;政策监测本文补立项 |
| 5 | 「最近浏览应该可以有多条,也可以删」 | 推荐横幅多组合 | 效果图已过目(chat),本文 §F2 即迷你文档 | 待拍板「改」 |

## 二、批次表

| 批次 | 内容 | 估量 | 卡点 | 完成标志 |
|---|---|---|---|---|
| **F1 数据双件** | E6-06 职位类型筛选 + E6-07 §A 入职要求解析 | 3-4 SP | 生产 DDL(4 列,纯新增) | 前端「职位类型」下拉可筛 + 弹窗「入职要求」段上线,双端生产复验 |
| **F2 横幅多组合** | 推荐横幅按省×大类组合记账,最多 2 条并排、每条可删 | 1 SP | 无(纯前端) | 双组合浏览实测两条并出、单删互不影响 |
| **F3 证书路径** | E6-07 §B 监管机构编辑数据集(TOP ~30 受监管职业 × 省) | 2 SP | 编辑工作量(官方名录逐条核) | 弹窗证书行尾「找谁考 →」+ 顾问引用 |
| **F4 择校与政策** | 源盘点(NOC↔CIP / DLI 名单 / 省 program 目录)→ E6-07 §C 读书路径段 + **省政策动态**(见下) | 盘点 1 SP + 实施另估 | 🔴 源盘点结果决定做法 | 盘点报告先出,拍板后另排 |

**顺序理由**:F1 两件共用同一条 `05b → mart → seed 白名单 → 前端` 管线,同批实施摊薄一半成本,数据当天就绪;
F2 是独立纯前端小刀,F1 期间任何空档可插队;F3 纯编辑数据集不依赖代码变更;F4 依赖 F1/F3 产出
(「要什么证 → 去哪学」的链条),且必须源盘点先行(本项目铁律:交接假设常被实测推翻)。
**F3/F4 建议发帖后看 umami/评论再定优先级**——若用户痛点集中在别处,让位给价值账正餐。

## 三、F2 迷你实施方案(推荐横幅多组合,效果图已给)

- **数据**:`jobsPref1` 新增组合账 `combo: Record<'省|大类', number>`(浏览 +1/收藏 +3;薪资档挂组合名下取主导)。
  旧画像无损兼容——组合账积累够(≥3)之前维持现单条行为。**顺带修根上的假组合问题**:现设计三维度各自
  最高票再拼,可能拼出用户从没浏览过的「ON·科技」。
- **展示**:权重 ≥3 的组合取前 2 条并排 chip;每条自带 × = 该组合清零(可随浏览再积累,不永久拉黑——
  口味会变且拉黑无解除入口);右端 × 仍为整幅当日关闭。geo 冷启动横幅不变(单条)。
- **验收**:注入双组合画像两条并出/计数各自正确(loadedAll 口径继承)/单删一条另一条保持/
  CTA 各套各的筛选/手机 chip 换行/三语/build。

## 四、F4 内「省政策动态」补立项(用户:「每省最近的 PNP 政策也要能找到是否有更新」)

- **已有底子**:E6-04 抽选事实(pnp_draws,BC/AB/MB/ON 周更)+ pnp 源周更抓各省清单页原文快照。
- **轻量做法(推荐)**:对 raw/pnp 快照做**结构化 diff**(清单 NOC 增删/流开关/分数线变化)→ 新 mart 表
  `pnp_changes`(省/日期/变化类型/摘要/来源链)→ 弹窗 PNP 块加「本省近期变化」行 + 榜单页可选「政策动态」节。
  「安省不稳定」这类判断**只摆事实频次**(如「ON 近 90 天 3 次流调整」),不做主观评级——诚实红线。
- **不做**:全网新闻监控/预测性解读(编辑成本高且易翻车)。

## 五、已闭环对照(2026-07-17 当天完成,均生产复验)

选项卡直呼登录框 · 建议问题一键发送 · 建议问题可答性红线 · 检索提速 4.3×(1580ms/22 长任务→506ms/2)·
定价弹框一屏零滚动 · 根域直出 + /jobs 301 · 站名 Offer2PR · 「仅雇主直发」改名 · 筛选下拉贴内容宽度 ·
顶栏导航/操作分组 · 地区冷启动横幅(E9-03)· 检索索引答复(前端内存过滤,不需要)· docker 答复
(cms-1 可删/postgres-1 停容器留卷)· 邮件线(RESEND_FROM 显示名 + alerts@/support@ 转发,Frank 亲手)。

## 六、拍板点(过目本文档后给一句话即可)

1. **F1 现在开工?**(E6-06+E6-07A 同批,是本计划推荐的第一刀)
2. **F2 方案确认?**(最多 2 条/×=清零可再积累——效果图已过目,不同意的点说哪条)
3. F3/F4 是否按「发帖后看反馈再排」处理?
