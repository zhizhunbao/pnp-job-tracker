# L2-08 换省对照节(「换一个省会更有利吗?」)

> 承接 [E12-09](../E12-移民路径引擎/09_省提名分数参考与换省建议.md) §3.4/3.5(方案 A 拍板 2026-08-01)与
> [L2-03](03_报告分层v2c.md) 的付费闸;版式照 `docs/assets/mockups/报告v5-定稿.html` 第三节(效果图
> `报告v5-定稿-桌面.png` / `-375.png`)。
> 立项原话(E12-09 头部):「如果用户觉得自己分不够,赶紧换省换工作,不要继续耗」——
> 本站已有「这个岗能不能提名」,缺的是「**你现在够不够分、不够该换去哪**」。

## 1. 目标与非目标

- 目标:报告新增一节「换一个省会更有利吗?」——现选省 vs 备选省并排,有官方分值表的省给**自算分**
  + 官方申请门槛 / 近期抽选线,没有分值表的省明说「该省未公布分值表」。
- **不加必答题**(方案 A 的核心):打分要 8 个字段,报告只问 4+2 题 —— 用**已答项**算,
  缺项当「补齐可提高精度」的钩子指向 `/pathways` 完整估分器。
- 非目标:自建 CRS 算分、新抓分值表(BC/SK/ON 三省已在 `pnp_score_factors`)、把打分题塞进报告问卷。

## 2. 方案

### 2.1 分数怎么来:**已答项的下界分**,不是完整估分

报告手上只有 `clb`(英语档)与 `canadianExpMonths`(加拿大经验档),官方分值表还要学历/年龄/二语/时薪/地区。
**分值全为非负 → 已答项之和是一个真实下界**,所以句子写「至少 N 分」、尾标 `N+ / 200`,并同一行说清
「完整估分还差 X 项」。这样既给得出 Frank 要的「分数」,又不冒充完整估分。

红线三条:
- **未答的因素一律不参与匹配**(不能让 CLB 缺答落到「Below 4」那档白捡分)——
  `scoreProvince` 加第 6 个参数 `only`(白名单),只有 `language/language1`(有 clb 时)与
  `work/work5`(有加拿大经验时)进自动匹配,其余全算未答。
- **只在下界已过门槛时下「已达标」判定**;下界低于门槛**不说「不达标」**(缺项补齐后只会更高)。
- 免责必须写:自算 ≠ 官方审核;**各省分制互不相通,分数之间不可直接比较**。

加拿大经验当「同职业经验年数」用是**下界口径**(境外经验也算分,我们没问)——句子里写「按你已答的
语言与加拿大经验」,不写「你的经验」。

### 2.2 引擎:`Report.switches`(第三个新增节,同 requirements/employers 先例)

`switchLines(facts, dims, profile, extra, targets)` 纯函数,产出:
1. **现选省一行**:有分值表 → `rpt.s.cur`(至少 N 分);无 → `rpt.s.cur.noTable`(该省未公布分值表)。
2. **备选省每省一行**(排除现选省与 QC,按具名命中 → 在招量排序,cap 3):
   有分值表且下界已过门槛 → `rpt.s.alt.pass`;有分值表有门槛未达 → `rpt.s.alt.mark`(只摆门槛,不判定);
   有分值表无门槛但有抽选记录 → `rpt.s.alt.band`;有分值表两者皆无 → `rpt.s.alt.plain`;无分值表 → `rpt.s.alt.noTable`。
3. **缺项钩子** `rpt.s.partial`(有分值表的省才出):「自算只用到已答的 {have}/{all} 项,补齐可提高精度」→ `/pathways`。
4. **免责** `rpt.s.note`(整节最后一行,灰字)。

挂在 pr / prov / job 三张卡(career 是职业跃迁,不涉及省);`ReportFacts` 新增 `scoreFactors`
(整张 `pnp_score_factors`,120 行,一次 SELECT),`scoreProvinces` 由它派生 —— 原先那条
`SELECT DISTINCT province` 撤掉,少一次往返。

### 2.3 付费闸(gateReport)

**免费=库里查得到的省级事实**(该省有没有官方分值表、官方申请门槛、近 N 次抽选线区间、当地在招岗数);
**付费=拿你答案算出来的**(自算下界分、达标判定、缺几项)。
免费层把带分数的键换成不带分数的同义句(`SWITCH_FREE`,与 `REQ_FREE` 同手法),
`total` 根本不下发;锁行落既有类别 `score`(LOCK_ORDER 里早有,此前从不触发,现在有真货了)。

### 2.4 版式

复用现成 `Sec` + `Line`(v5 定稿节结构),节标题 `rpt.q.switch`「换一个省会更有利吗?」,
位置在雇主线索之后、薪资之前(照定稿 HTML 的顺序)。无新组件、无新样式。

## 3. 落地清单(顺序即施工序)

- [x] `lib/pnpSelfScore.ts`:从 `app/(frontend)/jobs/` 搬到 `lib/`(纯引擎,现在前端与报告两处消费)+ `only` 白名单参数
- [x] `lib/reportFacts.ts`:整表 `pnp_score_factors` → `facts.scoreFactors`,`scoreProvinces` 派生
- [x] `lib/report.ts`:`switchLines` + `Report.switches` + 三卡接线 + gateReport 裁剪 + EN 渲染
- [x] i18n 三语键(`rpt.q.switch`、`rpt.s.*`)
- [x] `PlanPrView`:新节渲染(类型 + Sec)
- [x] 单测:下界口径 / 未答不白捡分 / 只在过线时判达标 / 免费层无分数 / 锁行 score / 无分值表的省
- [x] 本地实拍 375 + 1280,免费视角

## 4. 红线

- 未答因素不参与匹配(白名单),否则「没考语言」会被判成最低档白捡分。
- 下界低于门槛只陈述门槛,**不说不达标**。
- 各省分数不可横向比较,免责行常驻。
- 分值一分不许前端编,全部来自 `pnp_score_factors`(官方抓取入库)。
- 免费响应里 grep 不到自算分。

## 5. 验收

- [x] 有分值表的省(BC/SK/ON)出「至少 N 分」+ 已答项数;无分值表的省(AB/MB…)明说未公布
- [x] 免费视角:只见门槛/抽选线/岗数,不见分数;锁行出现 `score`;Pro(单测)见全量
- [x] 375 无横滚;三语齐

## 6. 落地记录

2026-08-01 同日施工完成(方案与施工一个 session)。105 测试绿(新增 8 条)、tsc 干净。
实装截图:`docs/assets/mockups/报告v5-换省对照-375.png` / `-桌面.png`(本地 dev 直连生产库,注册护士 31301、免费态)。

**免费视角实拍(目标省 BC)**:
```
换一个省会更有利吗?
  当前选择 BC:该省公布了官方分值表(SIRS)
  改选 SK:该省官方申请门槛 60 分(按官方 SINP Points Grid)
  改选 ON:该省有官方分值表,但这条通道还没有抽选记录
  改选 MB:该省未公布官方分值表,自算不了分数
  分数依据各省官方分值表自算,不代表官方审核结果;各省分制互不相通,分数之间不可直接比较
```
锁行新增「你在各省的自算分与够不够门槛」(`rpt.lock.score` 第一次真正被触发);375 与 1280 均无横滚;三卡(拿 PR / 找工作 / 选省份)都出这一节。

**施工时补上的三件方案没写死的**:
1. **抽选线通道红线**(E12-09 §5)差点被漏掉:第一版实拍里 ON 出现了「近 6 次抽选线 57–87 分」——
   那是改制前已关停通道的分,与新分值表不是同一套分制。改成:打分表 `system` 自报了通道就只认同通道的抽选,
   对不上出 `rpt.s.alt.noDraw`「这条通道还没有抽选记录」,不含糊说成「未公布」。
   为此把 `streamMatches` 从 `PnpScoreCard` 提到 `lib/pnpSelfScore.ts`（规则只许有一份）。
2. **分制名要短名**:`OINP EOI points (Ontario Workforce Priority stream)` 整串印进句子一行放不下,
   加 `systemShort()` 去掉自报通道那截;通道名本身只用于匹配,不进正文。
3. **缺项钩子不带数字**:各省因素条数不同(BC 5 / SK 7 / ON 11),拿其中一个省的 x/y 当整节说法就对不上 ——
   逐省的「已答 x/y」留在各自那行,钩子只说「学历、年龄、二语、工资与地区本页没问」。

**没做的**:Pro 真实视角靠单测锁(本地建不了 Pro 态,老规矩);打分题不进报告问卷(方案 A 的前提);
`/pathways` 完整估分器与报告这条下界分**目前是两套入口**,合并成一处是后续的事。
