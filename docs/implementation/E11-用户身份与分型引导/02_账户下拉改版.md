# E11-02 账户下拉改版(B.6,去论坛化)

> 承接规划 §6(已定稿)+ 待办 B.6。用上 E11-01 的头像/昵称。现状=JobsTable 主 header 已有下拉(邮箱+我的账户+Pro+退出),本件按 §6 改成「头像+身份头 + 求职向条目」。

## 1. 目标与非目标
- 目标:登录态下拉头=`Avatar`+昵称(空回退邮箱前缀)+邮箱+Free/Pro;条目=求职向(我的匹配/我的求职看板/移民档案/已保存筛选/升级Pro)+退出;各条目深链到账户页对应节。
- 非目标:论坛物(积分/消息中心/公开主页);SiteHeader(二级页)下拉(它仍链 /account,不重复造);设置页(语言切换已在 header,通知在账户页)。

## 2. 数据
- 无 schema。下拉需 displayName/avatar → 扩现有 `/api/users/me` 拉取(现只取 email/proUntil)多取 displayName/avatar。

## 3. 前端
- **JobsTable**:加 displayName/avatar state;`/api/users/me` fetch 多 set 两字段;`import { Avatar }`;
  - 按钮:`Avatar`(size 22)+ 昵称(displayName||邮箱前缀)+ Pro★ + ▾。
  - 下拉头:`Avatar`(size 40)+ 昵称 + 邮箱 + Free/Pro(到期日)。
  - 条目(求职向,复用现有 i18n 键):我的匹配(mv.entry→`/?view=match`)、我的求职看板(sj.title→`/account?sec=sjobs`)、移民档案(prof.title→`/account?sec=profile`)、已保存筛选(ss.title→`/account?sec=saved`)、升级Pro/Pro徽标 → 分隔 → 退出登录。
- **account/page.tsx**:读 URL `?sec=` 参数(profile/sjobs/saved/buy/overview)→ 初始 `setSec`,让深链落到对应节。

## 4. 运维 / 验收
- [x] typecheck 过(源码零错误,排除 .next 残留);复用现成三语键 mv.entry/sj.title/prof.title/ss.title/acct.plan.free
- [x] 未登录态未动(只改了登录态分支)
- [ ] **待 Frank 登录实测**:下拉头=头像(首字母块)+昵称+邮箱+Free/Pro;条目「我的匹配/求职看板/移民档案/已保存筛选/升级Pro」深链落对节;退出好使;窄屏不溢出

## 5. 落地记录(2026-07-18)
- JobsTable 主 header 登录态下拉重设计:按钮=`Avatar`(size22)+昵称(displayName||邮箱前缀)+Pro★+▾;
  下拉头=`Avatar`(40)+昵称+邮箱+Free/Pro(点头进 /account 概览);条目=求职向 4 条深链 `/?view=match`、`/account?sec=sjobs|profile|saved` + 升级Pro + 退出。去论坛化(无积分/消息/主页)。
- `/api/users/me` 拉取多取 displayName/avatar;account 页读 `?sec=` 初始落节。
- 复用 E11-01 的 `Avatar` 组件。
