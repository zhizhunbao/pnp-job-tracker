# L1-01 Landing 页(架构 v2.0 的用户目标层)

> 承接 [产品架构v2.0-决策智能平台.md](../../产品架构v2.0-决策智能平台.md) L1。本件 = **落地页本体**:三问式首屏 + 七目标入口 + 最新动态 + 信任带。
> 决策引擎(L2 报告契约)与实体页模板(L3)各自立项;本件只做**入口**,不改数据层。
> 效果图(真样式,取色来自 `ui/primitives.tsx`):[设计总表](../../assets/mockups/landing页-设计总表.html)

## 1. 目标与非目标

- 目标:① 首屏只答三问(这是什么、能帮我什么、我该点哪里);② 主按钮**开始个性化评估**唯一视觉重心,次按钮**先浏览数据**;
  ③ 七目标入口(找工作、拿 PR、选省份、选城市、选学校、职业规划、选专业)作次级排布,每个带**库内真数**小注;
  ④ 最新动态三行 = 回访钩的门面(抽选、日更、政策);⑤ 信任带 = 来源与覆盖,免责一行。
- 非目标:新问卷(**复用已上线的入口三问 `EntryQuiz`**,不另写);报告页改造(L2 立项);实体页(L3 立项);
  生活成本与专业数据(L4 缺口,本件只留灰态占位,**不上假入口**)。

![手机 375](../../assets/mockups/landing页-设计-1手机375.png)

![桌面](../../assets/mockups/landing页-设计-2桌面.png)

## 2. 红线落地

| 红线 | 本件约束 |
|---|---|
| 宁可留空不瞎猜 | `选专业` 无数据源 → 灰态虚线、不可点、注「数据补齐中」;生活成本一字不提 |
| 事实免费 | 七入口全部免费可达;付费只在评估结果页出现(本件不设付费墙) |
| 文案不折行无废话 | 375px 下每条小注单行不截断(已验:标题/小注/动态行均无 ellipsis) |
| 不承诺结果 | 主按钮文案是「开始个性化评估」,不是「查你能不能移民」;页脚免责常在 |
| 数字必须真 | 所有数字来自库内查询,不写死;查不到就不显示该行 |

## 3. 路由与 SEO(**需 Frank 拍板**)

现状:`/` 直出职位板(2026-07-17 拍板),`/jobs` 301 回根。Landing 要占根域就得反转这条。

| 方案 | 做法 | 代价 |
|---|---|---|
| **A 直接切**(蓝图终态) | `/` = landing;职位板回 `/jobs`;反转 301,旧查询串转发 | 根域现被 Google 收录为职位板内容,**88% 流量来自自然搜索**;换内容后职位类查询排名可能掉,恢复以周计 |
| **B 先并行**(推荐先走) | `/` 保持职位板;landing 落 `/start`,顶栏加入口;两周后按埋点对比再切根 | 短期两个入口并存,需一次二选一收口 |

**建议 B → A**:landing 的价值(转化)可以先在 `/start` 上用数据验证;根域内容大改是不可逆动作,拿排名做赌注不值。
切 A 时必做:`/jobs` 设 canonical、landing 强内链到板与统计页、sitemap 与 GSC 重提交、旧深链参数保留转发。

## 4. 版式与文案(定稿)

- 首屏**只有标题与两个按钮**:「你的下一步,用数据算出来」+ 开始个性化评估 / 先浏览数据。
  副标「官方公开数据,不是传言」与灰注「三题,免费出结果」**已删**(2026-07-30 Frank:废话)——
  这类自夸与流程说明不进 UI,题数与免费与否点进去两秒自明。
- 手机两列网格、桌面四列,**七卡一律同尺寸**(2026-07-30 Frank 施工当日改稿「卡片都改成一样大小」——
  `找工作` 不再跨列,仅保留蓝底描边强调它是现主产品)。
- **七卡保留**(2026-07-30 Frank 拍板),但在文档里挂回已采纳的三问旅程,不另立分类:

| 三问(2026-07-03 已采纳) | 首页卡片 |
|---|---|
| 投什么 | 找工作、职业规划 |
| 怎么拿身份 | 拿 PR |
| 去哪 | 选省份、选城市、选学校 |
| —(数据缺口) | 选专业:灰态不可点 |

- 小注一律「人话 + 真数」:39,933 个在招岗位、10 省规则与竞争、295 所可申 PGWP、2,436 城在招与薪资、3,435 个职业的行情。
- 最新动态每行「日期 + 来源标签 + 一句话」,末行右侧「全部动态」。行文本单行溢出省略,不折行。
- **首屏 banner 用 `PageBanner` 图版语法**(实景图 + 模块色暗化层 + 左下标题 + 右下数字胶囊 + 右上轮播点),
  只把定高 130 → 200(门面比二级页重),手机 150 且胶囊按原规则隐藏;需给 `MODULE_STYLE` 加 `home` 一档与一组 home 图。
- **信任带并入页脚**(2026-07-30 Frank):「数据来源」一行进 `SiteFooter` 全站生效(合规四件套之一);
  「覆盖」一行删 —— 数字与 banner 胶囊重复。
- 组件一律取自 `ui/primitives.tsx`(Button / Tag / 卡片色),**不新增配色**。
- **顶栏与页脚直接用 `SiteHeader` / `SiteFooter`,不为 landing 另做一套**:导航六项、语言段控、账户区、法务四链与免责全部照旧,
  断点也同源(≤640 导航收进汉堡且法务链撑触控靶,≤1350 副标语隐藏)。免责就是页脚那一条,页内不再重复写。
  已与生产站逐像素对过:桌面顶栏 48px、页脚 49px 单行;375 顶栏折两行 94px、页脚两段(中文 115 / 英文 135,差异来自文案长度)。

## 5. 数据与实现

- 页面:`app/(frontend)/start/page.tsx`(B 方案)→ 切 A 时把内容移到 `page.tsx`、职位板还给 `jobs/page.tsx`。
- 数字来源(一次聚合查询,进程内缓存 10 分钟,手法照 `jobs/page.tsx` 的 `ssrDimsCache`):
  在招岗数与昨日新增(jobs)、省数(provinces)、城市数(cities)、DLI 数(dli)、职业统计数(stats_occupation)、
  AIP 指定雇主数(designated_employers)、最近抽选(pnp_draws / ee_draws)、政策新闻(news)。
- 主按钮:复用 `jobs/EntryQuiz.tsx`。**不复制组件** —— 把它从 `jobs/` 提到 `(frontend)/quiz/`,jobs 与 landing 同一处 import;
  答案照旧写 `QUIZ_KEY` 与档案落库,结果页沿用 `QuizResult`。
- 次按钮:进职位板(B 方案 → `/`;A 方案 → `/jobs`)。
- 灰态 `选专业`:渲染为 `<span>` 非 `<a>`,无 cursor 无 hover。
- 埋点(umami):`landing_cta_quiz`、`landing_cta_browse`、七入口各一个 `landing_goal_{key}` —— 切 A 与否看这组数。

## 6. 验收

- [x] 375px 全页零横滚;标题、小注、动态行**无折行无截断**(实测 scrollWidth=375;全页唯一省略=动态行新闻标题,规则允许)
- [x] 七入口链接全部可达;`选专业` 不可点(渲染为 span,虚线灰态)
- [x] 数字与库内一致(抽查:在招 43,595 与职位板头同数;BC 抽选 07-23/50 分/60 邀请对上 raw/pnp/draws.json;DLI 295);缓存 10 分钟;查询失败该行整条不渲染(每项独立 catch→null)
- [x] 主按钮拉起入口三问,答完落本地记忆(jobs_quiz_v1 done:true 实测)、结果页真数、「看这些岗」深链 `/?prov=BC&q=63200` 套进职位板;注册落档案走与职位板同一份 `quizToProfile`
- [x] 顶栏页脚与全站同组件同结构(SiteHeader/SiteFooter 直用,零另做)
- [x] 免责一行在位(页脚原文);「数据来源」一行并入 SiteFooter 全站生效
- [ ] 埋点三类事件在 umami 可见(代码已埋 `landing_cta_quiz`/`landing_cta_browse`/`landing_goal_{key}`;umami 本地不注入,**待上线后在观察窗核**)
- [x] 桌面 1280 与手机 375 截图归档 `docs/assets/mockups/`(landing页-上线-1手机375.png / -2桌面.png)
- [ ] (切 A 时)`/jobs` canonical、sitemap 重提交、旧深链参数转发验证

## 7. 落地记录

2026-07-30 施工完成(B 方案,`/start` 上线待部署)。要点:

- **EntryQuiz 提级**:`jobs/EntryQuiz.tsx` → `(frontend)/quiz/EntryQuiz.tsx`(git mv 保历史),
  `quizToProfile`(三问答案→档案落库)一并从 JobsTable 抽进组件文件导出 —— jobs 与 /start 同一处 import,零复制。
- **聚合**:`start/page.tsx` 一次 Promise.all(fetchTotalAndProof + 5 个 count + 最近抽选/日更/最新新闻),
  进程缓存 10 分钟(手法照 jobs/page.tsx ssrDimsCache);每项独立 catch(null),缺哪项哪行不渲。
  最近抽选只查 `pnp_draws` 一张表——它本身含联邦轮次(province=FED 显示为 EE),无需另拼 ee 表。
- **PageBanner 加 `tall` 档**(200/手机 150,tall 时标题 30/22px)+ `MODULE_STYLE.home`(与 jobs 同蓝,不发明新色)+
  `BANNER_IMGS.home` 复用既有三张图(pathways-2/jobs-1/stats-3,SOURCES.md 已记),不新增下载。
- **七入口落点**(现有页面,L3 实体页立项后再各自换落点):找工作 `/`、拿 PR `/pathways`、选省份 `/stats`、
  选城市 `/stats`、选学校 `/pathways`、职业规划 `/occupations`;选专业灰态无链接。
  七卡同尺寸(找工作跨列撤,同日 Frank 改稿),蓝底强调保留。
- 结果页「看这些岗」= `/?prov=&q=` 深链(#92 语义),「注册保存」= AuthModal + quizToProfile 后进职位板。
- 动态三行:pnp_draws 最近一次有分/邀请的抽选、jobs 最新发布日新增+可提名数、news 最新一条(region=federal 显 IRCC;标题库内是英文,照实显示不编译文)。

### v2 改版(同日,Frank「太单薄」→ 出图 → 拍板「B 为主 抽选用 A 的表」)

- 效果图:[方案A数据橱窗](../../assets/mockups/landing页v2-方案A数据橱窗.html) /
  [方案B决策台](../../assets/mockups/landing页v2-方案B决策台.html)(截图同目录);定稿 = B 骨架 + A 的抽选五行表。
- **对齐架构 v2 判定式**:新增块全部是「既有事实源的投影 + 入口」,不是裸数据橱窗——
  今日日更卡(→职位板)、最近抽选表(pnp_draws,与 /pathways 同源同口径 →移民路径)、政策动态卡(news 3 条 →/news)、
  热门职业行情卡(fetchTopNocs 与 /api/quiz?top 同一 DAL,补了中位年薪列 →/occupations)、底部 CTA 带收束回评估主按钮。
- 文案红线:FED 轮 label 走现成 `eeKeyDisplay` 三语;省通道名映射不到显官方原文(不编译文);
  抽选表数字列(日期/分数线/邀请)375 下必须完整,只有通道列做省略。
- 版式:分 sections 不挤一屏(Frank「别人的公司页都是分 sections 的」)——节间距 36/56px、节标题 16/18px;
  首页卡片/职业卡全同尺寸;职业卡 168 定宽横滑。

### v3 分节版式(同日,Frank「参考 MongoDB 首页」「把地区统计的统计图表直接搬过来」)

- 参考 mongodb.com 骨架(实截对照):一节一事、全宽色带交替(白/页灰)、大节标题(20/24px)、
  节纵距 40/72px,信息纵向铺开不挤一屏。`.hmBand` 全宽带 + 1320 内轨;表与列表类窄轨 900 居中。
- 节序:hero(banner,**无按钮**)→ 七目标(白)→ 今日日更居中大数字(灰)→ 最近抽选表(白)→
  热门职业横滑卡(灰)→ **在招职位分布主图(白,1320 全轨与各节同宽)** → 政策动态(灰)→
  全宽蓝 tint CTA 带(**双按钮在此**:评估 + 先浏览数据)→ 页脚。
- v2 的「今天的数据」三块合排撤销,拆成独立三节;其余内容与口径不变。
- 统计节两次改稿(同日 Frank):先搬 `StatsCharts`(常见四图+自定义)→ 终稿换 **`MarketChart` 主图**
  (「用在招职位分布这个图,宽度保持一致」),与 /stats 同组件、同 `loadOccStats/loadCityStats/loadStats/loadChannelNocs`
  四个 loader,搜索/横轴/簇内/排序/筛选/下钻全功能原样;echarts 懒加载。
- hero 双按钮挪到页底 CTA 带(「这两个按钮放到最下面」)——首屏只留 banner 与七卡,行动收在页底;
  `landing_cta_quiz` 现仅 `pos: bottom` 会触发(top 分支保留,若日后回摆不改埋点)。

### 自检轮(同日,Frank「检查下还有没有问题,布局方面和内容组织」)

- **撤「热门职业行情」横滑卡节**:与紧邻主图默认职业轴同批职业同一排序,两节同一件事——按 Frank
  「不同 section 显示不同的信息」原则留超集(主图);topOccs 数据装载一并撤(git 可还原)。
- 色带交替重排:goals 白 / 日更灰 / 抽选白 / 主图灰 / 政策白 / CTA 蓝 tint。
- 政策动态同题去重(归一化尾部括注:「…(Toronto)」与同题不再并存),LIMIT 8 取前 3。
- 死代码清理:`.hmCta` 样式、`home.today`/`home.occ.*` 三语死键。
- 回归实测:三语 × 375/1280 零横滚、表内数字零截断、控制台零错误;底部按钮拉起三问 ✓;
  职位板/移民路径照常,两页页脚单行 ✓。
- **已知负债**:/start HTML 1.85MB(主图 occStats 全量 SSR;gzip 后约 200-300KB)——已挂后台任务照
  职位板 /api/dims 先例改客户端后台拉,B 期并行不阻上线。
- 宽度统一(Frank「宽度怎么不一样」):抽选表/政策/CTA 的 900 窄轨撤,全部节走 1320 内轨
  (实测 banner/七卡/表/图卡/政策卡左右边缘齐一)。
- 主图默认露 **top 10 职业**(Frank 拍板;MarketChart 新增 `firstScreen` prop,/stats 维持 12 不动;
  默认按在招量降序,排序控件可切「按中位年薪」)。
- 主图再加 **Top N 选择器**(Frank「数量 top10 和薪资 top10 都要,可选 10/20/50」):
  MarketChart 新增 `defaultTopN` prop + 职业轴 Top 10/20/50/全部下拉,与排序控件组合即数量榜/薪资榜;
  landing 默认 Top 10,/stats 默认全部(行为不变)。
- banner 与首节间距收紧:hero 带下内距归零,只留节自身上内距(128→86px)。
- **新增「职位榜」节**(Frank 三连拍板:单独 section 展示 job / 最新与薪资 × Top 10/20/50 / 「老外喜欢排名」):
  最新榜=fetchJobRows 发布序、高薪榜=fetchJobsPage 年薪序(与职位板同 DAL 同去重),各 50 条服务端备好,
  前端切换零请求;行首挂名次(前三名蓝),行点进 /jobs/[id];手机隐藏公司/地点两列保单行。
  高薪榜限全职(fEmp=full)—— 零工时薪折年薪的离群值(DJ $400/hr)不进榜;
  时薪折算根治属清洗层 04d,已挂后台任务(榜首仍可能是高时薪专业岗如验光师,与职位板年薪排序行为一致)。
  节序更新:hero → 七目标(白)→ 日更(灰)→ **职位榜(白)**→ 抽选(灰)→ 主图(白)→ 政策(灰)→ CTA。
- 埋点:`landing_cta_quiz` 加 `pos: top|bottom` 区分首屏与底部带;职业卡点击记 `landing_goal_career`。
- 验收补验:三语 × 375/1280 全零横滚、抽选表数字列无截断(playwright 实测)。
