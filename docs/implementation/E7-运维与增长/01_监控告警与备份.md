# E7-01 · 监控告警 + 库备份

> Epic **E7 运维与增长** · 负责人 Frank · 4 SP · Sprint 3 · 批次 B7
> 通用约定与索引见 [实现文档 README](../README.md)。目标：出事能知道、坏了能恢复——9 月后无人值守的底座。

---

## 1. 整体目标

三层监控（ETL 心跳 / 站点存活 / LLM 用量）+ 每日库备份。全部用免费托管服务，代码入侵仅「成功后 ping 一行」。

## 2. 验收标准

- [x] 每个 ETL 源（jobbank/pnp/ee/build+backup 共 5 check）有 healthchecks.io check：正常轮次 ping 到 ✅；**超时 → 邮件告警收到 ✅**（缩周期演练法，见 §5）。
- [x] UptimeRobot 监控 `/jobs`：宕机告警收到 ✅（404 演练法 + DOWN/UP 邮件对实收，见 §5；无 caddy，Render 架构）。
- [x] Anthropic Console 用量告警已设（2026-07-05 复核+补设：auto-reload 关=余额 $5.44 硬熔断 ✓；**Email notification 原来没设,已补「月度支出达 $3 通知所有 admin」**；本月已花 $0.05）。
- [x] 每日 pg_dump 落盘保留 14 天；**恢复演练**：从昨日备份恢复到临时库 → 行数一致。（2026-07-05 ✅，见 §5）

## 3. 实现步骤

- [x] **3.1** healthchecks.io 建 5 个 check（jobbank 2h+2h / pnp 7d+1d / ee 30d+2d / build 2h+2h / backup 1d+6h）→ URL 进 docker/.env,五容器重建后全部 ping 绿（2026-07-05）。
- [x] **3.2** `etl/auto_update.py`：每轮全部 steps 成功后 `httpx.get(ping_url, timeout=10)`（env 缺省则跳过——本地开发不 ping）。
- [x] **3.3** UptimeRobot 加 monitor（https://pnp-cms.onrender.com/jobs,5min,邮件告警;顺带给 Render Free 保温）（2026-07-05）。
- [x] **3.4** （R3 修订为 backup 源）2026-07-05 落地：`BACKUP_DATABASE_URI` 进 docker/.env → `docker compose build`（镜像 pg_dump 17.10）→ `up -d backup` 常驻日更；首份 `backups/2026-07-05.sql.gz` 6.8MB；**`/backups/` 补进 .gitignore**（生产 dump 含用户数据，原先未忽略）。
- [x] **3.5** 三演练全过（§5）。

## 4. 涉及目录 / 文件

| 路径 | 角色 |
|---|---|
| `etl/auto_update.py` | 成功后 ping（唯一代码改动） |
| VPS crontab · `backups/` | 备份 |
| healthchecks.io / UptimeRobot | 托管配置 |

## 5. 演练记录

- 断源告警：**✅ 2026-07-05** —— 演练手法=临时把 jobbank check 周期 2h→3min(上次 ping 已 37min 前 → 立即判超时)→ 仪表盘转「1 down」+ 告警邮件发 wangsansi9527@gmail.com；验后恢复 2h/2h,手动 ping 回绿(附带验证恢复邮件)。
- 宕机告警：**✅ 2026-07-05** —— 演练手法=monitor URL 临时改 `/jobs-drill-404` → 5min 内探测 404 → 仪表盘「Down | 404」+ incident 记录 + 告警邮件；验后 URL 改回 `/jobs`。(注:配通道时 UptimeRobot 先发过一封 `TEST:` 前缀的通道测试邮件,与演练触发的真告警是两封。)
- 恢复演练：**✅ 2026-07-05** —— `backups/2026-07-05.sql.gz`（Supabase 生产 dump）灌入本地 pnp-postgres 临时库 `restore_drill`；仅 4 条 supabase_vault 扩展错误（本地无该扩展，预期内，不涉业务表）；public 27 张表行数与生产**逐表全等**（jobs 12500 / companies 9081 / users 2 / stats 115 / rankings 80 / field_sources 30 / pnp_occupations 247 / ee_categories 94 / designated_employers 2917 …）。验后已 DROP 临时库。

## 6. 完成定义（DoD）

- [x] §2 全勾（三个演练都真做）→ **2026-07-05 起进入 M4 的 7 天无人值守观察**。

---

## 7. 实施记录(2026-07-05 凌晨,B8;R3 修订)

- **架构修订记档**:无 VPS —— 家里构建机(docker 全家桶)=运维盒。备份不用 host cron,新建 `backup` 源(etl/backup_db.py,日更 pg_dump Supabase → backups/ 保留 14 天;BACKUP_DATABASE_URI 未设=跳过)+ compose `backup` 服务;httpx 镜像 +postgresql-client(需 `docker compose build` 重建生效)。
- auto_update:每轮全部成功后 ping `HEALTHCHECK_PING_<SOURCE>`(env 缺省=本地不 ping);compose 各源已透传该 env。
- **剩余=用户手动**:① healthchecks.io 注册建 5 个 check(jobbank 2h/pnp 周/ee 月/build 2h/backup 日,各加宽限)→ ping URL 写 docker/.env;② UptimeRobot 监控 https://pnp-cms.onrender.com/jobs;③ Anthropic Console 用量告警复核;④ docker/.env 加 BACKUP_DATABASE_URI(=Supabase 连接串)+ `docker compose build` 重建镜像 + `--profile` 起 backup;⑤ 三个演练(断源/宕机/恢复)待 ①-④ 就绪后做,结果记 §5。
