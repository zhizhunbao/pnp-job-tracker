# E5-02 · 榜单 F8 第一批（rankings 维度 + 页面）

> Epic **E5 商业化功能** · 负责人 Frank · 4 SP · Sprint 2 · 批次 B6（超载时顺延 B7 的牺牲位）
> 通用约定与索引见 [实现文档 README](../README.md)。PRD F8；铁律：**榜单计算下沉数据层**。

---

## 1. 整体目标

首批两个命名榜单作为可分享 SEO 引流页（免费可看）：「本周新增 TOP N」「最可能担保雇主榜」。计算在 ETL，前端只渲染。

## 2. 验收标准

- [ ] `/rankings/<slug>` 两页三语可访问，随日更自动刷新（新一轮 seed 后内容变化）。
- [ ] 榜单行只含事实字段 + 官方链接（遵守 E4-03 约束）；点击岗位行回 `/jobs`（带筛选定位）或原帖。
- [ ] 榜单逻辑零前端计算（页面只 SELECT rankings 表渲染）。

## 3. 实现步骤

- [ ] **3.1** `etl/10_build_rankings.py`：读 mart jobs → 两榜（本周新增=firstSeen 7 天内按评分；担保榜=直接雇主+具名通道命中+第一方，按在招岗数/评分聚合到公司）→ `mart/rankings.json`（行=slug/rank/jobId 或 companyId/展示字段）。
- [ ] **3.2** 挂进 build 源 steps（09 之后）；新 `Rankings` collection + seed dims 映射（坑 2）+ 重启 dev 重灌。
- [ ] **3.3** `(frontend)/rankings/[slug]/page.tsx`：SQL 读 rankings join jobs/companies → 表格渲染 + generateMetadata（三语 title/desc，SEO 主体）。
- [ ] **3.4** `/jobs` footer/顶栏加榜单入口。

## 4. 涉及目录 / 文件

| 路径 | 角色 | 状态 |
|---|---|---|
| `etl/10_build_rankings.py`（新）· `etl/sources.py` | 榜单计算 | 新建/改 |
| `cms/src/collections/Rankings.ts`（新）· seed dims | 维度链路 | 新建/改 |
| `(frontend)/rankings/[slug]/page.tsx`（新） | 渲染 | 新建 |

## 5. 现有代码

- 「最可能担保」口径即 PRD §4 F8 第三榜：直接雇主 + 具名通道 + 第一方——全部字段 mart 已有，纯聚合。

## 6. 完成定义（DoD）

- [ ] §2 全勾 + 数据回归清单 + push。
