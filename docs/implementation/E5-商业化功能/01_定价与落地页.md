# E5-01 · 定价页 + 落地横幅

> Epic **E5 商业化功能** · 负责人 Frank · 4 SP · Sprint 2 · 批次 B5
> 通用约定与索引见 [实现文档 README](../README.md)。决策 D5：单档订阅试水（数值公测反馈后定）。

---

## 1. 整体目标

转化入口就绪：`/pricing` 三语对照页（免费 vs Pro + Checkout 按钮），`/jobs` 顶部对未登录用户加价值主张横幅。不做独立营销站。

## 2. 验收标准

- [ ] `/pricing`：免费/Pro 功能对照表（与 E3-05 的 plan.ts 分层一致，别写岔）+ 价格（env `NEXT_PUBLIC_PRICE_DISPLAY`）+ 按钮（未登录→注册，已登录→Checkout，已 Pro→管理订阅）。
- [ ] `/jobs` 未登录横幅：一句价值主张（移民信号 + AI 顾问）+ 注册/定价链接，可关闭（localStorage 记忆）。
- [ ] 三语齐；改价只动 Stripe Price + env，零代码。

## 3. 实现步骤

- [ ] **3.1** `(frontend)/pricing/page.tsx`：对照表数据从 `lib/plan.ts` 常量渲染（单一来源）；按钮三态。
- [ ] **3.2** `/jobs` 横幅组件（服务端判登录态传入）。
- [ ] **3.3** i18n 三语文案；埋点挂钩（E7-02 的 Checkout 发起事件在此触发）。

## 4. 涉及目录 / 文件

| 路径 | 角色 | 状态 |
|---|---|---|
| `(frontend)/pricing/page.tsx`（新） | 定价页 | 新建 |
| `(frontend)/jobs/*` 横幅组件 | 落地引导 | 改 |
| `cms/src/lib/plan.ts` | 分层单一来源 | 复用 |

## 5. 现有代码

- 依赖 E3-03（Checkout）与 E3-05（plan.ts）。价格数值待 D5 拍板——页面先用 env 占位显示。

## 6. 完成定义（DoD）

- [ ] §2 全勾 + push。
