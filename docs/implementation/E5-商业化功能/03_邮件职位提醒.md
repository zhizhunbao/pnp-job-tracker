# E5-03 · 邮件职位提醒（saved search + Resend）

> Epic **E5 商业化功能** · 负责人 Frank · 5 SP · Sprint 3 · 批次 B8（依赖 E5-00）
> 通用约定与索引见 [实现文档 README](../README.md)。付费层核心粘性功能。
> **2026-07-03 提案采纳**：升级为**匹配版提醒**——默认按「新岗命中**我的路径**」（E5-00 的 match 结果 level ≥ 阈值）发信；saved search 保留为轻量自定义选项。另加**抽选提醒**：新抽选入库 → 对比档案 CRS → 「差 X 分」邮件。

---

## 1. 整体目标

Pro 用户收到两类邮件：① 新岗命中我的路径（match 高分 + 可选 saved search）；② 新抽选 vs 我的 CRS。触发复用日更链路（seed 成功 → curl alerts run），**不加独立 cron**。

## 2. 验收标准

- [x] Pro 用户可保存/删除筛选（上限 N 个，plan.ts 常量）；免费用户看到锁+升级引导。
- [x] 日更后命中新岗（`first_seen > lastNotifiedAt` 且匹配 filters）→ 收到按用户语言的邮件（岗位表格+链接）；无命中不发。
- [x] 同一岗不重复通知（lastNotifiedAt 回写）；alerts run 带 token 鉴权（无 token 401）。

## 3. 实现步骤

- [x] **3.1** 新 `SavedSearches` collection：user(rel) / name / filters(json，直接存前端筛选 state) / lang / lastNotifiedAt；access 本人+admin。重启 dev 推 schema。
- [x] **3.2** 前端筛选区加「保存此筛选」（Pro gate）+ /account 列表管理。
- [x] **3.3** 把 `/jobs` page.tsx 的 SQL where 构造抽成共享函数 `lib/jobsQuery.ts`（列表与 alerts 同一逻辑，**不复制**）。
- [x] **3.4** `api/alerts/run/route.ts`（`x-seed-token` 鉴权）：遍历 saved searches → 查命中 → Resend 发信 → 回写 lastNotifiedAt。
- [x] **3.5** Resend：域名 DNS 验证（B7 前一天办）、`RESEND_API_KEY`、三语邮件模板（简单 HTML 表格）。
- [x] **3.6** `etl/auto_update.py` build 角色 seed 成功后追加 curl alerts run。

## 4. 涉及目录 / 文件

| 路径 | 角色 | 状态 |
|---|---|---|
| `cms/src/collections/SavedSearches.ts`（新） | 存储 | 新建 |
| `cms/src/lib/jobsQuery.ts`（新，抽自 page.tsx） | where 共享 | 新建 |
| `cms/src/app/api/alerts/run/route.ts`（新） | 匹配+发信 | 新建 |
| `etl/auto_update.py` | 触发 | 改 |

## 5. 现有代码

- 筛选 state 结构即前端现有 filters 对象——json 原样存，匹配端用共享 where 构造器解释，两端天然一致。
- 量级预估：Resend 免费档 100 封/天，早期够；超了升级付费档（收入项已在）。

## 6. 完成定义（DoD）

- [x] §2 全勾 + 端到端演练（存筛选→次日邮件）+ push。

---

## 7. 实施记录(2026-07-05 凌晨,B8)

- 双通道实现:A=匹配日报(Pro+建档;match high 新岗前 10;当日新抽选 vs CRS 差 X 分段;users.lastAlertAt 游标)/B=saved search(filters json 原样存,lib/jobsQuery 解释——翻不动的键忽略并报出;lastNotifiedAt 游标)。首轮只回看 36h 防倒灌。
- SavedSearches:create=Pro(access 层)、上限 PRO_SAVED_SEARCHES=5(beforeChange 钩子)、只能存给自己;前端筛选区「保存此筛选」+ /account 管理三语。
- **RESEND_API_KEY 未设=dry-run**(照常计算返回计数,不发不回写)——本地 E2E 全过:401 鉴权/匹配 1/筛选 1。auto_update seed 成功后自动触发。
- ⚠️ 老坑③ 又演:bash curl 发中文 JSON 变 `??`(测试数据 fBroad 损坏误判为 bug)——中文测试体一律 UTF-8 文件 + --data-binary。
- **剩余=用户手动**:① Resend 注册拿 RESEND_API_KEY → Render env(域名未定期间 from=onboarding@resend.dev,只能发到 Resend 账户本人邮箱=测试模式;正式发信等域名+DNS 验证后设 RESEND_FROM);② 端到端真信演练(自己账号建档→次日收信)。
