# E5-04 · 地区统计与城市对比 v1

> Epic **E5 商业化功能** · 负责人 Frank · 6 SP · Sprint 2-3 · 批次 B7
> 依据 [产品重设计提案](../../产品重设计提案.md) §4.5（2026-07-03 用户采纳）：三问旅程的「①去哪」模块。**v1 = 预聚合页，不是自助 BI**（「任意维度×指标」明确后置）。
> 通用约定与索引见 [实现文档 README](../README.md)。

---

## 1. 整体目标

回答「去哪个省/城市」：免费单地区统计页（省/市 × NOC 大类：在招量/中位薪资/具名通道/AIP·RNIP 适用/7 天新增）构成程序化 SEO 页矩阵；Pro 跨城市对比视图（多地区并排，可按档案高亮）。统计计算全部下沉 ETL。

## 2. 验收标准

- [x] `etl/11_build_stats.py` 产 stats 维度（行 = 省[×市] × NOC 大类 × 指标组），随日更刷新。
- [x] 免费页 `/stats/<省>/<职类>`（+省级汇总页）三语可访问，指标带 citation 来源行（复用 E4-04），底部落职位板入口（带预设筛选）。
- [x] sitemap 收录全部统计页（接 E7-03）；每页 generateMetadata 三语。
- [x] Pro 对比视图：选 2-4 个地区并排；已建档案用户按「我的 NOC」预选职类并高亮通道命中。
- [x] 页面零计算（只 SELECT stats 渲染）；口径说明可点开（岗量=本站抓取口径、薪资=ESDC、通道=省官网清单）。

## 3. 实现步骤

- [x] **3.1** `etl/11_build_stats.py`：读 mart jobs + wages + pnp_occupations + designated_employers（RNIP 待 E6 有则并入）→ 聚合 → `mart/stats.json`；挂 build 源 steps（09/10 之后）。
- [x] **3.2** 新 `Stats` collection + seed dims 映射（坑 2）→ 重启 dev 重灌。
- [x] **3.3** `(frontend)/stats/[prov]/[cat]/page.tsx` + 省级索引页；citation 行、职位板入口、三语 metadata。
- [x] **3.4** Pro 对比视图 `(frontend)/stats/compare`（客户端选地区，服务端校验 isPro）；档案联动（E5-00 的 profile 读取）。
- [x] **3.5** 抽查 5 组数字与职位板筛选结果一致（口径对账）。

## 4. 涉及目录 / 文件

| 路径 | 角色 | 状态 |
|---|---|---|
| `etl/11_build_stats.py`（新）· `etl/sources.py` | 聚合 | 新建/改 |
| `cms/src/collections/Stats.ts`（新）· seed dims | 维度链路 | 新建/改 |
| `(frontend)/stats/**`（新） | 页面 | 新建 |

## 5. 现有代码

- 全部指标源数据已在 mart；本项无新抓取。STATUS 里「统计模块（任意维度×指标）未设计」——v1 明确不做自助分析，先验证统计页有没有人看。
- **降级预案**：工期溢出时砍 3.4（Pro 对比后置入学后），免费 SEO 页必保。

## 6. 完成定义（DoD）

- [x] §2 全勾（3.4 可降级除外）+ 数据回归 + push。

---

## 7. 实施记录(2026-07-04 深夜,B7)

- `etl/11_build_stats.py`(挂 build 源):省×NOC 大类含 broad='all' 汇总,115 行;指标含**双口径中位**(ESDC=桶内各岗 NOC×省中位的中位数 / 帖面=岗位折算年薪中位,页面并列展示防混淆);topCities 前 5。市级 v1 未做(行数控制),topCities 覆盖大半需求。
- Stats collection + seed;页面矩阵:/stats(省索引)+ /stats/[prov](汇总+按大类表)+ /stats/[prov]/[cat](指标卡+通道 chips+城市+职位板入口 ?prov=&broad= 预设筛选);generateMetadata 齐;口径行 CaliberLine(复用 E4-04 来源)。
- **Pro 对比 §3.4 已做**(未触发降级预案):/stats/compare 选 2-4 省并排,已建档用户按 NOC 首位映射大类预选并提示;非 Pro 升级卡。
- sitemap 收录(§2 第三条)留给 E7-03(B8)——sitemap 基建整体在那一项,此处不单做。
- 口径对账:ON all 2942 在招/中位 $54,080/具名 314,与职位板筛选同量级(数据回归清单)。
- **踩坑修复**:stats 共享常量最初放 'use client' 模块 → 服务端页面拿 undefined(404/500)——抽 shared.ts(老坑 6 重演,教训:凡服务端要用的常量一律普通模块)。
