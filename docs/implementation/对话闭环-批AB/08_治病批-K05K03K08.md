# 治病批 · K05 自述进 claims + K03 省名串台 + K08 裸码误抽

> Lead · 2026-08-09(Frank 拍板「开治病批:K05/K03/K08 拿基线命中句当金标」)
> 上位=07_对话评测批(病例卡 §8 与基线报告 docs/evaluation/对话评测-2026-08-09.md);
> 三修全在引擎确定性层(抽槽后处理/出口闸/literalNoc),**对 K09 前缀缓存免疫**——
> 缓存回旧稿也拦得住,因为闸与滤在我们这侧。

## 0. 范围(scope-follows-decree)

- ✅ **只治 Frank 点名的三张卡**,金标=基线批跑命中句:
  K05「我持有有效的 PGWP 工签」(R11 实录)/「手里的 PGWP 还有效」(chat_logs #29);
  K03 R08 冒 NB + STATUS 原句「新不伦瑞克省 NL NLPNP International Graduate」;
  K08 R18「33102 (PSW) — does this have a shot in British Columbia?」→ 误抽 31102(chat_logs #94 同病)。
- ❌ **不在本批**:K02/K04/K12/K13(未点名);K05 的「没触发裁决」半边(status 槽抽取方差=模型侧,
  本批只治 claims 污染;记账);K03 的指代式串台(「这些省份」没点名省,判不了——findUnbackedCoverage
  同款盲区,如实记);K09 服务侧缓存;UI 零改动。
- ⚠️ **施工隔离**:并行 session 在主工作区改着 chatOrchestrate.ts(followups 去重,未提交)——
  本批在独立 worktree(batch/treat-k05k03k08,基于 origin/main)施工提交,不碰主工作区一个字;
  两边改动区域已对过(他们在 orchestrate 尾部 followups 区),无语义冲突。

## 1. 整体目标

三个病灶从「批跑层软记录」翻成「引擎确定性拦截+恒绿网金标」:
- **K05**:claims 只装「别人跟他说的话」——第一人称身份/工签自述照家庭自述先例(isSelfStatement)
  机械滤掉,点选卡 sendText 不再变成一条要对账的「主张」;
- **K03**:答复里点名的省必须在 facts 或用户原话里出现过——新逐句闸 findAlienProvinces 进
  sentenceBlockers(流式逐句拦)+ fired 硬拦组(重试→再犯降级),省名凭空出现一句都不见客;
- **K08**:用户原话里的裸 5 位码带库验证后压过模型抽取——bareNocCandidates(纯函数,金额/年薪
  语境排除)→ noc_descriptions 实存核验 → 与 literalNoc 同权覆盖,33102 不再被抄成 31102。

## 2. 验收标准

- [x] ① K05:金标两句(R11/#29)经 normalizeSlots 后 claims 为空;「中介说曼省有合作公司」等
  真主张一条不误伤(既有 isSelfStatement 断言全保持)
- [x] ② K03:金标两形(R08 冒 NB/STATUS 拼接句)被 findAlienProvinces 拦下;facts/原话提过的省、
  全名与中文别名、否定句境全放行;整段两犯降级路径实测(fixture orchestrate)
- [x] ③ K08:金标句 bareNocCandidates 命中 33102 且库验后压过模型;`$31264`/`45,000`/年薪语境/
  库不存在的五位数全不触发;`NOC 21222` 显式打头路径零改动
- [x] ④ 新恒绿 spec `chatCure.int.spec.ts` 全绿;`test:int` 全量 0 红;`tsc --noEmit` 绿
- [x] ⑤ 病例卡翻状态:K05/K03/K08 → ✅(指针=chatCure 金标);corpus/检查器保持当第二眼
- [x] ⑥ eval:chat 复跑:R18 转绿、K03/K05 命中清零(K13 活体红与本批无关,如实报)
- [x] ⑦ push(**不带 skip render**)→ 生产换版验证:受影响面=/api/chat,Node UTF-8 探针
  (英文金标句验 slots.noc=33102 + 中文金标句验 claims 空)
- [x] ⑧ 本文档节节勾账 + STATUS 交接块

## 3. 实现步骤

- [x] **3.1** K05:SELF_STATUS_RE(身份/工签自述词面,中英韩)并入 isSelfStatement
  (家庭 ∪ 身份,ATTRIBUTION_RE 豁免照旧)——claims 过滤复用既有调用点,零新调用
- [x] **3.2** K08:bareNocCandidates 纯函数(连续 5 位、非金额/年薪语境、≤3 个去重)+
  orchestrate 接线(literalNoc 没中时库验裸码,`SELECT noc FROM noc_descriptions WHERE noc = ANY($1)`,
  按原文出现序取首个实存码)——fixture 池默认空行为零变化
- [x] **3.3** K03:findAlienProvinces(text, facts, echo, slots)(检测=PROV_IN_TEXT 复用+ASCII 全名
  另建大小写不敏感式;allowed=facts label/valueText ∪ echo ∪ slots.provs;按 CLAUSE_SPLIT 子句判)
  → 进 sentenceBlockers 第十道 + fired 表 `provDrift` 归硬拦组
- [x] **3.4** 恒绿网:新 spec tests/int/chatCure.int.spec.ts(金标句全部来自基线报告/chat_logs 原文;
  fixture 纪律照 chatPreset1 文件头:只 mock DB 行与 LLM I/O)
- [x] **3.5** 病例卡与语料:diseaseCards K05/K03/K08 翻 treated;corpus R18 期望保持(复跑该转绿)
- [x] **3.6** 全量验证:tsc + test:int + eval:chat 复跑对拍基线
- [x] **3.7** STATUS 交接块 + push + 生产探针验换版(verify-deploy 铁律,构建 ~3 分钟)

## 4. 涉及文件

| 文件 | 动作 |
|---|---|
| `cms/src/lib/chatOrchestrate.ts` | 改:①isSelfStatement 扩身份自述 ②bareNocCandidates+orchestrate 裸码库验 ③findAlienProvinces+sentenceBlockers/fired 接线(三处均为点状插入,不动并行 session 的 followups 区) |
| `cms/tests/int/chatCure.int.spec.ts` | 新增:三卡金标恒绿用例(独立成文件=避开并行 session 在改的 chatVerdict.int.spec) |
| `cms/tests/cases/diseaseCards.ts` | 改:K05/K03/K08 状态翻 treated+指针 |
| `docs/implementation/对话闭环-批AB/08_治病批-K05K03K08.md` | 新增(本文档) |
| `STATUS.md` | 改:交接块 |

## 5. 现有代码(复用点)

| 件 | 复用 |
|---|---|
| 机械滤先例 | normalizeSlots 的问句滤/isSelfStatement 家庭滤(K05 照此纪律扩,不新开滤层) |
| 省名字典 | PROV_ALIAS/ALL_PROVS/normProv/PROV_IN_TEXT(K03 检测面零新词表;ASCII 全名从 PROV_ALIAS 键派生) |
| 子句判形制 | findUnbackedCoverage(CLAUSE_SPLIT/backed 集合/盲区注释口径) |
| 硬拦管线 | fired 表+leaks 组(重试→降级)与逐句门 sentenceBlockers(K03 全自动继承流式拦) |
| 码覆盖先例 | literalNoc(「机器能判死的不押给模型」;K08 只是把同一条理加上库验证扩到裸码) |

## 6. 完成定义(DoD)

- [x] §2 验收全勾
- [x] 生产库零写入(新增 SQL=SELECT noc FROM noc_descriptions,只读)
- [x] `git show --stat` 自查通过:commit `8fde2c2`(7 文件=§4 清单+eval 报告同日覆盖写,零并行 session 文件)
- [x] push 后生产换版实证:/api/version f9fd91f→8fde2c2(push 后 3.5 分钟翻版)+双探针冒烟 PASS(§7-8)

## 7. 实测发现

1. **恒绿网一把过**——chatCure 12 用例首跑全绿(provDrift 两犯降级/裸码验真压回/工资样拒收/NOC 显式零变全在日志);全量 28 文件/517 测试 0 红,三道新闸对既有 fixture 零误伤。
2. **K08 fixture 就能复现病灶全链**——模型抄 31102 时,旧链回落相似度,新链 bareNocCandidates→noc_descriptions 验真→33102 压回;51234(工资样,库不认)不覆盖照走相似度;「机器能判死的不押模型」同 literalNoc 一脉。
3. **术前探针意外证据:这仨是方差病,不是稳定错**——push 前拿换句式探针打现网,K08/K05 双 PASS;连 R18 原句都已翻对(下午 eval 两跑同错=K09 缓存钉死,缓存过期后重抽即对)。所以**行为探针当不了换版翻转证明**,生产验证以 `/api/version` 的 RENDER_GIT_COMMIT 翻新为硬信号,探针只当冒烟。
4. **eval 复跑(修后)**:corpus 24/24 全绿、R18 转绿(33102)、K03/K05 命中清零、降级 3→1、答上 35/36 保持;chatGold 仍 1 红=K13 这回猜 11100(基线猜 41101,每跑猜的码都不同=方差实锤;未点名,不顺手治)。
5. **残留计数器照出一条非本批的缝**——R23 答复「本站尚未收录…因为官方并未公布…」两态揉一句:findMixedStates 在整段复查里只 warn 不拦(fired 表 3459 行),**逐句门才拦**;无 onDelta 的调用方(评测器、JSON 态)走不到逐句门。生产 SSE 有逐句门在,缝主要在评测口径;记账,不顺手扩拦。
6. **R11轮2 转降级=治疗的可见代价**——claims 滤净后该轮组稿两犯改走事实清单(降级是诚实回落);全局降级 3→1 仍是净改善。
7. **worktree 隔离施工手法**——node_modules 与 data/mart 打 junction(ETL 产物不进 git,worktree 裸缺,4 个 spec 文件级 ENOENT 实撞后补)、.env 手拷;与并行 session 的同文件在建零接触。

8. **生产换版实证**——push 后轮询 `/api/version`,3.5 分钟 RENDER_GIT_COMMIT f9fd91f→8fde2c2;
   随后双探针冒烟 PASS(EN 裸码句 slots.noc=33102 / ZH PGWP 自述句 claims=[],Node UTF-8,匿名限额内)。
   照 §7-3 定性,探针是冒烟不是证明;证明=commit 翻新+本批确定性测试链。

## 8. 病例卡状态变更(验收 ⑤⑥ 已过,已翻)

| 卡 | 变更 | 依据 |
|---|---|---|
| K05 | 🔴 → ✅ | chatCure 金标(R11 sendText 句/#29 句)+既有家庭滤断言保持;eval 检查器保持当第二眼 |
| K03 | 🔴 → ✅ | chatCure 金标(R08 形/STATUS 拼接句)+流式逐句拦自动继承;指代式串台记盲区 |
| K08 | 🔶 → ✅ | chatCure 金标(#94 原句 fixture 复刻)+corpus R18 活体在守;裸码库验=「带库验证扩围」记账销 |
