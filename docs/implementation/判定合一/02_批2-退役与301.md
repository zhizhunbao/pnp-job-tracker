# 判定合一 · 批 2:旧页退役 + 301 + 配方并入(2026-08-10)

> 设计上位:[判定合一与SEO落地页-20260810.md](../../design/判定合一与SEO落地页-20260810.md) §6/§7 步 4-5。
> 批 1 见 [01_批1-决策页骨架.md](01_批1-决策页骨架.md)(已上线,79719ca,~3 分钟换版实测)。

## 任务清单

- [x] 301(next.config.ts,照 /stats 先例用 statusCode:301):`/pathways`、`/plan/{job,province,career}` → `/plan/pr`
- [x] 低门槛通道配方并入决策页:`PATHWAY_RECIPES` 直读,原生 `<details>` 折叠(SSR 全量在 DOM 爬虫可见,
  人看默认一行一条);措辞红线沿用(摆步骤+出处,不下结论)
- [x] 导航「移民路径」改指 `/plan/pr`、label=拿 PR 评估(桌面+抽屉;active 键沿用 'pathways')
  ⚠️ §10.3 命名默认拍「拿 PR 评估」(与 H1 一致、零新键),Frank 可翻
- [x] 站内 /pathways 内链清零:JobsTable 账户菜单项、StartView S5 抽选节「更多」→ /plan/pr
- [x] sitemap 撤 /pathways
- [x] 退役删除:`report.ts`(1,353 行)+ `/api/report` + `PlanPrView` + `/plan/{job,province,career}` 三页
  + `pathways/`(page+View)+ `lib/pathways.ts`(evalPathways)+ `report.int.spec` + `reportCards.int.spec`
- [x] 类型归位:`ReportFacts/OccProvFacts/ReportDraw/ProvDifficulty` 从 report.ts 搬进 `reportFacts.ts`
  (事实层自己的契约,不再反向 import 引擎)
- [x] `rules.int.spec` 拆刀:报告层用例(buildPrReport/gateReport,89 条)随引擎退役,
  判定核口径(evaluateRequirements/areaOfPlace/employerBar,黄金三例+七省挑行)全保留
- [x] 验证:四条 301 实测(curl 301 + Location=/plan/pr)、配方卡 SSR 可见、tsc 零错、vitest 471/471
- [x] 附带走查修(08-10 Frank 截图点名):职位卡胶囊三套并存(chip 色块 6px 圆角 / 公司名旁担保徽章 10.5px /
  TV_PILL)统一成一套胶囊规格(12px/圆角 999/1px 同族边框,语义色保留);担保档徽章下放胶囊排与
  LMIA chip 合一(#145 同源逻辑),公司名行回归一行

## 消费方核查记录(删前逐个查,设计稿两处笔误就地纠正)

- `reportFacts.ts` / `gateReport`:设计稿 §5 说「删 gateReport」——**错**,reportFacts 被 chatTools/jobsSql/
  pathVerdict(判定核)引用,保留;gateReport 定义在 report.ts 且站内唯一真实调用方是 /api/report,随删。
- `QuizForm` 不删(决策页答题在用);`pathwayRecipes.ts` 不删(配方卡数据源;并进判定核作路径候选=批 3)。
- PGWP 报告层组合(rpt.c.pgwp*)随 report 引擎死;工签窗口在判定卡个人关另有实现(pathVerdict)。

## 遗留(批 3 = 个人关接线批)

- 判定卡个人关接 `pnpSelfScore`(各省分数=付费实底)+ 6 道加分事实新题(见批 1 C6)
- `pathwayRecipes` 并进判定核作路径候选(现为静态信息卡)
- 对话侧撤建档点选卡(§7.7,答题已上线,可撤)
- 漏斗历史打标(§7.6,读数侧)+ i18n 孤儿键清扫(rpt.*/pw. 部分)
