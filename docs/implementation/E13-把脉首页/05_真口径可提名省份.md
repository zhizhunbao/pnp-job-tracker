# E13-05 真口径可提名省份(occ 级 pnp_provs 列)

> 2026-08-06 立项。起因:榜 A 原名「无省提名通道」对 Chefs(62200)等职业撒谎——
> `namedJobs` 只算**省具名紧缺清单命中**,漏了排除式省(AB/BC/SK/NB;ON 改制后排除集为空=全职业可)
> 与雇主担保类通用通道。文案侧已按方案一改口(「不在任何省紧缺清单」,commit d5c1d1a);
> 本工作项做方案二:把**真口径**算出来落库,榜 A 直接告诉用户「没清单,但这些省仍可走」。

## 1. 整体目标

occ 级新列 `pnp_provs`:该职业按 `pnp_eligible(noc, teer, prov)`(etl/08_score.py 行 191-206,
排除式/inclusion/overlay/QC 全处理)逐省判定后,可走雇主 offer 省提名的省代码清单。
数据层算好,前端只读——榜 A 的「可提名省份」列由它供数。

## 2. 验收标准

- [x] `data/mart/stats_occupation.json` 的 `province='all'` 行带 `pnpProvs`(全 492 职业已验;Chefs=`"BC、AB、SK、MB、ON、NS、PE、NL"`,NB 被 overlay 排除表挡掉——对,食品类;TEER 5 无清单职业=恰好 4 个排除式省)
- [ ] 生产库 `stats_occupation.pnp_provs` 列存在(docs/sql 手写,禁 DB_PUSH),seed 后有值
- [ ] `/api/market-stats` 的 occ 行透出 `pnpProvs`;缺列时读取层返回 null,前端整列不渲染(契约 v3 降级铁律)
- [ ] 首页榜 A 出「可提名省份」列:Chefs 显示含 ON/AB(≥5 省显示「N 省」,悬停看全名单)
- [ ] 榜 B/C 的「紧缺清单省份」列口径不变(两列语义并存,tooltip 各自说清)

## 3. 实现步骤

- [x] **3.1 ETL**:`etl/11_build_stats.py` 全国 occ 行加 `pnpProvs`(importlib 按路径加载 08_score——顶层只有轻量建表,重活在 `__main__` 守卫内,已核;08_score 零改动)
- [x] **3.2 SQL**:`docs/sql/e13-05-occ-pnp-provs.sql` 已写(幂等加列)
- [x] **3.3 seed**:`cms/src/app/seed/route.ts` 列清单 + 映射已补
- [x] **3.4 读取层**:`lib.ts` WANT + 映射、`shared.ts` OccRow 已补
- [x] **3.5 前端**:榜 A「可提名省份」列已加(与「紧缺清单省份」互斥出现,列缺整列不渲染);三语齐
- [ ] **3.6 上线**:prod 跑 SQL(**权限拦截,Frank 亲手**)→ seed(带 token,不带 reset)→ 生产 /start 验 Chefs 行

## 4. 涉及目录 / 文件

`etl/11_build_stats.py` · `etl/08_score.py`(只读/或抽共享模块) · `docs/sql/e13-05-occ-pnp-provs.sql` ·
`cms/src/app/seed/route.ts` · `cms/src/app/(frontend)/stats/{lib.ts,shared.ts}` ·
`cms/src/app/(frontend)/start/StartView.tsx` · `cms/src/app/(frontend)/jobs/i18n.ts`

## 5. 现有代码 / 备注

- `pnp_eligible` 真值表见 08_score 行 191-206;`PNP_BY_PROV` 由 `data/raw/pnp/*.json` 构建(program=AIP 不混入)
- **这是粗筛信号非资格认定**(各省另有语言/工资/雇主条件),tooltip 必带此句——信任边界不砍
- NL 无标准 NOC 清单(nl-priority 是职位文本),走 inclusion 默认分支(TEER 0-3 可)即现状,不另做假设

## 6. 完成定义(DoD)

生产 /start 榜 A 的 Chefs 行「可提名省份」非空且含 ON;三语无折行;上述勾选全绿。
