# E13-07 通道档(occ 级 channel_tier 四档)

> 2026-08-06 深夜 Frank 与助手把「难度地图」聊透后拍板:职业级四档(他排的梯子)+「把最难的放上面」。
> 条件型通道(AIP/RCIP/毕业生)**不进档**——挂在人的处境上不挂职业上,归判定层(同晚拍板)。

## 1. 整体目标

每个职业一颗结论药丸,三秒读懂「这职业移民路有多宽」;红档 = 「别为它来,别花钱学英语」的劝退词。

## 2. 验收标准

- [x] 档规则(etl/11_build_stats.py `channel_tier()`,交叉省具名清单 ∪ 联邦 EE 类别 ∪ TEER):
      both(省+联邦点名)/ prov(省点名)/ fed(联邦点名)/ ee(无点名 TEER0-3)/ employer(无点名 TEER4-5);
      分布实测 prov 137 / both 72 / fed 19 / ee 149 / employer 115,抽查 Chefs=ee、收银员=employer、注册护士=both
- [x] 生产库 `channel_tier` 列 + seed(3346 行)
- [x] 三榜「通道档」列(药丸,可排序,列缺整列不出);手机卡片 chips 槽同款
- [x] 榜 A 行序 = 难在上(employer → ee → fed,同档按在架量);列没落库自然退回按在架量

## 3. 实现步骤

- [x] **3.1 ETL**:11_build_stats `channel_tier()` + 全国行 `channelTier`(复用 08_score 的 NAMED_STREAM_NOCS_BY_PROV/EE_BY_NOC,零新抓)
- [x] **3.2 SQL**:docs/sql/e13-07-occ-channel-tier.sql(幂等,授权代跑生产)
- [x] **3.3 seed/读取层**:列清单+映射 / WANT 探测+OccRow 类型
- [x] **3.4 前端**:TIER_RANK/TIER_COLORS + 药丸列 + 榜A排序 + 三语文案(pulse.tier.*)
- [x] **3.5 上线**:SQL → 11 重跑 → seed 3346 → 本地截图验(红档压顶)

## 4. 涉及目录 / 文件

`etl/11_build_stats.py` · `docs/sql/e13-07-occ-channel-tier.sql` · `cms/src/app/seed/route.ts` ·
`cms/src/app/(frontend)/stats/{lib.ts,shared.ts}` · `cms/src/app/(frontend)/start/StartView.tsx` · `cms/src/app/(frontend)/jobs/i18n.ts`

## 5. 现有代码 / 备注

- 药丸字面自解释([[no-tooltips-copy-rules]]:tooltips 全撤后语义只能进列名/字面)
- E14 接入抽选线与担保率后,档位从规则档升级为带数字档(「联邦点名·上轮 391 分」)
- fed 档(联邦点了省没点)19 个职业第一次浮出水面——此前埋在榜 A 无任何标记

## 6. 完成定义(DoD)

生产三榜带通道档药丸、榜 A 红档压顶;抽查三职业档位与规则一致。生产换版验完勾此项:
- [x] 生产 /start 实测(473c301 推后 150s 换版,客户端包验到「仅雇主担保」标记)
