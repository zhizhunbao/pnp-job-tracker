# E13-06 政策动态中文注释(news.title_zh)

> 2026-08-06 Frank 走查:「这个下面加上中文注释吧,和职位描述一样」。英文标题主文案不动,
> 中文界面下加中文灰注——翻译在数据层批量做(本地 Ollama,[[local-llm-for-translation]] 过渡策略),前端只读。

## 1. 整体目标

news 行加 `titleZh`:ETL 抓完新闻后用本地 qwen3.6 增量直译标题,入 `news.title_zh`;
/start 政策动态在 `lang=zh` 且有译文时于英文标题下出灰注,没译文只出原题(绝不拿英文顶包)。

## 2. 验收标准

- [x] `data/raw/news/news.json` 存量补译(117/119;2 条模型回吐英文被校验拒收,下轮增量自愈)
- [ ] 生产库 `news.title_zh` 列存在(docs/sql/e13-06,与 e13-05 合并一次手跑),seed 后有值
- [ ] 生产 /start 中文界面政策动态行出中文灰注;EN/KO 界面不受影响

## 3. 实现步骤

- [x] **3.1 ETL**:`etl/news/scrape_immigration_news.py` 加 `translate_titles_missing()`(增量、单条 30s 超时、
  校验含 CJK 且长度合理,不合格留空;qwen3 长标题会跑偏成英文分析体 → 提示词前缀 `/no_think` 压制)
- [x] **3.2 mart**:`etl/09_build_mart.py` news 块透传 `titleZh`
- [x] **3.3 SQL**:`docs/sql/e13-06-news-title-zh.sql`(幂等加列;区别于懒缓存的 body_zh/summary_zh)
- [x] **3.4 seed**:news 列清单 + 映射补 `title_zh`(不进 staleClear 懒缓存清单——这是批量 ETL 列)
- [x] **3.5 前端**:/start 政策动态灰注(`SELECT *` 容缺列,列没加前整块行为不变)
- [ ] **3.6 上线**:prod 跑 SQL(与 e13-05 同批,Frank 亲手)→ seed → 生产中文界面验灰注

## 4. 涉及目录 / 文件

`etl/news/scrape_immigration_news.py` · `etl/09_build_mart.py` · `docs/sql/e13-06-news-title-zh.sql` ·
`cms/src/app/seed/route.ts` · `cms/src/app/(frontend)/start/{page.tsx,StartView.tsx}`

## 5. 现有代码 / 备注

- 复用脚本内既有 `LLM_BASE`/`LLM_LOCAL_MODEL`(http://192.168.1.150:11434,qwen3.6:latest,/api/chat think:false)
- 上游两条原始标题本身带截断省略号(ImmigrationNB 暂停公告等)——抓取侧既有毛病,不归本项
- 译文质量=本地小模型水准,专有名词缩写保留;公网实时翻译方案落地后此批量步骤退役([[local-llm-for-translation]])

## 6. 完成定义(DoD)

生产中文界面政策动态每行(有译文的)英文题下见中文灰注;2 条缺译随日更自愈;勾选全绿。
