# C4 数据补口 —— 库能撑住案例 C01 的每一个数字

> 上游:[路径裁决-C01复现计划](../design/路径裁决-C01复现计划-20260805.md) §C4。
> 金标:[案例C01-路径分析](../design/案例C01-马龙木匠路径-路径分析-20260805.md) §八(数据缺口清零)。

## 一、目标

C01 手算/手抄的数字全部改为从 mart 可 SELECT:MPNP EOI 积分表、NB 抽签(含职业类别)、MB 抽选分流、
SINP 清单适用范围、NL 指定雇主申报 NOC、OINP 审理时长与提名数。

## 二、基线(2026-08-05 已核)

- `pnp_occupations` 已有 `type`(indemand/ineligible)—— C01 §八D 说的 listKind 已存在;**真缺口是 `appliesTo`**
  (SK 152 条排除清单只管 OID/EE 子类,Employment Offer 不受约束 —— 现模型表达不了「清单管哪几条子通道」)。
- `pnp_draws` 已有 `stream` 子通道列,NL ITA 批次 2026-08-03 已接;**真缺口:NB 整省缺席、MB 多流抽选并成一行(632/825 分差只能定性)**。
- `pnp_score_factors` 有 BC/ON/SK 先例(factors/rows/bonus/rule 结构);缺 MB。
- `pnp_ops_stats` 泛化行结构(metric/value/asOf),装得下 OINP;`designated_employers`(2,931 AIP)装得下 NL 645 家,缺 `nocs` 列。
- crawl 缓存全在:nb-imm 141 页 / mb-mpnp 39 页 / sk-sinp 31 页 / nl-imm 772 页 / oinp-times 1 页 / fed-spouse-owp 1 页。

## 三、工作项(按文件区不重叠拆)

- [x] **W1 NB 抽选 + MB 分流**(`etl/pnp/build_draws.py` → `raw/pnp/draws.json`):NB 按邀请轮次入库
      (无分数,类别写 note);MB 多流抽选一流一行带各自分数线。
      ✅ 2026-08-05:NB 28 行(当前页+2026 历史页合并,NB_MAX=48——一轮拆多行,12 装不下一年,
      主 agent 收口时放宽);MB #276=632 / #275=825 各自成行,分数只在同一 <ul> 内配对不串流。
      已知瑕疵:MB #276 某行 LAA「605」是官方页 <strong>60</strong><strong>5</strong> 排版伤,照原文收不猜。
- [x] **W2 MPNP EOI 积分表**(新 `etl/pnp/build_mb_points.py` → `raw/pnp/mb-points.json`):六因子含风险负分。
      ✅ 2026-08-05:金标全绿(695 合计/CLB8 +20/风险 −200);适应性拆三因子共享 group,
      与官方「connection+regional 可叠、与 demand 不叠」一致;每因子对官方自印小计硬校验。
- [x] **W3 SINP 适用范围 + Job Offer 排除清单**(`etl/pnp/build_sk.py`;两个官方 PDF 落 crawl;
      新 `raw/pnp/sk-joboffer-excluded.json`):sk-excluded 标 `appliesTo=OID/EE`。
      ✅ 2026-08-05:quote-anchored 完成;Job Offer 路径实际排除 14 个 NOC,**72310 不在列**;
      PDF 里另有 5 类排除业务类型(按摩/美甲/招聘中介等)只记在脚本 docstring,未结构化。
- [x] **W4 NL 指定雇主 645 家**(新 `etl/pnp/build_nl_employers.py` → `raw/pnp/nl-employers.json`):含各自申报 NOC。
      ✅ 2026-08-05:实际 **639** 家(缓存实数,非 645);申报 72310 的 **3 家**(Green Bay Fibre +
      Black Rock Paving + Hyde Park Homes,逐页核对非解析误差)→ **C01 原文已修正**。
      查明该名录 = NL 官网 AIP 指定雇主全量(与旧聚合源 94 家同体系)→ 09 里 NL 整省取代旧行防重复。
- [x] **W5 OINP 运营数据**(新 `etl/pnp/build_on_stats.py` → `raw/pnp/on-stats.json`):审理时长 + 提名数 + 配额。
      ✅ 2026-08-05:配额 2026=14,119、提名数 2025=10,750/2019/2018 全 quote-anchored;
      **审理时长专页已官方下线**(注册 302 + 全 slug 负扫)→ processing=[] 是举证过的 not-collected。
      2020/2022-2024 年份被 Radware 反爬拦,如实跳过未补数。
- [x] **W6 集成(主 agent 自做)**:09_build_mart 接 W1-W5(IN_SCORE_TABLES 加 MB、pnp_occupations 透传
      `appliesTo`、designated_employers NL 整省取官方名录 + `nocs`、ops 加 ON 分支、抽选截断 8→12/NB 48);
      Payload 两列声明 + seed 白名单加列 + `docs/sql/c4-pnp-gaps.sql`(**已代跑生产**,seed_state 已清);
      金标脚本 `etl/audit_c01_gold.py`。
- [x] **W7 验证**:✅ 2026-08-05 金标 **20/20 全绿**(本地 mart);tsc 绿;mart 行数对账:
      draws 97→145、score_factors 120→164、occupations 616→630、designated 2931→3476、ops 126→130。
      生产侧:DDL 已跑,待 push + 容器镜像重建 + seed 后按 c4-pnp-gaps.sql 尾部三条 SQL 抽查。

## 四、验收金标(C01 数字逐条 SELECT 得出)

1. MPNP:马龙档案估分 **695**(语言80+年龄75+曼省1年工作40+两年制学历100+适应性500−外省学习100);
   CLB8 上限 **715**;风险扣分上限 −200。
2. MB 抽选:#276(2026-07-30)**632** 与 #275(2026-07-16)**825** 各自成行、带流名。
3. NB:2026 年建筑类(Construction trades)被选中 **≥5 轮**,07-16~18 SW 58 / 07-18 Graduates 209 / 07-22 AIP 114 在列。
4. SINP:72310 **不在** Employment Offer 路径的排除范围(152 条清单 `appliesTo` 不含 Job Offer);建筑不在受限行业。
5. NL:雇主总数 **639**(W4 核实,C01 原文 645 不准),申报 72310 的 **3 家**(同前修正)。
6. OINP:2026 提名配额 **14,119**、2025 提名数 10,750;~~审理时长按流可查~~ → 专页已官方下线,
   审理时长 = 举证过的 not-collected(302 证据存 raw)。

## 五、约束与风险

- 铁律:URL→数据→SQL 顺序;缓存/manifest 先行,禁猜 URL;宁可留空不猜;每个产出带 url+fetched。
- DB:只走 docs/sql additive;DB_PUSH 询问一律 N;Payload 不声明=数据静默丢(教训在案)。
- 风险:SINP PDF 若无现成解析依赖 → 只落下载 + 标 not-collected,不手抄;
  MB 积分页若因子表拆页 → 以 crawl 缓存页为准补抓,不拼记忆。
- 本批不动:cms 前端、chat 层、fed-spouse-owp 消费(归 C5)。

## 六、进度记录

- 2026-08-05 立项,基线核毕,W1-W5 并行派出。
- 2026-08-05 深夜:五子 agent 全收(两处推翻 C01 原文:NL 639/3 家、OINP 时长页官方下线);
  集成完毕,金标 20/20 全绿,生产 DDL 已跑。批次代码侧完。
