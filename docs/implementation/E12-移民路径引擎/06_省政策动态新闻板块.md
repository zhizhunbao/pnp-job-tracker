# E12-06 省政策动态·新闻板块(设计方案 v2)

> 2026-07-18 Frank:「每省最新新闻聚合到站内板块,带图带文字,用户可留言」+「匹配 docker 抓数据拆分,解耦互不影响」;
> **v2 拍板(同日)**:「我们就是聚合整合站,这才是最有价值的;官方原文转载还是要转;中国人吃了信息不全的亏;速读可以用听的」。
> 归位:C5 政策动态(模块架构总图)。**本文档=设计方案,未实施**;留言→P2 流量触发(D6)。

## 0. 定位(v2)

聚合·链条·规划就是本站正业(价值主张「跨四级门槛代劳」的新闻面)。**信息全**是使命——所以转载官方原文,
并转出比原文更值钱的三件:**中文全文翻译**(信息不全的解药,全网稀缺 SEO 独家内容)+ 速读 + 语音朗读。

## 1. 目标与非目标

- **P1 目标**:
  - **卡片流** `/news`:og 缩略图+原标题+AI 中文速读+日期+省筛选(全名 chips)+ ▶ 朗读钮;
  - **详情页** `/news/[slug]` 三件套:① **AI 中文全文翻译(主体)**(段对段,标「AI 翻译·以原文为准」)
    ② **官方英文原文全文**(折叠附录式,带 ©+非官方声明+原文链接+日期四件套)③ **▶ 用听的**(Web Speech API 朗读中文翻译);
  - 统计省页「该省移民动态」块 + PNP 弹框「本省最新公告」行;可命中时「看命中岗位 →」深链(新闻→找岗闭环)。
- **非目标(P1)**:媒体新闻聚合(只官方源)、评论(P2/D6)、EN/KO 翻译层(原文就是英文;韩文翻译后续)、云 TTS(v1 平台特性 speechSynthesis 零成本,音质诉求出现再上云 TTS 缓存 mp3)。

## 2. 转载姿势(v2:转,姿势摆对)

1. **法律面(如实记档)**:联邦 Crown copyright 条款**非商业转载明确免许可**(注明出处+不冒充官方版本);商业场景灰区(条款称需询部门);各省 King's Printer 类似宽严不一。业内(移民媒体)整转+改写为常态,执法风险极低;真实风险=被当官方版本/旧闻当新政——靠声明+日期治理。
2. **姿势四件套**(每篇详情页):© 出处方(Government of Canada / 省 King's Printer)· 「非官方版本」声明 · 原文链接 ↗ · 官方发布日期。**沿用 E4-03 框架:转载+导流+异议即下架**。
3. **只官方源**;解析不出宁可缺省,一省失败不编不猜。
4. **AI 翻译/速读 grounding**:只喂抓取的官方正文,禁外推;标「AI 生成/翻译·以原文为准」;按 URL 幂等缓存(只对新增条目调 LLM)。
5. og:image hotlink(链接预览惯例)+ onerror 隐藏,不落盘缓存原图。

## 3. 解耦架构(Frank 点名:匹配 docker 拆分,互不影响)

**现有 compose 已是「一源一 service」**(SOURCE: jobbank/pnp/ee/enrich 各自独立 interval/healthcheck)——news 照此原生解耦:

```
docker-compose:  news 服务(<<: *etl, SOURCE: news, SCRAPE_INTERVAL: 43200=12h, 独立 healthcheck ping)
etl/sources/news/__init__.py   # 调度单元 META(method=httpx, seed=False 只刷 raw,build 轮消费)
etl/_scrape_base.py            # ★ 母脚本(#55 §2.5,2026-07-18 拍板):client/重试/去重/原子写/逐源隔离/防线——通用件全在这
etl/news/                          # 域目录(#55 新结构原生,不用迁)
  scrape_immigration_news.py       # 入口:母脚本驱动全部子源
  scrape_ircc_newsroom.py          # 子脚本:只填 SOURCE={region, list_url, kind, parse}(联邦 Atom 已实测 ✓)
  scrape_sk_sinp_news.py / scrape_ab_aaip_news.py / …   # 一省一子脚本,谁验证通过谁上
```
- **母/子分工**:母管怎么抓/去重/写盘/隔离/报数;子只填抓哪+怎么从该站 HTML 挑行(选择器优先、函数兜底)。news 是该模式**首个原生样板**,以后新源照此长。
- **命名规范(#55 §2.5,见名知意)**:`<动词>_<机构/源>_<数据内容>.py`——文件名单独出现(日志/报错栈)也自述抓哪家的什么数据。

- **三层互不影响**:① news 容器与其它源容器互不牵连(compose 级);② build_news 内逐省 try/except,单省源改版只丢该省(解析器级);③ 只刷 raw 累积文件,抓挂了旧数据还在(数据级,同 pnp 源惯例)。
- 这套「service+调度单元+域目录」三件套 = **#55 目录重组的原生样板**,以后新源照此长。

## 4. 数据源(✅ P0 探源定稿,2026-07-18 三轮 httpx 实测)

| 源 | 真实入口(实测 200) | 结构 / 子脚本 |
|---|---|---|
| **IRCC 联邦** | api.io.canada.ca io-server news v2 **Atom feed** | feed 零 parse;条目=canada.ca 新闻稿页(httpx 可直取详情)→ `scrape_ircc_newsroom.py` |
| **BC** | welcomebc.ca PNP hub 内发现专页 `…/about-the-bc-provincial-nominee-program/news` | 单页日期段落式(h2=裸日期);标题取首段内嵌粗体 → `scrape_bc_pnp_news.py` |
| **AB** | alberta.ca/aaip-updates | h3.goa-title=「日期: 标题」自述最优;⚠️ 手风琴把条目包进 `<ul><li>`(踩坑记 §10)→ `scrape_ab_aaip_news.py` |
| **MB** | immigratemanitoba.com/feed/(WordPress **RSS**)| feed 零 parse;RSS 摘要截断 → 详情页 `div.entry-content` 补全文 → `scrape_mb_mpnp_news.py` |
| **NS** | novascotiaimmigration.com 已 301 → **liveinnovascotia.com**;Program Updates=`/taxonomy/term/3` | Drupal views-row(h2>a 标题+body 字段带日期)→ `scrape_ns_nsnp_news.py` |
| **ON** | ontario.ca `2026-ontario-immigrant-nominee-program-updates`(E6-04 同页)| h3=日期 + h4=条目(自带锚点 id);**年度页,2027 换 URL 只改一行** → `scrape_on_oinp_news.py` |
| **SK** | saskatchewan.ca 全政府新闻 hub 按部委筛「Immigration and Career Training」 | **POST-only**(Sitecore 筛选,GET/RSS 均不认参;带 scController token 后 httpx 可用——2026-07-18 浏览器复测破局);结果区 `section.search-results ul.results`,日期取 URL 路径 → `scrape_sk_immigration_news.py`(母脚本为此加 `post_data` 可选键) |

**排除(浏览器复核过,不硬上)**:PE=**连移民专属新闻列表都没有**(Office of Immigration 的「News」指向全政府 774 条流,关键词参数不生效;Radware 只是次要问题。它的 EOI Draws 页对 E6-04 有用,已另立后台任务);NL=「News and Updates」菜单是**站方死链**(真浏览器也 404;ITA Updates 页是抽选数据表→归 E6-04,已立后台任务);QC=自成体系不属 PNP。板块按源有数据才显省 chip(同榜单先例)。

## 5. 数据模型 / 链路

- `raw/news/news.json`:**按 URL 累积去重**(同 postings 增量惯例);行={region(federal/SK/…), title, date, url, ogImage, **bodyEn(官方正文全文)**, **bodyZh(AI 段对段翻译)**, summaryZh(速读), fetchedAt}。
- **AI 翻译+速读**:build_news 内 python 直调 Anthropic(haiku,与顾问同模型;**新增条目才调**,翻译+速读一次调用产出,随行存 raw=幂等缓存);运维项:`ANTHROPIC_API_KEY` 进 docker/.env(ETL 侧首次用 LLM;成本≈每条新公告一次调用,公告频率低,忽略不计)。
- 09 直通 → `mart/news`(近 N=60 条,老的留 raw 不进站)→ 新 `News` collection(DDL 照 pnp_draws 形状+locked_rels 清单;bodyEn/bodyZh 为 text 长列)→ seed dims(白名单列全)。
- og:image + 正文抓法:逐条 GET 官方页抽 `og:image` + 正文容器(带频控;抓不到正文=只卡片不出详情页,不硬造)。
- **朗读**:前端 `speechSynthesis`(平台特性,零成本零后端);卡片读速读、详情页读全文翻译;暂停/停止;不支持的浏览器隐藏钮。

## 6. 前端

- `/news` 页(SSR,SEO:「加拿大移民最新政策」类词):省筛选 chips(**全名**,#58 原则)+ 卡片流;SiteHeader 入口待定(顶栏已 5 项,可先挂账户下拉/统计页入口,避免顶栏过载——拍板点)。
- 统计省页块(该省近 3 条)+ PNP 弹框行(本省最新 1-2 条,「本省最近抽选」块下)。
- 深链:region 命中职位板省筛选 → 「看该省岗位 →」;EE 类别公告 → `?view=match` 或类别筛选(v1 按省深链就够)。

## 7. P2(不在本件,流量触发)

- 评论最小集 = D6(登录可评+举报+Payload admin 人工审核);冷启动发帖引流后再开(鬼城效应+中介广告审核成本)。

## 8. 工作项拆分

| 步 | 内容 | 规模 |
|---|---|---|
| ✅ P0 源发现 | 逐省找真实 news/updates 页,httpx+结构实测,清单落 §4(2026-07-18) | S |
| ✅ P1a ETL | 母脚本 `etl/_scrape_base.py` + etl/news/ 六子源 + AI 翻译/速读 + sources/news 单元 + compose news 服务(2026-07-18,§10) | M |
| P1b 数据链+前端 | mart/collection/DDL/seed + /news 页 + 省页块 + 弹框行 + i18n | M |
| P2 评论 | D6,流量触发另立项 | — |

## 9. 拍板点

- [x] 转载官方原文(v2 拍板:转;姿势=©+非官方声明+原文链+日期,E4-03 框架)
- [x] 详情页三件套:中文全文翻译主体 + 英文原文折叠 + 朗读(v2)
- [x] 朗读 v1 = Web Speech API 平台特性零成本;云 TTS 缓 (v2 需求出现再上)
- [x] P1 范围:IRCC+首批验证过的 2-4 省,按源有数据才显 chip(2026-07-18 拍板)
- [x] /news 入口:顶栏第 6 项「移民动态」,窄屏自动折行(2026-07-18 拍板)
- [x] ANTHROPIC_API_KEY 进 docker/.env(2026-07-18 拍板;运维项=Frank 填 key)

## 10. 落地记录

**2026-07-18:P0 + P1a 全部落地,本地端到端验证过(86 条全量入库+全翻译)。**

- **P0 三轮探源**(不拍脑袋写 URL 的方法实证):① 候选 URL 电池 httpx 扫状态码+结构嗅探(feed 线索/日期密度);② 全 404 的省改从 hub 页 grep 含 news/update 关键词的真实链接(BC 的 `/news` 专页、NS 的新域、NL 的排除结论都是这轮出的);③ 定稿源逐个取样条目级 DOM(标题元素/锚点 id/正文容器),为解析器服务。清单+排除见 §4。
- **母脚本 `etl/_scrape_base.py`**(#55 §2.5 首个原生落地):client/重试/详情页频控(1s)+ atom/rss 通用解析(feed 类子源零 parse)+ 日期段落式页助手 `section_body` + 详情页 `og:image`/正文抽取 + 按 URL 累积去重 + 逐子源 try/except 隔离 + 双防线(合并后**只增不缩**;总数 <10 拒写)+ 原子写盘。每轮每源详情页预算 15 条、翻译预算 12 条——首轮回填分几轮摊平,12h 节奏追平只是时间问题。
- **六子脚本**(命名循 `<动词>_<机构源>_<数据内容>`):`scrape_ircc_newsroom` / `scrape_bc_pnp_news` / `scrape_ab_aaip_news` / `scrape_mb_mpnp_news` / `scrape_ns_nsnp_news` / `scrape_on_oinp_news`;单页日期段落式源(BC/AB/ON)无逐条 URL → 锚点合成 `#日期-标题slug`(ON 优先用页面自带 id),bodyEn 就地取自本页不再抓详情。
- **AI 翻译+速读**:入口 `scrape_immigration_news.py` 直调 Anthropic(haiku,与顾问同模型,httpx 裸调**零新依赖**——镜像不用重建);随行存 raw=幂等缓存。**踩坑①**:让 LLM 输出 JSON,长译文里未转义引号 4/12 解析失败 → 改「`<<<BODY>>>` 哨兵行分隔的纯文本」后 12/12 全过。首批 86 条全翻译完成,抽检段落数完美对齐(NS 23=23 / ON 49=49 / federal 15=15)。
- **踩坑②(AB 解析空)**:alberta.ca 手风琴组件把每条更新包进 `<ul><li>`,「嵌套列表只收最外层」的过滤把布局 li 当内容列表整段跳光 → 修正为「li 祖先在收集范围**之内**才算嵌套」。
- **接线**:`etl/sources/news/__init__.py`(httpx/12h/seed=False,auto_update 自动发现)+ compose `news` service(SOURCE=news,透传 `ANTHROPIC_API_KEY`+`HEALTHCHECK_PING_NEWS`)+ `docker/.env.example` 补两行。`_paths.NEWS` 进路径真相源。
- **验证**:本地连跑 5 轮——首轮 65 条(federal 详情预算触发 deferred 15)→ 次轮补齐 86 → 三轮起全源 new 0(幂等)→ 翻译循环 8+12×6 补完 → 终轮「无待翻条目」。六 region 正文零空串。
- **待 Frank(运维项)**:① `ANTHROPIC_API_KEY` 进 docker/.env(空=只抓原文,到位后自动补翻);② ETL 盒 git pull 后 `docker compose up -d news`;③ healthchecks.io 建 news check(可选)。
- **下一步 = P1b**:09 → mart/news(近 60 条)→ News collection(DDL 照 pnp_draws 形状+locked_rels)→ seed → `/news` 卡片流 + 详情页三件套 + 省页块 + 弹框行 + 顶栏第 6 项。

**2026-07-18 续(Frank「用浏览器自己测」)——SK 破局成第 7 源,PE/NL 排除坐实:**
- **SK ✅**:浏览器扒出新闻 hub 筛选器真身——Sitecore POST 表单(`scController=GoS.NewsSearchController` + `MinistryId=9F26…`=Immigration and Career Training 部委 GUID);GET 参数/RSS 参数服务器都忽略,**但带 token 的 POST 用 httpx 就通**(不需要浏览器跑生产)。母脚本加可选 `post_data` 键,新子脚本 `scrape_sk_immigration_news.py`;首轮 10 条全链入库+翻译。注:部委口径含就业/学徒类新闻,比纯 SINP 宽,可接受(官方部委新闻)。
- **PE**:结论从「Radware 挡」升级为「**无移民专属新闻列表**」(浏览器实测:Office of Immigration 的 News 指向全政府 774 条流,关键词参数不生效)。
- **NL**:「News and Updates」菜单在真浏览器里也 404 = **站方死链**;ITA Updates 页是抽选数据表(Date/ITAs/NLPNP-AIP 拆分,月更)→ 归 E6-04,已立后台任务接进 build_draws。
