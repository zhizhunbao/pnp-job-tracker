# E12-06 省政策动态·新闻板块(设计方案 v2)

> 2026-07-18 Frank:「每省最新新闻聚合到站内板块,带图带文字,用户可留言」+「匹配 docker 抓数据拆分,解耦互不影响」;
> **v2 拍板(同日)**:「我们就是聚合整合站,这才是最有价值的;官方原文转载还是要转;中国人吃了信息不全的亏;速读可以用听的」。
> 归位:C5 政策动态(模块架构总图)。**本文档=设计方案,未实施**;留言→P2 流量触发(D6)。

## 0. 定位(v2)

聚合·链条·规划就是本站正业(价值主张「跨四级门槛代劳」的新闻面)。**信息全**是使命——所以转载官方原文,
并转出比原文更值钱的三件:**中文全文翻译**(信息不全的解药,全网稀缺 SEO 独家内容)+ 速读 + 语音朗读。

## 1. 目标与非目标

- **P1 目标**:
  - **卡片流** `/news`:og 缩略图+原标题+AI 中文速读+日期+省筛选(全名 chips)+ ▶ 朗读钮;
  - **详情页** `/news/[slug]` 三件套:① **AI 中文全文翻译(主体)**(段对段,标「AI 翻译·以原文为准」)
    ② **官方英文原文全文**(折叠附录式,带 ©+非官方声明+原文链接+日期四件套)③ **▶ 用听的**(Web Speech API 朗读中文翻译);
  - 统计省页「该省移民动态」块 + PNP 弹框「本省最新公告」行;可命中时「看命中岗位 →」深链(新闻→找岗闭环)。
- **非目标(P1)**:媒体新闻聚合(只官方源)、评论(P2/D6)、EN/KO 翻译层(原文就是英文;韩文翻译后续)、云 TTS(v1 平台特性 speechSynthesis 零成本,音质诉求出现再上云 TTS 缓存 mp3)。

## 2. 转载姿势(v2:转,姿势摆对)

1. **法律面(如实记档)**:联邦 Crown copyright 条款**非商业转载明确免许可**(注明出处+不冒充官方版本);商业场景灰区(条款称需询部门);各省 King's Printer 类似宽严不一。业内(移民媒体)整转+改写为常态,执法风险极低;真实风险=被当官方版本/旧闻当新政——靠声明+日期治理。
2. **姿势四件套**(每篇详情页):© 出处方(Government of Canada / 省 King's Printer)· 「非官方版本」声明 · 原文链接 ↗ · 官方发布日期。**沿用 E4-03 框架:转载+导流+异议即下架**。
3. **只官方源**;解析不出宁可缺省,一省失败不编不猜。
4. **AI 翻译/速读 grounding**:只喂抓取的官方正文,禁外推;标「AI 生成/翻译·以原文为准」;按 URL 幂等缓存(只对新增条目调 LLM)。
5. og:image hotlink(链接预览惯例)+ onerror 隐藏,不落盘缓存原图。

## 3. 解耦架构(Frank 点名:匹配 docker 拆分,互不影响)

**现有 compose 已是「一源一 service」**(SOURCE: jobbank/pnp/ee/enrich 各自独立 interval/healthcheck)——news 照此原生解耦:

```
docker-compose:  news 服务(<<: *etl, SOURCE: news, SCRAPE_INTERVAL: 43200=12h, 独立 healthcheck ping)
etl/sources/news/__init__.py   # 调度单元 META(method=httpx, seed=False 只刷 raw,build 轮消费)
etl/news/                      # 域目录(#55 新结构原生,不用迁)
  build_news.py                # 编排:逐源跑解析器,单源 try/except 隔离——一省挂不影响他省
  src_ircc.py                  # 联邦(Atom feed,已实测 ✓)
  src_sk.py / src_ab.py / …    # 一省一解析器文件,谁验证通过谁上
```

- **三层互不影响**:① news 容器与其它源容器互不牵连(compose 级);② build_news 内逐省 try/except,单省源改版只丢该省(解析器级);③ 只刷 raw 累积文件,抓挂了旧数据还在(数据级,同 pnp 源惯例)。
- 这套「service+调度单元+域目录」三件套 = **#55 目录重组的原生样板**,以后新源照此长。

## 4. 数据源(2026-07-18 初探)

| 源 | 实测 | 说明 |
|---|---|---|
| **IRCC 联邦** | ✅ **Atom feed 200**(api.io.canada.ca io-server news v2)| 机器可读金源,P1 锚点 |
| SK | 程序页 200,news 列表页待发现 | |
| AB/BC/MB/NS/ON | 猜测路径全 404 | **P0 工作项=逐省源发现**(找真实 news/updates 页+结构评估),验证过谁上谁——不拍脑袋写 URL |

- **范围化**:P1 = IRCC + 首批验证通过的 2-4 省;抓不动的省不硬上(板块按源有数据才显示该省 chip,同榜单先例)。

## 5. 数据模型 / 链路

- `raw/news/news.json`:**按 URL 累积去重**(同 postings 增量惯例);行={region(federal/SK/…), title, date, url, ogImage, **bodyEn(官方正文全文)**, **bodyZh(AI 段对段翻译)**, summaryZh(速读), fetchedAt}。
- **AI 翻译+速读**:build_news 内 python 直调 Anthropic(haiku,与顾问同模型;**新增条目才调**,翻译+速读一次调用产出,随行存 raw=幂等缓存);运维项:`ANTHROPIC_API_KEY` 进 docker/.env(ETL 侧首次用 LLM;成本≈每条新公告一次调用,公告频率低,忽略不计)。
- 09 直通 → `mart/news`(近 N=60 条,老的留 raw 不进站)→ 新 `News` collection(DDL 照 pnp_draws 形状+locked_rels 清单;bodyEn/bodyZh 为 text 长列)→ seed dims(白名单列全)。
- og:image + 正文抓法:逐条 GET 官方页抽 `og:image` + 正文容器(带频控;抓不到正文=只卡片不出详情页,不硬造)。
- **朗读**:前端 `speechSynthesis`(平台特性,零成本零后端);卡片读速读、详情页读全文翻译;暂停/停止;不支持的浏览器隐藏钮。

## 6. 前端

- `/news` 页(SSR,SEO:「加拿大移民最新政策」类词):省筛选 chips(**全名**,#58 原则)+ 卡片流;SiteHeader 入口待定(顶栏已 5 项,可先挂账户下拉/统计页入口,避免顶栏过载——拍板点)。
- 统计省页块(该省近 3 条)+ PNP 弹框行(本省最新 1-2 条,「本省最近抽选」块下)。
- 深链:region 命中职位板省筛选 → 「看该省岗位 →」;EE 类别公告 → `?view=match` 或类别筛选(v1 按省深链就够)。

## 7. P2(不在本件,流量触发)

- 评论最小集 = D6(登录可评+举报+Payload admin 人工审核);冷启动发帖引流后再开(鬼城效应+中介广告审核成本)。

## 8. 工作项拆分

| 步 | 内容 | 规模 |
|---|---|---|
| P0 源发现 | 逐省找真实 news/updates 页,httpx+结构实测,产出「可抓源清单」落本文档 §4 | S |
| P1a ETL | sources/news 单元 + etl/news/ 解析器(IRCC+首批省)+ AI 速读 + compose news 服务 | M |
| P1b 数据链+前端 | mart/collection/DDL/seed + /news 页 + 省页块 + 弹框行 + i18n | M |
| P2 评论 | D6,流量触发另立项 | — |

## 9. 拍板点

- [x] 转载官方原文(v2 拍板:转;姿势=©+非官方声明+原文链+日期,E4-03 框架)
- [x] 详情页三件套:中文全文翻译主体 + 英文原文折叠 + 朗读(v2)
- [x] 朗读 v1 = Web Speech API 平台特性零成本;云 TTS 缓 (v2 需求出现再上)
- [ ] P1 范围(IRCC+2-4 省起步,按源有数据才显 chip)认可?
- [ ] /news 入口位置:顶栏第 6 项 vs 账户下拉+统计页入口(顶栏防过载)?
- [ ] ANTHROPIC_API_KEY 进 docker/.env(ETL 首次用 LLM)认可?

## 10. 落地记录

（实施后补)
