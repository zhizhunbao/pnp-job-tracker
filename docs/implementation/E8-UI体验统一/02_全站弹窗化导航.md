# E8-02 · 全站弹窗化导航(定价先行,去「返回」按钮)

> Epic **E8 UI 体验统一** · 负责人 Frank · 3 SP · 批次 B10
> 通用约定与索引见 [实现文档 README](../README.md)。2026-07-06 用户拍板:「所有都应该是弹窗显示,不应该需要 返回这种按钮」「定价 也是弹窗的」。

---

## 1. 整体目标

/jobs 是唯一主页面,其余功能从 /jobs 出发一律**弹窗打开**(壳规范=[弹框规范](../../弹框规范.md)),不跳页、不需要「返回」。**SEO 例外原则**:stats/rankings/legal 这些有搜索流量价值的 URL(E5-04/E7-03 的 121 个 sitemap URL)**页面继续存在**供直链/收录,但站内导航不再跳过去——站内点击开弹窗,页面版只服务搜索引擎与外链。

## 2. 验收标准

- [x] 定价:顶栏 Pro/UpgradeModal 对比/stats 跨省 gate 全部开 **PricingModal**(PricingCard 单一来源,plan.ts 常量);/pricing 页保留可直达(SEO/Stripe 回跳),页面版共用 PricingCard。
- [x] 榜单:顶栏入口改弹窗(RankingModal,弹窗内两榜 tab 切换;RankingTable 单一来源与页面共用;/api/rankings-data 按需拉,lib/rankings.ts 与页面同一查询);/rankings/* URL 照常渲染。
- [x] 统计:顶栏入口改弹窗(StatsModal):四级内容组件(总览/省/大类/对比)与页面同一份(views.tsx *Content 拆分),弹窗内**点击拦截**把 `<a href="/stats/...">` 转 state 导航;「看职位」= 关弹窗直接落 省+大类 筛选;/api/stats-data 一次拉全(含用户分层态供对比段);/stats/* URL 照常渲染。
- [ ] 账户:/account 三合一(状态/档案/退订)评估弹窗化;「返回」钮已去,弹窗化待拍板——决策记录在 §5。
- [x] 全站无「返回/后退」按钮(account/rankings/pricing/legal/stats 五处 acct.back 链接删,三语键删);弹窗 ESC/遮罩可关(Modal 壳统一行为)。
- [x] 弹窗内容组件与页面版**同一份代码**(PricingCard),不许 fork 两份。

## 3. 实现步骤

- [x] **3.1** PricingCard 抽成单一来源(jobs/PricingModal.tsx)→ PricingModal 包 Modal 壳(新增 lg=720 档);替换全部 `/pricing` 站内链接(顶栏 Pro 钮、UpgradeModal 对比、stats 跨省 gate、横幅按钮删除)。AuthModal 加 z 透传(弹窗叠层)。
- [x] **3.2** 榜单:RankingTable 抽离 + /api/rankings-data + RankingModal;统计:*Content 拆分 + /api/stats-data + StatsModal(点击拦截 state 导航);页面壳都保留。
- [x] **3.3** /account 保留页面、「返回」钮已删;弹窗化另拍板。
- [ ] **3.4** tsc ✓ + playwright 定价弹窗开/ESC 关 ✓;其余弹窗走查与 E8-03 联测一并。

## 4. 涉及目录 / 文件

| 路径 | 角色 |
|---|---|
| `cms/src/app/(frontend)/pricing/PricingView.tsx` + `page.tsx` | 拆组件 + PricingModal |
| `cms/src/app/(frontend)/rankings/RankingView.tsx` · `stats/ui.tsx` | 弹窗复用 |
| `cms/src/app/(frontend)/jobs/JobsTable.tsx` · `Modal.tsx` | 入口接线;壳 token |

## 5. 现有代码 / 决策记录

- Modal.tsx 已是全站壳单一来源(sm/md 档);弹窗化=把「页面内容组件」塞进壳,反模式是重写内容。
- **决策待用户**:/account 是否弹窗化(表单+Stripe Portal 回跳,弹窗内做鉴权态切换较绕);legal 四件套建议留页面(合规文书,footer 直链)。
- Stripe Checkout 回跳 URL 指向页面(success/cancel)——定价弹窗不影响回跳,回跳落 /account?paid=1 逻辑不动。

## 6. 完成定义(DoD)

- [ ] §2 全勾 + sitemap URL 数量不减 + 支付回归(Checkout 从弹窗发起→回跳→Pro 解锁)+ push。
