// 兜底这一域跨文件共用的形状。

// =========================================================================
// 1. 槽位
// =========================================================================

// 兜底解析的产出:用户在说哪个职业(NOC)、哪些省,外加一句「为什么挑它」(只进日志,不见客)。
export type AgentSlots = {
  noc: string | null
  provs: string[]
  reason: string
}

// =========================================================================
// 2. 收件箱
// =========================================================================

// 工具在 execute 里写,循环结束后 planner 读(2026-08-19 收拢:原先 planner 与 tools 各写了一遍)。
// 🔴 `gaveUp` ≠ `slots=null`:前者是模型交回反问,后者是压根没跑到(超时/报错/env 关着)。
export type ResolveOut = {
  slots: AgentSlots | null
  gaveUp: boolean
}
