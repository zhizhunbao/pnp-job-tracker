// 决策页与初筛页要的两件取数(**不是算分**;算分在 lib/points)。
//
// 🔴 2026-08-20 把四套算分器搬进 `lib/points` 时发现的边界:这两件回答的是
//    「决策页首屏要哪几张表」「这个职业各省在招多少」——**一分都不算**,当初只是从路由里
//    抽出来时顺手放进了这个目录。它们的依赖链上还挂着 `payload.find` 与 `lib/jobs/server`,
//    进不了算分域(会把连接池打进浏览器包)。
//    **归属待定**:`fetchOccCompetition` 更像 `lib/jobs`,`getScoreTables` 更像决策页自己的取数。
//    留在这儿是记账,不是结论 —— 见 docs/implementation 的分值域卷宗。
//
// 这个桶现在是空的:两件都要连库,一律从 `./server` 取。

export {}
