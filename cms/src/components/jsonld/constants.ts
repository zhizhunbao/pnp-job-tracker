/**
 * jsonld 域的死值。
 *
 * @author Frank
 * @time 2026-08-29 07:30:00
 */

/**
 * JSON-LD 脚本标签的 MIME(schema.org 结构化数据的约定值)。
 */
export const MIME_LD_JSON = 'application/ld+json'

/**
 * 「这一页不出结构化数据」的哨兵:数据串为空就整个 script 不渲(2026-09-17 过期职位不出 JobPosting,Google 政策不许)。
 */
export const JSON_NONE = ''
