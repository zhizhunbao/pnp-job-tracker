/**
 * select 域的死值。
 *
 * @author Frank
 * @time 2026-08-24 10:00:00
 */

/**
 * 不指定壳宽时的默认档。选 sm(150px,职位板那档)当默认,是因为筛选行是本组件
 * 最密的用法 —— 默认取最窄的一档,忘了传也不会把一行筛选器挤到换行;要更宽的
 * (雇主板 md=170、就业把脉 lg=210)由调用点显式说。
 * ⚠️ 与 input 域的 SIZE_DEFAULT 不是一回事:那个是输入框**高度**档,这个是下拉的
 * **壳宽**档(量宽机制决定它的上限宽),两个默认值各判各的。
 */
export const BOX_SIZE_DEFAULT = 'sm'
