/**
 * select 组件域的桶 —— 量宽自适应下拉(收拢职位板/雇主板/就业把脉三份双胞胎)。
 * 2026-08-24 由 field 域拆出;输入件基座与尺寸档走 input 域。
 * 对应 lib 域:无(通用件)。
 *
 * @author Frank
 * @time 2026-08-24 15:00:00
 */
export { Select } from './select'
export type { SelectIn, SelectLabelFn, SelectSize } from './types'
