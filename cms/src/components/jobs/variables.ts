/**
 * jobs 页面域唯一的可变状态。写成一个容器对象(不是几个 `export let`)——
 * 跨模块的 `export let` 是只读活绑定,别的文件里赋值当场编译错;改属性才合法,
 * 顺带逼着每一处写明「我在改哪一格状态」。这个域一共有多少可变状态,一眼数得清。
 *
 * @author Frank
 * @time 2026-08-28 19:15:06
 */

/**
 * 跨组件的会话级缓存(整页刷新即清)。
 */
export const CACHE = {
  /**
   * 同岗 JD 正文(#126):三处调用点(事实块 / JD 弹框 / 详情页 JD 区)共用,
   * 键 = 原帖链接。同一岗反复开关不重复打端点烧额度(统一池 #124 下一次白开 = 一次额度)。
   * 只存非空正文;402 / 空 / 失败不进缓存,服务端负缓存(10min)照管懒抓重试节奏。
   */
  jobText: new Map<string, string>(),
}
