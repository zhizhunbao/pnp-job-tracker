/**
 * icons 域的形状:手画图标组件的 props 契约(lucide 词汇表走 LucideProps,不在这)。
 *
 * @author Frank
 * @time 2026-08-24 04:30:00
 */

/**
 * MaxIcon 的 props。
 */
export type MaxIconIn = {
  /**
   * 是否全屏态(定两态图标取哪个:放大/还原)。
   */
  maximized: boolean
}
