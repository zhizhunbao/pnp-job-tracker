/**
 * search 组件域的桶 —— 搜索框(左放大镜 + 有值时右清除钮)。
 * 2026-08-24 由 field 域拆出;输入框本体的基座/尺寸走 input 域。
 * 对应 lib 域:无(通用件)。
 *
 * @author Frank
 * @time 2026-08-24 15:00:00
 */
export { Search } from './search'
export type { SearchIn } from './types'
